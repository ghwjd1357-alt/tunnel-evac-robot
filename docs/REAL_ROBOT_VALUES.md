# REAL_ROBOT_VALUES.md — 구동부 실측값 정본 + 실차 반영 지점

> 07-20 개편으로 `~/Desktop/개발현황/실차값_수령체크리스트.md`(07-08 개시)에서 이관.
> ★ 이관과 함께 **반영 정책 개정**: 구판은 "시뮬 `tunnel_sim` 파일의 숫자를 직접 교체"였으나,
> 현행 정책은 **시뮬 자산 불변 + 실차는 `tunnel_bringup` 신규 패키지** (`MASTER_PLAN.md §3`).
> 시뮬 T자·쌍굴 회귀는 실차 작업 중에도 기준선으로 계속 돌아야 하므로 시뮬 수치를 덮어쓰지 않는다.

## 1. 수령 현황 — ✅ 4종 전부 수령 완료 (07-08 ~ 07-18)

| 항목 | 수령값 | 비고 |
|---|---|---|
| `wheel_separation` | **0.49 m** | 좌우 바퀴 중심 간격 |
| `wheel_radius` | **0.065 m** (지름 0.13m = "130파이") | ⚠ 시뮬 잠정값(반지름 0.10)보다 훨씬 작음 → 실차 URDF 차고·물리 별도 설정 |
| `footprint` | 가로 **0.55** × 세로 **0.57 m** | 외접반경 ≈ **0.40m** (시뮬 0.32보다 큼) |
| `encoder_ppr` | 모터축 **26 PPR**(13×2CH) → 바퀴축 **TOTAL_PPR 2644.3** | 바퀴 1회전 직접 실측값(역산 추정 아님) → 기어비 추가 확인 불필요. 소비자는 Teensy 펌웨어(구동부 몫) |
| (추가) IMU | **BNO055, I2C** — 구동부 코드 설정 50Hz / **실측 41.63Hz** (평균주기 **24.02ms** · min **18ms** · max **30ms** · σ **0.55ms** · window 295) | ★ 08-01 **2차 회신 §11 실측으로 정정** (아래 ⚠ 참조). 07-30 의 '50Hz 확정(실측 50.2Hz)'은 1차 회신 표 기반이라 폐기. 구 계획값 100Hz 도 폐기 |
| (추가) 모터 | 12.5W · **IG-36PGM + Encoder 06TYPE (24V)**, 감속비 **1:51** | 사양 수령 07-30. 배선 핀맵 = `0718_구동부_배선맵핑_검증절차.md` |

★ 실측값을 받았어도 최종 검증은 **R2 "3m 직진 오차 3% 이내" 통과한 `/odom`** 이다
(실측 PPR + 3m 검증 = 이중 안전망). 코드 이식 트리거도 이 통과 선언.

### ⚠ IMU 주기 — 무엇이 확정이고 무엇이 아직 열려 있나 (08-01 신설)

`50Hz 확정` 은 **우리가 R3 실측 전에 먼저 선언한 값**이었고, 2차 회신 §11 실측이 그것을
뒤집었다. 구동부 코드는 `IMU_PERIOD_US = 20000`(50Hz)인데 **실제 루프가 18~30ms 로 돈다**
— 즉 낮은 주파수 자체보다 *루프가 자기 주기를 못 지킨다*는 사실이 본체다.
수용 판단·요청 전문 = `~/Desktop/TEENSY_실차연동_3차요청_0730.md §1.2` · `~/Desktop/TEENSY_실차연동_3차요청_0730.md §2`.

- **수용한 것**: 41.63Hz 는 EKF 입력으로 충분하다 → 코드 최적화 요구 없음.
- **전제조건 (이게 깨지면 위 수용도 깨진다)**: 평균 41.63Hz · 최대 간격 30ms · σ 0.55ms 수준이
  유지될 것. 구동부에 **요청 ②(dt 를 고정값 20ms 로 쓰는가, `micros()` 실측인가)** 를 보냈고,
  그 답에 따라 주기 분포 자체가 바뀔 수 있다.
- **★ 30ms 는 상한이 아니라 '관측된 최대'다** — window 295 ≈ 7초짜리 창 하나의 최대값이다.
  EKF 주기(30Hz = 33.33ms)와의 여유가 **3.33ms 뿐**이라, 더 긴 창에서 33.33ms 를 넘는 간격이
  나오면 **EKF 한 주기가 IMU 없이 지나간다**. 지금 실측으로는 그 일이 일어나지 않는다
  (30 < 33.33 이므로 산술적으로 불가능).
- **재개방 조건**: ① 요청 ② 회신으로 루프 주기가 바뀌면 ② **R3 rosbag 에서 `/imu/data` 간격
  히스토그램을 다시 뽑아 최대 간격이 33.33ms 를 넘으면** — 둘 중 하나라도 발생하면 이 절과
  `src/tunnel_bringup/test/gate_fakes.py` 의 주기 정본을 함께 다시 판단한다.
- **하네스 반영**: 게이트 회귀의 가짜 IMU 가 위 분포를 재현한다 (`MASTER_PLAN.md §7` 예약 18).
  ⚠ 우리가 받은 것은 **5개 요약통계뿐**이고 원자료(타임스탬프 열)는 못 받았다 → 하네스
  시퀀스는 '그 5통계를 만족하는 한 대표'이지 실차의 실제 시간열이 아니다. R3 rosbag 으로 교체한다.

## 2. 실차 반영 지점 (★ 개정 — 시뮬 파일 수정 아님, `tunnel_bringup` 신규 작성)

★ 07-29 갱신: 신규 파일은 **전부 생성됐다** (`tunnel_bringup` S4 골격). 아래 '상태' 열은
파일 존재가 아니라 **그 값이 실제로 반영됐는가**를 가리킨다.

| # | 값 | 반영할 **신규** 파일 | 내용 | 상태 |
|---|---|---|---|---|
| 1 | separation 0.49 · 반지름 0.065 | `tunnel_bringup/urdf/robot_real.urdf` | 실측 치수로 **새로 작성**(시뮬 URDF 복사 아님, Gazebo 태그 0). 바퀴 반경이 작아지므로 belly clearance 재확인 (URDF 함정 ① — `PITFALLS.md §5`) | ✅ 반영 (TF 실측: `base_footprint→base_link` z=0.065, `base_link→wheel_left` y=0.245) |
| 2 | footprint 0.55×0.57 | `tunnel_bringup/config/nav2_params_real.yaml` | 꼭짓점 = `[[0.285,0.275],[0.285,-0.275],[-0.285,-0.275],[-0.285,0.275]]` (§3). `inflation_radius ≥ 외접반경 0.40` 확인 (현 0.9 여유 있음) | ✅ 반영 (local·global costmap 양쪽) |
| 3 | 라이다·IMU 장착 오프셋 | `tunnel_bringup/urdf/robot_real.urdf` | 실측 후 `base_link→lidar_link`·`base_link→imu_link` 고정 TF. **라이다는 몸통 최상면보다 위** (자기타격 방지 — 미확인 항목, §4) | ❌ **미실측** — `xyz="0 0 0"` + `TODO:` 주석. 0 은 물리적으로 불가능한 값이라 빼먹으면 즉시 드러난다 (추정치로 메우지 않음) |
| 3-b | 몸통 높이 · 차고 · wheelbase | `tunnel_bringup/urdf/robot_real.urdf` | 바퀴 반경이 0.10→0.065 로 낮아져 **시뮬과 같은 몸통 오프셋이면 지상고가 0.015 m 뿐**이다. URDF 숫자로 해결되는 문제가 아니라 기구 확인 사항 | ❌ **미실측** — TODO |
| 4 | TOTAL_PPR 2644.3 | Teensy 펌웨어 | 구동부 몫 — 역할 A 는 `/odom` 결과만 소비 | (구동부) |
| 5 | (시뮬 자산) | `src/tunnel_sim/**` | **변경 금지.** T자·쌍굴 회귀 기준선으로 보존 | ✅ 무변경 유지 |

미실측 항목 전수 확인: `grep -rn "TODO: " src/tunnel_bringup/`

⚠ 실측 footprint 가 시뮬(0.50×0.40)보다 **커서** 폭 2.5m 피난연결통로 여유가 현재 과대평가돼 있다.
실차 설정 작성 후 **좁은 통로 통과 검증 필수** (R6 항목).

## 3. footprint 변환 계산

`가로 0.55`(폭, 좌우 = y) · `세로 0.57`(길이, 앞뒤 = x) → 중심 기준 절반:

```
시뮬(보존):  [[0.25, 0.20], [0.25, -0.20], [-0.25, -0.20], [-0.25, 0.20]]
실차(신규):  [[0.285, 0.275], [0.285, -0.275], [-0.285, -0.275], [-0.285, 0.275]]
               └ 세로/2 = 0.285      └ 가로/2 = 0.275     → 외접반경 √(0.285²+0.275²) ≈ 0.40m
```

## 4. 남은 수령·합의 항목

> ⏸ **07-31 주의 — 아래 2개 항목은 구동부 2차 회신(07-30)으로 이미 갱신됐으나 본문은 아직 구판이다.**
> · "엔코더 체배 확인" → **quadrature ×4 확정**, 실측 `TOTAL_PPR = 2641.1` (4체배 이론값 2652 와 −0.41%)
> · "유효 반지름은 공압 조건부" → 타이어는 **밸브 없는 반공압** — 공기압 관련 우려·재개방 조건 **전부 철회**
> 정정 작업은 `MASTER_PLAN.md §3 S6-6` 에 **보류**로 등록돼 있다.
> 정정 전까지 아래 두 줄을 근거로 인용하지 말 것. 현행 판단 =
> `~/Desktop/TEENSY_실차연동_3차요청_0730.md §1.1` ·
> `~/Desktop/TEENSY_실차연동_3차요청_0730.md §9`.

- [ ] **micro-ROS 토픽 계약 확정** — frame_id·주기·단위·covariance·QoS·watchdog.
      합의표 정본 = `~/Desktop/TEENSY_실차연동_합의사항.md` (구동부 전달 완료, 회의에서 값 확정)
      ⚠ 구두 합의 금지 — **rosbag 으로 실물 검증** 후 확정.
- [ ] **라이다 장착 높이** (몸통 최상면보다 위) — 07-18 배선 문서에도 없음, **재전달 필요**.
- [ ] **엔코더 체배 방식 확인 (07-30 신규)** — 사양 `26 Pulses(13×2CH)` · 감속비 `1:51` 로
      역산하면 2체배 = 1326, **4체배 = 52 × 51 = 2652**. 실측 2644.3 은 4체배 값과 **−0.29%**
      로 일치 → 펌웨어가 quadrature ×4 로 세는 것으로 **추정**(구동부 확인 대기).
      ⚠ 사양서에는 "26" 이라 적혀 있어, 누군가 26 × 51 = 1326 으로 "고치면" **odom 이 정확히
      2배** 틀어진다 (3m 를 6m 로 읽음 → SLAM 지도가 2배로 그려짐). 확인 후 펌웨어 주석에 명시 요청.
- [ ] **유효 반지름은 공압 조건부 (07-30 신규)** — 타이어가 **공압**이라 유효 반지름이 시간·
      온도·하중에 따라 변한다. `Δr 1.71mm = odom 3%` 이므로 R2 통과는 **규정 공기압 조건에서만**
      유효하다. 구동부 회신값 0.05698(하중 눌림 실측)은 **측정 당시 공기압 미상** → 확정 보류.
      상세·재개방 조건 = `~/Desktop/TEENSY_실차연동_2차요청_0730.md §3` · `~/Desktop/TEENSY_실차연동_2차요청_0730.md §7`.
- [ ] 구동부 R0~R2 통과 → R2 "3m 직진 3%" 선언이 실차 트리거.

## 5. 인수일 수령 항목 (07-31 신설 — **값이 아니라 환경·절차**)

★ **B안**: 로봇·Jetson 을 **인수일에 일괄 수령**한다. 아래가 없으면 **D+0 에 로봇이 켜지지 않고**,
D+1 첫 스텝(R3 rosbag)이 성립하지 않는다.
전달 문서 = `~/Desktop/TEENSY_실차연동_인수요청_커버레터_0731.md` (근거 = 같은 폴더 3차요청 문서).
**우리 쪽 준비물(런북·udev·agent 확보 등)은 여기가 아니라 `MASTER_PLAN.md §3 S6`** — 책임 경계를 섞지 않는다.

| # | 수령 항목 | 상태 | 없으면 |
|---|---|---|---|
| 1 | **통합 펌웨어 1개** — `/odom` + `/imu/data` **동시 발행** | ❌ | 회신 §10.3 토픽 목록에 `/odom` 이 없다. EKF 는 둘을 동시에 받아야 해서 **기동 자체가 불가능** |
| 2 | 그 빌드의 **SHA 40자 + 짧은 SHA + 빌드일시** | ❌ | 합의서 §4.5. 어느 코드로 시험했는지 특정 불가 |
| 3 | **펌웨어 빌드·업로드 환경** (소스 위치·IDE/라이브러리 버전·굽는 절차) | ❌ | 현장 문제마다 구동부 호출 필요 |
| 4 | **micro-ROS agent 실제 설정** (transport·baudrate·패키지 버전) | ❌ | 합의서 §4.5 는 "임시 기준" — 실값 미확인 |
| 5 | **udev 규칙 `/dev/teensy_drive`** (없으면 "없다" 회신) | ❌ | 없으면 역할 A 가 작성 (S6-3). `/dev/ttyACM*` 번호가 매번 바뀜 |
| 6 | **전원·E-stop 인계** (켜는 순서·충전기·만충/방전 전압·E-stop 위치/해제·배선 사진) | ❌ | |
| 7 | **E-stop 시 Jetson·Teensy 전원 유지 여부** | ❌ | 합의서 §5.3 정의의 핵심. 2차 회신에서 **무응답**. Teensy 까지 꺼지면 해제 후 위치 소실 |
| 8 | **바퀴축 높이**(바닥→축, 하중 상태) + 라이다 스캔면 높이·x·y + IMU x,y,z·rpy + 몸통 지상고 | ❌ | ★ **4번째 요청.** §2 표 3·3-b 의 `xyz="0 0 0"` TODO 를 채우는 값. 없으면 **R5 이상 진행 불가** |
| 9 | **좌회전 시 `angular_velocity.z` 부호** (30초) | ❌ | 반대면 EKF 안에서 odom 과 IMU 가 충돌해 발산 |
| 10 | Jetson **여유 디스크·계정·네트워크 접속 방법** | ❌ | OS 22.04 는 07-31 확인 완료. 네트워크가 랩실 WiFi 전용이면 D+0 에 `apt`·`git` 불가 |

**8번 측정 기준면** — 라이다는 **바닥 기준**으로 받는다. `base_link` 는 바퀴축이라 실제로 재기 어렵고,
기준면을 안 정하면 기구팀은 바닥 기준으로 재고 역할 A 는 축 기준으로 알아들어 **수 cm 오차가 조용히 생긴다.**
변환은 `z = (받은 높이) − (8번의 축 높이)` 로 역할 A 가 수행한다.

⚠ **8번의 축 높이를 "유효 반지름 0.05698" 로 대체하지 않는다.** 0.05698 은 잰 값이 아니라
**3m 거리를 맞춰 역산한 계수**이고 `실제 눌린 반지름 + 구동 슬립 + PPR 잔차` 가 섞여 있다.
그대로 URDF 에 넣으면 최대 8mm 틀리고, 그 오차가 라이다·IMU 높이 전체로 전파된다
(상세 = `~/Desktop/TEENSY_실차연동_3차요청_0730.md §9.3` ·
`~/Desktop/TEENSY_실차연동_3차요청_0730.md §9.4`).
