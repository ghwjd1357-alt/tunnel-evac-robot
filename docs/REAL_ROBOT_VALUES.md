# REAL_ROBOT_VALUES.md — 구동부 실측값 정본 + 실차 반영 지점

> 07-20 개편으로 `~/Desktop/개발현황/실차값_수령체크리스트.md`(07-08 개시)에서 이관.
> ★ 이관과 함께 **반영 정책 개정**: 구판은 "시뮬 `tunnel_sim` 파일의 숫자를 직접 교체"였으나,
> 현행 정책은 **시뮬 자산 불변 + 실차는 `tunnel_bringup` 신규 패키지** (`MASTER_PLAN.md §3`).
> 시뮬 T자·쌍굴 회귀는 실차 작업 중에도 기준선으로 계속 돌아야 하므로 시뮬 수치를 덮어쓰지 않는다.

## 1. 수령 현황 — ✅ 4종 전부 수령 완료 (07-08 ~ 07-18)

| 항목 | 수령값 | 비고 |
|---|---|---|
| `wheel_separation` | **0.49 m** | 좌우 바퀴 중심 간격 |
| `wheel_radius` | **공칭 0.065 m** (지름 0.13m = "130파이") / **실측 축높이 0.053 m** / **오도메트리 계수 0.05698** | ★ 08-02 **3종 분리** — 아래 §1-b. URDF 는 **실측 0.053**, 오도메트리 계수는 펌웨어 몫 |
| `footprint` | 가로 **0.55** × 세로 **0.57 m** | 외접반경 ≈ **0.40m** (시뮬 0.32보다 큼) |
| `encoder_ppr` | 모터축 **26 PPR**(13×2CH) → 바퀴축 **TOTAL_PPR 2641.1** | ★ 08-02 **3차 회신으로 확정** (구 2644.3 폐기). 구동부 확인: **실측값은 2641.1 단일**. 4체배 이론값 `52×51=2652` 와 −0.41% 일치 → **quadrature ×4 확정**. 소비자는 Teensy 펌웨어(구동부 몫) |
| (추가) IMU | **BNO055, I2C** — 통합 펌웨어 **실측 46.4Hz** (주기 범위 **20~23ms**) | ★ 08-02 **3차 회신 §5 로 정정** (아래 ⚠ 참조). 구 '41.63Hz · 24.02ms · max 30ms'(2차 회신)는 **폐기**. 07-30 의 '50Hz 확정'·구 계획값 100Hz 도 폐기 |
| (추가) 모터 | 12.5W · **IG-36PGM + Encoder 06TYPE (24V)**, 감속비 **1:51** | 사양 수령 07-30. 배선 핀맵 = `0718_구동부_배선맵핑_검증절차.md` |

★ 실측값을 받았어도 최종 검증은 **R2 "3m 직진 오차 3% 이내" 통과한 `/odom`** 이다
(실측 PPR + 3m 검증 = 이중 안전망). 코드 이식 트리거도 이 통과 선언.

### ⚠ IMU 주기 — 재개방 조건 ①이 **실제로 발동했다** (08-01 신설 · 08-02 갱신)

**경위**: `50Hz 확정`(07-30, 우리가 R3 실측 전에 먼저 선언) → 2차 회신 §11 실측 **41.63Hz**
로 뒤집힘(08-01) → **3차 회신 §5 실측 46.4Hz 로 다시 갱신**(08-02).
08-01 절에 적어 둔 **재개방 조건 ①("요청 ② 회신으로 루프 주기가 바뀌면")이 그대로 발동**했다.
통합 펌웨어에서 IMU 단독 코드 대비 발행률이 개선된 결과다.

| | 구 정본 (2차 회신) | **현 정본 (3차 회신 §5)** |
|---|---|---|
| `/imu/data` | 41.63Hz · 평균 24.02ms · min 18 · max 30 · σ 0.55 · win 295 | **46.4Hz · 주기 범위 20~23ms** |
| `/odom` | (미실측) | **46.5Hz · 주기 범위 20~22ms** |
| EKF 30Hz(33.33ms) 여유 | 3.33ms | **약 10.3ms** |

- **수용한 것**: 46.4Hz 는 EKF 입력으로 충분하다 → 코드 최적화 요구 없음. 여유가 3배로 늘어
  "EKF 한 주기가 IMU 없이 지나가는" 시나리오가 더 멀어졌다.
- **✅ 함께 닫힌 것**: 08-01 의 전제 중 **요청 ②(dt 가 고정값 20ms 인가 `micros()` 실측인가)**
  는 3차 회신 §2.1 로 **`micros()` 실측 확인**됐다. "루프가 자기 주기를 못 지킨다"는 우려의
  절반이 해소됐다.
- **★ 23ms 도 상한이 아니다 — 같은 형태의 미공개가 반복됐다.** 3차 회신의 "20~23ms" 에도
  **관측 창 크기·표본 수·σ 가 적혀 있지 않다.** 2차 회신의 "max 30ms" 가 window 295(≈7초)
  짜리 창 하나의 최대였던 것과 정확히 같다. **23ms 를 계약상 상한으로 쓰지 않는다.**
- **재개방 조건 (갱신)**: **R3 rosbag 에서 `/imu/data` 간격 히스토그램을 다시 뽑아 최대 간격이
  33.33ms 를 넘으면** 이 절과 `src/tunnel_bringup/test/gate_fakes.py` 의 주기 정본을 함께
  다시 판단한다. (구 재개방 조건 ①은 위와 같이 **발동·소진**됐다.)
  ★ **판정은 `tools/bag_gap_report.py` 의 EKF 입력 계약이 한다** — 위 33.33ms 는 그 계약의
  *현재 값*이 아니라 **여유 산술의 근거**(EKF 30Hz 한 주기)로 여기 적혀 있다. 계약 값이
  바뀌면 정본은 도구의 `TOPIC_POLICY` 쪽이다(검토 §31.2·§32.2). ⚠ 이 조건은 **EKF 입력
  2종에만** 걸린다 — `/scan` 은 사양 10Hz 라 여기 해당하지 않는다(검토 §30.2).
- **하네스 반영 — ⏸ 보류**: `gate_fakes.py` 의 주기 정본은 **아직 구값(41.63Hz 계열)이다.**
  검토 §25 결정 B 로 그 트랙이 동결됐고(`TEST_GATES.md §2` 검증 상한 5), 지금 고치면 동결을
  되돌리게 된다. **R3 rosbag 실측이 오면 그때 한 번에 교체**한다 — 그때는 요약통계가 아니라
  실제 시간열을 넣을 수 있어 두 번 고치지 않아도 된다.
  ⚠ **그때까지 하네스는 실물과 다른 분포를 쓴다는 사실을 여기 공개해 둔다.** 이것은 "덮은 것"이
  아니라 **교체 시점을 R3 로 명시한 조건부 보류**다. ⏸ 하네스 교체는 **예약 19 와 무관하게 계속 보류**다 — 예약 19 는 08-02 에 종료됐지만 `gate_fakes.py` 는 그 경계에서 **명시적으로 제외**됐다(`FREEZE_MANIFEST.md §10.4`).

### ⚠ §1-b. 바퀴 반지름 3종 분리 (08-02 신설 — 3차 회신 §10 실측 수령)

3차 회신 §10 에서 **바닥 → 바퀴축 중심 = 53mm** 실측을 처음 받았다. 이제 세 값이 공존한다.

| 값 | 출처 | 성격 | 쓰는 곳 |
|---|---|---|---|
| **0.065 m** | 타이어 공칭 (지름 130파이) | 무부하 카탈로그 값 | **쓰지 않는다** |
| **0.05698 m** | 펌웨어 "유효 반지름" | 3m 거리를 맞춘 **역산 계수** | **펌웨어 오도메트리** (구동부 몫) |
| **0.053 m** | 3차 회신 §10 실측 (하중 상태) | **기하학적 실측** | **URDF `base_footprint→base_link`** |

**원칙**: URDF 는 기하학적 진실인 **0.053** 을 쓴다. 라이다·IMU 높이가 전부 여기서 파생되므로
역산 계수를 넣으면 센서 높이 전체가 함께 틀어진다. 두 값의 차이 4.7mm(8.8%)는 **슬립이 흡수한
몫**으로 이해한다. 이 구분은 아래 §5 의 "0.05698 로 대체하지 않는다" 경고가 예견한 그 상황이다.

✅ **URDF 반영 완료 (08-02, 예약 19 ③)** — `robot_real.urdf` 의 `base_joint` 가
`xyz="0 0 0.053"` 이다. 관측으로 확인: `tf2_echo base_footprint base_link` → **0.053**.

### ⚠ §1-c. 좌우 바퀴 간격 — 물리 0.49 / 실효 0.62 분리 (08-02 신설)

3차 회신 §4 에서 펌웨어의 `EFFECTIVE_WHEEL_BASE` 를 **0.49 → 0.62** 로 바꾼 사유를 3절에 걸쳐
설명받았다. **기구 치수가 바뀐 것이 아니다.**

- **물리 윤거 0.49 m** = 실제 좌우 바퀴 중심 간 거리 → **URDF 는 이 값을 유지**(`y = ±0.245`)
- **실효 WHEEL_BASE 0.62 m** = 스키드 스티어 횡슬립·타이어 스크럽을 반영한 **오도메트리 보정 계수**

★ 구동부가 이 구분을 먼저 설명해 준 덕에 URDF 를 0.62 로 고치는 사고를 막았다. 그대로 고쳤다면
costmap·footprint·TF 가 전부 어긋났을 것이다.

⚠ **조건부 수용**: 0.62 는 **현재 실험실 바닥 마찰 조건**에서 보정한 값이고, 구동부도 §12 에
`wheel_base_profiles: tunnel: TBD` 로 남겼다.
- **전제조건**: 시험 바닥의 마찰 조건이 유지될 것
- **재개방 조건**: 터널 환경에서 회전 오도메트리가 어긋나면 재조정한다. 그때 **펌웨어를 다시
  구워야 하므로** 소스·빌드환경 확보(§5 항목 2)가 선행 조건이다
- **완화 수단**: EKF 에서 IMU `angular_velocity.z` 가중을 높이고 odom yaw 신뢰도를 낮춘다
  (3차 회신 §11 이 먼저 요청한 방향이고 `ekf_real.yaml` 이 이미 그 구조다)

### ★★ §1-d. 펌웨어 소스에서 확정된 값 (08-02 신설 — **최종 정본**)

> 출처 = `~/Desktop/teensy_integrated_base_v1_4/teensy_integrated_base_v1_4.ino`
> (sha256 `13f929cb…2106`). **회신 PDF 와 다르면 이쪽이 맞다.**

**ⓐ QoS — 회신 PDF 가 틀렸다**

| 토픽 | Reliability | 근거 (소스 함수) |
|---|---|---|
| `/odom` | **RELIABLE** | `rclc_publisher_init_default` |
| `/imu/data` · `/imu/yaw_deg` · `/imu/gyro_bias` | **BEST_EFFORT** | `rclc_publisher_init_best_effort` |
| `/estop/state` · `/firmware/info` | **RELIABLE** | `rclc_publisher_init_default` |

⚠ 소스 주석은 이 블록 전체를 `// BEST_EFFORT / VOLATILE sensor publishers` 라고 적어 두었으나
`init_default` 는 rclc 에서 **RELIABLE** 이다. **주석이 아니라 함수 이름이 사실이다.**

★ **예약 19 ① 의 결론은 바뀌지 않는다.** 구독자가 BEST_EFFORT 면 양쪽 모두와 매칭되고,
`robot_localization` 은 이미 BEST_EFFORT 로 구독한다(08-02 실측). **고장 조합은 하나뿐이다:**
`pub BEST_EFFORT + sub RELIABLE`. → `d0_check.sh` 검사 [4][5] 를 이 규칙으로 정정했다.

**ⓑ 🔴 바퀴 명령 하한 0.02 m/s — Nav2 직결 (신규)**

```c
MIN_EFFECTIVE_WHEEL_CMD = 0.020;   // 0 이 아닌 모든 바퀴 명령이 여기로 끌어올려진다
```

| 상황 | 명령 | 실제 | 배율 |
|---|---|---|---|
| 제자리 미세회전 | ω = 0.01 rad/s | **0.0645 rad/s** | **6.5배 과회전** |
| 혼합 저속 | v=0.02, ω=0.05 | ω = 0.025 rad/s | **0.5배 언더회전** |

**제자리 회전의 물리적 하한 = `2 × 0.02 / 0.62` = 0.0645 rad/s.** 그 아래는 낼 수 없다.
⚠ **왜곡 방향이 상황마다 반대라 보정이 불가능하다** — 한쪽 바퀴만 승격되면 좌우 차이가
뭉개져 회전이 줄고, 양쪽 다 승격되면 튄다. `rotate_to_goal` 이 미세 조정하면 오버슈트 →
반대로 튐 → **진동**한다.
→ **대응은 Nav2 쪽에서 하한을 세우는 것**이다 (`MASTER_PLAN.md §7` **예약 22**).

**ⓒ 🔴 PWM 천장 — 최고속 0.12 m/s 가 도달 불가일 수 있다 (신규)**

```c
FEEDFORWARD_MAX_PWM = 145
MAX_CONTROL_PWM     = 160   // 소스 주석: "Raise only after ground-speed verification."
```

피드포워드 식 `30 + (v − 0.02) × 1300` 에 바퀴 상한 0.15 를 넣으면 **199 PWM 을 요구**하는데
145 에서 잘린다. 회전시험(0.20 rad/s → 바퀴 0.062 m/s)은 실증됐으나 **0.15 는 그 2.4배**다.
⚠ **"미검증"이 아니라 "도달 불가일 수 있다"** — 소스 주석이 스스로 미확인이라고 말한다.
→ **D+0 실측 필수.** 미달이면 `max_vel_x` 를 실측값으로 내린다 (예약 22).

**ⓓ `/odom` twist 는 필터값, pose 는 생값 (신규)**

```c
VELOCITY_FILTER_ALPHA = 0.10;   // 20ms 주기 → 시정수 ≈ 0.2초
// 소스 주석: "Pose integration uses raw encoder increments. Published twist is filtered."
```

**우리 EKF 는 twist 만 먹는데 3m 거리시험은 pose 를 검증한 것이다.**
정상상태에서는 평균이 보존되어 동일하나 **가감속 구간에서 최대 0.2초 지연**된다.
→ `MASTER_PLAN.md §8` 결정 색인 *"EKF 가 소비하는 양에 합격기준을 써야 한다"* 의 **세 번째 사례**.

**ⓔ 기타 확정값**

| 값 | 내용 |
|---|---|
| `GYRO_ZERO_THRESHOLD` | **0.005 rad/s (=0.29°/s) 데드밴드** — 그보다 느린 회전은 IMU 가 **0 으로 보고**한다. 장시간 초저속 선회에서 yaw drift 요인. R3 관찰 대상 |
| IMU 동작 모드 | `OPERATION_MODE_AMG` = **융합 없음** → `orientation_covariance[0] = -1`(사용 불가) 과 정합. 지자기 캘리브레이션 항목(E7)의 우선순위가 내려간다 |
| IMU 46.4Hz 원인 | `Wire.setClock(100000)` = **I2C 100kHz** + `getEvent()` **2회** 호출이 병목. 목표는 50Hz(`IMU_PERIOD_US=20000`) |
| 감속 수단 | **역 PWM 을 의도적으로 쓰지 않는다**(`MIN_RUNNING_PWM=15` 바닥). 목표보다 빠르면 PWM 을 줄일 뿐 **제동은 못 한다** → 터널 경사 하강 시 자유주행. R6 위험 |
| clamp 방식 | 바퀴 상한 초과 시 **좌우를 같은 비율로 축소** → **곡률은 보존되고 속도만 느려진다.** 경로 형상은 안 틀어진다 |
| PI 게인 | `Kp=30, Ki=5, Kd=0`, `USE_PID_D_TERM=false`, 적분 상한 20 PWM |
| 시작 부스트 | `START_BOOST_PWM=75`, 80ms, **1회만**(주기적 반복 아님) |
| 버전 표기 | ⚠ **파일명 v1.4 인데 `FW_VERSION`="…1.3.0", `FW_GIT_SHA`=0.** `/firmware/info` 로 버전 판별 **불가** → **sha256 이 유일한 근거** |

## 2. 실차 반영 지점 (★ 개정 — 시뮬 파일 수정 아님, `tunnel_bringup` 신규 작성)

★ 07-29 갱신: 신규 파일은 **전부 생성됐다** (`tunnel_bringup` S4 골격). 아래 '상태' 열은
파일 존재가 아니라 **그 값이 실제로 반영됐는가**를 가리킨다.

| # | 값 | 반영할 **신규** 파일 | 내용 | 상태 |
|---|---|---|---|---|
| 1 | separation 0.49 · 반지름 **0.053** | `tunnel_bringup/urdf/robot_real.urdf` | 실측 치수로 **새로 작성**(시뮬 URDF 복사 아님, Gazebo 태그 0). 바퀴 반경이 작아지므로 belly clearance 재확인 (URDF 함정 ① — `PITFALLS.md §5`) | ✅ **08-02 반영 완료** (예약 19 ③). separation 0.49(`y=±0.245`) 유지 · `z` **0.065 → 0.053**. 차고는 실측 **20mm** 를 주석에 기록(기구 문제라 URDF 로 못 고친다 — R6 항목) |
| 2 | footprint 0.55×0.57 | `tunnel_bringup/config/nav2_params_real.yaml` | 꼭짓점 = `[[0.285,0.275],[0.285,-0.275],[-0.285,-0.275],[-0.285,0.275]]` (§3). `inflation_radius ≥ 외접반경 0.40` 확인 (현 0.9 여유 있음) | ✅ 반영 (local·global costmap 양쪽) |
| 3 | **IMU** 장착 오프셋 | `tunnel_bringup/urdf/robot_real.urdf` | 실측 후 `base_link→imu_link` 고정 TF | ✅ **08-02 수령** — 3차 회신 §10: 바닥기준 `x/y/z = 0/0/392mm`, `rpy = 0/0/-90°`. → URDF 값 = `xyz="0 0 0.339" rpy="0 0 -1.5708"` (z = 0.392 − 0.053, §1-b). ✅ **08-02 반영 완료** (예약 19 ④) — `tf2_echo base_footprint imu_link` 가 **0.392 m · yaw −90°** 로 나와 **바닥 기준 실측과 일치**함을 확인했다. ⚠ 부호 검산은 여전히 R0 몫이다 |
| 3-a | **라이다** 장착 오프셋 | `tunnel_bringup/urdf/robot_real.urdf` | `base_link→lidar_link` 고정 TF. **라이다는 몸통 최상면보다 위** (자기타격 방지) | ❌ **미실측 — 5번째 미해결.** x/y = 정중앙(0,0)만 수령, **스캔면 높이 z 없음**(라이다 미장착이 원인). `xyz="0 0 0"` + `TODO:` 유지. ★ **08-02 방침 전환 — 장착·측정을 역할 A 가 흡수한다**(§4) |
| 3-b | 몸통 높이 · 차고 · wheelbase | `tunnel_bringup/urdf/robot_real.urdf` | 지상고 확인은 URDF 숫자가 아니라 기구 확인 사항 | 🟨 **부분** — 08-02 수령: **몸통 최저점→바닥 20mm**. ⚠ **매우 낮다** — 터널 바닥 요철·케이블·물웅덩이 걸림 위험(R6 항목). 앞뒤 wheelbase 는 여전히 ❌(시각화 전용이라 주행 영향 0) |
| 4 | TOTAL_PPR **2641.1** · 체배 ×4 | Teensy 펌웨어 | 구동부 몫 — 역할 A 는 `/odom` 결과만 소비 | (구동부) ✅ 08-02 확정 |
| 5 | (시뮬 자산) | `src/tunnel_sim/**` | **변경 금지.** T자·쌍굴 회귀 기준선으로 보존 | ✅ 무변경 유지 |

미실측 항목 전수 확인: `grep -rn "TODO: " src/tunnel_bringup/`

⚠ 실측 footprint 가 시뮬(0.50×0.40)보다 **커서** 폭 2.5m 피난연결통로 여유가 현재 과대평가돼 있다.
실차 설정 작성 후 **좁은 통로 통과 검증 필수** (R6 항목).

## 3. footprint 변환 계산

`가로 0.55`(폭, 좌우 = y) · `세로 0.57`(길이, 앞뒤 = x) → 중심 기준 절반:

```
시뮬(보존):  [[0.25, 0.20], [0.25, -0.20], [-0.25, -0.20], [-0.25, 0.20]]
실차(신규):  [[0.285, 0.275], [0.285, -0.275], [-0.285, -0.275], [-0.285, 0.275]]
               └ 세로/2 = 0.285      └ 가로/2 = 0.275     → 외접반경 √(0.285²+0.275²) ≈ 0.40m
```

## 4. 남은 수령·합의 항목

> ✅ **08-02 정리 완료** — 3차 회신(08-01)까지 반영해 아래 목록을 갱신했다.
> 최종 합의서 = `~/Desktop/TEENSY_실차연동_최종합의서_0802.md` (07-24 합의사항을 **대체**).
> 전수 대장(48건 상태) = `~/Desktop/구동부_합의내용_최종전_.md`.

**✅ 닫힌 것**

- [x] **엔코더 체배 방식** — **quadrature ×4 확정**, `TOTAL_PPR = 2641.1` (4체배 이론값 2652 와 −0.41%).
      ⚠ 사양서에는 "26 Pulses" 라 적혀 있어, 누군가 `26 × 51 = 1326` 으로 "고치면" **odom 이 정확히
      2배** 틀어진다 (3m 를 6m 로 읽음 → SLAM 지도가 2배로 그려짐). 펌웨어 주석에 체배 ×4 명시 권고.
- [x] **타이어 공압 우려 — 전부 철회** — 밸브 없는 **반공압**으로 확인. "어렵게 맞춘 보정이 시간이
      지나며 경고 없이 무효가 되는" 시나리오가 사라졌다. 대신 **좌우 편차를 맞출 수단도 함께
      사라졌으므로** 굴림 반지름 실측(아래)만 남는다.
- [x] **R2 "3m 직진 3%"** — 3차 회신 §7: 3회 평균 **−0.22%** (정밀 로그 −0.30%). ✅ **거리 기준 통과.**
      ⚠ **거리에 한정된 통과다** — 아래 '속도' 항목 참조.
- [x] **좌회전 `angular_velocity.z` 부호** — 양수 확인. 반대였으면 EKF 가 발산할 항목이었다.
- [~] **cmd_vel watchdog 회신 참고값 3종** — 0.010 / 0.002 / 0.134s, 전부 0.5초 이내라는
      구동부 측 결과다. **우리 R0 물리 실측 근거가 아니므로** `FREEZE_MANIFEST.md §6`을 닫지 않는다.
      확정·재개방의 유일한 근거는 `JETSON_SETUP.md §7-c`의 7-c-0 D+0 기록이다.

> ### 🔒 08-02 **최종 수령 종료** — 펌웨어 소스 v1.4 수령 후 전면 갱신
>
> 마지막 수령물 = `~/Desktop/teensy_integrated_base_v1_4/teensy_integrated_base_v1_4.ino` (1307줄).
> `sha256sum` = `13f929cb551ce3aa75d69bb615e04de5a0794c5259501684aae626eec2412106` — 동봉
> `SHA256SUMS.txt` 와 일치. **이 해시가 펌웨어 버전 식별의 유일한 근거다**(§1-d 참조).
>
> **이후 구동부에 아무것도 요청하지 않는다.** 남은 항목은 전부 D+0·D+1 실측으로 닫는다.
> 소스 전수 검토 = `~/Desktop/구동부_합의내용_최종전_.md §10`.
> **정본 우선순위: 소스 > 회신 PDF > 합의서** — 아래에서 회신 PDF 가 틀린 곳이 나왔다.

**✅ 소스로 닫힌 것 (08-02 · 14건)**

- [x] **`frame_id` 3종** — `"odom"` / `"base_footprint"` / `"imu_link"`. 합의서와 **일치**.
      3차에 걸쳐 아무도 묻지 않았던 항목이 소스 3줄로 닫혔다.
- [x] **QoS** — ⚠ **회신 PDF 가 틀렸다.** §1-d 표 참조. `/odom` 은 **RELIABLE** 이다.
- [x] **covariance** — `twist.covariance[0] = 0.02` **존재한다.** "`linear.x` 누락"은 우리 오판이었다.
- [x] **timestamp** — `publishOdometry/Imu` 호출 **직전** stamp = 센서 계산 직후. 수신시각이 아니다.
- [x] **NaN·Inf 방어** — `isfinite()` 검사 후 `stopAllMotors()` + `cmdVelReceived=false`.
- [x] **baudrate** — `set_microros_transports()` 는 **인자가 없다.** USB CDC 라 115200 은 순수
      명목값이고 실제 전송률과 무관하다. 대역폭 초과 우려는 **성립하지 않는 걱정이었다.**
- [x] **★ 속도 +8.32% (구 요청 4)** — **시나리오 (b) 배제 확정.** 거리와 속도가 `DISTANCE_PER_COUNT`
      **하나의 환산계수를 공유**하므로, 줄자 대비 거리가 −0.3% 면 계수가 맞는 것이고, 남는 변수
      `dt` 는 `micros()`(수정발진) 라 7% 씩 틀릴 수 없다. → **`/odom` 속도는 정직하다. EKF 무사.**
      명령 대비 초과분은 저속 PWM 8bit 분해능상 불가피한 PI 정상상태 오차다(1칸 ≈ 0.007 m/s).
- [x] **재연결 후 자동 재가동 금지 — 소스 논리 확인** — 정지 시 **적분항·이전오차·미분항·
      PWM·부스트가 전부 0** 이 되고 `cmdVelReceived=false` 로 떨어진다. 이는 소스상 방어이고
      `FREEZE_MANIFEST.md §6` 확정 근거를 대신하지 않는다. ⚠ **물리 시험은 D+0/D+1에 남는다.**
- [x] **배터리 전압 읽기** — ADC 읽기 코드가 **없다. 미지원 확정** → 요청 철회.

**⚠ 소스로 판정이 뒤집힌 것 — E-stop**

- [~] **물리 E-stop** — 최종 회신 PDF 8쪽에 **한 번도 안 나와** "완전 미응답"으로 판정했으나,
      **소스에는 구현이 전부 있다**: `ESTOP_PIN=21` (INPUT_PULLUP, active-low), 차단 **5지점**
      (`writeMotorPwm` · `updateWheelControllers` · `updateMotorOutputs` · `checkSafety` ×2 ·
      `cmdVelCallback`), `/estop/state` 발행. 주석 = *"hardwired **motor power** cutoff"* 를
      보완하는 소프트 입력이라 명시 → **하드웨어 차단이 별도로 존재**한다는 뜻.
      ⚠ **남은 것은 "실차에 배선돼 있는가" 하나뿐이고, 이것은 소스로 알 수 없다.**
      🔴 **핀이 안 물려 있으면 풀업이 HIGH 로 띄워 "안 눌림"으로 읽힌다** — 배선이 없어도
      소프트웨어는 아무 이상을 보고하지 않는다. **눌러서 바뀌는지만이 증거다**
      (`d0_check.sh` 검사 [8]). **false 에서 안 바뀌면 R1 이상 지면 주행 금지.**

**❌ 남은 것 — 전부 "구동부가 줄 수 없는 것"이다**

- [ ] **라이브러리 버전** — 목록 5종은 확정(`micro_ros_arduino` · `Encoder` · `Adafruit_BNO055` ·
      `Adafruit_Unified_Sensor` · `Adafruit_BusIO`)이나 **버전 번호는 소스에도 없다.**
      구동부도 *"소스만으로는 확정 불가, 인수 시 개발환경에서 함께 확인"* 이라고 회신했다.
      → **인수 현장에서 `~/Arduino/libraries/` 폴더를 통째로 복사받는다.** 번호가 아니라 폴더다.
      ⚠ 이게 없으면 **터널에서 실효 WHEEL_BASE 재조정이 필요할 때 재빌드를 못 한다**(§1-c).
- [ ] **전원·충전 인계** — 회신 §15: 문서 대신 **인수 현장 직접 시연**으로 전환됐다.
      ⚠ **현장에서 끝나면 증거가 안 남는다** → 배터리 라벨·퓨즈 위치·커넥터를 **사진**으로,
      만충/사용중단 전압을 **숫자**로 그 자리에서 받아 적는다. 안 하면 영원히 못 받는다.
- [ ] **횡편차** — 최종 회신 §10: *"수치 미기록, 육안상 큰 쏠림 없음"*. 구동부 주장(*EKF·Nav2
      적용 후 경로추종 오차로 재는 것이 더 적절*)은 **타당하다** — 지금 재는 값은 오픈루프
      편향이고 EKF 가 붙으면 달라진다. → **역할 A 가 D+1 에 측정.**
      ⚠ 그때까지 **오픈루프 편향을 모르는 상태**이므로 첫 직진은 **양옆 1m 이상 비운 자리**에서.
- [ ] **E-stop 배선 여부** — 위 참조. D+0 30초.
- [ ] **4륜 굴림 반지름** — 좌우 편차·슬립 몫 분리에 필요. 장비 없이 5분.
- [ ] **라이다 장착 높이** — ★ **08-02 방침 전환: 장착·측정을 역할 A 가 흡수한다.**
      5번 요청해도 안 온 이유가 **미장착**이므로 6번째도 오지 않는다. 구동부에는 더 요청하지 않는다.
      ⚠ 다만 **브래킷·체결점 등 기구 구조가 기구팀 소관이면 그 부분은 여전히 열려 있다** —
      담당자·예상 일자만 확인한다.
- [ ] 구동부 R0~R2 잔여 항목은 **인수 후 공동 진행** (인수 B안 — §5).

## 5. 인수일 수령 항목 (07-31 신설 — **값이 아니라 환경·절차**)

★ **B안**: 로봇·Jetson 을 **인수일에 일괄 수령**한다. 아래가 없으면 **D+0 에 로봇이 켜지지 않고**,
D+1 첫 스텝(R3 rosbag)이 성립하지 않는다.
전달 문서 = `~/Desktop/TEENSY_실차연동_최종합의서_0802.md` (07-24 합의사항·커버레터를 **대체**).
구판 = `~/Desktop/TEENSY_실차연동_인수요청_커버레터_0731.md` (역사).
**우리 쪽 준비물(런북·udev·agent 확보 등)은 여기가 아니라 `MASTER_PLAN.md §3 S6`** — 책임 경계를 섞지 않는다.

**★ 08-02 갱신** — 3차 회신(08-01) 반영. 10건 중 **4건 해결 · 1건 철회 · 5건 잔여**.

| # | 수령 항목 | 상태 | 비고 |
|---|---|---|---|
| 1 | **통합 펌웨어 1개** — `/odom` + `/imu/data` **동시 발행** | ✅ **해결** | ★ **최우선 요청 해결.** 없었으면 EKF 기동 자체가 불가능했다 |
| 2 | 그 빌드의 **SHA 40자 + 짧은 SHA + 빌드일시** | ⊘ **요청 철회** | git 저장소 미생성 + **자기참조 함정**(SHA 를 소스에 적으면 SHA 가 바뀐다 — 구동부가 §1.2 에서 직접 지적). → **소스 파일을 받아 역할 A 가 `sha256sum` 으로 대체**한다 |
| 3 | **펌웨어 빌드·업로드 환경** (소스·IDE/라이브러리 버전·굽는 절차) | ❌ **잔여** | 최종 합의서 **요청 1** 로 통합. 없으면 터널에서 WHEEL_BASE 재조정 불가(§1-c) |
| 4 | **micro-ROS agent 실제 설정** (transport·baudrate·패키지 버전) | 🟨 **부분** | serial · `/dev/ttyACM0` · **115200** 수령. agent 패키지 버전 미제공. ⚠ 115200 은 대역폭 산술상 불가능(≈410kbps 필요) → **Teensy USB CDC 명목값**으로 해석, 최종 합의서에 반증 요청 |
| 5 | **udev 규칙 `/dev/teensy_drive`** | ✅ **D+0 실측 완료** | "현재 없음" 회신 → **역할 A 가 작성**(S6-3). 08-03 Jetson 실측 `/dev/ttyACM0`, `idVendor=16c0`, `idProduct=0483`, serial `20379630`을 `tools/udev/99-teensy-drive.rules`에 반영했고 재삽입 뒤 `/dev/teensy_drive -> ttyACM0`을 확인했다 |
| 6 | **전원·충전 인계** (켜는 순서·충전기·만충/방전 전압·배선 사진) | ❌ **잔여** | 최종 합의서 **요청 3**. 없으면 **D+0 에 로봇을 못 켠다.** 전압을 모르면 과방전으로 배터리 손상 |
| 7 | **E-stop 장착 + 전원 유지 여부** | ❌ **잔여** | 최종 합의서 **요청 2**. 물리 E-stop **미구매·미장착**. 전원 유지는 **3회 무응답** → 질문이 아니라 **요구사양**으로 전환. `/estop/state`·GPIO 로직은 이미 구현됨 |
| 8 | **장착 좌표 4종** | 🟨 **3/4 해결** | ✅ 바퀴축 **53mm** · IMU **0/0/392mm, rpy 0/0/-90°** · 몸통 지상고 **20mm** 수령. ❌ **라이다 스캔면 높이만 미해결**(미장착) → **역할 A 가 흡수**(§4) |
| 9 | **좌회전 시 `angular_velocity.z` 부호** | ✅ **해결** | 양수 확인. 반대였으면 EKF 발산 |
| 10 | ~~Jetson 여유 디스크·계정·네트워크~~ | ⊘ **요청 철회** | **역할 A 가 이미 관리 중**(08-02). OS 22.04 확인 완료(07-31). 잔여 미확인은 `micro_ros_agent` 확보·성능뿐이고 둘 다 역할 A 몫 |
| **11** | **★ 신규 — 마지막 검증 시험 1회 (약 40분)** | ❌ **잔여** | 최종 합의서 **요청 4**. 3m 3회의 **소요시간·횡편차·yaw 시작/종료** + 4륜 굴림반지름 + 재연결 거동 + 회전 명령값. **같은 바닥·같은 세팅**이어야 이번 데이터와 대조 가능해 인수 후로 미룰 수 없다 |

★ **최종 요청은 4건으로 압축했다** (1·2·3·4 = 소스 / E-stop 장착 / 전원 인계 / 시험 1회).
나머지 측정 항목은 전부 역할 A 가 인수 후 수행한다 — 상세 = `~/Desktop/TEENSY_실차연동_최종합의서_0802.md`.

**8번 측정 기준면** — 라이다는 **바닥 기준**으로 받는다. `base_link` 는 바퀴축이라 실제로 재기 어렵고,
기준면을 안 정하면 기구팀은 바닥 기준으로 재고 역할 A 는 축 기준으로 알아들어 **수 cm 오차가 조용히 생긴다.**
변환은 `z = (받은 높이) − (8번의 축 높이)` 로 역할 A 가 수행한다.

⚠ **8번의 축 높이를 "유효 반지름 0.05698" 로 대체하지 않는다.** 0.05698 은 잰 값이 아니라
**3m 거리를 맞춰 역산한 계수**이고 `실제 눌린 반지름 + 구동 슬립 + PPR 잔차` 가 섞여 있다.
그대로 URDF 에 넣으면 최대 8mm 틀리고, 그 오차가 라이다·IMU 높이 전체로 전파된다
(상세 = `~/Desktop/TEENSY_실차연동_3차요청_0730.md §9.3` ·
`~/Desktop/TEENSY_실차연동_3차요청_0730.md §9.4`).
