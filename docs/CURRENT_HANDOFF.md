# CURRENT_HANDOFF.md — 지금 할 한 묶음 (매 세션 시작점)

> 작업 완료 시 구현자가 갱신하고 검토자가 사실관계·테스트 결과를 확인한다.
> **현재 묶음 + 미해결 보류만** 유지한다 (`AGENTS.md §5`) — 완료된 서사는 정본으로 보내고 링크한다.

- **동결 기준점**: `212885a8292d1e86677d5beeb9f5358d76fe9b40` = tag
  **`platform-core-freeze-260724`**. 증거 = `docs/FREEZE_MANIFEST.md`.
- **현재 기준 커밋**: 이 파일과 같은 커밋(main).
- **현재 단계**(이력 정본 = `MASTER_PLAN §8`): 5단 완료 · 6단(역할 B V1)은 회의 대기
  → D+0 검사 1~8 OK(08-07) · **D+0 잔여 0** — 마지막 `#11` 이 08-11 에 닫혔다(검토 §59)
  → 전기 시공 완주 · 검토 §51~§56 회수 · 릴레이 P0 수용(`§16-e`)
  → re-arm 설계·구현·동결 → 지문 이관 `rc=0` → **굽기 완주**(`§7-c-E` 13/13 · `§5-G6` 10/10)
  → §7-c-0 `#11` 영상 분석(`a642882`) · 검토 §57 보완(`d3c2a80`)
  → 검토 §58 보완(`aaa5076`)
  → §7-c-0 재측정 기록 · 검토 §59 종결(`cd3ddbf`·`51b158a`)
  → **§7-c 지면 사다리 1일차 — R1 통과 · 예약 32 로 정지** **(지금 — 2026-08-11)**

> ✅ **re-arm 래치는 보드에 있다** (`build=Aug 11 2026 15:13:20`). 서사 = `FREEZE_MANIFEST §10.25`.
> ✅ **`#11` 종결**(08-11 재측정 + 검토 §59) · `TODO(D+0)` **전량 종결** · **R1 직진 통과**.
> 🔴 **그러나 지면 주행은 다시 멈췄다 — 실제 속도가 명령의 약 2.6배다**(예약 32).
> **다음 세션 최우선 = 피드포워드 교정 + 재굽기.** 그전까지 **바퀴를 바닥에 내리지 않는다.**

**⚠ 미해결 보류 — 어느 것도 승인 완료로 읽지 말 것** (본문 정본 = `MASTER_PLAN.md §7`):

- **⚠ 미해결 보류 — 예약 23 / 검토 §33 (`8fcc1a2`)**: P0 0 · **P1 1** · P2 1.
  `handoff_single_check.sh` 는 `§` 없는 새 작업 꼬리를 붙여도 `OK` 를 내고, 숫자 계약 전수
  증거는 기록 6자리와 독립 실행 21자리가 불일치한다. **승인 완료로 읽지 말 것.** 런타임
  `src/**` 변경·회귀는 없으므로 현재 D+0 실행은 막지 않되, 보완은 다른 묶음에 섞지 않고
  `MASTER_PLAN.md §7` 예약 23 의 완료판정과 부정 회귀로 별도 구현한다. 전문 =
  `~/Desktop/개발현황/CODEX 현황/0801검토현황.md §33`.
- **예약 24 / §38** P1 2 · P2 4 — 라이다 실측·readiness flake 소유 정본이 갈라짐.
  **라이다 높이는 D+1 소유**이며 `TODO(D+0)` 목록 계약은 그대로다.
- **예약 26 / §40** — 사용자 결정 **'방법 B'** 미구현(결정은 `MASTER_PLAN §8` 에 복원).
- **예약 27** — 이 Jetson 에 `nvcc`·`nvidia-jetpack` 이 **없다.** 역할 A 는 비차단이지만
  **역할 B 성능 전제는 통째로 바뀐다.**
- **예약 28** — §44·§45 전기 P1 4건 중 **③④ 08-10 종결** · **①② 미해결**.
- **예약 29** (08-07) — 정본의 활성 계약·이력 절 분리. ⏸ 실차 사다리 뒤.
- **예약 30** (08-10) — §53 전기 P1 **6건**. 🔴 **5건이 같은 실수 하나**(통전 정격으로
  개폐·차단용량까지 승인) — **완료판정이 부품 품번이라 AI 왕복으로 안 닫힌다.**
- **예약 31** (08-11) — Desktop md **70건**(계약 원본 7건)이 **git 밖 · 백업 없음**. 1단계 =
  `개발현황/` private repo 화(이동 없음 · 위험 0 · ~10분). ⏸ **사용자 호출 시 즉시.**
  🔴 **08-11 에도 안 했다** — 굽기 가이드 블록 2 가 이것이었고 건너뛴 유일한 `[책상]` 블록이다.
- 🔴 **예약 32** (08-11) — **실제 속도가 명령의 약 2.6배.** 🔴 **지면 주행 정지 사유**이고
  `§7-c-0` `(c)` 위험 수용 문장의 전제를 깼다. 다음 세션 최우선.
- **⏸ 6단 보류** — 역할 B 회의가 잡히면 `30c5e87`의 핸드오프 원문을 그대로 복원한다.

## 이번 한 묶음 목표 — §7-c 지면 사다리 1일차 기록 · Tier B

> 08-11 현장 세션(R1·R2)의 실측을 정본에 넣고 **지면 주행을 멈춰 두는 일**이다.
> 변경 표면 = `docs/**` → `AGENTS §6` **Tier B**(검토 왕복 없음).

**① 좌전륜 엔코더 부호 반전 — 발견하고 고쳤다.** R1 1차(`r1_ground_0811_2047`)에서 육안은
직진인데 `/odom` 이 `69.5°` 회전을 주장했다. 바퀴별 분해(`enc_check_0811_2102`)로 좌전륜만
`Δyaw` 양수 = 우측으로 계산됨을 확정 → **A/B 배선 교환**(펌웨어 무변경) → 2차
(`r1_ground_0811_2127`)에서 **횡편차 1.8% · yaw 2.00°(IMU 1.70° 교차) · odom 676 vs 줄자
685mm**. ✅ **오도메트리는 정상이다.**

🔴 **② 속도가 명령의 약 2.6배 — 오늘의 정지 사유.** `0.05 → 0.123` · `0.12 → 0.3265 m/s`
(`d0_drive_0811_2146` · 3.000m/9.47s). **바닥이 아니라 비례 오차**다 — FF 가 `145 PWM` 로
포화하는데 그 PWM 이 `0.33 m/s` 를 내고 PI 권한은 **약 26 PWM** 뿐이다.
🔴 **`MAX_LINEAR_CMD=0.12` 는 명령 클램프이지 물리 속도 한계가 아니다.**
전문·교정값(`≈375`) = **예약 32** · 함정 = `PITFALLS §11`.

- 🔴 **`§7-c-0` `(c)` 위험 수용 문장은 효력 정지** — `7cm` 가 실제 **약 20cm**.
  🔴 **사용자 소유 문장이라 구현자가 고쳐 쓰지 않는다.** 다시 쓰는 조건 = 교정 후 실측 정지거리.
  ⚠ 21:30 갱신안(`0.13`/약 8cm)은 승인됐다가 30분 뒤 3m 실측에 뒤집혀 **채택되지 않았다.**
- ⚠ **회전 시험(`§7-c-2`) 미실시** — 붙여넣기 줄 연결이 깨져 명령이 안 나갔다(IMU 앞뒤 동일).
- 🔴 **`#11` 종결(§59)은 유효하나 R1 허가 근거로는 못 쓴다** — 승인의 한 축이 그 문장이다.

## 완료조건

1. `doc_check.sh --strict` rc=0 · `handoff_single_check.sh` OK · `test_ai_context` **37**.
   회귀 정본 불변: pytest **182 passed** / colcon **245 tests** / harness guards **24 검사** /
   gate regression **14 케이스** · `test_watchdog_video` **54** · `test_watchdog_report` **20**.
2. 🔴 **`firmware/` diff 0건 · `firmware_precheck` `rc=0` 유지.** 🔴 **지문을 옮기지 않는다.**
   이 묶음은 **기록만** 한다 — 피드포워드 교정은 예약 32(다음 세션).
3. 🔴 **지면 주행 금지.** R2·블록 8 은 예약 32 교정 + 재검증 전까지 열지 않는다.
4. 🔴 **`(c)` 사용자 위험 수용 문장을 구현자가 고쳐 쓰지 않는다** — 효력 정지만 표시하고,
   다시 쓰는 것은 교정 후 실측 정지거리로 **사용자가** 한다.
5. 🔴 **전기계통 문서는 건드리지 않는다.** 예약 30 은 속도 상한 상향까지 동결이다.

## ★ 현장 운용 — 시공은 끝났고 **절차가 바뀌었다** (상세는 링크가 정본)

> 시공안 `§5-A`~`§5-G8` **완주**(08-06~07). `H2` 사진 · `H3` 여분 보관만 잔여.
> 물리 E-stop 은 **있고 실측으로 동작한다** — ⚠ 관찰된 부하는 `0.05` **명령**(실제 그 2.6배)이고 끼임 11A 는 미관측이다.

1. 🔴 **기동 순서를 어기면 Teensy 가 영구히 멈춘다** — `퓨즈 → XT90 → Jetson 부팅 대기 →
   agent → **Teensy VIN 만 재삽입**`. 재연결 로직이 없어 `errorLoop()` 에 갇힌다
   (`§5-e`). 증상 = 토픽이 `rosout`·`parameter_events` 뿐.
2. 🔴 **종료는 `Jetson sudo shutdown -h now` 가 맨 앞** — 로직이 배터리로 도는 지금
   XT90 분리는 **리눅스 전원 뽑기**다 (`ELECTRICAL_BASELINE.md §7`).
3. 🔴 **배선을 만질 땐 XT90 분리다 — E-stop 으로 대신하지 않는다.** E-stop 은 **모터 가지만**
   끊는다. 유일한 예외 = 트레이스 절단 후의 펌웨어 업로드 (`FIRMWARE_REBUILD.md §5-b`).
4. 🔴 **되돌리면 안 되는 소스 3줄** — `.ino:111 = false` · `.ino:125-126 = 0.12 / 0.50`.
   오염 판정은 **`bash tools/firmware_precheck.sh` 뿐**이고 🔴 **`rc=2` 는 판정 불능이지
   통과가 아니다** (`FIRMWARE_REBUILD.md §4`).
5. 🔴 **브레드보드 상태다** — PWM/DIR 이 빠지면 **"돌 수도 있는"** 고장이다.
   🔴 버전 판별은 `/firmware/info` 가 아니라 `ros2 topic type /drive/diag` 다(v1_3 고정 문자열).
6. ✅ **`TODO(D+0)` 11건 전량 종결** — `#11` 이 08-11 재측정 + 검토 §59 로 닫혔다.
   다음 현장 세션 첫 명령 = `JETSON_SETUP.md §11-g`.
7. 🔴 **다음 굽기 때 PROGRAM 버튼을 누르지 않는다** — 10초 기다렸다 타임아웃 때만 누른다.
   성공하면 `§5-a` 승격, 실패해도 손실 0인데 **2회 연속** 못 닫았다 (`FIRMWARE_REBUILD §5-a`).

## 사용자 결정 — 유지

- 동결 예외 4회 소진 — `src/**` 를 다시 열려면 사용자 승인(`FREEZE_MANIFEST.md §10`).
- 순서 = **예약 32 교정** → R1·R2 재시행 → D+1 R3 rosbag → R4~R8 (`MASTER_PLAN.md §3`).
- 예약 20~31 과 역할 B 6단은 트리거 전까지 별건. 🔴 **32 만 예외 — 다음 세션 최우선.**
- `JETSON_SETUP.md §9` `TODO(D+0)` **11건**은 08-11 에 **전량 종결**됐다(마지막이 `#11`).
- ✅ **08-07 검사 1~8 전항목 OK.** ⚠ **주기의 상한은 증명하지 않는다**(정본 = R3 히스토그램).
- never-seen은 다른 놓침과 같이 역행 2회 → 보고 → 단독 탈출이다. 실차 R3 뒤 재평가한다.

| 트랙 | 범위 | 상태 |
|---|---|---|
| **진행 중 묶음** | §7-c 지면 사다리 1일차 기록 | **Tier B — 검토 왕복 없음.** `firmware/**` diff 0건 · 변경 표면 = `docs/**` + `tools/**` |
| ✅ `#11` R0 watchdog | 재측정 `516.0 ms` · 조건 2 ✅ · 정식화 확인 완료(§59) | ✅ **종결.** 🔴 **다만 R1 허가 근거로는 못 쓴다** — 승인의 한 축인 사용자 위험 수용 문장이 예약 32 로 효력 정지다. 재개방 = 속도 계통 · 재굽기/안전 배선 · 자율 발행자 |
| 🔴 예약 32 속도 계통 | 명령의 **2.6배**(`0.05→0.123` · `0.12→0.3265`) · FF 포화 + PI 권한 26 PWM | 🔴 **지면 주행 정지 사유.** 교정값 `≈375`(실측 2점) · **재굽기 = `§7-c-E` 13행 + `§5-G6` 10회.** 다음 세션 최우선 |
| ✅ re-arm 래치 | `d0bf9f8` → §54 → §55 → §56 동결 → 지문 이관 → 굽기 완주 | ✅ **보드에 있다**(`build=Aug 11 2026 15:13:20`) · §7-c-E 13/13 · §5-G6 10/10 |
| ⚠ §56.1 P1 | 전송 실패해도 보드만 무장 · **코드 미수정** | ⏸ **조건부 수용.** 재개방 1순위 = **자율 발행자가 실차 `/cmd_vel` 에 붙기 전**. 정본 = `REAL_ROBOT_VALUES §1-f` ⓵ |
| 전기계통 시공 | `전기계통_최종작업안_0806.md §5` (사용자 직접) | ✅ **A~G8 완주** · 🔴 `H2` 사진 · `H3` 여분 보관 잔여 |
| ⚠ 릴레이 | 🔴 정격 미증명 · 08-10 사용자 수용(A안 · `§16-e`) | 🔴 **전제 1(`0.12` 유지)의 뜻이 흔들렸다** — 명령은 그대로지만 실제 속도가 2.6배로 관측됐다. 예약 32 와 같이 재판정 |
| ⏸ 예약 30 (전기 P1 6) | 등록 완료 · **동결** | ⏸ **트리거 = 속도 상한 상향(≈R5·R6).** 전기 문서 작업은 멈춘다 |
| 배터리화 후속 (`§8`-⑰⑱⑲) | XT90 전류 · 로직 보호 · Jetson 급전 | ✅ ⑱(12V=5A·5V=3A, 🔴 미설치) · 🔴 ⑰ 이월 · 🟡 ⑲ **R1·R2 한정 허용 — 🔴 그 '저속' 전제는 관측된 적이 없다**(예약 32) |
| ✅ 검토 슬롯 | §44~§59 수행됨(**§59 = `cd3ddbf` 증거 승인 · `#11` 종결**, 사슬 3회차 = 상한) | ✅ **사슬 종료.** §59 P2 2건은 **왕복 없이** 이 묶음에서 닫았다. 다음 왕복 = §56.1 코드 보완. §53 = 예약 30 |
| 예약 21 · 24 | D0/R3 회귀 편입 + readiness 간헐성 / §38 정본 소유권 | ⏸ 다음 bringup 변경 전 |
| 예약 23·26·27·29·31 | 숫자 계약 / 방법 B / 역할 B CUDA / 활성-이력 분리 / Desktop 백업 | ❌ 각자 트리거 전 별건 |
| 역할 B V1 합의 | 커밋 `30c5e87` 원문 | ⏸ 회의 대기 |

## 직전 완료 — `cd3ddbf`·`51b158a` §7-c-0 재측정 + 검토 §59 종결 (정본 = `JETSON_SETUP §7-c-0`)

## 보존해야 할 안전 불변조건

- ✅ **`/cmd_vel` 금지 불변조건은 2026-08-07 검사 8 통과로 해제됐다.** `Ctrl+C` 를 E-stop 으로
  승격하지 않는다는 규칙은 그대로다. 🔴 **명령은 `0.12` 안에서만 쓰되, 그것이 물리 속도를
  묶지 않는다는 것이 08-11 에 관측됐다**(예약 32).
- 🔴 **R1·R2 지면 주행은 다시 금지다** — re-arm·`#11` 두 사유는 08-11 에 닫혔지만
  **예약 32(실제 속도 2.6배)** 가 새 사유다. 🔴 **`0.12 m/s` 는 명령 클램프일 뿐 물리
  속도를 묶지 않는다** — 그 위에 선 `600ms` 수용(정지거리 7cm)은 **효력 정지**이고,
  실측 정지거리로 사용자가 다시 써야 열린다. 그 밖에 안전요원 E-stop · 양옆 1m ·
  경사 금지 · 네 바퀴 방향 육안은 매번 그대로 지킨다.
- ✅ **`/estop/state` 는 신뢰한다**(08-11 `§5-G6` 10/10 재충전) — 이 토픽을 소비하는 로직을
  만들어도 된다. 🔴 **배선을 만지거나 펌웨어를 다시 구우면 10회를 다시 하기 전까지 신뢰를
  회수한다.** ⚠ 관찰된 것은 무부하와 **`0.05` 명령 부하**뿐이고(실제 속도는 그 2.6배였다) **끼임(약 11A) 차단은 미관측이다.**
- 🔴 **배선 전 XT90 분리 + 퓨즈 제거**(메인 스위치 없음) · 만졌으면 **개폐 10회 재검증** ·
  **XT90 분리 앞에 `Jetson shutdown`**.
- 🔴 **펌웨어 업로드는 모터 전력 0V·바퀴 공중에서만**(`FIRMWARE_REBUILD.md §5`) · 빌드 지문
  `ARDUINO=10607`·`TEENSYDUINO=158` 불일치면 업로드 금지.
  🔴 **트레이스 절단 후에는 `E-stop 누른 채 XT90 연결`**(`§5-b`) + **모터 단자 0V 직접 측정**.
- 🔴 **`.ino:111 = false` · `.ino:125-126 = 0.12 / 0.50` 을 되돌리지 않는다.**
- 🔴 **펌웨어를 또 구우면 `§7-c-E` 13행과 `§5-G6` 10회를 **다시** 한다.**
  설계·순서 정본 = `REAL_ROBOT_VALUES.md §1-f`.
- ⚠ **watchdog 정지는 무장을 풀지 않는다**(08-11 실측) — 발행자가 멈췄다 살아나면 재무장 없이
  다시 구른다. 수동 텔레옵이라 지금은 절차로 덮이고, **자율 발행자가 붙을 때 §56.1 과 같이 판단**한다.
- 🔴 **모터 명령 전에 네 바퀴가 같은 방향으로 도는지 본다** — `d0_check` 검사 6은 손으로 굴려
  보는 **엔코더 부호** 검사라 구동 방향을 검사하지 않는다. 08-07 에 DIR 핀 이탈로 좌전륜이
  혼자 역회전했고 **육안 배선 점검은 못 잡았다** (`PITFALLS.md §10`).
- 판정기가 **관측하지 않은 것을 말하지 않는다** — 상한 초는 계약값 표기이지 경과 주장이 아니다.
- 설정은 대상 소프트웨어가 실제로 읽는지 `param list`·`topic info -v`로 확인한다.
- N초 관측은 같은 관측자가 종료 상태와 **벽시계 경과시간**으로 창을 완주해야 한다.
- 토픽별 계약의 단일 출처는 `tools/bag_gap_report.py`의 `TOPIC_POLICY`다.
- `/odom`은 RELIABLE, `/imu/*`는 BEST_EFFORT다. EKF 소비자 QoS까지 함께 본다.
- Teensy 재연결 로직 없음, IMU 치명 실패 시 노드 자체가 안 뜸, 시각 역행 시 stamp가 사실상
  멈춘다는 펌웨어 전제를 유지한다.
- 실차 없이 확인한 합성 bag·가짜 `ros2` 결과를 실차 통과로 승격하지 않는다.
- `TODO(D+0)` **11건**을 추측값으로 닫지 않는다 — 미해결은 이유와 재확인 시점을 적는다.

## 금지 범위

- 사용자 추가 승인 없는 `src/**` 변경. 🔴 **이 묶음의 변경 표면은 `docs/**` + `tools/**` 뿐이다.**
- 🔴 **`firmware/**` 를 여는 것.** 주석 한 줄도 안 된다 — sha256 이 바뀌면 굽는 물건이
  **검토받은 물건과 달라진다.** §56.1 코드 보완은 **다음 펌웨어 묶음**이다.
- 🔴 **구현자가 `§7-c-0` 판정선을 정하기.** 사용자가 고른 것은 **"ⓐ로 간다"** 이고,
  구현자가 적은 정식화(1-a 구조 + 1-b `≤600ms`)는 **검토 대상**이지 확정이 아니다.
- 🔴 **영상 수치를 조건 1 통과 근거로 인용하기** — 하한이다(`JETSON_SETUP §7-c-0`).
- 🔴 **08-07 증거를 현행 펌웨어 결과로 승계하기** — 그 판단은 **검토자 몫**이다.
- 🔴 **위험을 "덮였다"로 기록하기** — 조건부 수용은 전부 전제조건 + 재개방 조건 형식으로만 쓴다.
- 🔴 **harness 통과를 안전의 증거로 쓰기.** 증거는 **변이로 죽는 것**이고, 🔴 **`rc=2` 는
  판정 불능이지 통과가 아니다**.
- 🔴 **등록을 종결로 쓰기.** 예약 28·29·31 이 생겼다고 고쳐진 것이 아니다.
- 🔴 **`§5-G8` 을 "부하 차단 검증 완료"로 뭉뚱그리기** — 관찰된 것은 `0.05 m/s` 정상 전류뿐이다.
- 🔴 **핸드오프 상한에 걸렸다고 상한을 올리기.** 먼저 **옮길 완료 서사**를 찾는다.
- 예약 20·21·22·23·24·26·27·28·29·30·31, 역할 B V1 을 이 묶음에 혼합.
- `make_map.sh` 실런, 지도 자산 변경, Gazebo E2E 동시 실행.

## 완료 판정

**현행 펌웨어로 잰 총 정지가 `1-b ≤600ms` 안에 있고, 조건 2·3 이 같은 시행에서 관찰되고,
그 증거가 정본과 조건 3 기록에 들어가 있으며, 활성 결정 절에 "확인 보류·재측정 없음" 이라는
옛 상태 문자열이 남지 않고, 핸드오프·진행표·`TODO #11` 이 모두 **`#11` 종결 / R1 진입 가능**
한 방향을 가리키면 완료다(§59.2 한 문장 완료판정).
🔴 `firmware/` diff 0건 · `firmware_precheck` `rc=0` 유지 · 08-07 승계 불인정 · 영상 하한 ·
재개방 조건은 역회귀로 유지한다.**

## 완료 후 다음 단계 — 🔴 **실차 사다리가 우선이다**

🔴 **`2026-08-15` 플랜 B 판정일까지 4일.**

1. 🔴 **예약 32 — 피드포워드 교정 + 재굽기. 최우선이고 다른 것이 여기 얹혀 있다.**
   교정값 `≈375`(실측 2점 · 굽기 전 중간 점 1개 더 재는 편이 낫다) → 굽기 →
   **`§7-c-E` 13행 + `§5-G6` 10회 재시험** → 지면 실측으로 `0.12 ±10%` 확인.
2. 🔴 **새 사용자 위험 수용 문장** — 교정 후 **실측 정지거리**로 사용자가 쓴다. 그전까지
   `§7-c-0` `(c)` 는 효력 정지이고 `516.0ms` 를 R1 허가 근거로 쓰지 않는다.
3. **R1 재시행** → **R2 3m ±3% · yaw 2π ±10%**. `TODO(D+0)` **11건**은 이미 전량 종결이라
   여기서 다시 열리는 것은 없다. 🔴 **R1 통과 직후 = §56.1 코드 보완
   기한**(자율 발행 전). ⚠ R2 회전은 **ω ≥ 0.07 rad/s** — `MIN_EFFECTIVE_WHEEL_CMD=0.020`
   이 물리 하한 `0.0645 rad/s` 를 만든다(예약 22). ⚠ 회전 명령은 **한 줄로** 친다(08-11 실패).
4. **예약 31** — Desktop md 70건 private repo 화. ~10분 · 위험 0 · **오늘도 안 했다.**
5. **팀 미팅 자료** — 역할 B V1 계약 + 예약 27(CUDA·JetPack 부재). 승인·검토 불필요.
6. D+0 증거 복제 · `H2` 사진 → R3 rosbag → 예약 22·29 와 R4~R8.
   🔴 **`R0→R1→R2→R3` 순서를 건너뛰지 않는다** (사다리 정본 = `MASTER_PLAN §3`).

## 근거 문서

- 규율 티어·회차·은퇴 = `AGENTS.md §6` · 결정 색인 = `MASTER_PLAN §8` · 예약 = `§7`
- 전기 위험·안전선 = `ELECTRICAL_BASELINE §11`~`§13` · 시공 = `~/Desktop/전기계통_최종작업안_0806.md`
- 펌웨어 재빌드·기대 내용 sha256 = `FIRMWARE_REBUILD §4`,`§5`
- 실차 값 = `REAL_ROBOT_VALUES §1`,`§4`,`§5` · 실행 순서·TODO = `JETSON_SETUP §9`~`§11`
- 검토 라우팅 = `TEST_GATES §7` · 검토 전문 = `~/Desktop/개발현황/CODEX 현황/0811검토현황.md §54`~`§56`
