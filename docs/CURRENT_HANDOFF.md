# CURRENT_HANDOFF.md — 지금 할 한 묶음 (매 세션 시작점)

> 작업 완료 시 구현자가 갱신하고 검토자가 사실관계·테스트 결과를 확인한다.
> 이전 묶음 내용은 날짜별 현황으로 보내고, 이 파일에는 현재 작업과 직전 주장만 유지한다.

- **동결 기준점**: `212885a8292d1e86677d5beeb9f5358d76fe9b40` = tag
  **`platform-core-freeze-260724`**. 증거 = `docs/FREEZE_MANIFEST.md`.
- **현재 기준 커밋**: 이 파일과 같은 커밋(main).
- **현재 단계**: 마스터플랜 5단 완료 → 역할 B V1 합의(6단)는 회의 대기 보류
  → 실차 D+0 인수·연결 **부분 완료(`d0_check` 종료 2 · 검사 8 미확인)**
  → 검토 §39 불승인·§40~§42 회신 수령(08-04)
  → **전기계통 정본 v2 전면 개정**(08-04)
  → §39 문서·코드 표면 보완 = **예약 25 종결**(08-05 `870f1da`~`3c62381`)
  → 전기계통 수급 실물 반영 + 착수 전 게이트 통과 = 정본 v3.1 (`10aa4c6`·`4e8d060`)
  → 검토 §43 불승인 보완 = 정본 v3.2 (`86b6ed4` · 검토 §44 **미수행**)
  → **전기계통 실물 시공 완주 = 정본 v3.3 · 검토 §45 대상** **(지금 — 2026-08-07)**

> 🔴 **검토가 두 묶음 밀려 있다.** v3.2(`86b6ed4`)의 **§44 재검토가 아직 안 됐고**, 이 묶음은
> 그 위에 실물 시공을 얹었다. 검토자는 두 슬롯을 갈라 본다 — §44 = **문서가 무엇을 되돌렸나**,
> §45 = **실물이 그 되돌림을 어떻게 종결시켰나**.
> §43 판정(불승인 · P0 1 · P1 8 · P2 2) 전문 = `~/Desktop/개발현황/CODEX 현황/0801검토현황.md §43`.

- **⚠ 미해결 보류 — 예약 23 / 검토 §33 (`8fcc1a2`)**: P0 0 · **P1 1** · P2 1.
  `handoff_single_check.sh` 는 `§` 없는 새 작업 꼬리를 붙여도 `OK` 를 내고, 숫자 계약 전수
  증거는 기록 6자리와 독립 실행 21자리가 불일치한다. **승인 완료로 읽지 말 것.** 런타임
  `src/**` 변경·회귀는 없으므로 현재 D+0 실행은 막지 않되, 보완은 다른 묶음에 섞지 않고
  `MASTER_PLAN.md §7` 예약 23 의 완료판정과 부정 회귀로 별도 구현한다. 전문 =
  `~/Desktop/개발현황/CODEX 현황/0801검토현황.md §33`.
- **⚠ 미해결 보류 — 예약 24 / 검토 §38**: P0 0 · **P1 2** · P2 4. 라이다 실측 소유 시점과
  readiness flake 소유 정본이 갈라졌다. **라이다 높이는 D+1 소유**이며 `JETSON_SETUP.md §9`
  의 `TODO(D+0)` 목록 계약은 그대로다.
- **⚠ 미해결 보류 — 예약 26 / 검토 §40**: 사용자 결정 **'방법 B'** 가 구현되지 않았고 저장소
  에서도 사라졌던 건. 결정은 `MASTER_PLAN.md §8` 에 복원했다. **§37.3 을 닫힌 것으로 인용하지
  않는다.**
- **⚠ 미해결 보류 — 예약 27**: 이 Jetson 에 `nvcc`·`nvidia-jetpack` 이 **없다**. 역할 A 는
  비차단, **역할 B 성능 전제는 통째로 바뀐다.** 역할 B 회의 안건.
- **⏸ 6단 보류**: 역할 B 회의가 잡히면 커밋 `30c5e87`의 핸드오프 원문을 그대로 복원한다.

```bash
git show 30c5e87:docs/CURRENT_HANDOFF.md
```

## 이번 한 묶음 목표 — §45 전기계통 실물 시공 완주 = 정본 v3.3

**2026-08-06~07 에 시공안 `§5-A`~`§5-G8` 을 사용자가 완주했다.** 이 묶음은 그 실측을 정본에
반영한 것이며 **새 설계 회차가 아니다.** v3~v3.2 는 문서 개정이었고 **이번엔 실물이 바뀌었다.**

- **변경 표면** = 문서 + **`firmware/` 1줄**(`ESTOP_ACTIVE_LOW true→false`, `.ino:111`)과 그 주석.
  🔴 **`src/**` 는 열지 않았다.**
- **실물 원본 기록**(수치·중단·절차 지적 15건) = `~/Desktop/전기계통_시공기록_0806.md`.
  정본은 **판정만** 갖는다 — `ELECTRICAL_BASELINE §14`.

### 닫은 것 / 안 닫은 것 (섞지 않는다)

| | 항목 |
|---|---|
| ✅ **닫힘 3건** (v3~v3.2 는 0건) | `§2`-⑧ E-stop fail-danger(`G6` 10/10 + `G8` 10/10 + **검사 8** — v3.2 가 건 승격 조건 3개와 같다) · `§2`-⑩ Teensy 급전 · `§2`-⑪ backfeed |
| 🟡 **격하 (닫힘 아님)** | **릴레이 DC 차단 정격** — datasheet 가 25.2V 를 덮어 **P0 → 🟡**(§15). 🔴 개체 제조사 미상 · `40A` 는 간헐 정격 · **끼임 11A 미관측** |
| 🔴 **안 닫힘** | **30A 퓨즈 협조** · **2A 유리관 차단용량**(둘 다 **각인이 없어** 촬영으로 못 닫는다) · `§2`-③ XT90 개폐 · `§2`-② 배터리 미고정 · `§2`-⑬ re-arm · 릴레이3 건접점 |
| 🔴 **새로 열림** | **로직 전원 배터리화**(작업안 `§8` 제외 항목인데 같은 날 수행)로 `§7` 재개방 조건 **2건 발동.** XT90 전류 · 로직 국소 보호 · Jetson 급전 안정성 **전부 미수행**(`§8`-⑰⑱⑲) |

🔴 **PIN21 완료는 re-arm 래치를 닫지 않는다.** R1·R2 차단 사유는 그대로다.

## 완료조건

1. `ELECTRICAL_BASELINE §14`·`§15` 가 **닫힌 것 / 격하 / 안 닫힌 것 / 새로 열린 것**을 가르고,
   각 판정에 **실측 근거**가 붙어 있다. `§2`-⑧⑩⑪ 본문도 같이 바뀌었다.
2. 🔴 **`G8` 의 한계가 명시돼 있다** — `0.05 m/s` 에서만 했고 **끼임(약 11A) 차단은 미관측.**
   "부하 차단 검증 완료"로 뭉뚱그리면 그것이 발견사항이다.
3. `FIRMWARE_REBUILD §5` 실측 반영(udev 선행 · `board list` 두 줄 · 절단 후 우회 `§5-b`).
   🟡 **"버튼 없이 되는지"는 미확인.** `PITFALLS`·`REAL_ROBOT_VALUES §1-e`·`JETSON_SETUP §9-#10` 정합.
4. `doc_check` · `handoff_single_check` · `test_ai_context` 전량 OK.
5. 회귀 정본이 그대로다: pytest **182 passed** / colcon **245 tests** /
   harness guards **24 검사** / gate regression **14 케이스**.
   ⚠ `src/**` 무접촉이라 직접 실행 대상은 `doc_check` · pytest · `test_ai_context` 뿐 —
   나머지는 **재실행했다고 쓰지 않는다.**

## ★ 이번 회차 최우선 확인 사항 — 시공은 끝났다. **운용 절차가 바뀌었다**

> 2026-08-07 현재: 시공안 `§5-A`~`§5-G8` **완주**. `§5-H2`(사진)·`H3`(여분 보관)만 잔여.
> 물리 E-stop 은 **있고, 무부하·저부하에서 실측으로 동작한다.**

1. 🔴 **기동 순서 — 지키지 않으면 Teensy 가 영구히 멈춘다.**
   `퓨즈 → XT90 → Jetson 부팅 대기 → agent 기동 → **Teensy VIN 만 재삽입** → 토픽 확인`.
   XT90 을 올리면 Teensy(5초)가 Jetson(30초+)보다 먼저 깨서 `rclc_support_init` 이 실패하고,
   **재연결 로직이 없어 `errorLoop()` 에 갇힌다.** 증상 = 토픽이 `rosout`·`parameter_events` 뿐.
2. 🔴 **종료 순서 — `Jetson sudo shutdown -h now` 가 맨 앞이다.**
   `Jetson 종료 → 완전 종료 확인 → 소프트웨어 종료 → E-stop 누름 → XT90 분리 → 0V 확인 → 퓨즈 제거`.
   로직이 배터리로 도는 지금 **XT90 분리 = 리눅스 전원 뽑기**다.
3. 🔴 **배선을 만질 땐 XT90 분리다. E-stop 으로 대신하지 않는다** — E-stop 은 **모터 가지만**
   끊는다. **유일한 예외**는 트레이스 절단 후의 펌웨어 업로드이며 `G8` 통과에 기댄다(`§5-b`).
4. 🔴 **되돌리면 안 되는 소스 3줄** — `.ino:111 = false`(되돌리면 `§2`-⑧ 재개방) ·
   `.ino:125-126 = 0.12 / 0.50`(**릴레이 정격 수용의 전제**. 올리면 끼임 11A 추정부터 다시 한다).
   구동부 대면 합의 6건 = 시공기록지 `F 선행 합의`. **구동부는 펌웨어에 관여하지 않는다(구두).**
5. 🔴 **브레드보드 상태다 — 가동 전 점검이 필요하다.** PWM/DIR 이 빠지면 "안 도는" 고장이
   아니라 **"돌 수도 있는" 고장**이다. 절차 = 시공기록지 `가동 전 점검`. 근본 해결은 기판 제작.
6. 🔴 **`/firmware/info` 로 버전을 판별하지 않는다** — `version`·`source`·`git_sha` 가 전부
   실제와 다르다(v1_3 시절 고정 문자열). **쓸 수 있는 건 `build` 와 매크로 2개뿐.**
7. 다음 현장 세션의 **첫 명령**은 D+0 증거 복제다(`JETSON_SETUP.md §11-g`).
   🔴 **시공이 끝났다고 `TODO(D+0)` **11건**이 다 닫힌 것은 아니다** — 닫힌 것은 `#10` 하나고
   **`#11` R0 watchdog 은 그대로 미확인**이다.

## 사용자 결정 — 유지

- 동결 예외 4회는 전부 소진됐다. `src/**`를 다시 열려면 사용자 승인이 필요하다
  (`FREEZE_MANIFEST.md §10`).
- 순서는 전기계통 시공 → E-stop 검증 → D+0 잔여 종결 → D+1 R3 rosbag → R4~R8이다
  (`MASTER_PLAN.md §3`).
- 예약 20·21·22·23·24·26·27과 역할 B 6단은 각각 정해진 트리거 전까지 별건으로 유지한다
  (`MASTER_PLAN.md §7`). **예약 25 는 08-05 에 종결**됐다.
- `JETSON_SETUP.md §9`의 `TODO(D+0)` **11건** 중 ✅ **#10 물리 E-stop 이 2026-08-07 에 닫혔다**
  (검사 8 통과). 미해결은 **#11 R0 watchdog 실측 하나**다. 목록 계약은 바꾸지 않는다.
- 08-03 D+0 판정은 자동 항목 통과·**종료 2**였다. ✅ **2026-08-07 재실행에서 검사 1~8 전항목
  OK · D+0 판정 통과** — 전량 통과는 이번이 처음이다. ⚠ 판정문 자체의 단서는 그대로다:
  **"여기 통과는 주기의 상한을 증명하지 않는다"**(관측 창이 짧다). 상한 정본은 R3 rosbag 히스토그램.
- never-seen은 다른 놓침과 같이 역행 2회 → 보고 → 단독 탈출이다. 실차 R3 뒤 재평가한다.

| 트랙 | 범위 | 상태 |
|---|---|---|
| **진행 중 묶음** | §45 전기계통 실물 시공 완주 = 정본 v3.3 (문서 + `firmware/` 1줄) | **착수** |
| 전기계통 시공 | `전기계통_최종작업안_0806.md §5` (사용자 직접) | ✅ **A~G8 완주 (08-06~07)** · 🔴 `H2` 사진 · `H3` 여분 보관 잔여 |
| 예비 릴레이 2개 | §43.10 — 주회로용 2개 정책 | ✅ **확보 (5개 · 전량 `JD2914`)** |
| 🔴 릴레이 datasheet 대조 | `JD2914` 각인 `40A/24V` — 코일 정격인지 접점 DC 정격인지 미확정 | 🔴 **P0 종결의 유일한 입구. 다음 세션** |
| 🔴 배터리화 후속 | XT90 무부하 전류 · 로직 국소 보호 재평가 · Jetson 급전 안정성 | 🔴 **재개방 조건 발동 · 미수행** (`§8`-⑰⑱⑲) |
| 🔴 밀린 독립 검토 | §39 보완(코드) · §44 · §45 | 🔴 **3슬롯 미수행** — 범위는 `완료 후 다음 단계 1` |
| 실차 D+0 | ✅ **검사 1~8 전항목 통과 (08-07)** | ⏸ `#11` R0 watchdog 실측만 남음 |
| AI 입력 최적화 | `ef25ad3` 기준, 품질 불변 + raw 입력 30% 절감 | §40 승인 · 방법 B 잔여 = 예약 26 |
| 예약 21 | D0/R3 판정기 회귀 편입 + readiness case 9·11·12·14 간헐성 규명 | ⏸ 다음 bringup 변경 전 |
| 예약 23 | 핸드오프 ID·숫자 계약 전수 폐포 | ❌ 별도 보류 |
| 예약 24 | 검토 §38 정본 소유권·예약 완료판정 정합 | ❌ 다음 bringup/예약 21 전 |
| 예약 26 | 검토 §40.3 방법 B 계약 표면 재설계 | ❌ AI 라우터 표면 여는 묶음 |
| 예약 27 | 역할 B CUDA·JetPack 전제 | ❌ 역할 B 회의 |
| 역할 B V1 합의 | 커밋 `30c5e87` 원문 | ⏸ 회의 대기 |

## 직전 완료 — §44 = 정본 v3.2 (`86b6ed4` · 재검토 **미수행**)

검토 §43 불승인에 대한 철회·되돌림·조건부 재기술. **닫은 위험 0건.** 전수 = `ELECTRICAL_BASELINE §13`.
그 이전은 날짜별 현황으로 넘겼다.

## 보존해야 할 안전 불변조건

- ✅ **`/cmd_vel` 금지 불변조건은 2026-08-07 검사 8 통과로 해제됐다.** `Ctrl+C` 를 E-stop 으로
  승격하지 않는다는 규칙은 그대로다. 🔴 **R1·R2 는 여전히 금지** — 사유는 E-stop 이 아니라
  **re-arm 래치 미구현**이다(아래). 🔴 **속도는 `0.12` 상한 안에서만 쓴다.**
- ✅ **`/estop/state` 는 2026-08-07 부터 신뢰한다**(`§5-G6` 10/10 · 상태 ④) — 이 토픽을 소비하는
  로직을 만들어도 된다. 🔴 **배선을 만지거나 펌웨어를 다시 구우면 10회를 다시 하기 전까지 신뢰를
  회수한다.** ⚠ **"하드웨어 차단은 어느 경우에도 온전하다"는 과잉이었다** — 무부하는 `§5-E`,
  **`0.05 m/s` 부하는 `§5-G8`** 에서 관찰됐고 **끼임(약 11A) 차단은 미관측이다.**
- 🔴 **배선 작업 전 XT90 분리 + 퓨즈 제거** (메인 스위치가 없다), 배선을 만졌으면
  **개폐 10회 재검증**. 🔴 **로직이 배터리로 도는 지금, XT90 분리 앞에 `Jetson shutdown` 이 온다.**
- 🔴 **펌웨어 업로드는 모터 전력 0V·바퀴 공중에서만** (`FIRMWARE_REBUILD.md §5`).
  빌드 지문 `ARDUINO=10607`·`TEENSYDUINO=158` 이 안 맞으면 업로드하지 않는다.
  🔴 **트레이스 절단 후에는 0V 를 만드는 방법이 바뀌었다** — `E-stop 누른 채 XT90 연결`
  (`§5-b`). **모터 단자 0V 를 직접 재고 시작한다** — E-stop 을 눌렀다는 사실로 대신하지 않는다.
- 🔴 **`.ino:111 = false` · `.ino:125-126 = 0.12 / 0.50` 을 되돌리지 않는다.** 앞은 `§2`-⑧ 을,
  뒤는 P0 조건부 수용을 떠받치는 줄이다.
- 🔴 **re-arm 래치는 아직 없다** — PIN21 완료가 이것을 닫지 않는다. 구현·부정/전환 시험 전
  R1·R2 금지 (`REAL_ROBOT_VALUES.md §1-d`).
- 판정기가 **관측하지 않은 것을 말하지 않는다** — 상한 초는 계약값 표기이지 경과 주장이 아니다.
- 설정은 대상 소프트웨어가 실제로 읽는지 `param list`·`topic info -v`로 확인한다.
- N초 관측은 같은 관측자가 종료 상태와 **벽시계 경과시간**으로 창을 완주해야 한다.
- 토픽별 계약의 단일 출처는 `tools/bag_gap_report.py`의 `TOPIC_POLICY`다.
- `/odom`은 RELIABLE, `/imu/*`는 BEST_EFFORT다. EKF 소비자 QoS까지 함께 본다.
- Teensy 재연결 로직 없음, IMU 치명 실패 시 노드 자체가 안 뜸, 시각 역행 시 stamp가 사실상
  멈춘다는 펌웨어 전제를 유지한다.
- 실차 없이 확인한 합성 bag·가짜 `ros2` 결과를 실차 통과로 승격하지 않는다.
- `TODO(D+0)` **11건**을 추측값으로 닫지 않는다 — 미해결은 이유와 재확인 시점을 적는다.

## 금지 범위

- 사용자 추가 승인 없는 `src/**` 변경. **변경 표면은 문서 + `firmware/` 1줄뿐이다.**
- 예약 20·21·22·23·24·26·27, 역할 B V1, 관제 통보 표면을 이 묶음에 혼합.
- 🔴 **위험을 "덮였다"로 기록하기** — 조건부 수용 3건·스위치 미구매·건접점·re-arm 은 전부
  전제조건 + 재개방 조건 형식으로만 쓴다.
- 🔴 **`§5-G8` 을 "부하 차단 검증 완료"로 뭉뚱그리기** — 관찰된 것은 **`0.05 m/s` 정상 전류**
  뿐이다. **끼임 11A 차단·릴레이 DC 정격은 여전히 미증명**이며, 이 둘을 섞어 적으면
  §43.6 오기록의 재발이다.
- 🔴 **`§2`-⑧ 이 닫힌 것을 P0(릴레이 DC 정격)이 닫힌 것으로 읽기.** 신호의 고장 방향과
  접점의 차단 능력은 별개다.
- 🔴 **펌웨어를 실제로 굽거나 업로드하기** — 업로드는 사용자가 현장에서
  `FIRMWARE_REBUILD.md §5` 전제 아래 직접 한다. Claude 는 소스 편집·빌드까지만 한다.
- `make_map.sh` 실런, 지도 자산 변경, Gazebo E2E 동시 실행.
- Claude가 작성한 이번 diff를 Claude가 최종 승인하기.

## 완료 판정

**`ELECTRICAL_BASELINE` v3.3 이 `§14` 에서 닫힌 3건·안 닫힌 것·새로 열린 것을 갈라 적고,
각 닫음에 실측 근거가 붙어 있으며, `§5-G8` 의 한계(`0.05 m/s` 뿐 · 끼임 미관측)가 명시돼 있고,
`FIRMWARE_REBUILD §5` 가 실측 반영본이며(udev 선행 · `board list` 두 줄 · 절단 후 우회),
`PITFALLS` 에 08-06 함정이 들어 있고, `JETSON_SETUP §9-#10` 이 실측으로 닫혔으며,
`doc_check`·`handoff_single_check`·`test_ai_context` 와 회귀 4수치가 전부 그대로면 이 묶음은 완료다.**
단, 완료는 **구현자 주장**이고 승인은 독립 검토(§45)다. 🔴 **§44(v3.2) 재검토도 아직 안 됐다.**
🔴 **조건부 수용 3건은 이 묶음에서도 종결되지 않았다** — `JD2914` datasheet 대조와
각인 없는 부품의 대체 증거 방법이 선행이다.

## 완료 후 다음 단계 — 🔴 **두 갈래를 병렬로 돈다** (사용자 결정 08-07)

**갈래 ㉮ 독립 검토 3슬롯** — `6af8997..3c62381`(§39 보완 · **유일하게 코드 표면**) →
`4e8d060..86b6ed4`(§44) → `86b6ed4..HEAD`(§45). 🔴 **§44 가 §45 보다 먼저다** — §45 의 닫음이
§44 가 건 조건에 기댄다.

**갈래 ㉯ 실차 사다리** — 🔴 **검토 대기 중에도 실측·기록은 한다**(§45 잔여를 닫는 일이다).
**막는 것은 새 설계와 `src/**` 다.**

1. **전기 잔여 3건**(`ELECTRICAL_BASELINE §8`-⑰⑱⑲) — `G8` 셋업 재사용.
   🔴 **⑲ 가 `§7-a` ①의 "바닥 주행 금지"를 푸는 유일한 열쇠**다.
2. **R0 완성** — 부호·E-stop 은 통과했고 **`cmd_vel` 단절 0.5초 정지만 남았다**(`TODO(D+0)`
   **11건** 중 마지막 `#11` · `JETSON_SETUP §7-c-0`). 60fps 영상 + raw pose 로 판정하며
   **zero Twist 로 세우는 시험이 아니다.** 닫히면 `FREEZE_MANIFEST §6` 재판정도 열린다.
3. 🔴 **re-arm 래치 + 부정/전환 시험** — `src/**` 이고 동결 예외 소진이라 **사용자 승인 선행.**
   **R1 을 막는 유일한 항목이자 크리티컬 패스.**
4. **R1**(0.05 m/s) → **R2 3m ±3% · yaw 2π ±10%**. 🔴 **`2026-08-15` 플랜 B 판정일**
   (`MASTER_PLAN §3`). 오픈루프 편향 미측정이라 **첫 직진은 양옆 1m 이상 비운 자리.**
5. D+0 증거 복제 · `H2` 사진 · 퓨즈 2건 증거 방법 — 시한 낮은 잔여.
6. R3 rosbag → 예약 22 와 R4~R8. 🔴 **`R0→R1→R2→R3` 순서를 건너뛰지 않는다.**

## 근거 문서

- 전기계통 위험·안전선 · v3/v3.1/v3.2 개정 전수: `docs/ELECTRICAL_BASELINE.md §11`, `§12`, `§13`
- 전기 시공 절차(결선·순서·검증): `~/Desktop/전기계통_최종작업안_0806.md`
  (구판 역사 = `~/Desktop/전기계통_최종작업안_0804.md`)
- 펌웨어 재빌드·업로드·빌드 지문: `docs/FIRMWARE_REBUILD.md`
- 실차 값·부품 계약값: `docs/REAL_ROBOT_VALUES.md §1`, `§4`, `§5`
- 실행 순서·TODO: `docs/JETSON_SETUP.md §9`, `§10`, `§11`
- 검토 범위 라우팅: `docs/TEST_GATES.md §7` · 사다리·보류: `docs/MASTER_PLAN.md §3`, `§7`, `§8`
- 검토 전문: `~/Desktop/개발현황/CODEX 현황/0801검토현황.md §39`
