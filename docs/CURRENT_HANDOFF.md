# CURRENT_HANDOFF.md — 지금 할 한 묶음 (매 세션 시작점)

> 작업 완료 시 구현자가 갱신하고 검토자가 사실관계·테스트 결과를 확인한다.
> 이전 묶음 내용은 날짜별 현황으로 보내고, 이 파일에는 현재 작업과 직전 주장만 유지한다.

- **동결 기준점**: `212885a8292d1e86677d5beeb9f5358d76fe9b40` = tag
  **`platform-core-freeze-260724`**. 증거 = `docs/FREEZE_MANIFEST.md`.
- **현재 기준 커밋**: 이 파일과 같은 커밋(main).
- **현재 단계**: 마스터플랜 5단 완료 → 역할 B V1 합의(6단)는 회의 대기 보류
  → 실차 D+0 인수·연결 **검사 1~8 전항목 OK**(08-07 재실행) · 잔여 = R0 watchdog `#11` 하나
  → 전기계통 정본 v2~v3.3 = 실물 시공 완주(`7884b40`) · 예약 25 재종결(`04694e1`)
  → firmware precheck 사슬 §46 (`90e451a`) → §47(`7267829`) → §48(`c462d73`) → §49(`9675826`)
  → **검토 §50 보완 = 그 사슬의 마지막 묶음** **(지금 — 2026-08-07)**

> 🔴 **§50은 `c462d73..9675826`을 불승인(P1 1 · P2 2)**했다. §49 셋은 34종 재측정으로 실제로
> 닫힌 것이 확인됐지만, §49.2 를 닫는 데 **쓴 수단**이 새 표면을 열었다 — 기대 지문이 파일
> 내용이 아니라 `git diff` 출력 텍스트를 고정해 **깨끗한 작업 트리가 오염 판정**을 받았다.
> 사용자 명시로 역할이 다시 바뀌었다 — **구현자 = Claude / 최종 검토자 = Codex**(교대 2회차).
> 🔴 **§50 을 닫힌 것으로 인용하지 않는다** — 종결은 Codex 독립 검토가 한다.
>
> 🔴 **이 묶음 뒤 firmware precheck 사슬은 R1 전까지 닫는다** (§47→§48→§49→§50 4연속).
> 근거 = 현재 실제 노출 0(`firmware/` 에 `.cc/.cxx` 0개) · `2026-08-15` 플랜 B 판정일까지
> 8일 · R1 크리티컬 패스는 re-arm 래치(`src/**`)다. **덮였다고 쓰지 않는다** —
> **재개방 조건** = ① 다음 `arduino-cli compile/upload` 직전 ② re-arm 래치를 펌웨어 쪽에
> 두기로 결정되는 순간(`FIRMWARE_REBUILD §6` 표 3행 **미결**) ③ Jetson 등 **다른 기계에서
> 이 검사기를 돌리게 되는 순간**.

**⚠ 미해결 보류 — 어느 것도 승인 완료로 읽지 말 것** (본문 정본 = `MASTER_PLAN.md §7`):

- **⚠ 미해결 보류 — 예약 23 / 검토 §33 (`8fcc1a2`)**: P0 0 · **P1 1** · P2 1.
  `handoff_single_check.sh` 는 `§` 없는 새 작업 꼬리를 붙여도 `OK` 를 내고, 숫자 계약 전수
  증거는 기록 6자리와 독립 실행 21자리가 불일치한다. **승인 완료로 읽지 말 것.** 런타임
  `src/**` 변경·회귀는 없으므로 현재 D+0 실행은 막지 않되, 보완은 다른 묶음에 섞지 않고
  `MASTER_PLAN.md §7` 예약 23 의 완료판정과 부정 회귀로 별도 구현한다. 전문 =
  `~/Desktop/개발현황/CODEX 현황/0801검토현황.md §33`.
- **예약 24 / §38** P1 2 · P2 4 — 라이다 실측 소유 시점과 readiness flake 소유 정본이 갈라짐.
  **라이다 높이는 D+1 소유**이며 `TODO(D+0)` 목록 계약은 그대로다.
- **예약 26 / §40** — 사용자 결정 **'방법 B'** 미구현(결정은 `MASTER_PLAN §8` 에 복원).
  **§37.3 을 닫힌 것으로 인용하지 않는다.**
- **예약 27** — 이 Jetson 에 `nvcc`·`nvidia-jetpack` 이 **없다.** 역할 A 는 비차단이지만
  **역할 B 성능 전제는 통째로 바뀐다.**
- **⏸ 6단 보류** — 역할 B 회의가 잡히면 `30c5e87`의 핸드오프 원문을 그대로 복원한다.

```bash
git show 30c5e87:docs/CURRENT_HANDOFF.md
```

## 이번 한 묶음 목표 — 검토 §50 **P1 1 + P2 2** (지문이 내용을 가리키게 한다)

**진단·근거·의미 변화의 정본 = `FIRMWARE_REBUILD.md §4`** (여기 다시 쓰지 않는다) ·
결정 색인 = `MASTER_PLAN §8` · 묶음 기록 = `FREEZE_MANIFEST §10.19`.

- 🔴 **§50.1 P1** 기대 지문이 `git diff` **출력 텍스트**의 sha256 이라, 작업 트리 무접촉
  상태에서 `core.abbrev`·`diff.context`·`diff.noprefix`·`diff.mnemonicPrefix` 중 하나만
  줘도 `rc=1` **"오염"** 이 났다(4/4 재현). §46.2 "늘 울리는 경보"의 재발이다.
- **§50.2 P2** 기대 지문을 가진 곳이 스크립트뿐인데 스크립트와 정본이 **서로를 가리킨다.**
- **§50.3 P2** 픽스처 사례 ID 충돌(`⑦-g` 5회 · `⑩-f` 2회) — 증거를 되짚을 수 없다.

**🔴 채택 = ⓐ(내용 해시). ⓑ(렌더링 옵션 고정)는 쓰지 않는다** — ⓑ 는 또 하나의 "사람이
유지하는 목록"이고 이 사슬은 그 실패 모드로 3연속 터졌다. ⓐ 는 **정본이 이미 가진 값을 읽는
것**이라 §50.2 가 같이 닫힌다. **변경 표면** = `tools/` 2 + 문서. 🔴 **`.ino`·`src/**`는 안 연다.**

## 완료조건

1. 작업 트리 무접촉 상태에서 `GIT_CONFIG_*` 로 렌더링을 흔들어도 **`rc=0`**, 그 축이
   **픽스처에 실재**한다. 🔴 기대값은 판정기가 안 쓰는 **독립 경로**로 만든다 — 59 검사가 이
   결함을 원리적으로 못 잡은 이유가 "같은 자리에서 같은 설정으로" 계산한 것이다.
2. 기대 지문이 **정본(`FIRMWARE_REBUILD §4`)에 64자리로 실재**하고, `--expect` 의 SHA 가
   **patch 가 아니라 파일 내용**임을 두 곳이 같은 말로 적는다.
3. 픽스처 사례 ID 가 **1:1** 이다. 기존 59 검사의 판정은 **하나도 뒤집히지 않는다**(라벨만).
4. 역회귀 불변: root 12종·`src/**` 10종 FAIL · 비컴파일 4경계 PASS · symlink 전량 FAIL ·
   판정 불능 `rc=2` · 실제 저장소 `rc=0`.
5. inventory **84 → 85** 가 `ai_known_p0_p1.json` · `AI_CONTEXT §3` · `TEST_GATES §7` ·
   `test_ai_context.py` **네 자리 동시**에 반영되고 새 앵커가 profile-only 로 회수된다.
6. 목표·완료판정·상단이 **같은 수(85)와 같은 상태**를 말한다. 🔴 **예약 23 을 구현했다고
   쓰지 않는다.**
7. 회귀 정본 불변: pytest **182 passed** / colcon **245 tests** / harness guards **24 검사** /
   gate regression **14 케이스**. ⚠ `src/**` 무접촉이라 직접 실행 대상은 `doc_check`·pytest·
   `test_ai_context`·펌웨어 픽스처뿐 — 나머지는 **재실행했다고 쓰지 않는다.**
8. `FREEZE_MANIFEST §10` 에 **"동결 예외 아님"** 절로 기록한다(`src/**` 무접촉 증거 포함).

## ★ 현장 운용 — 시공은 끝났고 **절차가 바뀌었다** (상세는 링크가 정본)

> 시공안 `§5-A`~`§5-G8` **완주**(08-06~07). `H2` 사진 · `H3` 여분 보관만 잔여.
> 물리 E-stop 은 **있고, 무부하·`0.05 m/s` 부하에서 실측으로 동작한다**(끼임 11A 는 미관측).

1. 🔴 **기동 순서를 어기면 Teensy 가 영구히 멈춘다** — `퓨즈 → XT90 → Jetson 부팅 대기 →
   agent → **Teensy VIN 만 재삽입**`. 재연결 로직이 없어 `errorLoop()` 에 갇힌다
   (`JETSON_SETUP.md §5-e`). 증상 = 토픽이 `rosout`·`parameter_events` 뿐.
2. 🔴 **종료는 `Jetson sudo shutdown -h now` 가 맨 앞** — 로직이 배터리로 도는 지금
   XT90 분리는 **리눅스 전원 뽑기**다 (`ELECTRICAL_BASELINE.md §7`).
3. 🔴 **배선을 만질 땐 XT90 분리다 — E-stop 으로 대신하지 않는다.** E-stop 은 **모터 가지만**
   끊는다. 유일한 예외 = 트레이스 절단 후의 펌웨어 업로드 (`FIRMWARE_REBUILD.md §5-b`).
4. 🔴 **되돌리면 안 되는 소스 3줄** — `.ino:111 = false`(되돌리면 `§2`-⑧ 재개방) ·
   `.ino:125-126 = 0.12 / 0.50`(릴레이 정격 수용의 전제). 🔴 **수령 원본 해시 대조는 이제
   `실패` 가 정상**이고 오염 판정은 **`bash tools/firmware_precheck.sh` 뿐**이다 — 굽기 전에
   돌린다. 🔴 **`rc=2` 는 판정 불능이지 통과가 아니다** (`FIRMWARE_REBUILD.md §4`).
5. 🔴 **브레드보드 상태다** — PWM/DIR 이 빠지면 "안 도는" 고장이 아니라 **"돌 수도 있는"**
   고장이다(가동 전 점검 = 시공기록지). 🔴 **`/firmware/info` 로 버전을 판별하지 않는다** —
   `version`·`source`·`git_sha` 가 v1_3 시절 고정 문자열이고 쓸 수 있는 건 `build` 뿐.
6. 🔴 **시공이 끝났다고 `TODO(D+0)` 11건이 닫힌 게 아니다** — 닫힌 것은 `#10` 하나고
   **`#11` R0 watchdog 은 미확인**이다. 다음 현장 세션 첫 명령 = `JETSON_SETUP.md §11-g`.

## 사용자 결정 — 유지

- 동결 예외 4회는 전부 소진됐다. `src/**`를 다시 열려면 사용자 승인이 필요하다
  (`FREEZE_MANIFEST.md §10`).
- 순서는 전기계통 시공 → E-stop 검증 → D+0 잔여 종결 → D+1 R3 rosbag → R4~R8이다
  (`MASTER_PLAN.md §3`).
- 예약 20·21·22·23·24·26·27·**28** 과 역할 B 6단은 각각 정해진 트리거 전까지 별건으로 유지한다
  (`MASTER_PLAN.md §7`). 🔴 **예약 28 = §44·§45 의 미해결 P1 넷**(전기 정본, 등록만 됨).
- `JETSON_SETUP.md §9` `TODO(D+0)` **11건** 중 ✅ **#10 물리 E-stop 이 08-07 에 닫혔다**.
  미해결은 **#11 R0 watchdog 실측 하나**다. 목록 계약은 바꾸지 않는다.
- ✅ **08-07 재실행 = 검사 1~8 전항목 OK**(전량 통과는 처음). ⚠ 단서는 그대로다 —
  **"여기 통과는 주기의 상한을 증명하지 않는다"**(상한 정본 = R3 rosbag 히스토그램).
- never-seen은 다른 놓침과 같이 역행 2회 → 보고 → 단독 탈출이다. 실차 R3 뒤 재평가한다.

| 트랙 | 범위 | 상태 |
|---|---|---|
| **진행 중 묶음** | §50 P1/P2 = 지문을 내용에 묶기 (`tools/` 2 + 문서) | ✅ **구현 완료 주장 · 구현자 = Claude(교대 2회차)** · 🔴 **Codex 독립 검토 대기** |
| 전기계통 시공 | `전기계통_최종작업안_0806.md §5` (사용자 직접) | ✅ **A~G8 완주 (08-06~07)** · 🔴 `H2` 사진 · `H3` 여분 보관 잔여 |
| 🔴 릴레이 | 예비 확보 ✅(5개 · 전량 `JD2914`) / 🔴 **§45.2 불승인 — 대조본이 실물과 다른 모델(NOCO `JD2912`)** | 🔴 **P0 격하를 되돌릴지 = 사용자 결정. 예약 28-④** |
| 🔴 배터리화 후속 | XT90 무부하 전류 · 로직 국소 보호 재평가 · Jetson 급전 안정성 | 🔴 **재개방 조건 발동 · 미수행** (`§8`-⑰⑱⑲) |
| 🔴 검토 슬롯 | §44~§50 수행됨 / `c397975..6af8997`(전기 v1→v2) 미검토 | 🔴 **§44·§45 불승인 P1 4 = 예약 28** |
| 실차 D+0 · AI 입력 | ✅ 검사 1~8 전항목 통과(08-07) / `ef25ad3` 대비 raw 30% 절감 | ⏸ `#11` R0 watchdog · 방법 B = 예약 26 |
| 예약 21 · 24 | D0/R3 회귀 편입 + readiness 간헐성 / §38 정본 소유권 | ⏸ 다음 bringup 변경 전 |
| 예약 23 · 26 · 27 | 숫자 계약 전수 폐포 / 방법 B 재설계 / 역할 B CUDA 전제 | ❌ 각자 트리거 전 별건 |
| 역할 B V1 합의 | 커밋 `30c5e87` 원문 | ⏸ 회의 대기 |

## 직전 완료 — §49 보완 (`9675826` · 🔴 **§50이 불승인함**)

§49 셋을 실제로 닫았고 §50 의 34종 재측정에서 **root 12종·`src/**` 10종이 과부족 없이 실측과
일치**했다. 남은 문제는 §49.2 를 닫는 데 **쓴 수단** 하나였고 이번 묶음이 그것을 닫는다.

## 보존해야 할 안전 불변조건

- ✅ **`/cmd_vel` 금지 불변조건은 2026-08-07 검사 8 통과로 해제됐다.** `Ctrl+C` 를 E-stop 으로
  승격하지 않는다는 규칙은 그대로다. 🔴 **R1·R2 는 여전히 금지** — 사유는 E-stop 이 아니라
  **re-arm 래치 미구현**이다(아래). 🔴 **속도는 `0.12` 상한 안에서만 쓴다.**
- ✅ **`/estop/state` 는 2026-08-07 부터 신뢰한다**(`§5-G6` 10/10 · 상태 ④) — 이 토픽을 소비하는
  로직을 만들어도 된다. 🔴 **배선을 만지거나 펌웨어를 다시 구우면 10회를 다시 하기 전까지 신뢰를
  회수한다.** ⚠ **"하드웨어 차단은 어느 경우에도 온전하다"는 과잉이었다** — 무부하는 `§5-E`,
  **`0.05 m/s` 부하는 `§5-G8`** 에서 관찰됐고 **끼임(약 11A) 차단은 미관측이다.**
- 🔴 **배선 작업 전 XT90 분리 + 퓨즈 제거** (메인 스위치가 없다), 배선을 만졌으면
  **개폐 10회 재검증**. 🔴 **로직이 배터리로 도는 지금, XT90 분리 앞에 `Jetson shutdown` 이 온다.**
- 🔴 **펌웨어 업로드는 모터 전력 0V·바퀴 공중에서만** (`FIRMWARE_REBUILD.md §5`).
  빌드 지문 `ARDUINO=10607`·`TEENSYDUINO=158` 이 안 맞으면 업로드하지 않는다.
  🔴 **트레이스 절단 후에는 0V 를 만드는 방법이 바뀌었다** — `E-stop 누른 채 XT90 연결`
  (`§5-b`). **모터 단자 0V 를 직접 재고 시작한다** — E-stop 을 눌렀다는 사실로 대신하지 않는다.
- 🔴 **`.ino:111 = false` · `.ino:125-126 = 0.12 / 0.50` 을 되돌리지 않는다.** 앞은 `§2`-⑧ 을,
  뒤는 P0 조건부 수용을 떠받치는 줄이다.
- 🔴 **re-arm 래치는 아직 없다** — PIN21 완료가 이것을 닫지 않는다. 구현·부정/전환 시험 전
  R1·R2 금지 (`REAL_ROBOT_VALUES.md §1-d`).
- 판정기가 **관측하지 않은 것을 말하지 않는다** — 상한 초는 계약값 표기이지 경과 주장이 아니다.
- 설정은 대상 소프트웨어가 실제로 읽는지 `param list`·`topic info -v`로 확인한다.
- N초 관측은 같은 관측자가 종료 상태와 **벽시계 경과시간**으로 창을 완주해야 한다.
- 토픽별 계약의 단일 출처는 `tools/bag_gap_report.py`의 `TOPIC_POLICY`다.
- `/odom`은 RELIABLE, `/imu/*`는 BEST_EFFORT다. EKF 소비자 QoS까지 함께 본다.
- Teensy 재연결 로직 없음, IMU 치명 실패 시 노드 자체가 안 뜸, 시각 역행 시 stamp가 사실상
  멈춘다는 펌웨어 전제를 유지한다.
- 실차 없이 확인한 합성 bag·가짜 `ros2` 결과를 실차 통과로 승격하지 않는다.
- `TODO(D+0)` **11건**을 추측값으로 닫지 않는다 — 미해결은 이유와 재확인 시점을 적는다.

## 금지 범위

- 사용자 추가 승인 없는 `src/**` 변경. **변경 표면은 `tools/` 2 + 문서뿐이다** —
  `.ino` 는 이 묶음에서 열지 않는다(§45.3 의 fail-safe 문구는 §45 슬롯이 소유한다).
- 🔴 **`rc=2`(판정 불능)를 통과로 읽기**, **저장소가 깨끗함을 "보드에 이 펌웨어가 올라가
  있다"로 승격하기**(보드 대조 수단은 없다), **§47.2·§47.3 을 예약 23 종결로 쓰기**.
- 예약 20·21·22·23·24·26·27, 역할 B V1, 관제 통보 표면을 이 묶음에 혼합.
- 🔴 **위험을 "덮였다"로 기록하기** — 조건부 수용 3건·스위치 미구매·건접점·re-arm 은 전부
  전제조건 + 재개방 조건 형식으로만 쓴다.
- 🔴 **성공 입력이 통과하는 조건을 넓히기.** 벽시계 비교의 허용 오차, 사전검사의 허용 판정을
  "대충 맞으면 통과"로 무르게 하는 것 둘 다. ⚠ 증감의 판정 강등은 이 금지에 걸리지 않는다 —
  **내용 해시가 파일 바이트를 완전히 결정**해 계약이 좁아진 것이다(논증 = `FIRMWARE_REBUILD §4`).
- 🔴 **등록을 종결로 쓰기.** 예약 28 이 생겼다고 §44·§45 P1 4건이 고쳐진 것이 아니다.
  **§44·§45·§45.3 을 닫힌 것으로 인용하지 않는다.** 전기 정본 활성 문장 스윕은 별도 묶음이고
  §45.2 되돌림은 **사용자 결정 선행**이다.
- 🔴 **합성 픽스처 결과를 실차 통과로 승격하기.** 🔴 **`§5-G8` 을 "부하 차단 검증 완료"로
  뭉뚱그리기** — 관찰된 것은 **`0.05 m/s` 정상 전류**뿐이고 끼임 11A 차단·릴레이 DC 정격은
  여전히 미증명이다(§43.6 오기록의 재발).
- 🔴 **펌웨어를 실제로 굽거나 업로드하기** — 업로드는 사용자가 현장에서
  `FIRMWARE_REBUILD.md §5` 전제 아래 직접 한다.
- `make_map.sh` 실런, 지도 자산 변경, Gazebo E2E 동시 실행.
- **Claude가 작성한 이번 diff를 Claude가 최종 승인하기.**

## 완료 판정

**펌웨어 사전검사가 기준점 이후의 committed·staged·unstaged·미추적 소스를 한 판정 입력으로
보고, 허용 파일을 **파일 내용 sha256** 으로만 판정해 git 렌더링 설정을 흔들어도 깨끗한 트리는
`rc=0` 이며 진짜 오염은 여전히 `rc=1` 이고, 그 축을 포함한 **73검사**가 사례 ID **1:1** 로
남고, 판정 불능은 `rc=2`, 소유 문서는 `[참고]`이며, 기대 지문 64자리가 정본
`FIRMWARE_REBUILD §4` 에 실재하고, inventory가 **85**이며 새 앵커가 profile-only로 회수되고,
상단이 08-07 실측(검사 1~8 통과 · 잔여 R0 `#11`)과 같고, `doc_check`·`handoff_single_check`·
`test_ai_context` 와 회귀 4수치가 그대로면 완료다.**
단, 완료는 **구현자 주장**이고 승인은 독립 검토다.
🔴 **전기 조건부 수용 3건·§15 릴레이 격하는 이 묶음이 안 건드렸다** — 되돌릴지는
**사용자 결정**이다(예약 28-④).

## 완료 후 다음 단계 — 🔴 **두 갈래를 병렬로 돈다** (사용자 결정 08-07)

**갈래 ㉮ 검토·보완** — 🔴 이 diff(`9675826..HEAD`)는 **구현자 = Claude**라 **Codex 독립
검토**가 먼저다. 그 뒤 우선순위는 **예약 28 보완**(§44·§45 의 P1 4건). 미검토 슬롯
`c397975..6af8997`는 시한이 낮다.

**갈래 ㉯ 실차 사다리** — 🔴 **검토 대기 중에도 실측·기록은 한다.** 막는 것은 새 설계와 `src/**` 다.

1. **전기 잔여 3건**(`ELECTRICAL_BASELINE §8`-⑰⑱⑲) — `G8` 셋업 재사용.
   🔴 **⑲ 가 `§7-a` ①의 "바닥 주행 금지"를 푸는 유일한 열쇠**다.
2. **R0 완성** — 부호·E-stop 은 통과했고 **`cmd_vel` 단절 0.5초 정지만 남았다**(`TODO(D+0)`
   **11건** 중 마지막 `#11` · `JETSON_SETUP §7-c-0`). 60fps 영상 + raw pose 로 판정하며
   **zero Twist 로 세우는 시험이 아니다.** 닫히면 `FREEZE_MANIFEST §6` 재판정도 열린다.
3. 🔴 **re-arm 래치 + 부정/전환 시험** — `src/**` 이고 동결 예외 소진이라 **사용자 승인 선행.**
   **R1 을 막는 유일한 항목이자 크리티컬 패스.**
4. **R1**(0.05 m/s) → **R2 3m ±3% · yaw 2π ±10%**. 🔴 **`2026-08-15` 플랜 B 판정일**.
   오픈루프 편향 미측정이라 **첫 직진은 양옆 1m 이상 비운 자리.**
5. D+0 증거 복제 · `H2` 사진 · 퓨즈 2건 증거 → R3 rosbag → 예약 22 와 R4~R8.
   🔴 **`R0→R1→R2→R3` 순서를 건너뛰지 않는다** (사다리 정본 = `MASTER_PLAN §3`).

## 근거 문서

- 전기 위험·안전선 = `ELECTRICAL_BASELINE §11`~`§13` · 시공 = `~/Desktop/전기계통_최종작업안_0806.md`
- 펌웨어 재빌드·빌드 지문·**기대 내용 sha256** = `FIRMWARE_REBUILD §4`,`§5`
- 실차 값 = `REAL_ROBOT_VALUES §1`,`§4`,`§5` · 실행 순서·TODO = `JETSON_SETUP §9`~`§11`
- 검토 라우팅 = `TEST_GATES §7` · 사다리·예약·결정 = `MASTER_PLAN §3`,`§7`,`§8`
- 검토 전문 = `~/Desktop/개발현황/CODEX 현황/0801-4검토현황.md §44`~`§50`
