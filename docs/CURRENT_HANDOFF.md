# CURRENT_HANDOFF.md — 지금 할 한 묶음

> 현재 묶음 + 미해결 보류만 유지한다. 구현자의 완료 주장은 독립검토 판정이 아니다.

- **동결 기준점**: `212885a8292d1e86677d5beeb9f5358d76fe9b40` = `platform-core-freeze-260724`.
- **저장소 기준**: `main`의 이 인계 커밋. 부모 `c5becb9` 위에 **41-g 계약 4판 +
  1.6.3 계측 구현 + 08-18 현장 판독**을 한 묶음으로 남긴다.
- **실제 보드**: 현장 후보 `rearm-latch-pi-link-probe-1.6.3` `build=Aug 18 2026 12:32:35`.
  🔴 **승인 지문은 여전히 1.6.1** 이라 셋을 섞지 않는다.
  되돌릴 `.hex` = `~/d0_evidence/fw163/`(sha256 `c40880ad…`).
- **현재 단계**: 검토 §79 → 보완 `c5becb9` → **§79 뒤 41-g 계약 4판 + 1.6.3 계측 구현 +
  08-18 현장 판독 완료 · 독립검토 대기**.
- 🔴 **41-g 가 처음으로 답을 냈다** — 08-18 현장 662초의 300ms급 공백 **3건 전부
  `발행 층`**. loop 정지가 아니다. 상세 = 아래 §"08-18 현장".

## 이번 한 묶음 목표 — §79 뒤 계약 4판 · 1.6.3 계측 · 현장 판독

3판 계약을 **host harness 가 반증**해 4판으로 올리고, 그대로 1.6.3 을 구현해 굽고, 실기 bag 을
9행 분류표로 읽었다. 안전 경로(`rearm_gate.h`·`drive_wiring.h`·PWM 출력단)는 **한 글자도
바꾸지 않았다** — 이번 판은 계측 추가뿐이다.

### 계약 4판이 3판을 어디서 반증했나

**6행** = code **2종 이상**(3판은 부분집합 검사라 `{LOOP_INTERNAL, PUBLISH_FAIL}` 이 샜다) ·
**④ `sample_seq`** = 표본 tick 마다. 전문 = `MASTER_PLAN §7` 41-g.

### 🔴 구현자가 주장하는 것 (검토자가 반증할 대상)

1. **합성 주입 ⓐ~ⓙ 가 서로 다른 분류로 갈린다** — `bash tools/link_stall_host_test.sh`
   3단(MCU **367** · 분류 **23** · 구조 **14**), 서로 다른 분류 **9종**.
2. **분류표는 code 조합에 전칭·상호배타다** — 못 덮는 조합 **0개**를 양방향 고정
   (`tools/test_link_stall_classify.py` **28건**).
3. **계측이 차량을 세우지 않는다** — 662초 실기 `y=7` **0건**(661 전수) ·
   `runtime_overruns`·`disarm_runtime` 0 · `publish_max_us[PULSE]` **7µs**.
4. **계측 자신이 아무것도 안 잃었다** — `evt_dropped_total`·`pulse_fail` 0 ·
   `sync_ok` 내내 true · `publish_failures 47 == evt_seq 47`.
5. **300ms 공백 3건은 loop 정지가 아니다** — `LOOP_INTERNAL`·`BETWEEN_LOOPS` 0 ·
   공백 내내 `sample_seq` +1씩.

⚠ 주장하지 **않는** 것: 300ms 막힘의 **뿌리 원인**. 분류는 *"발행 층"* 까지이고
그 아래(USB CDC·agent 스케줄)는 후보로 남는다. 08-17 6.85초와 동일 현상인지도 주장 안 한다.

## §3-10 전수 열거와 구현 대조 — 🔴 8 → 9 로 옮겼다

주기 publisher · `publishMeasured` 경유 `rcl_publish` · runtime publish slot · bag override
telemetry 가 각각 **8 → 9**(`/firmware/pulse`), 사건 publisher **0 → 1**(`/firmware/event`).
🔴 **runtime phase 7 · 해제 5사유 · RELIABLE timeout 2 · 직접 구독 12 · info 필드 7 은
불변이 역회귀**다. `tools/test_firmware_runtime_guard.py` 가 둘을 갈라서 세고, 측정 밖
`rcl_publish` 가 **정확히 두 자리**뿐임을 함수 단위로 못박는다.

## 08-18 현장 — R3 재시험과 41-g 첫 판독

원장 = `~/Desktop/개발현황/0818_현황.md`. 판정에 쓰는 bag 은
`~/d0_evidence/r3_0818`(662.419초 · 9토픽 · 81,045건) 하나다.

### R3 = 🔴 FAIL — 다만 실패 사유가 둘에서 하나로 줄었다

`/scan` ✅ **PASS**(6,629건 · 10.01Hz) — **08-17 무표본 사유 닫힘**.
`/odom`·`/imu/data` ❌ 각 **4건** 33.33ms 초과 · 최대 **348.84 / 349.57ms**.
🔴 **`/odom` stamp 297ms vs `/imu` 36.8ms 가 핵심** — imu(BEST_EFFORT)는 늦게 배달돼 표본이
살았고, odom(RELIABLE·700B>MTU)은 **ACK 20ms 를 꽉 채우고 실패**해 잃었다. 41-g 가설
*"상류가 서고 RELIABLE 상한이 그것을 `/odom` 유실로 번역한다"* 를 직접 확인했다.

### 41-g 판독 — 세 공백 전부 **7행 발행 층**

`tools/link_stall_bag_extract.py` 가 재현한다. 사건 35건 전부 `PUBLISH_FAIL` ·
`LOOP_INTERNAL`·`BETWEEN_LOOPS` **0**. pulse 수신지연이 한 주기씩 줄어 **공백이 끝나는 순간
한꺼번에** 배달됐다 = 큐가 잠긴 것이지 끊긴 게 아니다. 커널 층 **USB/ACM 0건**.
숫자 = `REAL_ROBOT_VALUES §1-g-1` · 분류 = `MASTER_PLAN §7` 41-g.

### 08-18 오후·저녁 — 구동 특성과 Nav2 예행 (측정만)

전량 = `REAL_ROBOT_VALUES §1-h`. 🔴 **전부 판재 천공·PCB 이식 *전* 값이다.**

- **예약 40 원인 확정** — 무부하 대조가 갈랐다. 제어가 아니라 **마찰**, "전달률"이 아니라
  **불감대**(`실제 ≈ 0.73×(명령−0.32)`). 🔴 주행 중엔 **불감대 없음**(전달률 8~13%).
- **예약 39 원인 갈림** — 180° 뒤집어 3m 두 번, **둘 다 로봇 기준 왼쪽** →
  **로봇 8.37°/3m + 바닥 1.12°/3m**. 바닥이 아니라 로봇이다.
- **예약 44** — 허브 고정 후 좌우 구름반지름 **56.61 = 56.61 mm** 일치. 🔴 실효 윤거는
  34% 어긋나 안 닫힘 → **예약 48 신설**(`odom` 이 전진 중 회전을 **절반만** 본다).
- **B6 Nav2 예행** — 게이트 A·B·C 통과 · 위치추정 원점 2cm · 🟢 **0.86m 완주**(오차
  0.137m) / 🔴 3m 은 좌향 33cm 누적 뒤 **전진 불감대** 교착. 🔴 **R6 판정 안 만들었다.**

### ⚠ 이 bag 을 쓸 수 없는 곳

주행 중 **손으로 앞부분을 밀어 방향을 바꾼 구간**이 있다. 주기·단조·링크 판정엔 영향이
없지만 **odom 정확도·예약 44·39 근거로는 쓰지 않는다.**

## 08-19 새벽 — 역할 B 왕복 정리 · 예약 45 착수 · 튜닝판 준비 (로봇 없음)

- 🟢 **`src/tunnel_interfaces` 신설**(예약 45) — `.msg` 2종 · 빌드 · `ros2 interface show`
  로 계약 §15-a 대조 완료. 🔴 **Jetson 빌드·태그·SHA 는 08-19 오전.**
- 🟢 **역할 B 수신 원문 6건 보관** — 그전까지 **0건**이라 P3-b(Jetson 메모리 완화책 3개)를
  통째로 누락했다. 전량 = `PROJECT_CONTEXT §4.2`.
- 🟢 **Q1 실측**(`REAL_ROBOT_VALUES §1-i`) — 표본 31,330 · **10° 초과 0.080%** →
  🔴 **M2 재상정 없음**. 회신 = `~/Desktop/YOLO_탐지연동_역할A_5차회신_0819.md`.
- 🟢 **M6 판정** — depth 동기화 실패는 §15-c **2행(처리 실패) → 미발행**. 계약 변경 0.
- 🟢 **`ELECTRICAL_BASELINE §4-f-6`** — E-stop 코일 2A 퓨즈 접촉 불량(치수) 조치.
  🔴 확정 아님 — 08-18 bag 은 전·후 모두 무장 해제 **0회**라 중립이다.
- 🟢 **불감대 시험판 + 시뮬 A/B** — `nav2_params_real_db.yaml` · `nav2_params_db.yaml`.
  원본 불변 · 키 집합 동일 · 값 **3개만** 다르다. Gazebo A/B(`tools/nav2_rotate_probe.py`):
  **A4 32.7s/회전 4.2% vs B4 33.1s/6.1%** — 비용 +1.2%, 진동·정체·실패 없음.
  🔴 **하방만 봤다**(시뮬엔 불감대 없음) — 상방은 08-19 실차. 전량 = `MASTER_PLAN §7` 40.
- 🟢 **`tools/goal_ahead.py`** — goal 손계산 제거(08-18 `<현재x+2.0>` 오류 방지).
- 📌 **정정** — 예약 32-e *"굽기 대기"* 는 사실이 아니다. 정정쌍은 **이미 보드에 있다.**

## 완료조건

| # | 항목 | 결과 |
|---|---|---|
| 1 | 41-g 계약 4판 | ✅ 6행·④ 두 자리 개정 + 사유·재현 경로 명기 |
| 2 | 1.6.3 계측 구현 | ✅ 굽고 실기 확인(`build=Aug 18 2026 12:32:35` · publish slot 9칸 · pulse 9.8Hz) |
| 3 | 합성 주입이 갈리는가 | ✅ harness 3단 367·23·14 · 분류 9종 |
| 4 | 전수 표 8→9 이동 | ✅ 이 문서 표 + `test_firmware_runtime_guard` 같은 커밋 |
| 5 | 안전 경로 불변 | ✅ `rearm_gate.h`·`drive_wiring.h` 바이트 동일 · phase 7 · 해제 5사유 |
| 6 | R3 | 🔴 **FAIL** — `/scan` 닫힘, `/odom`·`/imu` 공백 남음 |
| 7 | 41-g 완료 주장 | 🔴 **안 한다** — 계약+구현을 묶어 독립검토 뒤에 |
| 8 | 굽기 뒤 검증 | ✅ `§7-c-E` **13행 전량 수행**(축소판은 예약 47 로 거부됨) |
| 9 | 기준선 유지 | ✅ `TODO(D+0)` **11건** 종결 계약 불변 |

## 완료 판정

현 상태는 **계약 4판 확정 · 1.6.3 현장 후보 굽기 완료 · 41-g 가 실기에서 처음으로
원인 층을 분류 · 독립검토 대기**다. 🔴 **1.6.3 은 승인본이 아니라 보드 이력**이고,
`FIRMWARE_REBUILD §4` 허용 지문은 1.6.1 에 그대로 둔다.
R3 는 FAIL 이므로 **R4·R6 판정에 진입하지 않는다**(Nav2 예행은 아래 조건부 예외).
`TODO(D+0)` **11건**과 `§7-c-E` **13행 전량 수행** 계약은 그대로다.

## 완료 후 다음 단계

1. 🔴 **계약 4판 + 1.6.3 구현을 묶어 한 번에 독립검토**를 받는다. 41-g 사슬 **4회차**라
   `AGENTS §6` 회차 상한대로 **P0 만 반영**한다.
2. ✅ **실험 A 완료** — 후보 ④ 기각 · ③ 약화 · ② 잔존. ③을 완전히 닫으려면 같은 조건으로
   **30분 이상**이 더 필요하다(현재 표본으로는 p=0.23).
3. 펌웨어 보완은 **B(메시지 타입 교체) 하나만 남았고 별도 묶음**이다. 🔴 A·C·E 는
   **재시도 금지** 등록. B 선결 = **publisher 슬롯**(10/10 포화). 표 = `MASTER_PLAN §7` 41-g.
4. 🔴 **예약 47**(게이트 도구 기대 해시 미이관 — 축소판 6행이 08-17 이후 계속 거부)과
   🔴 **예약 48**(윤거 상수 하나로 두 상황)의 전문은 `MASTER_PLAN §7` 이 소유한다.
5. 🔶 **예약 45 `tunnel_interfaces`** — 🟢 노트북 몫 완료(아래 08-19 블록) ·
   🔴 **Jetson 빌드·태그·40자 SHA 전달이 남았다.** §72.2·H′ 도 잇는다.

## 🔴 일정 (08-19 사용자 결정 · 전문 = `MASTER_PLAN §5-b`)

**촬영 = 08-22(토)~08-23(일) · 2분 원테이크.** 앞 작업일은 **08-19·20·21 사흘**.
원테이크라 **모든 계통이 2분간 동시에** 살아야 한다. 🔴 지금 0% 인 셋이 앞을 막는다 —
① 3m 이상 자율주행 ② 미션 상태머신 실차 ③ Jetson 동시 부하. **08-19 는 ①에 건다.**
⚠ 축소 규칙 발동 조건은 충족됐고 **범위는 사용자 결정**이다.

## 금지 범위

- 🔴 **1.6.3 source hash 를 `FIRMWARE_REBUILD §4` 허용 지문으로 옮기지 않는다.**
  구현자가 자기 diff 를 승인하지 않는다.
- 🔴 **`rearm_field_wiring.py` 의 기대 해시를 지금 옮기지 않는다** — 그 도구가 스스로
  적은 갱신 규칙이 *"13행 전량을 밟은 뒤 그 결과로 옮긴다"* 라, 건너뛰려고 옮기면 순환이다.
- 새 펌웨어 업로드·`CMD_WHEEL_BASE`·FF/게인·`ODOM_WHEEL_RADIUS`·지도 재생성·URDF 수정
  은 하지 않는다.
- 🔴 **`nav2_params_real.yaml` 원본 수정 금지** — 튜닝은 `_db.yaml` 사본으로만. 되돌리기를
  남긴다. 시험 통과해도 원본 이관은 **독립검토 뒤 별도 묶음**이다.
- 🔴 **`ekf_real.yaml` 수정 금지** — 바꾸면 역할 B 통보 의무가 생긴다(`PROJECT_CONTEXT §4.2`).
  예약 41(EKF NaN)은 이 묶음에서 건드리지 않는다.
- §72.2 보완과 H′ 를 이 묶음의 독립검토 diff 에 섞지 않는다.
- 08-14 torn odom 원인, 예약 36 IMU I2C, 39 좌향, 40 각속도 전달률, **41-g**, 43 R3,
  **44** 우전륜, **45**, 46 dmesg 경계, **47** 게이트 지문, **48** 윤거 상수는 열린 상태다.
- 🔴 **08-18 구동 실측은 판재 천공·PCB 이식 *전* 값이다** — 그 작업이 무게·강성·배선을
  바꾸므로, 뒤에 최소 ① `§7-c-E` 무장·해제·watchdog ② 3m 직진 좌향 ③ `/firmware/pulse`
  10Hz 를 다시 재기 전에는 `ODOM_WHEEL_RADIUS`·윤거·게인·Nav2 파라미터를 바꾸지 않는다.
- `TODO(D+0)` **11건**을 추측으로 다시 열거나 닫지 않는다.
- 🔴 **R3 통과 전 R4/R6 금지 — 08-18 조건부 예외 (사용자 결정)**. 저녁 Nav2 실차
  **예행**은 R3 결과와 무관하게 연다. 판정 순서는 그대로다.
  * **여는 것** = 스택 기동·TF 완결·goal 수납·직선 추종·collision 정지 관측.
  * **열지 않는 것** = **R6 판정**. 오차 ≤0.3m·정지거리·부정 회귀 실차판·H′ 종료는
    오늘 하지 않으며, 예행 결과를 R6 통과 근거로 쓰지 않는다.
  * **전제 6종** — ⓐ 라이다 연결·`/scan` 정상 ⓑ EKF·TF·readiness gate 통과
    ⓒ 물리 E-stop 담당자 상시·저속·넓은 공간 ⓓ **회전 최소 직선 goal 2~3m 부터**
    ⓔ bag 기록 ⓕ 🔴 `nav2_params_real.yaml` 튜닝 금지.
  * **중단 조건** — 제자리 회전만 반복(예약 40 증상) · `/odom` 공백 중 추종 이상 ·
    바퀴 이탈 조짐. ⚠ 예상 abort 지점은 R3 가 아니라 **예약 40 × progress checker** 다.
    **abort 는 버그가 아니다.**
  * **재개방 조건** — 전제 중 하나라도 깨지면 예행을 닫는다.
- 🔴 **예약 44 전제 (스페어 도착 전까지 매 시행)** — 우전륜(FR) 유격이 크다.
  ⓐ `ODOM_WHEEL_RADIUS`·윤거·FF/게인을 08-17/08-18 숫자로 바꾸지 않는다 ⓑ 예약 39·40 을
  이 데이터로 판정하지 않는다 ⓒ C10 **57.64mm** 를 확정값으로 인용하지 않는다
  ⓓ 추가 지면 주행은 바퀴 공중 우선·물리 E-stop 담당자 상시·저속.
- 🔴 **예약 46 전제** — `dmesg_coverage_check.py` 의 rc=0 만으로 커널 무사고를 확정하지
  않는다. **08-18 취득은 구간을 덮었고 USB 0건**이지만, 그것이 가르는 것은 **커널이
  기록하는 종류의 사고**뿐이다. 버퍼 포화·스케줄·autosuspend 는 로그를 안 남긴다.
- 🔴 **예약 46 조건부 수용 — 잔여 2건 (08-18 동결, 되돌릴 수 있다)**. dmesg-coverage
  사슬 **4회차**라 P0 만 반영한다. 둘 다 **fail-open 이 아니다.**
  * **잔여 ⓐ (§78.2 · P1)** — sidecar 의 `tool`/`version`/`log` 는 셋을 동시 변조해도
    rc=0 이다. **전제** = sha256·시각 형상 검사는 살아 있다.
  * **잔여 ⓑ (§79.5 · P2)** — sidecar 쓰기만 실패하면 같은 이름 재시도가 rc=2 로
    끈적하다. **전제** = 끊기면 **새 이름을 쓴다**(`JETSON_SETUP §5-d`).
  * 🔴 **재개방** — ⓑ 가 현장에서 1회라도 나면 즉시 P1 · ⓐ 가 만든 rc=0 이 어떤 판정의
    단독 근거로 쓰이면 즉시 P0 · 새 P0 가 나오면 회차 상한과 무관하게 연다.
- 🔴 **1.6.3 조건부 수용 — publisher 슬롯 10/10 (08-18 신설)**. micro-ROS 정적 상한
  `RMW_UXRCE_MAX_PUBLISHERS=10` 을 정확히 다 쓴다. **전제** = 1.6.3 이후 새 telemetry
  토픽을 만들지 않는다. 🔴 **재개방** = 토픽이 하나라도 더 필요해지면 `libmicroros.a`
  재생성이 필요한데 그건 vendor drop(`firmware/VENDOR_DROP.md`)이라 이 저장소에서 못
  한다 — 그때는 41-g 계측을 되돌리거나 기존 토픽을 합치는 판단을 새로 한다.
- 🔴 **1.6.3 조건부 수용 — pulse slot 이 400ms 를 치면 사유 7 (08-18 신설)**.
  `publishMeasured` 는 slot 무관하게 400ms 초과 시 해제한다. **전제** = pulse 는
  BEST_EFFORT·depth 1·≤128B 라 ACK 대기가 없고, 662초 실측 최악이 **7µs**(상한의
  0.002%)다. 🔴 **재개방** = 실기에서 `y=7` 이 **1회라도** 나오면 즉시 P1 로 올려
  pulse 를 계측 대상에서 뺄지 slot 상한을 따로 둘지 다시 판단한다.
- **⚠ 미해결 보류 — 예약 23 / 검토 §33 (`8fcc1a2`)**: P0 0 · **P1 1** · P2 1.
  `handoff_single_check.sh` 는 `§` 없는 새 작업 꼬리를 붙여도 `OK` 를 내고, 숫자 계약 전수
  증거는 기록 6자리와 독립 실행 21자리가 불일치한다. **승인 완료로 읽지 말 것.** 런타임
  `src/**` 변경·회귀는 없으므로 현재 D+0 실행은 막지 않되, 보완은 다른 묶음에 섞지 않고
  `MASTER_PLAN.md §7` 예약 23 의 완료판정과 부정 회귀로 별도 구현한다. 전문 =
  `~/Desktop/개발현황/CODEX 현황/0801검토현황.md §33`.
- **현재 미해결 보류**: 예약 36·39·40·**41-g**·43·**44**·**45**·**46**·**47** 과
  §72.2·H′ 는 위 순서를 유지한다.
- 🔴 **예약 45 — `/detections` V1 계약은 닫혔는데 `tunnel_interfaces` 가 없다**.
  미제출은 역할 A 하나뿐이다. ⏸ 착수는 이 D0-FW 묶음 종료 직후.
  상세·회신 대기 4건 = `PROJECT_CONTEXT §4.1`.
- 🔴 **예약 41-g 예약 진입 금지 문장은 유지된다** — R4/R6 **판정**은 `R3 PASS` AND
  41-g 완료가 둘 다 선 뒤 R4, 이어 `R4 PASS` 뒤에만 R6 다.

## 진행표

| 트랙 | 묶음/근거 | 상태 |
|---|---|---|
| **진행 중 묶음** | §79 뒤 — 41-g 계약 4판 + 1.6.3 계측 구현 + 08-18 현장 판독 | ✅ 구현 완료 · 🔴 독립검토 대기 |
| 1.6.3 굽기 | ✅ `build=Aug 18 2026 12:32:35` · 현장 후보(승인 아님) |
| 예약 41-g 링크 정지 | 🔶 **발행 층으로 분류됨** · 후보 ①④ 기각 · ③ 약화 · ② 잔존 |
| 예약 43 R3 | 🔴 **FAIL** — `/scan` 닫힘 · `/odom`·`/imu` 300ms 공백 3건 |
| 예약 39 좌향 | 🟢 로봇 8.37°/3m + 바닥 1.12°/3m |
| 예약 40 각속도 | 🟢 마찰. 제자리 불감대 0.32 · 주행 중 없음 |
| 예약 44 우전륜 유격 | 🔶 허브 조치·반지름 일치 · 🔴 실효 윤거 미해결 → **예약 48** 신설 |
| 예약 45 `tunnel_interfaces` | 🔶 노트북 빌드 완료 · 🔴 Jetson·태그·SHA 남음 |
| 예약 46 dmesg / 47 지문 | ✅ 구간 덮음 / 🔴 축소판 6행 거부 |
| 승인 지문 이관 | 🔴 독립검토 전 금지 |
| `TODO(D+0)` **11건** | ✅ 종결 계약 유지 |

## 최종 회귀

- `bash tools/doc_check.sh` rc=0.
- 기준선 표기: pytest **184 passed** / colcon **247 tests** /
  **test_harness_guards 24 검사** / test_gate_regression **14 케이스**.
- tools pytest **222/222 + skip 1**(§79 **194** → 41-g 분류·문맥 회귀 **+28**;
  skip 1건은 `--capture` 왕복으로, 노트북은 커널 버퍼 접근이 막혀 있고 **Jetson 에서는
  돌아야 한다** — 08-18 에 `sudo` 로 실제 취득 성공), mission pytest **184/184**.
- 🔴 **link-stall harness 3단** `bash tools/link_stall_host_test.sh`:
  MCU 동작 **367** · 호스트 분류 **23**(서로 다른 분류 9종) · 구조 **14**.
- re-arm host **989/989 + 구조 11/11** (역회귀 — 숫자 불변).
- runtime/watchdog 묶음 **11/11**, firmware-info 상한 **1418/1664**(여유 245).
- firmware precheck 실제 저장소는 승인 지문 미이관으로 **rc=1 이 정상**.
- harness guards **24/24**, gate regression **14/14**, colcon **247/247**.
- 변경 Python flake8(99), `py_compile`, `git diff --check` rc=0.
- 알려진 P0/P1 코퍼스 **158/158**.

## 역할 교대

대상 `c5becb9` 구현자 = **Claude**. 이번 묶음 구현자도 **Claude** 이고, 그 diff 의 최종
판정은 다른 검토자가 쓴다. 🔴 **41-g 4판은 구현자 논증이 아니라 host harness 가 3판을 반증한 결과**이고 6행 개정은
사용자 결정이다. 계약+1.6.3 을 묶어 독립검토 뒤에야 41-g 완료를 주장한다.

## 근거 문서

- `MASTER_PLAN §7` — **41-e·41-f·41-g**(4판) · **예약 42·43** · 예약 **45·47·48**.
- `REAL_ROBOT_VALUES §1-g` runtime · **§1-h** 구동 특성 · **§1-i** Q1 기울기 ·
  `FREEZE_MANIFEST §10.28` · `FIRMWARE_REBUILD §4-c-3`.
- `PROJECT_CONTEXT §4.1·§4.2` `/detections` 계약 · `ELECTRICAL_BASELINE §4-f` 계열.
- `JETSON_SETUP §7-c-E` 13행 · `§7-c-E2` 축소판(예약 47 로 거부 상태).
- 도구: `tools/link_stall_host_test.sh` · `link_stall_classify.py` ·
  `link_stall_bag_extract.py` · `goal_ahead.py`.
- 시간순 역사: `~/Desktop/개발현황/0817_현황.md`·`0818_현황.md`.
