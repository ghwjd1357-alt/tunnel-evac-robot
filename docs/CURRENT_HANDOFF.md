# CURRENT_HANDOFF.md — 지금 할 한 묶음 (매 세션 시작점)

> 작업 완료 시 구현자가 갱신하고 검토자가 사실관계·테스트 결과를 확인한다.
> **현재 묶음 + 미해결 보류만** 유지한다 (`AGENTS.md §5`) — 완료된 서사는 정본으로 보내고 링크한다.

- **동결 기준점**: `212885a8292d1e86677d5beeb9f5358d76fe9b40` = tag
  **`platform-core-freeze-260724`**. 증거 = `docs/FREEZE_MANIFEST.md`.
- **현재 기준 커밋**: 이 파일과 같은 커밋(main).
- **현재 단계**(이력 정본 = `MASTER_PLAN §8`): 5단 완료 · 6단(역할 B V1)은 회의 대기
  → D+0 검사 1~8 OK(08-07) · 전기 시공 완주 · re-arm 래치 굽기 완주(08-11)
  → 예약 32 계수 `335` 굽기·지면 확정(08-12) · ❌ 32-d 는 전제 오류로 기각(08-13)
  → ✅ **예약 32-e 재굽기 · R2 · A-10 · R5 지도 완주**(08-14 낮)
  → ✅ **예약 41 EKF NaN 원인 확정 + `odom_guard` 완주**(08-14 저녁 · 동결 예외 6회차)
  → ✅ **예약 43 ② 원인 확정 + 보완**(08-17 · 로봇 없이) · **플랜 B 판정 = 미발동**(08-15 기준)
  → **🔴 `MASTER_PLAN §7` 예약 41 뿌리 물리 보완 · Tier A(전기·기구)** (지금 — 2026-08-17)

> ✅ **08-14 에 닫은 것 — 서사는 정본으로 보냈다.** 굽기 · `§5-G6` 10/10 · 라이다 `0.779` ·
> **R2 `0.997`·`0.993`** · A-10 · **R5 지도**(132.5 m · 최협부 **1.65 m**) · **EKF NaN 원인
> 확정 + 가드** · 뿌리 조사 7종. 전문 = `~/Desktop/개발현황/0814_현황.md`.
> 서사 = `MASTER_PLAN §7` **32-c-2**·**40**·**41**·**42**·**43** · `FREEZE_MANIFEST §10.26`·**§10.27**.

**⚠ 미해결 보류 — 어느 것도 승인 완료로 읽지 말 것** (본문 정본 = `MASTER_PLAN.md §7`):

- **🔴 예약 41 뿌리** (08-14) — **이 묶음.** 지면 주행 충격이 접촉을 열화시킨다(추정).
  ✅ 가드로 NaN 은 막았다. 🔴 **원인은 안 없앴다.**
- **🔴 예약 43** (08-14) — ① **R3 미통과**(`/odom` stamp 역행 8,181 · 13.1%) 🔴 **R6 보다 먼저다.**
  ② `/cmd_vel` 미기록 = ✅ **08-17 원인 확정·보완**(rosbag2 구독이 TRANSIENT_LOCAL 로 굳어
  VOLATILE 인 teleop 과 매칭 안 됨 · `tools/bag_qos_overrides.yaml`). 🔴 **현장 확인만 남았다.**
- **🔴 예약 40** (08-14) — 주행 중 각속도 **전달률 25%**. 곡률 권한은 편향의 6배라 추종은
  성립한다. `rotate_to_heading` 동결 예외는 **지도가 선 뒤**에 요청한다.
- **⏸ 예약 42** (08-14) — 아크릴 3mm 4면(+3.2kg). **미장착.** 붙이면 R2·C10·차고·위험문장·
  URDF 를 전부 다시 한다. 08-14 R2 는 **"현 하중 한정 잠정"** 이다.
- **⚠ 미해결 보류 — 예약 23 / 검토 §33 (`8fcc1a2`)**: P0 0 · **P1 1** · P2 1.
  `handoff_single_check.sh` 는 `§` 없는 새 작업 꼬리를 붙여도 `OK` 를 내고, 숫자 계약 전수
  증거는 기록 6자리와 독립 실행 21자리가 불일치한다. **승인 완료로 읽지 말 것.** 런타임
  `src/**` 변경·회귀는 없으므로 현재 D+0 실행은 막지 않되, 보완은 다른 묶음에 섞지 않고
  `MASTER_PLAN.md §7` 예약 23 의 완료판정과 부정 회귀로 별도 구현한다. 전문 =
  `~/Desktop/개발현황/CODEX 현황/0801검토현황.md §33`.
- **예약 24·26** — 라이다 실측·readiness 소유 분리 / 사용자 결정 '방법 B' 미구현.
- **예약 27** — ✅ **08-11 에 해소됐다**(`nvcc` 는 `PATH` 오진 · `nvidia-jetpack` 설치 · TensorRT
  10.3 · `torch.cuda` True). 🔴 **남은 것은 그 결정을 역할 B 계약에 적는 것뿐이다.**
- **예약 28·29·30** — 전기 P1(§44·§45 ①② · §53 6건) · 정본 절 분리. ⏸ 실차 사다리 뒤.
- **예약 31** (08-11) — ✅ 로컬 **72건**. 🔴 **원격 push 는 사용자 몫** — 아직 백업이 아니다.
- **⏸ 예약 32-b** (08-12) — 조건부 수용 보류. **재개방 = 재발현 3회 + 만충.**
- **🔴 예약 36** — IMU 반복 사망(I2C 물림) · 복구 = 전원 재인가 3분.
- **예약 33·34·39** — PI 재조정+`appliedPwm`(R6 전) · 문서→코드 취약 참조 48곳 · 좌향 편향.
- **⏸ 6단 보류** — 역할 B 회의가 잡히면 `30c5e87` 핸드오프 원문을 복원한다.

## 이번 한 묶음 목표 — `MASTER_PLAN §7` 예약 41 뿌리 물리 보완

**어디까지 왔나** — 08-14 저녁에 시험 **7종**을 돌려 가설 **6개를 기각**했다. 전문 = `§7` 41-d.

```
필요조건    🔴 바퀴가 지면에 닿아 구를 것
            띄워서 같은 부하로 179초 돌려도 결함 0 (3,922 cps = 지면의 1.11배)
            같은 bag 의 지면 주행 181초는 역행 11.36%   ← 음성·양성 대조가 한 판 안에 있다
아님        엔코더 인터럽트 · 모터 전류 · SLAM 부하 · 지속시간 · /firmware/info 주기 · 손 진동
발현        즉시가 아니라 **갑자기** 시작하고(지면 2분은 깨끗) 시작하면 **악화**(44%→50%)
```

🔴 **제1 용의자 = Teensy USB 커넥터·케이블.** 증상이 모터 오작동이 아니라 **직렬 전송 결함**
이다. PWM/DIR 점퍼가 헐거우면 모터가 이상하게 돌지 메시지가 찢어지지 않는다.

## 완료조건

1. 🔴 **발현하는 주행에서 `sudo dmesg -T -w` 를 같이 받는다.** 발현 시각에 `usb … reset` ·
   `cdc_acm … error` 가 찍히면 USB 확정, 안 찍히면 USB 기각. **08-14 `dmesg_A` 는 정지
   시험이라 깨끗했다 — 발현하는 주행에서는 아직 안 받았다.**
   ✅ **그 주행 한 번이 예약 43 ② 현장 확인도 같이 만족시킨다** — bag 에 `--qos-profile-
   overrides-path` 를 붙이고, teleop 을 켠 뒤 `ros2 topic info /cmd_vel -v` 에서 rosbag2
   구독의 `Durability: VOLATILE` 을 확인한 다음, 사후에 **비영 `/cmd_vel` 이 조작 횟수만큼**
   담겼는지 본다. 🔴 **따로 주행하지 않는다.**
2. 물리 보완 — 확정된 자리에 맞춰 **스트레인 릴리프**(USB) 또는 **락킹 커넥터·나사 터미널**
   (신호선). ⚠ **PCB + 헤더핀 + 듀폰은 접촉이 여전히 마찰이다** — 브레드보드를 없애는
   이득은 진짜지만 절반이다.
3. 🔴 **보완 뒤 `lift_I` 와 같은 설계로 재시험** — 같은 bag 에 음성(띄움)·양성(지면) 대조를
   둘 다 넣는다. `map_1512` 가 9.3분 주행에 0 이어서 **단일 음성으로는 아무것도 못 닫는다.**
4. `bag_gap_report` 로 R3 재판정(예약 43) — 20분 이상 주행에서 rc=0.
5. 회귀 보조: `test_odom_guard` · `rearm_gate_host_test` **967+7** · `test_ai_context`
   **37 검사** · `firmware_precheck` **rc=0**.
6. 기준선 유지: pytest **184 passed** · colcon **247 tests** · harness guards **24 검사** ·
   gate regression **14 케이스**.
7. ⚠ 이 묶음은 `firmware/**` 를 굽지 않는다 — `§7-c-E2` **배선 6행**과 `§5-G6` 10/10 을
   다시 하지 않는다. 🔴 **계약은 그대로 6행이다**(축소 근거 = `JETSON_SETUP §7-c-E2`).
8. `JETSON_SETUP.md §9` `TODO(D+0)` **11건**은 08-11 에 전량 종결됐다 — 여기서 다시 열리는
   것은 없다.

**금지** — `firmware/**` 재굽기 · `CMD_WHEEL_BASE` 변경 · FF/게인(예약 33) ·
`rotate_to_heading`(예약 40) · 아크릴 장착(예약 42) · **R6(Nav2) 진입**(예약 43 이 먼저) ·
`make_map.sh` 실런 · Gazebo E2E 동시 실행.

## 🔴 노트북 병행 트랙 — 로봇 없이 · 다른 세션이 집어가도 된다

이 묶음(Tier A)과 **섞지 않는다.** 로봇 앞 작업이 막히거나 사람이 남을 때 여기를 연다.
🔴 **순서가 있다 — ①이 ②의 입력이다.**

**① 🔴 08-14 Tier A 검토 2건 — 아직 안 태웠다** (`AGENTS §6`: Tier A = 독립 검토 왕복 필수)

| 커밋 | 무엇 | 왜 Tier A |
|---|---|---|
| `773d1a7` | `urdf/robot_real.urdf` 라이다 `0 0 0 → 0 0 0.779` | `src/**` · 동결 예외 **5회차** |
| `e9ea6af` | `config/ekf_real.yaml` `odom0 → /odom_guarded` + 런치 2개 | `src/**` · 동결 예외 **6회차** |

🔴 **`git log --grep='Review-round'` 에 둘 다 없다** — 회차가 붙은 마지막은 `8def39c` 로
08-14 Tier A 작업 **이전**이다. 즉 **동결 예외 두 번이 검토 없이 들어갔다.**
⚠ 검토가 뒤집을 수 있는 것은 *"증명됐다"는 주장*이지 로봇의 상태가 아니다 —
불승인이 나와도 P0 만 멈추고 P1 은 조건부 수용으로 등록하고 간다(`AGENTS §6`).

**② R4 — EKF 단독 검증 (bag replay)** · 절차 = `docs/D1_FIRST_STEP.md` · 설정 = `ekf_real.yaml` 머리말
합격 = *"yaw 널뜀 없음 · 정지 드리프트 미미"*. 재료 = `map_0814_1611`(가드 통과 · NaN 0 · 22.8분).
🔴 **①이 먼저다** — R4 는 `ekf_real.yaml` 이 정의한 융합을 검증하는데, 그 파일의 변경이
아직 검토를 안 받았다. 검토가 `odom0` 을 뒤집으면 R4 결과가 통째로 무효다.
⚠ **R4 가 R3 를 대신하지 않는다** — 사다리는 `R3 → R4` 이고 R3 는 예약 43 ①로 비어 있다.
R4 를 통과해도 **R3 를 건너뛴 것은 그대로 남는다.**

**③ 그 밖 (순서 무관)** — 예약 23(`handoff_single_check.sh` P1) · 예약 34(문서→코드 취약
참조 48곳) · 🔴 예약 31(Desktop md **72 개** 원격 push — **사용자 몫**).

## 🔴 이번 묶음의 함정

**① 단일 음성으로 닫지 않는다.** `map_1512` 는 9.3분을 주행했는데 결함이 0 이었다.
**양성 대조가 같은 판 안에 없으면 "그날은 안 나는 날" 과 구별되지 않는다.**

**② 부하가 모자란 시험을 "음성"으로 적지 않는다.** `spin_H` 는 손으로 바퀴를 돌렸는데
주행의 **0.24배**밖에 못 냈다. 그건 **판정 불능**이지 기각이 아니다.

**③ 자기 가설을 자기가 확증하지 않는다.** 08-14 에 세운 가설 6개 중 5개가 재현·전수에서
무너졌다. `/firmware/info` 주기는 **표본 6개에서 5초 배수를 보고** 세운 것이었고, 전수로
재니 대조군과 같았다.

**④ 깨진 메시지의 자기 값으로 상태를 판정하지 않는다.** 찢어진 `/odom` 의 `vx` 는 쓰레기라
그걸로 "전진/후진"을 매기면 순환논법이다. **이웃 표본으로 판정한다.**

## ★ 현장 운용 (상세는 링크가 정본)

1. 🔴 **기동 순서** — `퓨즈 → XT90 → Jetson 부팅 대기 → 런치 → **Teensy VIN 만 재삽입**`.
   ⚠ `real_mapping.launch.py` 는 agent·가드를 **자기가 띄운다.** 런치 앞에
   `source ~/uros_ws/install/local_setup.bash` 를 안 하면 즉사한다.
2. 🔴 **종료는 `Jetson sudo shutdown -h now` 가 맨 앞** (`ELECTRICAL_BASELINE §7`).
3. 🔴 **배선을 만질 땐 XT90 분리.** 유일한 예외 = 펌웨어 업로드(`FIRMWARE_REBUILD §5-b`).
4. 오염 판정은 **`firmware_precheck.sh` 뿐** · 🔴 `rc=2` 는 판정 불능이지 통과가 아니다.
5. 🔴 **브레드보드 상태다** — 08-14 조사가 여기를 가리켰다.
6. 🔴 런치 화면에 **`[odom_guard]: 통과 … · 버림 …`** 이 안 보이면 가드가 안 뜬 것이고,
   `/odom_guarded` 가 조용히 비어 **EKF 가 odom 없이 돈다.** 그 상태로 지도를 만들지 않는다.
   버림 비율이 **1% 를 넘으면** 창을 넓히지 말고 원인을 본다.
7. 🔴 **지도 저장은 둘이고 타입은 `SerializePoseGraph`** · `map_saver_cli` 는
   `-p save_map_timeout:=20.0` 을 붙인다 · **저장 → 파일 4개 확인 → 종료** (`PITFALLS §7`).
   현장 절차 정본 = `~/Desktop/0814_지도제작_런북.md`.

## 사용자 결정 — 유지

- 동결 예외 **6회 소진**(08-14 §10.26 라이다 · §10.27 가드) — 다시 열려면 사용자 승인.
- 순서 = **예약 41 뿌리 → 예약 43(R3·`/cmd_vel`) → 예약 40 → R6** (`MASTER_PLAN §3`).
- 🔴 **지면 주행 승인은 "그날 하루" 단위다.** 08-14 분은 받았다. 안전요원 E-stop ·
  양옆 1m · 경사 금지 · **네 바퀴 방향 육안**은 매번 그대로다.
- 🔴 **위험 수용 문장은 08-14 에 개정됐다** — 정본 = `MASTER_PLAN §7` **32-c-2**.
  적용 속도 `0.128 m/s` · 정지거리 `6.6 cm` · **재개방 = ① 안전요원 없음 ② 11 cm 초과
  ③ 순항 0.2 m/s 초과.** 🔴 **사용자가 직접 정하고 확정했다 — 구현자가 안 고친다.**

| 트랙 | 범위 | 상태 |
|---|---|---|
| **진행 중 묶음** | §7 예약 41 뿌리 물리 보완 | 🔴 **Tier A.** 완료조건 = 위 1~8 |
| 🔴 예약 43 ① | R3 미통과 (stamp 역행 13.1%) | 🔴 **R6 보다 먼저** |
| ✅ 예약 43 ② | `/cmd_vel` 미기록 = rosbag2 구독 durability | ✅ 08-17 보완. 현장 확인만 |
| 🔴 검토 미태움 | `773d1a7`·`e9ea6af` (동결 예외 5·6회차) | 🔴 **Tier A 인데 회차 0** |
| ⏸ R4 | EKF 단독 검증 (bag replay) | ⏸ 노트북 · **검토 뒤** |
| 🔴 예약 40 | 주행 중 각속도 전달률 25% | 🔴 지도 선 뒤 동결 예외 요청 |
| ⏸ 예약 42 | 아크릴 3mm 4면 (+16%) | ⏸ 미장착. R2 는 **잠정** |
| ✅ 예약 41 1차 | `odom_guard` | ✅ 실차 22.8분 NaN 0 (§10.27) |
| ✅ 예약 32-e | 상수 정정 재굽기 | ✅ **닫힘**. R2 `0.997`·`0.993` |
| ⏸ 예약 32-b | 무장 해제 12회+ | ⏸ 조건부 수용. "해소" 아님 |
| 🔴 예약 36 | IMU I2C 물림 | 🔴 복구 = 전원 재인가 3분 |
| 🔴 예약 39 | 좌향 계통 편향 | 🔴 08-14 C10: 좌측만 3.2% 갈림(잡음 경계) |
| ⚠ 릴레이 | 정격 미증명 · 08-10 수용(`§16-e`) | 🔴 끼임 11A 미관측 |
| ⏸/❌ 예약 21·23·24·26·27·29·30·31·33·34 · 역할 B | — | ❌ 이 묶음에 섞지 않는다 |

## 보존해야 할 안전 불변조건

- 🔴 **정지 수단은 E-stop 이다.** `Ctrl+C` 를 E-stop 으로 승격하지 않는다.
- 🔴 **끼임(약 11A) 상태의 의도적 E-stop 시험 금지**(`ELECTRICAL_BASELINE §13`).
  ⚠ 08-14 `stall_E` 는 PWM 30~42(추정 2.3A)라 그 조건이 아니었고, E-stop 도 안 썼다.
- 🔴 **`§5-G6` 10/10 이 보인 것은** *"누르면 토픽이 따라온다"* 이지 *"안 눌렀을 때 안
  뜬다"* 가 아니다. **거짓 양성은 그 시험이 원리적으로 못 잡는다.**
- 🔴 **`/estop/state`·`/drive/diag` 는 1Hz 다** — 수십 ms 사건은 표본 사이로 빠진다.
- 🔴 **배선 전 XT90 분리 + 퓨즈 제거** · 만졌으면 **개폐 10회 재검증**.
- 🔴 **펌웨어 업로드는 모터 전력 0V·바퀴 공중에서만** · 지문 `10607`/`158` 불일치면 금지.
- 🔴 **펌웨어를 또 구우면 `§5-G6` 10회 + `§7-c-E` 를 다시 한다.**
- ⚠ **watchdog 정지는 무장을 풀지 않는다** — 발행자가 살아나면 재무장 없이 다시 구른다.
- 🔴 **모터 명령 전에 네 바퀴가 같은 방향으로 도는지 본다** (`PITFALLS §10`).
- 🔴 **지면 주행 전 `ros2 bag record -a`** · 지도 세션은 `-x "/map|/map_updates"` 로 줄인다.
  🔴 **`--qos-profile-overrides-path ~/ros2_ws/tools/bag_qos_overrides.yaml` 을 반드시 붙인다** —
  없으면 teleop 명령이 **에러 없이 0건**으로 빈다(08-14 에 3세션을 그렇게 날렸다 · 예약 43 ②).
- 🔴 **무장 직후 500 ms 는 정숙 장벽이다** — 그 안에 비영 명령을 넣으면 `y=6` 으로 풀린다.
- 🔴 **명령이 0 이면 PWM 도 0 이다**(`.ino:609` `COMMAND_DEADBAND`). 무장만으로는 모터가
  구동되지 않는다 — 08-14 `stall_E` 설계가 이 사실 위에 섰다.
- 🔴 **각도를 양끝점으로 재지 않는다** — 쿼터니언 yaw 는 ±180° 로 접힌다(`PITFALLS §12`).
- 🔴 **`TODO(D+0)` 11건**을 추측값으로 닫지 않는다 — 미해결은 이유와 재확인 시점을 적는다.
- 실차 없이 확인한 합성 bag·가짜 `ros2` 결과를 실차 통과로 승격하지 않는다.
- 🔴 **실물이 안 하는 일을 해 주는 합성 데이터는 검사를 통과시키는 일만 한다**(08-14 실증).

## 금지 범위

- 🔴 **`330Ω` 을 25V·5V·VIN 에 붙이기** — Teensy 자체 `3.3V` 핀뿐이다.
- 🔴 **단일 음성으로 예약 41 을 닫기** — 양성 대조가 같은 판에 있어야 한다.
- 🔴 **부하 미달 시험을 "기각"으로 적기** — `spin_H` 는 판정 불능이다.
- 🔴 **가드 판정 창을 넓혀 버림을 줄이기** — 나쁜 값이 EKF 로 들어간다.
- 🔴 **08-14 R2 결과를 "확정"으로 쓰기** — 아크릴 미장착의 **잠정**이다(예약 42).
- 🔴 **R3 를 건너뛰고 R6 로 가기** (예약 43).
- 🔴 **도구 상수를 손으로 베껴 적기** — `tools/firmware_constants.py` 로 `.ino` 에서 읽는다.
- 🔴 **위험을 "덮였다"로 기록하기** — 전제조건 + 재개방 조건 형식으로만.
- 🔴 **등록을 종결로 쓰기.** 예약 40·41·42·43 이 생겼다고 고쳐진 것이 아니다.
- 🔴 **핸드오프 상한에 걸렸다고 상한을 올리기.** 먼저 **옮길 완료 서사**를 찾는다.
- 사용자 추가 승인 없는 `src/**` 변경 · `make_map.sh` 실런 · 지도 자산 변경.
- 예약 20~34·39·40·42, 역할 B V1 을 이 묶음에 혼합.

## 완료 판정

**발현하는 주행의 `dmesg` 로 USB 여부가 갈리고, 그에 맞는 물리 보완이 들어가고, `lift_I` 와
같은 설계(같은 bag 에 띄움·지면 대조)로 재시험했을 때 지면 구간의 `stamp` 역행이 0 이며,
`bag_gap_report` 가 20분 이상 주행 bag 에서 rc=0 이면 완료다.**
🔴 양성 대조가 없는 판은 판정에 안 쓴다. 🔴 부하가 모자란 판도 안 쓴다.
⚠ 굽기가 없는 묶음이라 `§7-c-E2` **배선 6행**·`§5-G6` 10/10 은 판정에 안 들어간다 —
**계약이 바뀐 게 아니라 이번에 해당이 없는 것**이다. `TODO(D+0)` **11건**도 종결 상태 그대로다.

## 완료 후 다음 단계

✅ **`2026-08-15` 플랜 B 판정 = 미발동** (R2 오차 `0.3%` · 기준 3% · 정본 = `MASTER_PLAN §6`).
🔴 **`2026-08-18` 완주 — 내일이다.**

1. **예약 43** — ① R3 재판정. 🔴 **R6 보다 먼저다.** ② `/cmd_vel` 은 ✅ 보완 완료 · 현장 확인만.
2. **예약 40** `rotate_to_heading_min_angle 0.6→0.2` — 🔴 런타임 파라미터라 **동결 예외 없이
   시험할 수 있다**(`ros2 param set`). 값이 정해진 뒤에 yaml 을 고친다.
3. **A-5 최종 확인** — 지도 최협부 `1.65 m`(줄자 확인). 🔴 지도에서 잰 폭은 격자 때문에
   실제보다 **최대 0.10 m 좁다** — 판정 때 이 보정을 빼먹지 않는다.
4. R4 → R6. 🔴 **`R0→R1→R2→R3` 순서를 건너뛰지 않는다.**
   `TODO(D+0)` **11건** 전량 종결이라 여기서 다시 열리는 것은 없다.
5. **블록 12** 증거 복제 · **예약 31** Desktop md **72 개** private repo 화(**아직도 안 했다**).

## 근거 문서

- 규율 티어·회차·은퇴 = `AGENTS.md §6` · 결정 색인 = `MASTER_PLAN §8` · 예약 = `§7`
- 뿌리 조사 7종 = `MASTER_PLAN §7` **41-d** · R3·`/cmd_vel` = **예약 43**
- 동결 예외 = `FREEZE_MANIFEST §10.26`(라이다)·**§10.27**(가드) · 전기 = `ELECTRICAL_BASELINE §7`,`§13`
- 지도·저장 함정 = `PITFALLS §7` · 줄자·각도 함정 = `§12` · 반지름 3종 = `REAL_ROBOT_VALUES §1-b-0`
- 실행 순서 = `JETSON_SETUP §4-d`,`§6-b`,`§7-c-E2`,`§9` · 현장 런북 = `~/Desktop/0814_지도제작_런북.md`
