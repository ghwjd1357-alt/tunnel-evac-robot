# CURRENT_HANDOFF.md — 지금 할 한 묶음 (매 세션 시작점)

> 작업 완료 시 구현자가 갱신하고 검토자가 사실관계·테스트 결과를 확인한다.
> 이전 묶음 내용은 날짜별 현황으로 보내고, 이 파일에는 현재 작업과 직전 주장만 유지한다.

- **동결 기준점**: `212885a8292d1e86677d5beeb9f5358d76fe9b40` = tag
  **`platform-core-freeze-260724`**. 증거 = `docs/FREEZE_MANIFEST.md`.
- **현재 기준 커밋**: 이 파일과 같은 커밋(main).
- **현재 단계**: 마스터플랜 5단 완료 → 역할 B V1 합의(6단)는 회의 대기 보류
  → 실차 D+0 인수·연결 **부분 완료(`d0_check` 종료 2 · 검사 8 미확인)**
  → 검토 §39 불승인·§40~§42 회신 수령(08-04)
  → **전기계통 정본 v2 전면 개정**(08-04)
  → §39 문서·코드 표면 보완 = **예약 25 종결**(08-05 `870f1da`~`3c62381`)
  → **전기계통 수급 실물 반영 = 정본 v3 · 검토 §43 대상** **(지금 — 2026-08-06)**

> ⚠ **§43 은 아직 존재하지 않는 번호다.** 이 묶음은 검토 발견에서 나온 것이 아니라
> **부품 수급이라는 현장 사실**에서 나왔다. `handoff_single_check` 의 세 자리 계약이 § 하나를
> 요구하므로 **이 묶음을 검토할 슬롯 번호를 미리 잡은 것**이며, 발견사항이 아니다.
> 따라서 검토현황 문서의 §43 을 인용하지 않는다 — 그런 절은 아직 없다.

- **⚠ 미해결 보류 — 예약 23 / 검토 §33 (`8fcc1a2`)**: P0 0 · **P1 1** · P2 1.
  `handoff_single_check.sh` 는 `§` 없는 새 작업 꼬리를 붙여도 `OK` 를 내고, 숫자 계약 전수
  증거는 기록 6자리와 독립 실행 21자리가 불일치한다. **승인 완료로 읽지 말 것.** 런타임
  `src/**` 변경·회귀는 없으므로 현재 D+0 실행은 막지 않되, 보완은 다른 묶음에 섞지 않고
  `MASTER_PLAN.md §7` 예약 23 의 완료판정과 부정 회귀로 별도 구현한다. 전문 =
  `~/Desktop/개발현황/CODEX 현황/0801검토현황.md §33`.
- **⚠ 미해결 보류 — 예약 24 / 검토 §38**: P0 0 · **P1 2** · P2 4. 라이다 실측 소유 시점과
  readiness flake 소유 정본이 갈라졌다. **라이다 높이는 D+1 소유**이며 `JETSON_SETUP.md §9`
  의 `TODO(D+0)` 목록 계약은 그대로다.
- **⚠ 미해결 보류 — 예약 26 / 검토 §40**: 사용자 결정 **'방법 B'** 가 구현되지 않았고 저장소
  에서도 사라졌던 건. 결정은 `MASTER_PLAN.md §8` 에 복원했다. **§37.3 을 닫힌 것으로 인용하지
  않는다.**
- **⚠ 미해결 보류 — 예약 27**: 이 Jetson 에 `nvcc`·`nvidia-jetpack` 이 **없다**. 역할 A 는
  비차단, **역할 B 성능 전제는 통째로 바뀐다.** 역할 B 회의 안건.
- **⏸ 6단 보류**: 역할 B 회의가 잡히면 커밋 `30c5e87`의 핸드오프 원문을 그대로 복원한다.

```bash
git show 30c5e87:docs/CURRENT_HANDOFF.md
```

## 이번 한 묶음 목표 — 전기계통 수급 실물 반영 = 정본 v3 (검토 §43 대상)

**08-06 부품 수급이 끝나면서 시공안의 전제 4개가 실물과 갈라졌다.** 그 상태로 시공에 들어가면
세 자리에서 막힌다. 이 묶음은 **새 설계를 하는 회차가 아니라, 실물이 바꾼 것을 정본에
반영하고 그 파급을 전수로 닫는** 회차다. `src/**` 는 열지 않는다 — 변경 표면은 문서뿐이다.

### 수급 완료분 (사용자 확인, 08-06)

릴레이 **4개** · LiPo 저전압 알람 · 배선 · **30A 차량용 퓨즈 + 소켓** · **2A 차량용 퓨즈 + 소켓** ·
E-stop 스위치(**LAZ16-11ZS** · 단자 3개) · 다이오드 · Wago · 멀티미터 · 열수축 튜브 · 절연테이프

### 실물이 바꾼 전제 4개 (구현자가 주장하는 것)

- **① 메인 차단 스위치를 사지 않았다 → 위험 `ELECTRICAL_BASELINE.md §2`-③ 은 닫히지 않는다.**
  E-stop 이 들어가도 **줄어드는 것은 아크 위험뿐**이고 정비 안전 수단은 여전히 커넥터 하나다.
  "E-stop 이 있으니 덮였다"로 쓰지 않고 **전제조건 4 + 재개방 조건 4** 로 공개했다(`§7`).
  그 결과 **배터리 스트랩 고정이 완화책에서 성립 조건으로 승격**됐다 — XT90 을 상시 개폐
  수단으로 쓰면 뽑는 힘이 그대로 배터리에 전달되고, 그 지점은 퓨즈보다 앞이라 무보호다.
- **② 릴레이가 4개라 PIN21 2단계를 이번 회차에 넣는다.** 신호릴레이 몫(릴레이3)이 생겼고
  08-05 라이브러리 수령으로 재빌드 능력이 확보됐다 — 두 전제가 같은 시점에 맞았다.
  🔴 **하드웨어와 펌웨어는 반드시 함께 바뀐다**: 현재 `ESTOP_ACTIVE_LOW = true` 는 PIN21 LOW 를
  "눌림"으로 읽는데 릴레이3 정상 상태가 LOW 라, **펌웨어를 안 바꾸고 꽂으면 로봇이 항상 정지**
  한다. 그래서 실행 순서를 **배선(PIN21 절연) → 1차 검증 → 펌웨어 → PIN21 → 2차 검증** 으로
  쪼갰다. 한 번에 바꾸면 배선 탓과 펌웨어 탓을 못 가른다(`AGENTS.md §3-6`).
- **③ 메인 퓨즈홀더가 리드선형이다 → 리드선 굵기가 하드 게이트가 됐다.** 구판이 *MAXI 나사식*
  을 요구한 이유는 "12AWG 를 물리려면"이었다. 리드선형은 12AWG 를 물릴 필요가 없으므로
  **같은 원칙(전선 ≥ 퓨즈)이 리드선 굵기로 옮겨 적용**된다 — 완화가 아니다.
  **16AWG 는 허용 22A 로 최악 통전 23A 를 이미 넘어 즉시 탈락**이고, 이 판정은 **전선을 자르기
  전에** 해야 한다. 자른 뒤 막히면 로봇이 통째로 멈춘다.
- **④ 링터미널이 계통 전체에서 0개가 됐다.** 볼트 단자가 걸리던 자리는 ⓐ 메인 차단 스위치
  ⓑ 볼트식 퓨즈홀더 둘뿐이었는데 **둘 다 사라졌다.** 구판 `§3-3` 의 링터미널 항목과
  `§8`-⑧⑨ 확인 항목을 삭제했다.

### 전수로 센 것 (`AGENTS.md §3-10`)

구판 시공안(0804)을 가리키던 참조를 `grep -rn` 으로 전수 = **7자리**(4파일)를 전부 열어
0806 으로 옮기거나 역사로 표기했다. "메인 차단 스위치"가 걸리는 절은 `ELECTRICAL_BASELINE.md`
안에서 **§0·§2-③·§3-a·§3-b·§3-c·§3-d·§4-a·§6·§7·§8** 열 자리였고 한 자리씩 대조했다.
⚠ **이 열거는 `grep` 과 문서 목차로 만든 것이지 기억이 아니다.** 다만 **의미 수준의 누락은
기계로 못 센다** — 그 사각은 독립 검토가 본다.

### 닫은 것 / 안 닫은 것 (섞지 않는다)

| | 항목 |
|---|---|
| ✅ **닫힘** | `ELECTRICAL_BASELINE.md §2`-⑧ E-stop 신호 fail-danger — 릴레이3 a접점 + `ESTOP_ACTIVE_LOW = false` 로 모든 단선이 정지 쪽으로 떨어진다 |
| 🔴 **안 닫힘** | `§2`-③ XT90 개폐(스위치 미구매) · `§2`-② 배터리 미고정(전제로 승격) · `§2`-⑬ re-arm 래치 · 릴레이3 건접점(신규 🟡) |

🔴 **PIN21 완료는 re-arm 래치를 닫지 않는다.** R1·R2 차단 사유는 그대로다.

## 완료조건

1. `~/Desktop/전기계통_최종작업안_0806.md` 가 존재하고, **착수 전 하드 게이트 4개**(리드선
   굵기 · LAZ16 접점 식별 · Wago 포트 수 · 2A 퓨즈 형식)와 **부품 공백 4종**(배터리 스트랩+
   절연캡 · 케이블타이 · 스텝드릴 16mm · 선택 330Ω)이 **작업 시작 전 위치**에 있다.
2. `docs/ELECTRICAL_BASELINE.md` 가 v3 으로 개정되고, **v3 이 바꾼 것·닫은 것·안 닫은 것**이
   `§11` 한 곳에 전수로 있으며, 위험 `§2`-③ 이 **"닫힘"이 아니라 `§7` 전제조건 + 재개방 조건**
   으로 기록돼 있다.
3. `docs/REAL_ROBOT_VALUES.md §1-e` 의 차단소자·PIN21·퓨즈·추가 보호 네 행이 실물과 일치하고,
   **같은 값을 `ELECTRICAL_BASELINE` 과 중복해 적지 않는다**(링크로 연결).
4. 펌웨어 변경(`ESTOP_ACTIVE_LOW`)이 **구동부 협의 사항**임과 **되돌아가면 로봇이 항상 정지**
   한다는 사실이 인계 목록에 있다.
5. `bash tools/doc_check.sh` 전 항목 OK · `bash tools/handoff_single_check.sh` OK.
6. 회귀 정본이 그대로다: pytest **182 passed** / colcon **245 tests** /
   harness guards **24 검사** / gate regression **14 케이스**.
   ⚠ 변경 표면이 **문서 전용**이라 `TEST_GATES.md §7` 라우팅상 직접 실행 대상은
   **`doc_check.sh` 와 pytest** 뿐이다. colcon·harness·gate regression 은 `src/**`·`tools/**`
   무접촉이므로 재실행하지 않고 문서 수치 스윕으로만 확인한다 — **재실행했다고 쓰지 않는다.**

## ★ 이번 회차 최우선 확인 사항 — 전기계통 시공 (사용자 직접 · **미착수**)

> 2026-08-06 현재: **아직 시작하지 않았다.** 아래 안전 불변조건은 **전부 그대로 유효**하고,
> 물리 E-stop 은 **없는 것으로 취급**한다.

1. 시공 정본은 `~/Desktop/전기계통_최종작업안_0806.md` **§5 작업 순서**다. 판단 근거·위험
   논리가 필요하면 `docs/ELECTRICAL_BASELINE.md`, 부품 계약값은 `REAL_ROBOT_VALUES.md §1-e`,
   펌웨어 절차는 `docs/FIRMWARE_REBUILD.md`.
2. 🔴 **작업 전 XT90 분리 + 퓨즈 제거 + 멀티미터 0V 확인.** **v3 에는 메인 차단 스위치가
   없다.** E-stop 만 누르고 배선을 만지지 않는다 — E-stop 은 **모터 가지만** 끊고
   Wago·퓨즈·DCDC·로직은 살아 있다.
3. 🔴 **전선을 자르기 전에 퓨즈홀더 리드선이 14AWG 이상인지 먼저 잰다.** 16AWG 면 그 자리에서
   중단하고 홀더를 교체한다.
4. **퓨즈는 맨 마지막에 끼운다.** 다이오드 **3개**의 방향을 눈대중하지 않는다(거꾸로 = 코일
   통전 순간 배터리 단락). **절연 대상은 4자리** — 릴레이1·2·3 `[87a]` + LAZ16 `[NO]`.
   릴레이1·2 의 87a 에는 **+25V**, 릴레이3 의 87a 에는 **Teensy GND** 가 나타난다.
5. 🔴 **펌웨어를 바꾸기 전에 PIN21 을 꽂지 않는다.** 꽂으면 로봇이 항상 정지 상태가 된다.
6. `§6 중단 기준` 7가지 중 하나라도 걸리면 **멈추고 상의**한다(리드선 16AWG · 퓨즈 끊김 ·
   발열 · 눌러도 전압 유지 = 융착 의심 · 릴레이 "딱" 무음 · 타는 냄새/연기/부푼 배터리 ·
   빌드 지문 불일치).
7. 시공 끝 → **E-stop 개폐 10회 토글**로 실제 차단을 확인한다. 배선을 다시 만졌으면 10회를
   **다시** 한다. PIN21 까지 끝냈으면 **`/estop/state` 토글 10회**를 별도로 더 한다.
8. 다음 현장 세션의 **첫 명령**은 D+0 증거 복제다(`JETSON_SETUP.md §11-g` 보존 규정) —
   bag 2개·tegrastats 로그가 아직 Jetson 한 대에만 있다.
9. 전기 시공 자체는 `TODO(D+0)` **11건**을 닫지 않는다. 그 목록의 정본은
   `JETSON_SETUP.md §9` 한 곳이며, 시공 결과는 E-stop 항목의 **전제**만 만든다.
   ⚠ 단 **v3 에서 PIN21 2단계가 이 회차에 들어왔으므로**, 시공이 완주되면 검사 8 은
   **원리적으로 통과 가능**해진다 — 통과했다고 미리 적지는 않는다.

## 사용자 결정 — 유지

- 동결 예외 4회는 전부 소진됐다. `src/**`를 다시 열려면 사용자 승인이 필요하다
  (`FREEZE_MANIFEST.md §10`).
- 순서는 전기계통 시공 → E-stop 검증 → D+0 잔여 종결 → D+1 R3 rosbag → R4~R8이다
  (`MASTER_PLAN.md §3`).
- 예약 20·21·22·23·24·26·27과 역할 B 6단은 각각 정해진 트리거 전까지 별건으로 유지한다
  (`MASTER_PLAN.md §7`). **예약 25 는 08-05 에 종결**됐다.
- `JETSON_SETUP.md §9`의 `TODO(D+0)` **11건** 중 **열 자리**가 값과 함께 닫혔고, 미해결은
  **물리 E-stop 하나**다. 목록 계약은 바꾸지 않는다.
- 08-03 D+0 판정: 장치·주기·간격·QoS·전진 부호·EKF 생존·펌웨어 정체는 **통과**했고, 수정판
  `--no-manual` 재실행은 자동 항목 전부 통과·**종료 2**였다. **전량 통과로 기록하지 않는다.**
- 🔴 **08-06 개정 — 검사 8 의 전제가 바뀌었다.** 구판은 *"PIN21 미연결이므로 검사 8 은
  원리적으로 통과할 수 없다"* 였다. v3 은 **PIN21 2단계를 이 회차에 포함**하므로, 시공 +
  펌웨어 + 2차 검증이 **전부 끝난 뒤에는** 종료 0 이 가능하다. 그 전까지는 `--no-estop`
  종료 2 가 이 트랙의 정상 상한이며, 이는 **물리 차단이 없다는 뜻이 아니라 Teensy 가 그것을
  못 본다는 뜻**이다. 물리 차단 자체는 개폐 10회 토글로 증명한다.
- never-seen은 다른 놓침과 같이 역행 2회 → 보고 → 단독 탈출이다. 실차 R3 뒤 재평가한다.

| 트랙 | 범위 | 상태 |
|---|---|---|
| **진행 중 묶음** | §43 전기계통 수급 실물 반영 = 정본 v3 + 시공안 0806 (문서 전용) | **착수** |
| 전기계통 시공 | `전기계통_최종작업안_0806.md §5` (사용자 직접) | ⏸ **미착수** (08-06 확인) |
| 실차 D+0 | 연결·주기·QoS·부호·펌웨어 통과, 종료 2 | ⏸ E-stop 설치 뒤 잔여 종결 |
| AI 입력 최적화 | `ef25ad3` 기준, 품질 불변 + raw 입력 30% 절감 | §40 승인 · 방법 B 잔여 = 예약 26 |
| 예약 21 | D0/R3 판정기 회귀 편입 + readiness case 9·11·12·14 간헐성 규명 | ⏸ 다음 bringup 변경 전 |
| 예약 23 | 핸드오프 ID·숫자 계약 전수 폐포 | ❌ 별도 보류 |
| 예약 24 | 검토 §38 정본 소유권·예약 완료판정 정합 | ❌ 다음 bringup/예약 21 전 |
| 예약 26 | 검토 §40.3 방법 B 계약 표면 재설계 | ❌ AI 라우터 표면 여는 묶음 |
| 예약 27 | 역할 B CUDA·JetPack 전제 | ❌ 역할 B 회의 |
| 역할 B V1 합의 | 커밋 `30c5e87` 원문 | ⏸ 회의 대기 |

## 직전 완료 — §39 코드 표면 = 예약 25 종결 (구현자 = Claude, 독립 검토자 = Codex 예정)

`3c62381`. `d0_check.sh` 가 **기다린 적 없는 시간을 사실로 말하는** 것을 끝냈다. `hard_timeout`
8자리 전부를 `clock_begin`/`clock_end` 로 재게 하고 실패 문장은 `why_snapshot()` 한 곳에서만
쓰게 했다. **역할 교대는 이 묶음 경계에서 원위치했다.**

## 직전 완료 — 실차 D+0 현장 기록 (구현자 = Codex, 검토 = §39 **불승인**)

`c397975`. Claude 의 §39 독립 검토가 **P0 0 · P1 2 · P2 12 로 불승인**했다. 08-05 로 §39 의
발견은 문서·코드 양쪽에서 **저장소상 전부 닫혔지만**, `c397975` **자체의 판정은 불승인
그대로**이고 그것을 뒤집는 것은 보완 커밋이 아니라 검토자다.

## 보존해야 할 안전 불변조건

- 물리 E-stop 이 없거나 검사 8이 미확인인 동안 **`/cmd_vel` 발행·R0~R2·Nav2·미션 금지**.
  `Ctrl+C` 를 E-stop 으로 승격하지 않는다. **08-06 현재 전기 시공은 미착수다.**
- 🔴 **`/estop/state` 는 시공안 `§5-G6`(2차 검증 개폐 10회 토글) 전까지 신뢰하지 않는다.**
  그 전에는 이 토픽을 소비하는 로직을 만들지 않는다. **하드웨어 차단 자체는 어느 경우에도
  온전하며**, 문제는 ROS 가 그것을 아는가일 뿐이다.
- 🔴 **배선 작업 전 XT90 분리 + 퓨즈 제거** (v3 에는 메인 스위치가 없다), 배선을 만졌으면
  **개폐 10회 재검증**.
- 🔴 **펌웨어 업로드는 모터 전력 0V·바퀴 공중에서만** (`FIRMWARE_REBUILD.md §5`).
  빌드 지문 `ARDUINO=10607`·`TEENSYDUINO=158` 이 안 맞으면 업로드하지 않는다.
- 🔴 **re-arm 래치는 아직 없다** — PIN21 완료가 이것을 닫지 않는다. 구현·부정/전환 시험 전
  R1·R2 금지 (`REAL_ROBOT_VALUES.md §1-d`).
- 판정기가 **관측하지 않은 것을 말하지 않는다** — 상한 초는 계약값 표기이지 경과 주장이 아니다.
- 설정은 대상 소프트웨어가 실제로 읽는지 `param list`·`topic info -v`로 확인한다.
- N초 관측은 같은 관측자가 종료 상태와 **벽시계 경과시간**으로 창을 완주해야 한다.
- 토픽별 계약의 단일 출처는 `tools/bag_gap_report.py`의 `TOPIC_POLICY`다.
- `/odom`은 RELIABLE, `/imu/*`는 BEST_EFFORT다. EKF 소비자 QoS까지 함께 본다.
- Teensy 재연결 로직 없음, IMU 치명 실패 시 노드 자체가 안 뜸, 시각 역행 시 stamp가 사실상
  멈춘다는 펌웨어 전제를 유지한다.
- 실차 없이 확인한 합성 bag·가짜 `ros2` 결과를 실차 통과로 승격하지 않는다.
- `TODO(D+0)` **11건**을 추측값으로 닫지 않는다 — 미해결은 이유와 재확인 시점을 적는다.

## 금지 범위

- 사용자 추가 승인 없는 `src/**` 변경. **이 묶음의 변경 표면은 문서뿐이다.**
- 예약 20·21·22·23·24·26·27, 역할 B V1, 관제 통보 표면을 이 묶음에 혼합.
- 🔴 **위험을 "덮였다"로 기록하기** — 스위치 미구매·건접점·re-arm 은 전부 전제조건 +
  재개방 조건 형식으로만 쓴다.
- 🔴 **전기 시공이 미착수인데 E-stop·검사 8 을 닫은 것으로 기록하기.**
- 🔴 **펌웨어를 실제로 굽거나 업로드하기** — 이 묶음은 문서 표면이다. 업로드는 사용자가
  시공 현장에서 `FIRMWARE_REBUILD.md §5` 전제(모터 0V·바퀴 공중) 아래 직접 한다.
- `make_map.sh` 실런, 지도 자산 변경, Gazebo E2E 동시 실행.
- Claude가 작성한 이번 문서 diff를 Claude가 최종 승인하기.

## 완료 판정

**시공안 0806 이 하드 게이트 4개와 부품 공백 4종을 착수 전 위치에 두고, `ELECTRICAL_BASELINE`
v3 이 바뀐 전제 4개·닫은 것 1건·안 닫은 것 4건을 `§11` 에 전수로 적었으며, 위험 `§2`-③ 이
전제조건 + 재개방 조건으로만 기록되고, `doc_check`·`handoff_single_check` 와 회귀 4수치가
전부 그대로면 이 묶음은 완료다.**
단, 완료는 **구현자 주장**이고 승인은 Codex 독립 검토다.

## 완료 후 다음 단계

1. **전기계통 시공**(사용자 직접) — `전기계통_최종작업안_0806.md §5` 완주 + 개폐 10회
   + (PIN21 까지 하면) `/estop/state` 토글 10회.
2. D+0 증거 복제(`JETSON_SETUP.md §11-g`) 후 `d0_check.sh` 현장 재실행 — 예약 25 보완으로
   실패 시 원인(상한 발동 vs 토픽 부재)이 화면에서 갈린다.
3. `TODO(D+0)` **11건** 중 남은 물리 E-stop, `/scan` 포함 판정, R0 watchdog 실측.
4. R0 watchdog 실측으로 `FREEZE_MANIFEST.md §6` 조건부 수용 재판정.
5. `docs/D1_FIRST_STEP.md`의 D+1 R3 rosbag 분석 → 예약 22와 R4~R8.

## 근거 문서

- 전기계통 사실·위험·최소 안전선·v3 개정 전수: `docs/ELECTRICAL_BASELINE.md §11`
- 전기 시공 절차(하드 게이트·결선·순서·검증): `~/Desktop/전기계통_최종작업안_0806.md`
- 구판 시공 절차(역사 — 메인 스위치·MAXI 나사식 전제): `~/Desktop/전기계통_최종작업안_0804.md`
- 펌웨어 재빌드·업로드 절차·빌드 지문: `docs/FIRMWARE_REBUILD.md`
- 실차 값·부품 계약값·펌웨어 전제: `docs/REAL_ROBOT_VALUES.md §1`, `§4`, `§5`
- 실행 순서·TODO: `docs/JETSON_SETUP.md §9`, `§10`, `§11`
- 변경 표면별 검토 범위: `docs/TEST_GATES.md §7`
- 사다리·보류·유효 결정: `docs/MASTER_PLAN.md §3`, `§7`, `§8`
- 검토 전문(§39~§42): `~/Desktop/개발현황/CODEX 현황/0801검토현황.md §39`
