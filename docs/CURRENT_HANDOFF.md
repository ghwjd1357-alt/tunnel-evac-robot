# CURRENT_HANDOFF.md — 지금 할 한 묶음 (매 세션 시작점)

> 작업 완료 시 구현자가 갱신하고 검토자가 사실관계·테스트 결과를 확인한다.
> 이전 묶음 내용은 날짜별 현황으로 보내고, 이 파일에는 현재 작업과 직전 주장만 유지한다.

- **동결 기준점**: `212885a8292d1e86677d5beeb9f5358d76fe9b40` = tag
  **`platform-core-freeze-260724`**. 증거 = `docs/FREEZE_MANIFEST.md`.
- **현재 기준 커밋**: 이 파일과 같은 커밋(main).
- **현재 단계**: 마스터플랜 5단 완료 → 역할 B V1 합의(6단)는 회의 대기 보류
  → 실차 D+0 인수·연결 **부분 완료(`d0_check` 종료 2 · 검사 8 미확인)**
  → 검토 §39 불승인·§40~§42 회신 수령(08-04)
  → **전기계통 정본 v2 전면 개정**(08-04)
  → §39 문서·코드 표면 보완 = **예약 25 종결**(08-05 `870f1da`~`3c62381`)
  → 전기계통 수급 실물 반영 + 착수 전 게이트 통과 = 정본 v3.1 (`10aa4c6`·`4e8d060`)
  → **검토 §43 불승인 보완 = 정본 v3.2 · 검토 §44 대상** **(지금 — 2026-08-06)**

> 🔴 **§43 판정 = 불승인 (P0 1 · P1 8 · P2 2)** — v3·v3.1(`10aa4c6`+`4e8d060`) 한 슬롯 검토.
> 전문 = `~/Desktop/개발현황/CODEX 현황/0801검토현황.md §43`. **이 묶음(v3.2)은 그 보완이라
> 새 슬롯 §44 를 딴다.** 3건은 수정이 아니라 **사용자 결정에 의한 조건부 수용**이므로,
> 재검토자는 **전제조건·재개방 조건이 실제로 지켜지는 형태인지**를 본다 — "덮였다"로 적혔으면
> 그것이 발견사항이다.

- **⚠ 미해결 보류 — 예약 23 / 검토 §33 (`8fcc1a2`)**: P0 0 · **P1 1** · P2 1.
  `handoff_single_check.sh` 는 `§` 없는 새 작업 꼬리를 붙여도 `OK` 를 내고, 숫자 계약 전수
  증거는 기록 6자리와 독립 실행 21자리가 불일치한다. **승인 완료로 읽지 말 것.** 런타임
  `src/**` 변경·회귀는 없으므로 현재 D+0 실행은 막지 않되, 보완은 다른 묶음에 섞지 않고
  `MASTER_PLAN.md §7` 예약 23 의 완료판정과 부정 회귀로 별도 구현한다. 전문 =
  `~/Desktop/개발현황/CODEX 현황/0801검토현황.md §33`.
- **⚠ 미해결 보류 — 예약 24 / 검토 §38**: P0 0 · **P1 2** · P2 4. 라이다 실측 소유 시점과
  readiness flake 소유 정본이 갈라졌다. **라이다 높이는 D+1 소유**이며 `JETSON_SETUP.md §9`
  의 `TODO(D+0)` 목록 계약은 그대로다.
- **⚠ 미해결 보류 — 예약 26 / 검토 §40**: 사용자 결정 **'방법 B'** 가 구현되지 않았고 저장소
  에서도 사라졌던 건. 결정은 `MASTER_PLAN.md §8` 에 복원했다. **§37.3 을 닫힌 것으로 인용하지
  않는다.**
- **⚠ 미해결 보류 — 예약 27**: 이 Jetson 에 `nvcc`·`nvidia-jetpack` 이 **없다**. 역할 A 는
  비차단, **역할 B 성능 전제는 통째로 바뀐다.** 역할 B 회의 안건.
- **⏸ 6단 보류**: 역할 B 회의가 잡히면 커밋 `30c5e87`의 핸드오프 원문을 그대로 복원한다.

```bash
git show 30c5e87:docs/CURRENT_HANDOFF.md
```

## 이번 한 묶음 목표 — §44 전기계통 불승인 보완 = 정본 v3.2 (직전 검토 = 불승인 판정)

**독립 검토 §43 이 v3·v3.1 을 불승인했다** (P0 1 · P1 8 · P2 2). 이 묶음은 그 보완이며
**새 설계를 하는 회차가 아니다.** 한 일은 세 가지다 — ⓐ **틀린 논거 철회**, ⓑ **오기록
되돌림**, ⓒ **증명 못 한 것을 조건부 수용으로 재기술**. `src/**` 도 `firmware/**` 도 열지
않았다 — 변경 표면은 문서뿐이다.

### §43 처리 요약 — 전수표는 `ELECTRICAL_BASELINE.md §13` 에 있다

| 성격 | 건수 | 항목 |
|---|---|---|
| 🔴 **조건부 수용** (사용자 결정) | **3** | §43.2 릴레이 DC 차단 정격 · §43.3 퓨즈 시간-전류 협조 · §43.4 유리관 조합 차단용량 — 셋 다 **부품 품번이 없어 datasheet 대조 불가**가 사유였다 |
| ✅ **수정** | **8** | §43.5 종료 순서 · §43.6 오기록 3자리 · §43.7 업로드 절차 · §43.8 E10→G8 · §43.9 E1 분해 · §43.10 예비 2개 · P2-A · P2-B |

- 🔴 **가장 무거운 것은 §43.6 오기록이다.** 시공 미착수 · 소스가 아직 `ESTOP_ACTIVE_LOW = true`
  인데 위험 `§2`-⑧ 을 **닫힘으로 기록**했었다. **설계상 닫는 경로**와 **실차에서 닫힘**을 섞은
  것이며, `§11-a` 를 0건으로 되돌렸다.
- 🔴 **조건부 수용 3건의 공통 전제는 `MAX_LINEAR_CMD = 0.12`** (`.ino:125-126`)다. 실사용 전류가
  낮다는 것이 근거이므로 **속도 상한을 올리면 세 판정이 동시에 무효**가 된다.
- 🔴 **v3.2 가 닫은 위험은 0건이다.** 되돌리고 조건을 명시한 것뿐이다.
- 🔴 **부품 1건 부족 발생** — 예비 릴레이 1 → 2 (§43.10). `§5-G8` 전까지 확보.
- v3·v3.1 의 배경(수급 완료분 · 게이트 4개 판정 · 바뀐 전제 4개 · 전수 대조 목록)은
  `ELECTRICAL_BASELINE.md §11`·`§12` 로 옮겼다 — **콜드스타트 파일에 역사를 쌓지 않는다.**

### 전수로 센 것 (`AGENTS.md §3-10`)

`review_history_findings()` 를 Desktop 역사에 직접 돌려 §43 신규 **9건**(P0 1 + P1 8)을
확인하고 인벤토리를 **67 → 76** 으로 옮겼다(`tools/ai_known_p0_p1.json` 9행 ·
`AI_CONTEXT.md` 3자리 · `TEST_GATES.md §7` · `test_ai_context.py` 3자리).
9행의 anchor 가 **profile-only 회수**되도록 `ai_context.py` 의 `firmware` 프로파일에
`ELECTRICAL_BASELINE §7`·`§13` 을 추가했다 — 그 전엔 전기 안전 anchor 가 회수되지 않았다.
⚠ **이 열거는 스크립트 실행 결과다.** 다만 **의미 수준 누락은 기계로 못 센다.**

### 닫은 것 / 안 닫은 것 (섞지 않는다)

| | 항목 |
|---|---|
| ✅ **닫힘** | 🔴 **없다 (0건).** v3.2 에서 되돌렸다 |
| 🔴 **안 닫힘** | `ELECTRICAL_BASELINE.md §2`-⑧ E-stop fail-danger **(v3.2 에서 닫힘 → 미해소로 되돌림)** · `§2`-③ XT90 개폐(스위치 미구매) · `§2`-② 배터리 미고정(전제로 승격) · `§2`-⑬ re-arm 래치 · 릴레이3 건접점(🟡 — 330Ω 으로 **완화**했을 뿐) · 유리관 2A 조합 차단용량(🟡 조건부 수용) · **릴레이 DC 25.2V 차단 정격**(🔴 신규 — 조건부 수용) · **30A 퓨즈 시간-전류 협조**(🟡 신규 — 조건부 수용) |

🔴 **§2-⑧ 승격 조건** = 시공안 `§5-G6`(토글 10회) + `§5-G8`(부하 차단 10회) + `d0_check`
**검사 8** 이 전부 실측 통과했을 때. 되돌린 사유 상세 = `ELECTRICAL_BASELINE.md §13-c`.
🔴 **PIN21 완료는 re-arm 래치를 닫지 않는다.** R1·R2 차단 사유는 그대로다.

## 완료조건

1. 시공안 0806 이 **§43 실행 절차 지적 6건을 반영**해 `§5` 를 위에서부터 실행할 수 있다 —
   E1a/b/c · E10→G8 · F5 정량 기준 · F6 업로드 · F6b 중간상태 · 예비 릴레이 2개.
   조건부 수용 3건은 **전제조건 + 재개방 조건**으로만 적혀 있고 **"덮였다"가 없다.**
2. `ELECTRICAL_BASELINE.md` v3.2 — 철회·되돌림이 `§13` 한 곳에 전수로 있고 **`§11-a` 의
   "닫힌 것"이 0건**이며, `§2`-⑧ 이 **미해소**로 기록돼 있다.
3. `REAL_ROBOT_VALUES.md §1-e` 네 행이 v3.2 판정과 일치하고 값을 중복하지 않는다(링크).
   특히 **`MAX_LINEAR_CMD = 0.12` 가 P0 수용의 전제임**이 명시돼 있다.
4. 펌웨어 변경(`ESTOP_ACTIVE_LOW`)이 **구동부 협의 사항**임과 **되돌아가면 로봇이 항상 정지**
   한다는 사실이 인계 목록에 있다.
5. `doc_check.sh` 전 항목 OK · `handoff_single_check.sh` OK ·
   **`python3 tools/test_ai_context.py` 전량 통과**(인벤토리 76 · 콜드스타트 예산 30%).
6. 회귀 정본이 그대로다: pytest **182 passed** / colcon **245 tests** /
   harness guards **24 검사** / gate regression **14 케이스**.
   ⚠ `tools/**`(인벤토리·라우터)를 건드렸으므로 `TEST_GATES.md §7` 라우팅상 직접 실행 대상은
   **`doc_check.sh` · pytest · `test_ai_context.py`** 다. colcon·harness·gate regression 은
   `src/**` 무접촉이라 문서 수치 스윕으로만 확인한다 — **재실행했다고 쓰지 않는다.**

## ★ 이번 회차 최우선 확인 사항 — 전기계통 시공 (사용자 직접 · **미착수**)

> 2026-08-06 현재: **아직 시작하지 않았다.** 아래 안전 불변조건은 **전부 그대로 유효**하고,
> 물리 E-stop 은 **없는 것으로 취급**한다.

1. 시공 정본은 `~/Desktop/전기계통_최종작업안_0806.md` **§5 작업 순서**다. 판단 근거·위험
   논리가 필요하면 `docs/ELECTRICAL_BASELINE.md`, 부품 계약값은 `REAL_ROBOT_VALUES.md §1-e`,
   펌웨어 절차는 `docs/FIRMWARE_REBUILD.md`.
2. 🔴 **작업 전 XT90 분리 + 퓨즈 제거 + 멀티미터 0V 확인.** **v3 에는 메인 차단 스위치가
   없다.** E-stop 만 누르고 배선을 만지지 않는다 — E-stop 은 **모터 가지만** 끊고
   Wago·퓨즈·DCDC·로직은 살아 있다.
3. 🔴 **착수 범위가 두 단계다** (v3.2). `§5-A~D`(무통전 조립)는 착수 가능. `§5-E` 이후는
   §43 반영본을 쓴다. **`§5-G8` 전까지 예비 릴레이 1개를 추가 확보**해야 한다 (§43.10).
   🔴 **퓨즈를 30A 보다 올리지 않는다** — 리드선이 41A 여도 천장은 14AWG·Wago 의 32A 다.
4. 🔴 **PIN21 은 펌웨어 변경 전에도, 변경 후 연결 전에도 로봇을 "항상 정지"로 만든다.**
   방향만 다르고 둘 다 **정상**이니 배선을 뜯지 않는다 — 네 상태 진리표 = 시공안 `§7-2`.
   330Ω(`§5-D14b`)은 🔴 **Teensy 3.3V 핀인지 재고 꽂는다.** 25V 면 Teensy 가 죽는다.
5. `§6 중단 기준` **12가지**(공통 6 + v3.2 추가 6 = E1a·E1b·E1c·F5·F6 포트·예비 2개 미확보)
   중 하나라도 걸리면 **멈추고 상의**한다. 목록 본문은 시공안 `§6`.
6. 시공 끝 → **E-stop 개폐 10회 토글**로 실제 차단을 확인한다. 배선을 다시 만졌으면 10회를
   **다시** 한다. PIN21 까지 끝냈으면 **`/estop/state` 토글 10회**(`§5-G6`)와
   **부하 차단 10회**(`§5-G8`)를 별도로 더 한다.
7. 다음 현장 세션의 **첫 명령**은 D+0 증거 복제다(`JETSON_SETUP.md §11-g` 보존 규정) —
   bag 2개·tegrastats 로그가 아직 Jetson 한 대에만 있다.
8. 전기 시공 자체는 `TODO(D+0)` **11건**을 닫지 않는다. 그 목록의 정본은
   `JETSON_SETUP.md §9` 한 곳이며, 시공 결과는 E-stop 항목의 **전제**만 만든다.
   ⚠ 단 **v3 에서 PIN21 2단계가 이 회차에 들어왔으므로**, 시공이 완주되면 검사 8 은
   **원리적으로 통과 가능**해진다 — 통과했다고 미리 적지는 않는다.

## 사용자 결정 — 유지

- 동결 예외 4회는 전부 소진됐다. `src/**`를 다시 열려면 사용자 승인이 필요하다
  (`FREEZE_MANIFEST.md §10`).
- 순서는 전기계통 시공 → E-stop 검증 → D+0 잔여 종결 → D+1 R3 rosbag → R4~R8이다
  (`MASTER_PLAN.md §3`).
- 예약 20·21·22·23·24·26·27과 역할 B 6단은 각각 정해진 트리거 전까지 별건으로 유지한다
  (`MASTER_PLAN.md §7`). **예약 25 는 08-05 에 종결**됐다.
- `JETSON_SETUP.md §9`의 `TODO(D+0)` **11건** 중 **열 자리**가 값과 함께 닫혔고, 미해결은
  **물리 E-stop 하나**다. 목록 계약은 바꾸지 않는다.
- 08-03 D+0 판정: 장치·주기·간격·QoS·전진 부호·EKF 생존·펌웨어 정체는 **통과**했고, 수정판
  `--no-manual` 재실행은 자동 항목 전부 통과·**종료 2**였다. **전량 통과로 기록하지 않는다.**
- 🔴 **08-06 — 검사 8 의 전제가 바뀌었다.** v3 이 **PIN21 2단계를 이 회차에 포함**하므로
  시공 + 펌웨어 + 2차 검증이 끝난 뒤에는 종료 0 이 가능하다. 그 전까지 `--no-estop` 종료 2 가
  정상 상한이며, **물리 차단이 없다는 뜻이 아니라 Teensy 가 그것을 못 본다는 뜻**이다.
- never-seen은 다른 놓침과 같이 역행 2회 → 보고 → 단독 탈출이다. 실차 R3 뒤 재평가한다.

| 트랙 | 범위 | 상태 |
|---|---|---|
| **진행 중 묶음** | §44 검토 §43 불승인 보완 = 정본 v3.2 + 시공안 0806 (문서 전용) | **착수** |
| 전기계통 시공 | `전기계통_최종작업안_0806.md §5` (사용자 직접) | 🔧 **A~D 착수 가능 · E 이후 v3.2 반영 완료** (08-06) |
| 예비 릴레이 1개 추가 | §43.10 — 주회로용 2개 정책. `§5-G8` 전 확보 | 🔴 **미확보 (부품 부족 1건)** |
| 실차 D+0 | 연결·주기·QoS·부호·펌웨어 통과, 종료 2 | ⏸ E-stop 설치 뒤 잔여 종결 |
| AI 입력 최적화 | `ef25ad3` 기준, 품질 불변 + raw 입력 30% 절감 | §40 승인 · 방법 B 잔여 = 예약 26 |
| 예약 21 | D0/R3 판정기 회귀 편입 + readiness case 9·11·12·14 간헐성 규명 | ⏸ 다음 bringup 변경 전 |
| 예약 23 | 핸드오프 ID·숫자 계약 전수 폐포 | ❌ 별도 보류 |
| 예약 24 | 검토 §38 정본 소유권·예약 완료판정 정합 | ❌ 다음 bringup/예약 21 전 |
| 예약 26 | 검토 §40.3 방법 B 계약 표면 재설계 | ❌ AI 라우터 표면 여는 묶음 |
| 예약 27 | 역할 B CUDA·JetPack 전제 | ❌ 역할 B 회의 |
| 역할 B V1 합의 | 커밋 `30c5e87` 원문 | ⏸ 회의 대기 |

## 직전 완료 — §39 코드 표면 = 예약 25 종결 (구현자 = Claude, 독립 검토자 = Codex 예정)

`3c62381`. `d0_check.sh` 가 **기다린 적 없는 시간을 사실로 말하는** 것을 끝냈다. `hard_timeout`
8자리를 `clock_begin`/`clock_end` 로 재게 하고 실패 문장은 `why_snapshot()` 한 곳에서만 쓴다.

## 직전 완료 — 실차 D+0 현장 기록 (구현자 = Codex, 검토 = §39 **불승인**)

`c397975`. §39 독립 검토가 **P0 0 · P1 2 · P2 12 로 불승인**했다. 08-05 로 §39 발견은
저장소상 전부 닫혔지만 **`c397975` 자체의 판정은 불승인 그대로**이고, 그것을 뒤집는 것은
보완 커밋이 아니라 검토자다.

## 보존해야 할 안전 불변조건

- 물리 E-stop 이 없거나 검사 8이 미확인인 동안 **`/cmd_vel` 발행·R0~R2·Nav2·미션 금지**.
  `Ctrl+C` 를 E-stop 으로 승격하지 않는다. **08-06 현재 전기 시공은 미착수다.**
- 🔴 **`/estop/state` 는 시공안 `§5-G6`(2차 검증 개폐 10회 토글) 전까지 신뢰하지 않는다.**
  그 전에는 이 토픽을 소비하는 로직을 만들지 않는다. **하드웨어 차단 자체는 어느 경우에도
  온전하며**, 문제는 ROS 가 그것을 아는가일 뿐이다.
- 🔴 **배선 작업 전 XT90 분리 + 퓨즈 제거** (v3 에는 메인 스위치가 없다), 배선을 만졌으면
  **개폐 10회 재검증**.
- 🔴 **펌웨어 업로드는 모터 전력 0V·바퀴 공중에서만** (`FIRMWARE_REBUILD.md §5`).
  빌드 지문 `ARDUINO=10607`·`TEENSYDUINO=158` 이 안 맞으면 업로드하지 않는다.
- 🔴 **re-arm 래치는 아직 없다** — PIN21 완료가 이것을 닫지 않는다. 구현·부정/전환 시험 전
  R1·R2 금지 (`REAL_ROBOT_VALUES.md §1-d`).
- 판정기가 **관측하지 않은 것을 말하지 않는다** — 상한 초는 계약값 표기이지 경과 주장이 아니다.
- 설정은 대상 소프트웨어가 실제로 읽는지 `param list`·`topic info -v`로 확인한다.
- N초 관측은 같은 관측자가 종료 상태와 **벽시계 경과시간**으로 창을 완주해야 한다.
- 토픽별 계약의 단일 출처는 `tools/bag_gap_report.py`의 `TOPIC_POLICY`다.
- `/odom`은 RELIABLE, `/imu/*`는 BEST_EFFORT다. EKF 소비자 QoS까지 함께 본다.
- Teensy 재연결 로직 없음, IMU 치명 실패 시 노드 자체가 안 뜸, 시각 역행 시 stamp가 사실상
  멈춘다는 펌웨어 전제를 유지한다.
- 실차 없이 확인한 합성 bag·가짜 `ros2` 결과를 실차 통과로 승격하지 않는다.
- `TODO(D+0)` **11건**을 추측값으로 닫지 않는다 — 미해결은 이유와 재확인 시점을 적는다.

## 금지 범위

- 사용자 추가 승인 없는 `src/**` 변경. **변경 표면은 문서 + `tools/` 인벤토리뿐이다.**
- 예약 20·21·22·23·24·26·27, 역할 B V1, 관제 통보 표면을 이 묶음에 혼합.
- 🔴 **위험을 "덮였다"로 기록하기** — 조건부 수용 3건·스위치 미구매·건접점·re-arm 은 전부
  전제조건 + 재개방 조건 형식으로만 쓴다.
- 🔴 **미착수인데 E-stop·검사 8 을 닫은 것으로 기록하기.**
- 🔴 **펌웨어를 실제로 굽거나 업로드하기** — 업로드는 사용자가 현장에서
  `FIRMWARE_REBUILD.md §5` 전제(모터 0V·바퀴 공중) 아래 직접 한다.
- `make_map.sh` 실런, 지도 자산 변경, Gazebo E2E 동시 실행.
- Claude가 작성한 이번 diff를 Claude가 최종 승인하기.

## 완료 판정

**검토 §43 의 11건이 전부 처리표에 있고(수정 8 · 조건부 수용 3), 시공안 0806 §5 가
E1a/b/c·G8·F5·F6·F6b 반영으로 위에서부터 실행 가능하며, `ELECTRICAL_BASELINE` v3.2 가
철회·되돌림을 `§13` 에 전수로 적고 `§11-a` 의 "닫힌 것"이 0건으로 돌아갔으며, 조건부 수용
3건이 전제조건 + 재개방 조건으로만 기록되고(“덮였다”가 없고), `doc_check`·`handoff_single_check`
와 회귀 4수치가 전부 그대로면 이 묶음은 완료다.**
단, 완료는 **구현자 주장**이고 승인은 독립 검토(§44)다.
🔴 **문서 보완만으로 실물 설치 PASS 를 선언하지 않는다** (§43.14-4). 조건부 수용 3건은
`§5-G8` 실측과 부품 품번 확보 뒤에야 종결 판단이 가능하다.

## 완료 후 다음 단계

1. **전기계통 시공**(사용자 직접) — `전기계통_최종작업안_0806.md §5` 완주 + `§5-G6`·`§5-G8`
   각 10회. 🔴 **부품 각인 촬영**(`§5-H2`)으로 조건부 수용의 종결 근거를 만든다.
2. D+0 증거 복제(`JETSON_SETUP.md §11-g`) 후 `d0_check.sh` 현장 재실행.
3. `TODO(D+0)` **11건** 중 남은 물리 E-stop, `/scan` 포함 판정, R0 watchdog 실측.
4. R0 watchdog 실측으로 `FREEZE_MANIFEST.md §6` 조건부 수용 재판정.
5. `docs/D1_FIRST_STEP.md`의 D+1 R3 rosbag 분석 → 예약 22와 R4~R8.

## 근거 문서

- 전기계통 위험·안전선 · v3/v3.1/v3.2 개정 전수: `docs/ELECTRICAL_BASELINE.md §11`, `§12`, `§13`
- 전기 시공 절차(결선·순서·검증): `~/Desktop/전기계통_최종작업안_0806.md`
  (구판 역사 = `~/Desktop/전기계통_최종작업안_0804.md`)
- 펌웨어 재빌드·업로드·빌드 지문: `docs/FIRMWARE_REBUILD.md`
- 실차 값·부품 계약값: `docs/REAL_ROBOT_VALUES.md §1`, `§4`, `§5`
- 실행 순서·TODO: `docs/JETSON_SETUP.md §9`, `§10`, `§11`
- 검토 범위 라우팅: `docs/TEST_GATES.md §7` · 사다리·보류: `docs/MASTER_PLAN.md §3`, `§7`, `§8`
- 검토 전문: `~/Desktop/개발현황/CODEX 현황/0801검토현황.md §39`
