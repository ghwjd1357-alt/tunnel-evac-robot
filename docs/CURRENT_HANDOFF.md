# CURRENT_HANDOFF.md — 지금 할 한 묶음 (매 세션 시작점)

> 작업 완료 시 구현자가 갱신하고 검토자가 사실관계·테스트 결과를 확인한다.
> **현재 묶음 + 미해결 보류만** 유지한다 (`AGENTS.md §5`) — 완료된 서사는 정본으로 보내고 링크한다.

- **동결 기준점**: `212885a8292d1e86677d5beeb9f5358d76fe9b40` = tag
  **`platform-core-freeze-260724`**. 증거 = `docs/FREEZE_MANIFEST.md`.
- **현재 기준 커밋**: 이 파일과 같은 커밋(main).
- **현재 단계**(이력 정본 = `MASTER_PLAN §8`): 5단 완료 · 6단(역할 B V1)은 회의 대기
  → D+0 검사 1~8 OK(08-07) · **D+0 잔여 0**
  → 전기 시공 완주 · re-arm 래치 굽기 완주(08-11)
  → **예약 32 교정 계수 `335` 굽기·지면 확정 완주**(08-12 · 32-a·32-c)
  → **`MASTER_PLAN §7` 예약 32-b 거짓 정지 · Tier A(전기계통)** **(지금 — 2026-08-12 저녁)**

> ✅ **오늘 닫힌 것**: 굽기(`build=Aug 12 2026 15:24:31` · `version=…-1.4.0`) ·
> `§7-c-E` **13/13** · `§5-G6` **10/10** · 지면 `0.12` **줄자앵커 순항 `0.1223`·`0.1237` =
> 명령의 1.02·1.03배**(합격선 `0.108~0.132`) · **새 사용자 위험 수용 문장**(아래).
> 🔴 **새 정지 사유 — 주행 중 이유 없는 무장 해제가 오늘 4회.** 원인 후보는
> **릴레이3 건접점**(문서가 미리 예측해 둔 항목)이고 완화(330Ω)를 시공 중이다.

**⚠ 미해결 보류 — 어느 것도 승인 완료로 읽지 말 것** (본문 정본 = `MASTER_PLAN.md §7`):

- **⚠ 미해결 보류 — 예약 23 / 검토 §33 (`8fcc1a2`)**: P0 0 · **P1 1** · P2 1.
  `handoff_single_check.sh` 는 `§` 없는 새 작업 꼬리를 붙여도 `OK` 를 내고, 숫자 계약 전수
  증거는 기록 6자리와 독립 실행 21자리가 불일치한다. **승인 완료로 읽지 말 것.** 런타임
  `src/**` 변경·회귀는 없으므로 현재 D+0 실행은 막지 않되, 보완은 다른 묶음에 섞지 않고
  `MASTER_PLAN.md §7` 예약 23 의 완료판정과 부정 회귀로 별도 구현한다. 전문 =
  `~/Desktop/개발현황/CODEX 현황/0801검토현황.md §33`.
- **예약 24 / §38** P1 2 · P2 4 — 라이다 실측·readiness flake 소유 정본이 갈라짐.
- **예약 26 / §40** — 사용자 결정 **'방법 B'** 미구현(결정은 `MASTER_PLAN §8`).
- **예약 27** — 이 Jetson 에 `nvcc`·`nvidia-jetpack` **없다.** 🔴 역할 B 성능 전제가 바뀐다.
- **예약 28** — §44·§45 전기 P1 4건 중 **③④ 08-10 종결** · **①② 미해결**.
- **예약 29** (08-07) — 정본의 활성 계약·이력 절 분리. ⏸ 실차 사다리 뒤.
- **예약 30** (08-10) — §53 전기 P1 **6건**. 🔴 **5건이 같은 실수 하나**이고 **완료판정이
  부품 품번이라 AI 왕복으로 안 닫힌다.**
- **예약 31** (08-11) — ✅ 1단계 완료: `~/Desktop` 루트 md **72건** 로컬 저장소.
  🔴 **원격 private repo 생성·푸시는 사용자 몫** — 아직 백업이 아니다.
- ✅ **예약 32** (08-11) — **닫혔다**(08-12). 계수 `335` 를 도구가 뽑고 굽고 지면에서 쟀다.
  서사·한계·근거 = `MASTER_PLAN §7` **32-a**(산출) · **32-c**(지면 확정 + 위험 수용 문장).
- 🔴 **예약 32-b** (08-12) — **이 묶음의 대상.** 주행 중 이유 없는 무장 해제. 아래 참조.
- 🔴 **예약 35** (08-12) — 엔코더 Δyaw 가 IMU Δyaw 보다 **2.1~2.3° 크다**(2회 재현).
  ⚠ **줄자 횡편차는 증거에서 내렸다** — 초기 정렬 오차 `1.3~1.9°` 로 100% 설명돼 원리적으로
  못 가른다(08-12 사용자 지적). 판정법 = 몸통 앞뒤 두 점으로 **방향 변화**를 직접 재기.
- **예약 33** (08-12) — PI 재조정 + **`appliedPwm` 관측**. ⏸ **트리거 = R6 전.**
- **예약 34** (08-12) — 문서가 코드를 깨지기 쉽게 가리키는 자리 **48곳**. ⏸ 이 묶음 뒤.
- **⏸ 6단 보류** — 역할 B 회의가 잡히면 `30c5e87`의 핸드오프 원문을 그대로 복원한다.

## 이번 한 묶음 목표 — `MASTER_PLAN §7` 예약 32-b 거짓 정지 · 🔴 **전기계통 = Tier A**

> 🔴 **`ELECTRICAL_BASELINE.md` 를 먼저 읽는다** (`CLAUDE.md §0`). E-stop·전원·배선을 만진다.

**증상** — ARMED 주행 중 아무도 안 껐는데 `z: 2 → 0` 이 되고 로봇이 선다. **오늘 4회**
(지면 44.4s · 6.5s / 공중 37.7s · 6.3s). 🔴 **고장 방향은 정지 쪽(fail-closed)이라 안전
위험이 아니다.** 문제는 **못 믿는 것**이고, 그래서 R2 를 못 간다.

🔴 **관측·배제·수치 전수는 `ELECTRICAL_BASELINE §4-f`~`§4-f-3` 이 정본이다.** 요약:

- **핀이 뜨는 것을 직접 관측했다**(08-12 밤 `base_2026` `t=46.6`) — 주행 중 `/estop/state`
  가 **True**. 🔴 후보 "유한하지 않은 명령"은 죽었고 **E-stop 경로 하나만 남았다.**
- 🔴 **원인은 진동이 아니라 전류다** — 실내·저진동인데도 나고, 정지 중 가진은 무반응이며,
  🔴 **`0.05`(40 PWM) 99초 vs `0.12`(63.5 PWM) 6초**로 전류에 비례한다.
  → **접지 바운스** 가설: 모터 귀환 `1~3 A` 가 신호 GND 도체를 공유하면 `I×R` 이 생겨
  Teensy 의 0V 와 릴레이3 `[30]` 의 0V 가 갈린다. 공유 `0.7 Ω` 이면 `2.1 V` = HIGH 문턱.
- 🔴 **330Ω 자리에 3.3 kΩ 이 꽂혀 있었다**(색띠 갈색↔빨강). 웨팅 `10 mA` 가 아니라 `1 mA`.
  → **08-12 의 12회 이상은 "완화 실패"가 아니라 "완화 없이 12회"** 다.
- **기준선**(판정용): `0.05`·3.3kΩ·n=5 → 평균 **6.0초**(9.6·5.3·3.9·6.3·5.1).
- ⚠ `/drive/diag` `y` 는 **서비스 경로에서만** 설정돼 E-stop·NaN·부팅이 전부 `y=0` 이고,
  진단은 **1Hz** 다. 🔴 **관측성 결함이 규명을 막았다** — 펌웨어 묶음 과제.

## 완료조건

🔴 **절차 정본 = `~/Desktop/0813_12시간_작업가이드.md`.** 여기엔 계약만 둔다.

1. 🔴 **접지 바운스 판정 (제일 먼저)** — **Teensy GND ↔ 배터리 `−`** 전압을 정지 시와
   `0.05`·`0.12` 주행 중에 잰다. 같은 GND 라 항상 `0V` 여야 한다. 주행 시 **`0.3V` 이상**이면
   확정. 🔴 결과가 2번 설계를 "납땜 이관" ↔ **"스타 접지 재구성"** 으로 가른다.
2. **만능기판 이관** — E-stop 2 + BNO055 4 우선, 모터 PWM/DIR 8 차순.
   🔴 바운스가 확인되면 **스타 접지**로 간다 — 부품이 아니라 **구조**가 대책이다.
3. **진짜 `330Ω`**(주황·주황·**갈색**) — `PIN21 ↔ Teensy 3.3V`. 🔴 25V·5V 아니다.
4. 🔴 **`§5-G6` 토글 10회 재수행** — PIN21·GND 경로를 만졌으므로 재개방 조건이 발동한다.
   `tools/estop_toggle_check.py` 가 계수한다. **9/10 은 통과가 아니다.**
5. 재현 판정 — `0.05` 60초 3판 + `0.12` 20초 2판, `ros2 bag record -a`.
   🔴 **기준선이 평균 6.0초이므로 60초 3판 완주면 통계적으로 의미 있는 개선**이다.
6. **IMU 부활 확인**(예약 36) — 정지 시 `accel z ≈ 9.4~9.8` 이고 10분 주행 동안 0 표본 **0건**.
7. `doc_check --strict` rc=0 · `handoff_single_check` OK · `test_ai_context` **37** ·
   `firmware_precheck` **rc=0**. 회귀: pytest **184 passed** · colcon **247 tests** ·
   `test_drive_checks` **22** + `test_drive_ff_calibrate` **12** ·
   harness guards **24 검사** · gate regression **14 케이스** · watchdog **54**/**20**.

## 🔴 이번 묶음의 함정

**① 순서.** 측정(1) → 구조(2) → 부품(3) 이다. 거꾸로 하면 3시간 납땜한 뒤 구조를 다시 잡는다.

**② 원인 판정을 펌웨어 디바운스보다 먼저 끝낸다.** 디바운스가 들어가면 **증상이 덮여**
접지 바운스인지 아닌지 영원히 못 가린다. 디바운스는 일정 보험이지 원인 규명 수단이 아니다.

**③ 오귀속 금지.** 330Ω·이관·스타 접지를 같은 판에 넣으면 뭐가 고쳤는지 모른다.
가능한 한 **측정으로 원인을 먼저 좁히고**, 바꾼 뒤에는 무엇을 바꿨는지 기록한다.
🔴 **PCB 이관 후 조용히 사라지는 경우도 330Ω 의 공로로 적지 않는다.**
330Ω 후에도 재발하면 그때 배선을 손보고, PCB 이관에서 자연히 닫힐 수도 있다.

## ★ 현장 운용 — 시공은 끝났고 **절차가 바뀌었다** (상세는 링크가 정본)

1. 🔴 **기동 순서를 어기면 Teensy 가 영구히 멈춘다** — `퓨즈 → XT90 → Jetson 부팅 대기 →
   agent → **Teensy VIN 만 재삽입**`. 재연결 로직이 없어 `errorLoop()` 에 갇힌다
   (`§5-e`). 증상 = 토픽이 `rosout`·`parameter_events` 뿐.
2. 🔴 **종료는 `Jetson sudo shutdown -h now` 가 맨 앞** — 로직이 배터리로 도는 지금
   XT90 분리는 **리눅스 전원 뽑기**다 (`ELECTRICAL_BASELINE.md §7`).
3. 🔴 **배선을 만질 땐 XT90 분리다 — E-stop 으로 대신하지 않는다.** E-stop 은 **모터 가지만**
   끊는다. 유일한 예외 = 트레이스 절단 후의 펌웨어 업로드 (`FIRMWARE_REBUILD.md §5-b`).
4. 오염 판정은 **`bash tools/firmware_precheck.sh` 뿐**이고 🔴 **`rc=2` 는 판정 불능이지
   통과가 아니다** (`FIRMWARE_REBUILD.md §4`).
5. 🔴 **브레드보드 상태다** — PWM/DIR 이 빠지면 **"돌 수도 있는"** 고장이다.
6. ✅ **버튼 없는 굽기가 08-12 에 1회 관측됐다**(udev 만으로 자동 진입 · `§5-a` 재개방 충족).
   ⚠ **n=1 이다.** ✅ **`TODO(D+0)` 11건 전량 종결**(`#11` = 08-11 재측정 + 검토 §59).
7. 🔴 **`/firmware/info` 는 이제 `version`·`source` 가 참이다**(08-12 굽기). `git_sha` 만
   `0` 이고 그건 의도다(자기참조). 정체 판별은 여전히 **`build` 문자열**이 정본이다.

## 사용자 결정 — 유지

- 동결 예외 4회 소진 — `src/**` 를 다시 열려면 사용자 승인(`FREEZE_MANIFEST.md §10`).
- `JETSON_SETUP.md §9` `TODO(D+0)` **11건**은 08-11 에 **전량 종결**됐다(마지막이 `#11`).
- 순서 = **예약 32-b 거짓 정지** → R2 → D+1 R3 rosbag → R4~R8 (`MASTER_PLAN.md §3`).
- 🔴 **지면 주행 예외는 오늘 하루 전량 승인됐다**(08-12 사용자). 안전요원 E-stop · 양옆 1m ·
  경사 금지 · 네 바퀴 방향 육안은 **매번 그대로**다.
- 🔴 **새 사용자 위험 수용 문장 (08-12 · 사용자가 직접 씀 · 구현자가 고쳐 쓰지 않는다)**:

  > "적용 속도 **0.12 m/s** 에서 명령이 끊긴 뒤 약 **6.3 cm** 더 나아가는 것까지 받아들인다.
  > 안전요원이 E-stop 을 들고 양옆 1m 를 비운 상태라 그 거리가 물리적 위험을 만들지 않는다.
  > **재개방 = ① 안전요원 없이 돌릴 때 ② 정지거리가 10 cm 를 넘을 때.**"

  ⚠ "적용 속도를 올릴 때"가 없는 이유는 `.ino MAX_LINEAR_CMD = 0.12` 하드 클램프가 그걸
  지키기 때문이다. 근거·이탈 기록 = `MASTER_PLAN §7` 32-c.

| 트랙 | 범위 | 상태 |
|---|---|---|
| **진행 중 묶음** | §7 예약 32-b 거짓 정지 + 이관 | 🔴 **Tier A(전기계통).** 완료조건 = 위 1~7 |
| ✅ 예약 32 속도 계통 | 계수 `335` · 굽기 · 지면 `0.12` | ✅ **닫힘**(32-a·32-c). 순항 1.02·1.03배 |
| 🔴 예약 32-b | 무장 해제 **12회+** · 핀 뜸 직접 관측 | 🔴 **R2 선행 조건.** 접지 바운스 가설 |
| 🔴 예약 36 | BNO055 접점 열화 → IMU 반복 사망 | 🔴 **R2·R3 를 막는다**(yaw 판정 불가) |
| 🔴 예약 35 | 엔코더 Δyaw − IMU Δyaw = **2.1~2.3°** | ⏸ 32-b·36 뒤. 줄자 횡편차는 증거에서 내렸다 |
| ⚠ §56.1 P1 | 전송 실패해도 보드만 무장 · 코드 미수정 | ⏸ 조건부 수용. 재개방 = 자율 발행자가 붙기 전 |
| ⚠ 릴레이 | 🔴 정격 미증명 · 08-10 수용(`§16-e`) | ⚠ 전제 ⓐ(`0.12`)는 실측 성립. 🔴 끼임 11A 미관측 |
| ⏸ 예약 21·24·30 | 회귀 편입 / §38 소유권 / 전기 P1 6 | ⏸ 각자 트리거 전 |
| ❌ 예약 23·26·27·29·31·33·34 · 역할 B | — | ❌ 이 묶음에 섞지 않는다 |

## 문서 부채

🔴 **남은 1건** — `§5-G6` 10회 기록. 08-11 21:20 A/B 인코더 교체 뒤 **돌렸으나 기록이 없다**
(사용자 확인). 이번 330Ω 뒤 재수행할 때 같이 정리한다.
✅ 08-12 에 닫은 것 3건: 런북의 "USB 전원 재인가"(→ DCDC `+단자`) · "2상태 표 = `§7-a`"
포인터 3곳(→ `JETSON_SETUP §5-d`) · `FIRMWARE_REBUILD` 역사표의 죽은 지문 `e6e15b5f…`.

## 보존해야 할 안전 불변조건

- ✅ **`/cmd_vel` 금지는 08-07 검사 8 로 해제됐다.** `Ctrl+C` 를 E-stop 으로 승격하지
  않는 규칙은 그대로다. 🔴 **정지 수단은 E-stop 이다.**
- 🔴 **`/estop/state` 신뢰의 뜻을 좁힌다** — `§5-G6` 10/10 이 보인 것은 *"버튼을 누르면
  토픽이 따라온다"* 이지 *"버튼을 안 눌렀을 때 안 뜬다"* 가 아니다. **거짓 양성은 그 시험이
  원리적으로 못 잡는다.** 오늘 4회가 그 공백에서 나왔다.
- 🔴 **`/estop/state`·`/drive/diag` 는 1초에 한 번 발행된다** — 수십 ms 짜리 사건은
  표본 사이로 빠져나간다. **토픽이 조용한 것을 "아무 일도 없었다"로 읽지 않는다.**
- 🔴 **배선 전 XT90 분리 + 퓨즈 제거**(메인 스위치 없음) · 만졌으면 **개폐 10회 재검증** ·
  **XT90 분리 앞에 `Jetson shutdown`**.
- 🔴 **펌웨어 업로드는 모터 전력 0V·바퀴 공중에서만** · 지문 `10607`/`158` 불일치면 금지 ·
  트레이스 절단 후엔 **E-stop 누른 채 XT90 연결** + 모터 단자 0V 실측(`FIRMWARE_REBUILD §5-b`).
- 🔴 **펌웨어를 또 구우면 `§7-c-E` 13행 + `§5-G6` 10회를 다시 한다**(`REAL_ROBOT_VALUES §1-f`).
- ⚠ **watchdog 정지는 무장을 풀지 않는다**(08-11·08-12 실측) — 발행자가 살아나면 재무장 없이
  다시 구른다. 수동 텔레옵이라 절차로 덮이고, **자율 발행자가 붙을 때 §56.1 과 같이 판단**한다.
- 🔴 **모터 명령 전에 네 바퀴가 같은 방향으로 도는지 본다** — `d0_check` 검사 6은 손으로 굴려
  보는 **엔코더 부호** 검사라 구동 방향을 검사하지 않는다. 08-07 에 DIR 핀 이탈로 좌전륜이
  혼자 역회전했고 **육안 배선 점검은 못 잡았다** (`PITFALLS.md §10`).
- 🔴 **지면 주행 전 `ros2 bag record -a`** — 08-12 오전에 `/drive/diag`·`/drive/enabled` 를
  안 담아 급정지 원인을 못 좁혔고, `-a` 로 담은 뒤에야 경로 4개를 배제했다.
- 🔴 **토픽 발행 명령(`ros2 topic` 의 `pub`)은 값 인자가 없으면 에러 없이 `0.0` 을 발행한다**
  — 붙여넣기가 잘려도 조용하다. **`publishing #1:` 줄의 값을 눈으로 본다**(08-12 에 1시간 손실).
  전량 서술 = `PITFALLS.md` · 깨지기 쉬운 참조 48곳 = 예약 34.
- 판정기가 **관측하지 않은 것을 말하지 않는다** — 상한 초는 계약값 표기이지 경과 주장이 아니다.
- 설정은 대상이 실제로 읽는지 `param list`·`topic info -v`로 확인한다. 토픽 계약의 단일
  출처는 `tools/bag_gap_report.py` 의 `TOPIC_POLICY` 다. `/odom` 은 RELIABLE, `/imu/*` 는 BEST_EFFORT.
- Teensy 재연결 로직 없음, IMU 치명 실패 시 노드가 안 뜸, 시각 역행 시 stamp 가 멈춘다는
  펌웨어 전제를 유지한다.
- 실차 없이 확인한 합성 bag·가짜 `ros2` 결과를 실차 통과로 승격하지 않는다.
- `TODO(D+0)` **11건**을 추측값으로 닫지 않는다 — 미해결은 이유와 재확인 시점을 적는다.

## 금지 범위

- 🔴 **330Ω 과 배선 정리를 같이 하기.** 원인 분리가 깨진다(위 함정 절).
- 🔴 **330Ω 을 25V·5V·VIN 에 붙이기** — Teensy 자체 `3.3V` 핀뿐이다. 오결선이면 보드가 즉사한다.
- 🔴 **"330Ω 을 달았으니 건접점이 해소됐다"고 쓰기** — 10mA 가 이 릴레이의 산화막을 뚫는지는
  **미증명**이다(품번을 모른다 · §8-⑬). **완화이지 해소가 아니다.**
- 🔴 **거짓 정지 원인을 확정 서술하기** — 경로는 E-stop 핀으로 좁혀졌지만 **왜 뜨는지**는
  미확정이다(접지 바운스 · 릴레이 접점 · 빵판). GND 측정 전에 하나로 적지 않는다.
- 🔴 **`§5-G6` 재수행을 건너뛰고 지면으로 가기.**
- 🔴 **검토 전에 굽기.** 펌웨어 묶음 `9127226` 은 **Tier A · 검토 대기**다.
  `firmware_precheck` `rc=1` 이 지금 유일한 자동 차단이고, 지문은 승인 뒤에만 옮긴다.
- 🔴 **예약 35 를 "로봇이 휜다"로 적기** — 줄자 횡편차는 초기 정렬과 못 가른다.
  살아 있는 관측은 **엔코더 Δyaw vs IMU Δyaw** 하나뿐이다.
- 사용자 추가 승인 없는 `src/**` 변경.
- 🔴 **위험을 "덮였다"로 기록하기** — 조건부 수용은 전부 전제조건 + 재개방 조건 형식으로만 쓴다.
- 🔴 **등록을 종결로 쓰기.** 예약 28·29·30·35 가 생겼다고 고쳐진 것이 아니다.
- 🔴 **`§5-G8` 을 "부하 차단 검증 완료"로 뭉뚱그리기** — 관찰된 것은 `0.05 m/s` 정상 전류뿐이다.
- 🔴 **핸드오프 상한에 걸렸다고 상한을 올리기.** 먼저 **옮길 완료 서사**를 찾는다.
- 예약 20~31·33·34, 역할 B V1 을 이 묶음에 혼합.
- `make_map.sh` 실런, 지도 자산 변경, Gazebo E2E 동시 실행.

## 완료 판정

**GND 두 점 전압으로 접지 바운스가 판정되고, 그 결과에 맞는 구조(스타 접지)로 이관이
끝나고, `§5-G6` 10/10 이 다시 서고, `0.05` 60초 3판이 무장 해제 없이 완주하고(기준선
평균 6.0초), IMU 0 표본이 0건이면 완료다.**
🔴 재발하면 **실패로 기록한다** — 감추지 않는다. 🔴 **어느 쪽이든 "해소"라고 쓰지
않는다**(원인 미확정 · 소표본 · 오귀속 위험).

## 완료 후 다음 단계 — 🔴 **실차 사다리가 우선이다**

🔴 **`2026-08-15` 플랜 B 판정일까지 3일 · `2026-08-18` 완주까지 6일.**

1. **블록 10 R2** — 3m ±3% · yaw 2π ±10%. `TODO(D+0)` **11건**은 이미 전량 종결이라
   여기서 다시 열리는 것은 없다. ⚠ 회전은 **ω ≥ 0.07 rad/s** —
   `MIN_EFFECTIVE_WHEEL_CMD=0.020` 이 물리 하한 `0.0645 rad/s` 를 만든다(예약 22).
   ⚠ 회전 명령은 **한 줄로** 친다(08-11 실패). 🔴 **R2 가 예약 35 의 첫 추가 관측**이다.
2. **블록 11** `d0_check` + 라이다 — 주행 불필요. 🔴 **거짓 정지와 무관하므로 지금도 가능**하다.
3. **블록 12** 증거 복제 · USB 2차 사본.
4. **예약 31** — Desktop md 72건 private repo 화. ~10분 · 위험 0 · **아직도 안 했다.**
5. ⏳ **펌웨어 묶음 `9127226` — 작성·컴파일 완료, 검토 대기** (Tier A · `Review-round:
   estop-debounce 1`). E-stop 디바운스 `30ms` + 무장 해제 사유를 `y` 에(값 4·5·6 신설) +
   글리치 계수를 `/firmware/info` 에. 호스트 시험 **938/0**. 🔴 `firmware_precheck` **rc=1**
   유지 = 굽기 차단. 요청문 = `~/Desktop/개발현황/0813_검토요청_estop_debounce.md`.
   🔴 **증상 완화다** — 원인 판정(위 1번)을 이 필터보다 **먼저** 끝낸다.
6. R3 rosbag → 예약 22·29 와 R4~R8.
   🔴 **`R0→R1→R2→R3` 순서를 건너뛰지 않는다** (사다리 정본 = `MASTER_PLAN §3`).

## 근거 문서

- 규율 티어·회차·은퇴 = `AGENTS.md §6` · 결정 색인 = `MASTER_PLAN §8` · 예약 = `§7`
- 전기 위험·안전선 = `ELECTRICAL_BASELINE §7`,`§11`~`§13` · 배선도 = `§4-c`
- 펌웨어 재빌드·기대 내용 sha256 = `FIRMWARE_REBUILD §4`,`§5`
- 실차 값 = `REAL_ROBOT_VALUES §1`,`§4`,`§5` · 실행 순서·TODO = `JETSON_SETUP §9`~`§11`
