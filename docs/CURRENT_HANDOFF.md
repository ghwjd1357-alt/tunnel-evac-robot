# CURRENT_HANDOFF.md — 지금 할 한 묶음 (매 세션 시작점)

> 작업 완료 시 구현자가 갱신하고 검토자가 사실관계·테스트 결과를 확인한다.
> 이전 묶음 내용은 날짜별 현황으로 보내고, 이 파일에는 현재 작업과 직전 주장만 유지한다.

- **동결 기준점**: `212885a8292d1e86677d5beeb9f5358d76fe9b40` = tag
  **`platform-core-freeze-260724`**. 증거 = `docs/FREEZE_MANIFEST.md`.
- **현재 기준 커밋**: 이 파일과 같은 커밋(main).
- **현재 단계**: 마스터플랜 5단 완료 → 역할 B V1 합의(6단)는 회의 대기 보류
  → 실차 D+0 인수·연결 **부분 완료(`d0_check` 종료 2 · 검사 8 미확인)**
  → 검토 §39 불승인·§40~§42 회신 수령(08-04)
  → **전기계통 정본 v2 전면 개정**(08-04)
  → **§39 보완 + 정본 최신화 + 전기계통 설치 실행** **(지금 — 2026-08-05)**
- **⚠ 미해결 보류 — 예약 23 / 검토 §33 (`8fcc1a2`)**: P0 0 · **P1 1** · P2 1.
  `handoff_single_check.sh` 는 `§` 없는 새 작업 꼬리를 붙여도 `OK` 를 내고, 숫자 계약 전수
  증거는 기록 6자리와 독립 실행 21자리가 불일치한다. **승인 완료로 읽지 말 것.** 런타임
  `src/**` 변경·회귀는 없으므로 현재 D+0 실행은 막지 않되, 보완은 다른 묶음에 섞지 않고
  `MASTER_PLAN.md §7` 예약 23 의 완료판정과 부정 회귀로 별도 구현한다. 전문 =
  `~/Desktop/개발현황/CODEX 현황/0801검토현황.md §33`.
- **⚠ 미해결 보류 — 예약 24 / 검토 §38**: P0 0 · **P1 2** · P2 4. 라이다 실측 소유 시점과
  readiness flake 소유 정본이 갈라졌다. **라이다 높이는 D+1 소유**이며 `JETSON_SETUP.md §9`
  의 `TODO(D+0)` 목록 계약은 그대로다.
- **⚠ 미해결 보류 — 예약 25 / 검토 §39 (`c397975` 불승인)**: P0 0 · **P1 2** · P2 12 중
  **코드 표면만** 남았다 — `d0_check.sh` 스냅샷 2자리의 벽시계 미측정·원인 문자열 폐기,
  `--secs` 분리, udev 선택도 주석. **다음 `d0_check.sh` 현장 실행 전에 닫는다.**
- **⚠ 미해결 보류 — 예약 26 / 검토 §40**: 사용자 결정 **'방법 B'** 가 구현되지 않았고 저장소
  에서도 사라졌던 건. 결정은 `MASTER_PLAN.md §8` 에 복원했다. **§37.3 을 닫힌 것으로 인용하지
  않는다.**
- **⚠ 미해결 보류 — 예약 27**: 이 Jetson 에 `nvcc`·`nvidia-jetpack` 이 **없다**. 역할 A 는
  비차단, **역할 B 성능 전제는 통째로 바뀐다.** 역할 B 회의 안건.
- **⏸ 6단 보류**: 역할 B 회의가 잡히면 커밋 `30c5e87`의 핸드오프 원문을 그대로 복원한다.

```bash
git show 30c5e87:docs/CURRENT_HANDOFF.md
```

## 이번 묶음 — §39 보완 + 정본 최신화 (구현자 = Claude, 독립 검토자 = Codex 예정)

★ **역할 교대 (`AGENTS.md §5` 예외 경로 — 명시한다)**: **§39·§40·§41·§42 의 검토자는
Claude 였고 구현자는 Codex 였다.** 이번 회차는 사용자 명시 요청(2026-08-05 "전부 최신화하고
안 맞는 부분 전부 고쳐줘")으로 **그 검토자였던 Claude 가 이 보완 diff 한 번에 한해 구현자가
됐다.** 따라서 ① 이 커밋은 **Codex 가 독립 검토**한다 ② 구현자 주장은 아래에 있다
③ 역할은 **이 묶음 경계에서 원위치**한다.

⚠ **교대에서 오는 고유 위험을 공개한다** (`AGENTS.md §5` ③ · `66db5c5` 가 실제로 밟은 자리):
**검토자가 구현하면 자기가 지적한 그 자리만 고치기 쉽다.** 이번에 클래스 전수로 처리한 것은
무상한 `topic echo`(`grep` 출력 10자리 전량)와 예약 신설 슬롯 목록(4자리 규약)뿐이고,
**§39.6.2 P2-A~G 7건은 검토가 준 목록을 그대로 따라간 것**이라 같은 클래스가 다른 활성 런북
(`D1_FIRST_STEP.md` 등)에 남아 있을 수 있다. 검토자는 **내가 준 목록 밖**을 먼저 공격한다.

08-03 현장·08-04 검토·08-04 전기계통이 **저장소 정본에 반영되지 않은 채** 이틀이 지난 것을
바로잡는 묶음이다. `src/**` 는 열지 않았고 런타임 변경은 0이다. 구현자가 주장하는 것:

- **검토 §39~§42 넉 건이 저장소에 한 글자도 없었다**(`grep "§39" docs/ tools/` = 0건).
  그중 §39 는 **불승인**이었다. 문서 표면은 이번에 닫고 코드 표면은 예약 25 로 분리했다.
- **안전 P1 §39.6.1 을 닫았다** — `JETSON_SETUP.md §10` 이 *"종료 0이면 R1·R2"* 로 낡아
  §11-g 복귀점과 갈라져 있었다. 이제 §10 은 현재 상태(종료 2 · 검사 8 미확인)와 §11-g 를
  먼저 가리키고, 주행 전제는 §7-c 를 **재진술이 아니라 참조**로 건다.
- **inventory 계약을 64 → 67 로 한 커밋에 올렸다**(§42.4 요구). `ai_known_p0_p1.json` 3행 +
  `test_ai_context.py` 3자리 + `AI_CONTEXT.md §3` 4자리. 앵커는 각 P1 이 사는 프로필 정본
  (`FREEZE_MANIFEST.md §10.13` · `AI_CONTEXT.md §3`)에만 뒀다.
- **§40.4 의 미공개 상한을 공개했다** — 역사 스캐너는 P0/P1 제목을 `###` **정확히 세 개**
  에서만 센다. `####` 로 쓰면 조용히 샌다(§39.6.1 이 실제로 그렇게 안 세어졌다). 상한 자체를
  `test_02c` 부정 회귀로 고정했다.
- **§39.3·§39.6.2 의 문서 P2 를 닫았다** — PAT 종류(fine-grained 실측), 빌드 시간 28초 기준선,
  CUDA 소유자·트리거(예약 27), 증거 보존 규정(§11-g), `§8` 막힐 자리 2행, 기한 재설정 2자리,
  `§0` 신뢰도 표, TF 인과를 "분류"에서 **미확정 가설**로 하향.
- **무상한 `topic echo` 10자리를 전부 상한으로 감쌌다.** 손으로 세지 않고
  `grep -n "ros2 topic echo" docs/*.md | grep -v timeout` 출력을 그대로 썼고, 남은 2자리
  (관찰용 스트림·역사 인용)는 **예외 규약으로 `PITFALLS.md §2` 에 명시**했다.
- **전기계통 정본을 라우터에 등록했다** — `AGENTS.md §2` · `CLAUDE.md §0`. 08-04 전기 결정
  6건과 방법 B 결정을 `MASTER_PLAN.md §8` 색인에 넣었다.
- **예약 신설 시 채울 자리 4개를 규약으로 고정했다**(`AGENTS.md §5`, §42.3 완료판정).

## 직전 완료 — 전기계통 정본 v2 (구현자 = Claude, 독립 검토자 = Codex 예정)

08-04 커밋 5개(`4d43b67`·`0f34dbb`·`8dd8f59`·`e65552e`·`6af8997`). 사용자가 E-stop 전장을
직접 맡기로 하면서 전기계통 전체를 재검토했다. **뒤 4커밋은 핸드오프에 기록되지 않았다** —
그래서 여기 구현자 주장으로 남긴다. 변경 표면은 문서뿐이다.

- 차단소자를 **DC 컨택터 → 24V 자동차 릴레이 2개 직렬**로 바꿨다. 수급 불가가 이유이고,
  접점 여유가 작다는 점은 **전제조건(주행 전 매번 개폐 1회 검증 + 예비품)과 재개방 조건
  (R1 이상 진입 시 컨택터 재검토)** 으로 공개한다.
- **LAZ16-11ZS 는 단자 3개(C 공유)라 PIN 21 을 이번 회차에 연결하지 않는다.** 따라서
  `/estop/state` 는 눌러도 `false` 다 — 이 토픽을 소비하는 로직을 만들지 않는 것이 전제조건.
- 퓨즈는 **메인 MAXI 30A 1개 + 코일 인라인 2A**. 코일선(18~20AWG)과 LAZ16 접점(DC 3A)이
  30A 로 덮이지 않는 유일한 구간이다. **과전류 보호가 완결됐다고 기록하지 않는다.**
- 메인 차단 스위치를 `연속 100A급`(과잉 사양·수급 실패)에서 **최소 30A·권장 50A** 로 정정하고,
  DC 차단기 통합안을 추가하되 **차단기는 보호 상한이라 30A 가 최대**임을 함께 경고했다.
- LiPo 알람은 **셀당 3.5V(=21V)**, 임계값 조절식만. DCDC 입력 하한 19V 가 정한 값이다.
- `PITFALLS.md §10` 신설(전기계통), 실행 절차는 `~/Desktop/전기계통_최종작업안_0804.md` 소유.

## 직전 완료 — 실차 D+0 현장 기록 (구현자 = Codex, 검토 = §39 **불승인**)

`c397975`. Claude 의 §39 독립 검토가 **P0 0 · P1 2 · P2 12 로 불승인**했다. 문서 P2 와 안전
P1 은 이번 묶음에서 닫았고 코드 P1·P2 는 예약 25 다. 승인으로 읽지 않는다.

## ★ 이번 회차 최우선 확인 사항 — 전기계통 시공(사용자 직접)

1. 시공 정본은 `~/Desktop/전기계통_최종작업안_0804.md` **§4 작업 순서**다. 판단 근거·위험
   논리가 필요하면 `docs/ELECTRICAL_BASELINE.md`, 부품 계약값은 `REAL_ROBOT_VALUES.md §1-e`.
2. **작업 전 메인 스위치 OFF + XT90 분리 + 멀티미터 0V 확인.** E-stop 만 누르고 배선을
   만지지 않는다 — E-stop 은 **모터 가지만** 끊고 Wago·퓨즈·로직은 살아 있다.
3. **퓨즈는 맨 마지막에 끼운다.** 다이오드 방향을 눈대중하지 않는다(거꾸로 = 코일 통전 순간
   배터리 단락). **87a 선은 반드시 절연**한다 — E-stop 을 누르면 거기에 +25V 가 나타난다.
4. `§5 중단 기준` 5가지 중 하나라도 걸리면 **멈추고 상의**한다(퓨즈 끊김 · 발열 · 눌러도
   전압 유지 = 융착 의심 · 릴레이 "딱" 무음 · 타는 냄새/연기/부푼 배터리).
5. 시공 끝 → **E-stop 개폐 10회 토글**로 실제 차단을 확인한다. 배선을 다시 만졌으면 10회를
   **다시** 한다.
6. 다음 현장 세션의 **첫 명령**은 D+0 증거 복제다(`JETSON_SETUP.md §11-g` 보존 규정) —
   bag 2개·tegrastats 로그가 아직 Jetson 한 대에만 있다.
7. 전기 시공 자체는 `TODO(D+0)` **11건**을 닫지 않는다. 그 목록의 정본은
   `JETSON_SETUP.md §9` 한 곳이며, 시공 결과는 E-stop 항목의 **전제**만 만든다.

## 사용자 결정 — 유지

- 동결 예외 4회는 전부 소진됐다. `src/**`를 다시 열려면 사용자 승인이 필요하다
  (`FREEZE_MANIFEST.md §10`).
- 순서는 전기계통 시공 → E-stop 검증 → D+0 잔여 종결 → D+1 R3 rosbag → R4~R8이다
  (`MASTER_PLAN.md §3`).
- 예약 20·21·22·23·24·25·26·27과 역할 B 6단은 각각 정해진 트리거 전까지 별건으로 유지한다
  (`MASTER_PLAN.md §7`).
- `JETSON_SETUP.md §9`의 `TODO(D+0)` **11건** 중 아홉 자리가 값과 함께 닫혔고, 미해결은
  Arduino 라이브러리 원본과 물리 E-stop 이다. 목록 계약은 이번 묶음에서 바꾸지 않는다.
- 08-03 D+0 판정: 장치·주기·간격·QoS·전진 부호·EKF 생존·펌웨어 정체는 **통과**했고, 수정판
  `--no-manual` 재실행은 자동 항목 전부 통과·**종료 2**였다. 남은 항목은 Arduino 라이브러리
  원본 수령, 물리 E-stop, 그 전제가 필요한 R0 watchdog이다. **전량 통과로 기록하지 않는다.**
- 🔴 **이번 회차 E-stop 은 PIN 21 미연결이므로 `d0_check.sh` 검사 8 은 원리적으로 통과할 수
  없다.** 종료 0 은 2단계(신호릴레이 a접점 + `ESTOP_ACTIVE_LOW = false`) 이후에만 가능하다.
  그때까지 `--no-estop` 종료 2 가 이 트랙의 정상 상한이며, 이는 **물리 차단이 없다는 뜻이
  아니라 Teensy 가 그것을 못 본다는 뜻**이다. 물리 차단 자체는 개폐 10회 토글로 증명한다.
- never-seen은 다른 놓침과 같이 역행 2회 → 보고 → 단독 탈출이다. 실차 R3 뒤 재평가한다.

| 트랙 | 범위 | 상태 |
|---|---|---|
| **진행 중 묶음** | §39 보완 + 정본 최신화 + **전기계통 시공**(`ELECTRICAL_BASELINE.md`) | **착수** |
| 실차 D+0 | 연결·주기·QoS·부호·펌웨어 통과, 종료 2 | ⏸ E-stop 설치 뒤 잔여 종결 |
| AI 입력 최적화 | `ef25ad3` 기준, 품질 불변 + raw 입력 30% 절감 | §40 승인 · 방법 B 잔여 = 예약 26 |
| 예약 21 | D0/R3 판정기 회귀 + readiness case 9·11·12·14 간헐성 규명 | ⏸ 다음 bringup 변경 전 |
| 예약 23 | 핸드오프 ID·숫자 계약 전수 폐포 | ❌ 별도 보류 |
| 예약 24 | 검토 §38 정본 소유권·예약 완료판정 정합 | ❌ 다음 bringup/예약 21 전 |
| 예약 25 | 검토 §39 판정기 벽시계·`--secs`·udev 잔여 | ❌ **다음 `d0_check` 실행 전** |
| 예약 26 | 검토 §40.3 방법 B 계약 표면 재설계 | ❌ AI 라우터 표면 여는 묶음 |
| 예약 27 | 역할 B CUDA·JetPack 전제 | ❌ 역할 B 회의 |
| 역할 B V1 합의 | 커밋 `30c5e87` 원문 | ⏸ 회의 대기 |

## 이번 한 묶음 목표 — §39 보완: 정본 최신화 + 전기계통 시공

08-03~08-04 에 실제로 일어난 일(현장 D+0 · 검토 4회 · 전기계통 v2)을 저장소 정본에 빠짐없이
반영하고, 검토 §39~§42 의 각 발견을 **닫음 또는 예약**으로 분류한다. 같은 날 사용자는
`~/Desktop/전기계통_최종작업안_0804.md` 대로 전기계통을 시공한다. 새 알고리즘·새 절차를
설계하는 회차가 아니라 **기록과 현실을 일치시키는 회차**다.

## 완료조건

1. 검토 §39·§40·§41·§42 의 **모든 발견이 저장소에서 추적 가능**하다 — 닫힌 것은 그 자리에,
   미해결은 `MASTER_PLAN.md §7` 예약에 완료판정·부정 회귀와 함께 있다.
2. `python3 tools/test_ai_context.py` 전량 통과하고 inventory·역사 계수가 **67 로 일치**한다.
3. `bash tools/doc_check.sh` 전 항목 OK, `bash tools/handoff_single_check.sh` OK.
4. 회귀 정본이 그대로다: pytest **182 passed** / colcon **245 tests** /
   harness guards **24 검사** / gate regression **14 케이스**.
   ⚠ 이번 변경 표면은 문서·계약이라 `TEST_GATES.md §7` 라우팅상 **직접 실행 대상은
   `test_ai_context` 전량·`test_todo_scan`·pytest·harness guards** 다. colcon 245 와
   gate regression 14 는 `src/**` 무접촉이므로 재실행하지 않고 **문서 수치 전수 스윕으로만**
   확인한다 — 재실행했다고 쓰지 않는다. `src/**` 를 여는 다음 묶음에서
   `colcon test-result --verbose`로 판정한다.
5. 전기 시공: `전기계통_최종작업안_0804.md §4` 순서를 완주하고 **§5 중단 기준이 한 번도
   발동하지 않았으며**, 마지막에 **E-stop 개폐 10회**가 전부 실제 차단으로 관찰된다.

## 보존해야 할 안전 불변조건

- 물리 E-stop 이 없거나 검사 8이 미확인인 동안 **`/cmd_vel` 발행·R0~R2·Nav2·미션 금지**.
  `Ctrl+C` 를 E-stop 으로 승격하지 않는다.
- **`/estop/state` 는 이번 회차에 항상 `false`** 다. 이 토픽을 소비하는 로직을 만들지 않는다.
- 배선 작업 전 **메인 스위치 OFF + XT90 분리**, 배선을 만졌으면 **개폐 10회 재검증**.
- 설정은 대상 소프트웨어가 실제로 읽는지 `param list`·`topic info -v`로 확인한다.
- N초 관측은 같은 관측자가 종료 상태와 **벽시계 경과시간**으로 창을 완주해야 한다.
- 토픽별 계약의 단일 출처는 `tools/bag_gap_report.py`의 `TOPIC_POLICY`다.
- `/odom`은 RELIABLE, `/imu/*`는 BEST_EFFORT다. EKF 소비자 QoS까지 함께 본다.
- Teensy 재연결 로직 없음, IMU 치명 실패 시 노드 자체가 안 뜸, 시각 역행 시 stamp가 사실상
  멈춘다는 펌웨어 전제를 유지한다(`REAL_ROBOT_VALUES.md §1-d`). **re-arm 래치는 아직 없다** —
  구현·부정/전환 시험 전 R1·R2 금지.
- 실차 없이 확인한 합성 bag·가짜 `ros2` 결과를 실차 통과로 승격하지 않는다.
- `TODO(D+0)` **11건**을 추측값으로 닫지 않는다 — 미해결은 이유와 재확인 시점을 적는다.

## 금지 범위

- 사용자 추가 승인 없는 `src/**` 변경.
- 예약 20·21·22·23·24·25·26·27, 역할 B V1, 관제 통보 표면, 깜빡임 원인 규명을 이 묶음에 혼합.
  특히 **예약 25 는 판정 도구 코드**라 합성 입력 부정·역회귀를 함께 실행하는 별도 커밋이다.
- `make_map.sh` 실런, 지도 자산 변경, Gazebo E2E 동시 실행.
- TODO를 추측값으로 닫기, `d0_check.sh` 종료 2를 PASS로 기록하기, 개폐 10회 없이 E-stop
  설치를 완료로 기록하기.
- Claude가 구현한 이번 정본 최신화 diff와 08-04 전기계통 diff를 Claude가 최종 승인하기.

## 완료 판정

**검토 §39~§42 의 모든 발견이 저장소에서 닫힘 또는 예약으로 추적되고, `doc_check`·
`handoff_single_check`·`test_ai_context`(67)·회귀 4수치가 전부 그대로이며, 전기 시공이
중단 기준 없이 끝나 E-stop 개폐 10회가 실제 차단으로 관찰되면 이 묶음은 완료다.**

## 완료 후 다음 단계

1. **예약 25** — `d0_check.sh` 벽시계·원인 문자열·`--secs`·udev 보완 (다음 현장 실행 전).
2. D+0 증거 복제(`JETSON_SETUP.md §11-g`) 후 D+0 잔여 종결 — `TODO(D+0)` **11건** 중 남은
   Arduino 라이브러리 원본·물리 E-stop, `/scan` 포함 판정, R0 watchdog 실측.
3. R0 watchdog 실측으로 `FREEZE_MANIFEST.md §6` 조건부 수용 재판정.
4. `docs/D1_FIRST_STEP.md`의 D+1 R3 rosbag 분석 → 예약 22와 R4~R8.

## 근거 문서

- 전기계통 사실·위험·최소 안전선: `docs/ELECTRICAL_BASELINE.md`
- 전기 시공 절차(BOM·결선·순서·검증): `~/Desktop/전기계통_최종작업안_0804.md`
- 실행 순서·TODO: `docs/JETSON_SETUP.md §9`, `§10`, `§11`
- 실차 값·펌웨어 전제: `docs/REAL_ROBOT_VALUES.md §1`, `§4`, `§5`
- 사다리·보류·유효 결정: `docs/MASTER_PLAN.md §3`, `§7`, `§8`
- 검토 §39 인벤토리: `docs/FREEZE_MANIFEST.md §10.13`
- 변경 표면별 검토: `docs/TEST_GATES.md §7`
- AI 입력 최적화 계약: `docs/AI_CONTEXT.md §2`, `§3`, `§4`
