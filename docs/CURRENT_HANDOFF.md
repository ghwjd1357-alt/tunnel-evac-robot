# CURRENT_HANDOFF.md — 지금 할 한 묶음

> 현재 묶음 + 미해결 보류만 유지한다. 구현자의 완료 주장은 독립검토 판정이 아니다.

- **동결 기준점**: `212885a8292d1e86677d5beeb9f5358d76fe9b40` = `platform-core-freeze-260724`.
  🔴 08-21 에 여덟·아홉 번째 예외(`FREEZE_MANIFEST §10.29`·`§10.30`).
  🔴 **08-22 에 열 번째를 열었다 — 펌웨어를 구웠다**(`build=Aug 22 2026`. `FREEZE_MANIFEST` 미기재 = 부채, 예약 62).
- **저장소 기준**: 노트북·젯슨 **`shoot-0823`**. 🔴 **08-23 실차 종료 후 젯슨은 역할 B 에 인계**했다.
- **실제 보드**: `build=Aug 22 2026`. 승인 지문은 1.6.1 이라 `firmware_precheck.sh` 는 `rc=1`(FAIL)이고
  **그 상태로 구웠다**(의식적 무시 · 예약 62).
- **항법 지도**: `jetson:~/Desktop/maps_0820/corridor_0820` (원점 오차 1 cm).
  🔵 바닥 표시 = **지도 원점 `map (0,0) yaw 0`** (08-23 실측 확인).
- **현재 단계**: §91(P0 사슬 5회차 종결) → §92(08-23 실차 촬영 · 본편 확보) → **§93 = 관제 전면 개편**

## 이번 한 묶음 목표 — 관제 전면 개편 (§93)

🔵 **§92 에서 본편(`realtake6`)을 확보했다.** 이 묶음은 **관제 시스템**이다.
정본 = `console/README.md`(개편 전체 · 지도 정리 기준 · 모드 규칙 · MOCK · 열린 위험).

```
🖥 이 묶음   관제 5메뉴 개편 + 디스플레이 모드           ← 지금 여기
🔧 그다음    예약 81·82(관제 명령 신뢰성) · 75~80 · §90.1·90.2 · 62·63·74
```

**한 일** — 단일 파일 513줄 → 모듈 15개(3,055줄). 메뉴 5개(관제·영상·진단·기록·비상)
+ `?display=1` 디스플레이 모드. 상태 12종 한글화, 경보 규칙 10종, 평시/대응/긴급 모드 전환,
지도 정리 5단계, `/drive/diag` 해독, 의사소통(`/display_msg`).

🔴 **구현자가 주장하는 것** — 브라우저 육안 확인은 2560x1600·1920x1080 둘 다 했다.
자동 검사는 배선 정합·경보 15케이스·실데이터 하니스 3종이 통과한다. **독립 검토는 안 받았다.**

🔴 **`/scan` 을 10 Hz 전량 구독으로 올렸다** — 화면 부드러움을 위해서다(`0718 §5.5` 가
피하려던 그 부하). 약 100 KB/s. localhost 는 무시할 수준이지만 **젯슨 실차 연결 시험에서
재평가**한다. 되돌리는 지점 = `js/ros.js` `/scan` 스로틀 100 → 200.

## 🎬 영상 소스 — 진행 상황

렌더링 도구 = **`tools/viz/`**(08-24 저장소 반입). bag → mp4 를 직접 그린다.
RViz 화면 녹화가 아니라서 창 테두리·커서가 안 들어가고 몇 번이든 다시 뽑는다.
산출물 = `~/robot_evidence/viz/` (용량 때문에 저장소 밖).

| | 소스 | 상태 |
|---|---|---|
| ① | **관제화면** — 지도·위치·경로·궤적·점군·costmap·미션상태 (전체 임무, 2배속 156초) | 🟢 초안 |
| ② | **클러스터 판정** — 로봇 중심 확대, 추종 판정 (분기점~주행재개, 실속도 134초) | 🟢 초안 |
| ③ | ① 다듬기 — 구도·색·배속·마커 위치 | 🔴 |
| ④ | **HUD·미션로그 터미널** — `mission_hud.render()` 를 import 해 재렌더 | 🔴 |

🔴 **④ 는 화면 녹화가 아니라 재렌더다** — 데이터·문구는 실제 기록이지만
*"터미널을 캡처했다"* 로 소개하지 않는다.

🔴 **영상 자막에 쓰면 안 되는 말** (`tools/viz/README.md` 에 전문)
```
"카메라로 사람을 인식한다"  — /scan 판정이다        "사람을 추적한다" — 프레임 간 연결이 없다
"새로운 알고리즘"           — 클러스터링+크기 필터는 고전 기법이다
```

## 🟢 08-23 실차 — 본편 확보 (`realtake6`)

전 구간 완주 2판(`realtake5`·`realtake6`). 본편 = **`realtake6`**.
증거 = `~/robot_evidence/` bag 11개(2.6 GB) + 관제화면 영상. 서사 = `0823_현황.md §13`.

```
PATROL → APPROACH → SCAN_AREA → GATHER → GUIDE → HOLD → SEARCH_BACK → GUIDE → ESCAPED
미션 306초
```

| # | 항목 | 관찰값 |
|---|---|---|
| ① | **§0-c 정본 G8 10회** | **10/10 정지 · "딱" 균일 · 매회 0V 실측 · 발열·냄새·변색 없음** |
| | *덤* `§5-G6` 개폐 10회 | **10/10** → `/estop/state` 신뢰 복구(08-22 재굽기로 회수됐던 것) |
| ② | `param get` 두 줄 | `Double value is: 0.2` / `array('d', [0.2, 0.0, 0.83])` |
| ③ | `drive_health` | **9.0~10.2 °/s** (12회 전부 🟢) · 엔코더 오차 −2.8~+1.6% |
| ④ | **SCAN_AREA** | 4스텝 · **44.0초** · 총회전 **정확히 360°** · 위치 0.272 m 고정(이동 2 cm) |
| ⑤ | **HOLD** | **4.5초** 정지 후 역행 전환 |
| ⑥ | 🔴 **SEARCH_BACK** | **178° + 1.10 m 역행 · 20.0초 → 재발견 → GUIDE 복귀** |
| ⑦ | FAULT · BLOCKED | **본편 0건** |
| ⑧ | `/scan` 끊김 | **0건** (최대 공백 0.189초 / 임계 1.0) |

**`/drive/diag` 330초 내내 `x=1 y=0 z=2` 불변 · `/estop/state` true 0건.**

## 🟢 §91 4회차 P0 가 실차에서 닫혔다

*"재발견 후 정상 취소가 미션에 통보되지 않아 GUIDE 복귀가 영구 정지"* 는
**r9·r10·r11·realtake5·realtake6 다섯 판 전부 정상 복귀**했다(재발견 → 취소 종결 → GUIDE 재개 ≈ 1.0초).
정지 직렬화(§87~§91)도 알람·HOLD·재발견 **매회 20~30 ms 에 CANCELED 종결**.
🔴 **닫힌 것은 이 경로의 실차 재현뿐이다** — 결합 회귀 부재(예약 74)와 §90.1·90.2 는 그대로다.

## 🔴 08-23 신설 예약 75~80 (상세 = `MASTER_PLAN §7`)

```
75  xy_goal_tolerance 전환이 사람 손이다   🔴 재현성 0. 이 묶음의 1번
76  미션 첫 goal·service 유실 (DDS 매칭 미완) — 예열 기동으로 회피 중
77  SEARCH_BACK 은 되돌아가지 않는다 (실측 0.03 m). 0.75 m 시험은 x축 하드코딩
78  map→odom 침묵을 감시하는 층이 없다 (08-23 에 21분 침묵 · 40분 소모)
79  ros2 bag record 의 토픽별 기록 지연 — bag 분석은 header.stamp 기준
80  벽 모서리가 폭 0.5~0.7 m 조각으로 잘려 사람 판정을 통과한다 (아래복도 93.9%)
```

## 🔴 열린 채 남는 위험

**P0-1 전기** — `§0-c` 정본 G8 을 **통과**(10/10 · 매회 0V)해 08-23 수용 조건을 채웠다.
🔴 **증명된 것은 정상 주행 전류(1~3A) 차단뿐이다.** `0.20` 차단 · 끼임(약 21A) 차단 ·
릴레이 DC25.2V 정격 서류는 **미검증**으로 남는다(예약 63). 영상·문서에 *"검증했다"* 로 쓰지 않는다.

**P0-3 라이다** — 코드 미변경 · 사람 방어층 유지. 08-23 끊김 0건이지만
그것이 *"사람 E-stop = 자동 정지"* 를 증명하지 않는다(§91 2회차 판정 유지).

**§90.1·90.2 미지 goal 봉쇄** — P0. 열려 있다.
**예약 73·74** — 검사가 자기 대상을 안 지나는 자리들 · §91 을 닫은 결합 회귀가 저장소 밖.

**🔴 전기 신규 — Teensy 전원 스위치 접점** (`ELECTRICAL_BASELINE §4-f-7`)
08-23 에 재부팅 1회(`/drive/diag` `x` 1→0 · `/estop/state` 는 false 유지).
원인 = 스위치 접점이 알루미늄 새시에 접촉. 조치 = **공중 이격** 후 8판·2,000초 재발 0건.
🔴 **"해소"가 아니다** — 절연 시공이 아니라 임시 이격이다. 재개방 = 스위치를 고정하거나 배선을 만질 때.

## 🔴 정본에 못 들어간 값 — 런타임뿐이다

본편을 만든 값 셋 중 **하나만** 파일에 있다.

```
rotate_to_heading_angular_vel 0.63   ✅ 파일에 있음 (회전은 병목이 아니었다 — r7 이 반증)
yaw_goal_tolerance            0.10   ✅ 이 묶음에서 파일 반영 (오차 22° → 5°, 두 판 대조)
xy_goal_tolerance   0.25 → 도착 후 0.50   🔴 **값이 아니라 동작이라 파일에 못 넣는다** = 예약 75
```

- **⚠ 미해결 보류 — 예약 23 / 검토 §33 (`8fcc1a2`)**: P0 0 · **P1 1** · P2 1.
  `handoff_single_check.sh` 는 `§` 없는 새 작업 꼬리를 붙여도 `OK` 를 내고, 숫자 계약 전수
  증거는 기록 6자리와 독립 실행 21자리가 불일치한다. **승인 완료로 읽지 말 것.** 런타임
  `src/**` 변경·회귀는 없으므로 현재 D+0 실행은 막지 않되, 보완은 다른 묶음에 섞지 않고
  `MASTER_PLAN.md §7` 예약 23 의 완료판정과 부정 회귀로 별도 구현한다. 전문 =
  `~/Desktop/개발현황/CODEX 현황/0801검토현황.md §33`.
- **현재 미해결 보류**: 예약 36·39·**40**·41-g·43·**44**·46·**47**·**48**·49·**50**·
  51·**53**·**54**·**55**·**58**·**60**·**61** · **62~70** · **71·72** · **73·74** ·
  **75~80**(08-23 실차) · 🆕 **81~85**(09-04 관제) 와 §72.2·H′ · **검토 §90.1·90.2 P0**(🔴 보류가 아니라 **열린 채**).

## 완료조건

| # | 항목 | 통과선 |
|---|---|---|
| 1 | 🔴 **예약 75** xy 전환 자동화 | SCAN_AREA 진입/이탈에서 코드가 바꾼다. 사람 개입 0 |
| 2 | 🔴 **§90.1·90.2** 미지 goal 봉쇄 | 4회차 미종결분. 독립 검토 통과 |
| 3 | 🔴 **예약 74** 결합 회귀 | 진짜 GM + 진짜 상태머신 층. 변이로 검출력 확인 |
| 4 | 🔴 **예약 62** 펌웨어 사후 독립 검토 | `firmware/**` 가 아직 한 번도 안 받았다 |
| 5 | 🔴 **예약 63** 전기 전제 재작성 | 끼임 전류 실측 · 릴레이 정격 서류 |
| 6 | 예약 76~80 | 각 예약의 완료판정. 🔴 **80 은 77 과 같은 묶음**(둘 다 follower_monitor) |
| 7 | 🔴 M4 `low_west` 실측 | 08-23 에도 못 했다 |
| 8 | 기준선 유지 | `TODO(D+0)` **11건** 종결 계약 불변 · `§7-c-E` **13행 전량 수행** |

## 완료 판정

🔴 **`firmware/**` 변경이 독립검토를 안 받았다.** 굽힌 뒤에도 안 받았다(예약 62).
🔴 **`src/**` 의 HOLD·사람 판정 사슬을 불승인인 채로 촬영에 투입했다.** 실차 다섯 판이
정상 동작했지만 그것은 **관측이지 승인이 아니다** — §90.1·90.2 는 여전히 열려 있다.
🟢 **본편은 확보했다.** 08-22 종료 시점의 *"쓸 수 있는 영상 0"* 은 해소됐다.

`TODO(D+0)` **11건** 과 `§7-c-E` **13행 전량 수행** 계약은 그대로다.

## 최종 회귀

- 기준선(08-23 갱신 없음 — 실차 묶음이라 코드 변경이 파라미터 1줄·도구 1줄뿐):
  pytest **285 passed** / adapter **120** / tools **369 + skip 1** /
  colcon **477 tests, 0 fail, 3 skip** / **test_harness_guards 24 검사** /
  test_gate_regression **14 케이스**.
- 🔴 `firmware_precheck.sh` = **FAIL**. 승인 지문 미이관 + 08-22 변경. 굽고 나서도 그대로다(예약 62).
- 🔴 **이 묶음의 `nav2_params_real_db.yaml` 변경(`yaw_goal_tolerance`)은 독립 검토를 안 받았다.**
  근거는 실차 두 판 대조(`REAL_ROBOT_VALUES §1-o-3`)이고, **구현자가 주장하는 것**으로 남긴다.
- 🎯 실차가 잡은 것: 회전 병목을 세 판(`0.63/0.50/0.35`)에 걸쳐 **반증**했고, 진짜 병목이
  **도착 허용치의 성질**임을 같은 좌표 네 판 대조로 특정했다(`§1-o-2`).

## 금지 범위

- 🔴 **관제의 "비상 정지" 를 안전 수단으로 소개하지 않는다** — 실제 정지는 `abort` 가 하고
  그 발행이 단발이라 유실되면 안 멈춘다(예약 81). `cmd_vel 0` 은 `velocity_smoother`(20 Hz)에
  덮인다(예약 82). 🔵 하드 정지(Teensy 0.5초 워치독)는 별개로 살아 있다.
- 🔴 **관제 지도를 "실측 그대로" 로 소개하지 않는다** — 다섯 기준으로 걸러 그린다
  (`console/README.md`). 벽 모양은 안 건드리지만 **복도 밖과 노이즈는 안 그린다.**
- 🔴 **지도를 바꾸면 `console/js/map.js` 의 `OPERATION_AREA`·`CORRIDOR_SPAN` 을 같이 고친다**
  (예약 83). 코엑스 현장 지도 제작보다 **먼저** 닫는다.
- 🔴 **관제 화면의 MOCK 4곳을 "실측" 으로 소개하지 않는다** — 카메라·탐지목록·배터리·Jetson 부하.
  화면에 `데모 데이터` 로 적혀 있다.
- 🔴 **`/display_msg` 를 "로봇 디스플레이에 떴다" 로 쓰지 않는다** — 받는 쪽이 없다.
  화면도 *"도착 확인 불가"* 로 적는다.

- 🔴 **`TODO(D+0)` 11건을 추측으로 다시 열거나 닫지 않는다** — 종결 계약이다.
- 🔴 **알람은 대본 좌표 `(12.5, −0.1)` 로만 쏜다** — `BLOCKED` 도달 = P0 경로 활성화.
- 🔴 **`MAX_*_CMD` 를 더 올리지 않는다** — `ELECTRICAL_BASELINE` 전제 재작성(예약 63)이 선행한다.
  🔴 08-23 G8 통과는 **`0.05` 정상 전류 차단**만 증명했다. 상향의 근거가 아니다.
- 🔴 **Teensy 전원 스위치를 다시 고정하거나 그 배선을 만지면** `§4-f` 재개방 — `§5-G6` 10/10 부터 다시.
- 🔴 **카메라·열화상 ESP32 를 꽂은 채 주행하지 않는다** — 라이다가 죽는다(예약 66).
- 🔴 **`nav2_params_real.yaml` 원본 수정 금지** — 튜닝은 `_db.yaml` 사본으로만.
  🔴 `inflation_radius` 를 외접반경 0.40 미만으로 내리지 않는다(`PITFALLS §6`).
- 🔴 **`use_rotate_to_heading` 을 `false` 로 되돌리지 않는다** — 여덟 시행이 기각했다(예약 40).
- 🔴 **`ekf_real.yaml` 수정 금지** — 바꾸면 역할 B 통보 의무.
- 🔴 **URDF 수정 금지** · **`waypoints.yaml`(시뮬 T자) 수정 금지**(회귀 184건이 쓴다).
- 🔴 **`make_map.sh` 실런 금지** — 지도 자산 변경은 사용자 명시 승인 + 정기 제작 때만.
- 🔴 **bag 분석에 기록시각을 쓰지 않는다** — `header.stamp` 기준(예약 79).
  기록시각으로 맞추면 회전 구간이 **30° 어긋난다**(08-23 실측).

## 완료 후 다음 단계

1. 🎬 **영상 소스 ③④ 를 먼저 끝낸다**(이번 주). 그다음 🔴 예약 75 → 76 → (77+80) 순. **75 가 안 닫히면 다음 촬영도 사람이 손으로 던져야 한다.**
2. 🔴 §90.1·90.2 보완 + 예약 74 결합 회귀 — 같은 묶음.
3. 🔴 예약 62·63(펌웨어·전기) — **안전 항목**. 속도 상향 결정보다 먼저 닫는다.
4. 🔴 예약 58 `SEARCH_BACK` yaw 본수정 — 예약 77·80 과 같은 계열이다. 같이 본다.
5. 예약 48 바퀴별 엔코더 발행 · 예약 55 keepout 배선.
6. 🔵 08-22 서사(고장 3종·곡선 A/B) = `REAL_ROBOT_VALUES §1-n` · `0822_현황.md`.
   08-23 검토 서사(§91 1~5회차) = `CODEX 현황/0823검토현황.md`.

| 트랙 | 상태 | 진행 |
|---|---|---|
| **진행 중 묶음** | §93 — 🟢 관제 5메뉴 + 디스플레이 모드 · 🔴 독립 검토 미수 · 🔴 예약 81~85 | 🔶 진행 |
| **§92 영상 소스** | 🟢 본편 확보 · 🎬 소스 2/4 · 🟢 관제 화면 촬영 가능 상태 | 🔶 2/4 |
| 🆕 **예약 81~85** | 관제 — 정지 단발발행 · cmd_vel 중재 · 지도좌표 하드코딩 · 경보 ack · 위생 | 📋 등록 |
| 🆕 **예약 75~80** | 실차가 만든 여섯 — xy 전환 · 첫 goal 유실 · 역행 거리 · map→odom 침묵 · bag 지연 · 벽 모서리 오탐 | 📋 등록 |
| **예약 73·74** | §91 P1·P2 여섯 · 🔴 결합 회귀가 저장소 밖 | 📋 등록 |
| **어댑터 / 역할 B** | 🔴 실물 0회 (카메라 미결합) · 젯슨은 08-23 에 역할 B 로 인계 | ⏸ |
| `TODO(D+0)` **11건** | ✅ 종결 계약 유지 | ✅ |

## 근거 문서

- **`REAL_ROBOT_VALUES §1-o`**(08-23 실차 — 회전 실효율·도착 허용치·정합) · `§1-n`(08-22).
- **`ELECTRICAL_BASELINE §4-f-7`**(전원 스위치 접점) · `§7`(G8 재실행 · `/estop/state` 4회차 회복).
- `MASTER_PLAN §7` 예약 **75~79** · 58·62·63·73·74 · `§8` 결정 색인.
- `PITFALLS §6`(inflation ≥ 외접반경) · `§14`·`§17`.
- `~/Desktop/개발현황/0823_현황.md §13` — 실차 세션 전문(시간표·실패판 포함).
- `~/Desktop/개발현황/CODEX 현황/0823검토현황.md §91` — 1~5회차 전문.
- 운영: `~/Desktop/0823_촬영_런북.md`.
- 증거: `~/robot_evidence/` — bag 11개(2.6 GB) + `viz/` 영상. 렌더러 = **`tools/viz/README.md`**.
