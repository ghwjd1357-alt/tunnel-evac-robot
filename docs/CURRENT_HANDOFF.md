# CURRENT_HANDOFF.md — 지금 할 한 묶음

> 현재 묶음 + 미해결 보류만 유지한다. 구현자의 완료 주장은 독립검토 판정이 아니다.

- **동결 기준점**: `212885a8292d1e86677d5beeb9f5358d76fe9b40` =
  `platform-core-freeze-260724`.
- **저장소 기준**: `main`의 이 인계 커밋. 부모 `ee2f398` 위에 **§79 보완과
  코퍼스 이관**을 한 묶음으로 남긴다.
- **실제 보드**: 현장 시험 후보 `rearm-latch-pi-runtime-guard-1.6.2`,
  `build=Aug 17 2026 21:49:19`. 승인 지문은 여전히 1.6.1이라 둘을 섞지 않는다.
- **현재 단계**: D0-FW 1.6.2 현장 사다리 완료 → **검토 §77** → 보완 `5eb1511`·`761cb83`
  → **검토 §78** → 보완 `2cb931e`·`ee2f398` → **검토 §79 완료(P0 0 · P1 3 · P2 2 ·
  조건부 수용)** → **§79 보완 구현 완료, 다음은 1.6.3 계측 구현(B1)**.
- 🔴 **§79가 41-g 계약을 다시 열었다** — ① 생존 표본에 host 로 갈 주기 경로가 없었다
  ② coalescing 이 7 burst 를 한 칸으로 합치고 분류표가 전칭이 아니었다 ③ 이 문서가
  구현 완료와 구현 전을 동시에 지시했다. 셋 다 이 묶음에서 닫는다. 이전 회차
  서사(§77·§78)는 `REAL_ROBOT_VALUES §1-g` 앵커와 검토 전문 §77·§78 로 남긴다.

## 이번 한 묶음 목표 — §79 보완과 정본·코퍼스 이관

구현자 **Claude**의 `ee2f398`을 검토자 **Codex**가 독립 검토했다.
전문은 `~/Desktop/개발현황/CODEX 현황/0813검토현황.md` §79다. 이번 커밋은
**41-g 계측 계약을 3판으로 올리고** 검토 서사와 P0/P1 코퍼스 4행을 같은 묶음으로
옮긴다. source·firmware·승인 지문은 고치지 않는다.

### §79 판정

**P0 0 · P1 3 · P2 2 · 조건부 수용.** `ee2f398`은 유지하지만,
🔴 **41-g 2판을 확정 계약으로 승인하지 않는다.** 승인 지문은 1.6.1에 둔다.

전문은 검토 §79 가 소유한다. 여기는 **무엇을 고쳤는지**만 남긴다.

| | 지적 | 이번 보완 |
|---|---|---|
| P1 §79.2 | `sample_seq` 가 사건 발행 경로에만 얹혀 있어 조용한 구간에 host 로 갈 메시지가 없다 — 분류표의 답 내는 줄이 **평가 불가**였다 | ✅ **주기 100ms·BEST_EFFORT·≤128B `/firmware/pulse`** 신설. 판정 단위 = 복귀 후 첫 pulse 의 `sample_seq` 증가량. 영구 정지는 분류에서 빼고 한계를 공개 |
| P1 §79.3 | `code` 단독 coalescing 이 떨어진 7 burst 를 `count=70` 한 칸으로 합치고, 분류표가 `PUBLISH_FAIL` 단독·복합을 안 덮으며 `evt_dropped` 누계가 영구 판정 불능을 만든다 | ✅ 키 `(code, publish_slot, phase, burst_id)` + `first/last_epoch` 보존. 분류표 **9행 전칭·상호배타**(맨 아래 catch-all). `evt_dropped` 누계/구간 delta 분리 |
| P1 §79.4 | 상단·목표는 구현 전, 완료조건·진행표는 구현 완료 — 같은 `§77` 안의 서로 다른 단계라 게이트가 **false green** 을 냈다 | ✅ 네 자리를 §79 한 단계로 통일. 검사기 phase marker 대조는 예약 등록 |
| P2 §79.5 | `--capture` 중간 실패 시 log 만 남고 같은 이름 재시도가 rc=2 로 sticky 하다 | 🔶 **동결**(4회차·fail-open 아님) — 런북 경고 + 조건부 수용 |
| P2 §79.6 | §8 유효 결정 색인이 41-g 가 기각한 구판 `evt_seq` heartbeat 를 아직 "유효" 로 쓴다 | ✅ 색인 행을 3판으로 교체. 41-g 진입 금지 문장에 Nav2 예행 예외 링크 |

🔴 **§78.2 identity schema 잔여는 여기서 다시 구현하지 않는다** — 검토자도 그 동결이
*"정당"* 하다고 확인했다. 다만 *"예약 46 도구 측 **완전** 닫힘"* 이라는 표현이 잔여를
숨기므로 완료조건에서 **"재현 2건 닫힘 · schema 잔여 동결"** 로 범위를 줄인다.

### 왜 1.6.2가 생겼나 — 경위는 `MASTER_PLAN §7` 41-e·41-f 가 소유한다

1.6.1 이 telemetry 8개를 전부 BEST_EFFORT 로 바꾸자 512B MTU 를 넘는
`/odom`·`/firmware/info` 가 아예 안 갔다. 1.6.2 는 그 큰 둘만 RELIABLE 로 되돌리고
ACK 대기를 각각 **20ms** 로 묶었다. 나머지는 그대로다.

🔴 **구현자 주장의 범위** — *"수 초 감시-loop 정지 재발 0 · runtime overrun 0"* 은
**현장 관측 범위**이고, 최대 349ms 수신 공백이 남아 R3 주기 품질은 주장하지 않는다.
§77~§79 는 7사건의 원인을 **사건별로 연결할 계측이 아직 없음**을 확인했으므로,
이 주장을 1.6.2 승인 지문 이관 근거로 쓰지 않는다.

## §3-10 전수 열거와 구현 대조

생산 소스와 소비자를 기계 검색해 다음 클래스를 고정했다.

| 클래스 | 전수 | 1.6.2 대조 |
|---|---:|---|
| 주기 publisher 초기화 | 8 | 큰 2 RELIABLE · 작은 6 BEST_EFFORT |
| RELIABLE session timeout | 2 | odom·firmware info 각각 20ms |
| 실제 `rcl_publish` 경로 | 8 | 전부 `publishMeasured` 경유 |
| runtime phase | 7 | spin·odom·IMU·diagnostics·info·sync·loop |
| runtime publish slot | 8 | info 배열과 enum 순서 일치 |
| executor spin | 1 | 실패한 ARMING은 사유 8로 해제 |
| 자동 해제 사유 | 5 | E-stop·nonfinite·nonzero·runtime·spin |
| 현장 직접 구독 | 12 | sensor-data/BEST_EFFORT 소비 유지 |
| bag override telemetry | 8 | 구독은 BEST_EFFORT라 양쪽 발행 QoS와 호환 |
| runtime info 계약 필드 | 7 | 존재·개수·배열 순서 변이 회귀 |

`tools/test_firmware_runtime_guard.py`는 publisher 증감 양쪽, 대형/소형 QoS 교환,
timeout 대상 교환·20→1000ms, publish 우회, 400ms `>=` 경계, spin 성공/실패 역전,
runtime 필드 소실·배열 교환, 현장 구독·bag topic 증감 변이를 실패시킨다.

🔴 **1.6.3 이 이 표를 바꾼다 (41-g 3판 · §79.2).** 생존 표본 `/firmware/pulse` 가
들어오면 **주기 publisher 8 → 9 · `rcl_publish` 8 → 9 · publish slot 8 → 9 · bag
override telemetry 8 → 9** 다. `runtime phase 7` 과 **자동 해제 5사유**는 불변이 계약이고,
사건 publisher 는 비주기라 이 전수에 넣지 않고 따로 센다. 굽기 전 이 표와 회귀
기대값을 **같은 커밋에서** 옮긴다 — 표만 남고 회귀가 8이면 pulse 가 계측 밖이다.

## 08-17 현장 사다리 결과

### 펌웨어·통신

시행별 원장은 `~/Desktop/개발현황/0817_현황.md`, 최종 계수 전량은
`REAL_ROBOT_VALUES §1-g` 가 소유한다. 판정에 쓰는 것은 지면 soak
`fw162_ground_soak_0817_2320` **744.697초**(비영 명령 341.7초, odom 경로 22.256m)
하나이고, 그중에서도 아래 넷뿐이다.

- `runtime_overruns=0` · `disarm_runtime=0` · `disarm_spin=0` · loop 최대 **47.165ms**.
- `publish_failures=13→83(+70)`, `/odom`·`/imu/data` 수신 최대 공백 **346.35/349.41ms**
  (33.33ms 초과 각 **7회**). `/odom` header stamp 는 엄격 단조지만 7회 210~297ms 건너뜀.
- `/scan` 은 라이다 미연결로 0건이라 독립 비교축이 없다.
- 🔴 **커널 층은 이번에도 미관측이다** (§77.2 정정). dmesg mtime 이 soak 시작보다
  **8.50초** 이르러, 이 증거는 `--trust-mtime` 없이 **rc=2** · 붙이면 **rc=1** 이다.
  어느 쪽도 "커널 무사고" 가 아니다(앞 판의 "34.0/46.5분" 은 취소). Teensy 포트
  **4회 재열거**는 굽기로 설명되지 않아 USB 재열거는 **후보로 남는다** — 상세 = 41-f.

7개 공백은 전체 loop 정지가 아니다. 결론은 **원래 수 초 안전감시 정지는 현 시험에서
재발하지 않음 · EKF 입력 단기 결측은 남음**이다.

### 외장 판재 후 예약 42 재시험 — ✅ 닫힘 (정본으로 이관)

측정 전량과 판정은 `MASTER_PLAN §7` **예약 42** 표가 소유한다(R1·R2 직진/회전·C10·
라이다 831mm/TF 832mm·watchdog 578.5ms). 차고 **19mm**·물리 좌향 **10.0%** 는
`REAL_ROBOT_VALUES §1-b·§3-a-1`. 여기에 남기는 것은 **아직 열린 두 조건**뿐이다.

- 🔴 C10 평균 **57.64mm** 는 우전륜(FR) 유격 차량의 값이다 — 예약 44 가 닫히기 전
  확정값으로 인용하지 않는다.
- 🔴 **예약 42 재개방 조건 (§76.5-③)** — LR 회전수 표기 `4/7` 을 `7 4/7` 로 해석한
  것 **위에** C10 평균이 서 있고, 그 값이 *"반지름·윤거 무변경"* 판정의 근거다.
  원장부를 다시 읽어 `7 4/7` 이 아니면 C10 과 무변경 판정을 같이 다시 세운다.
- 19mm 차고는 평탄 바닥 전제이고, 물리 좌향 10.0% 는 예약 39 를 닫지 않는다.

## 근거 문서

- `MASTER_PLAN §7` — D0-FW 원인·field 판정 **41-e·41-f**, 계측 계약 **41-g**,
  판재 재시험 **예약 42**, R3·cmd_vel **예약 43**.
- `REAL_ROBOT_VALUES §1-g` runtime 숫자 해독 · `FREEZE_MANIFEST §10.28·§10.28-a`
  승인 경계 · `FIRMWARE_REBUILD §4-c-3·§4-c-4` clean compile·보드 이력.
- 전체 시간순 역사: `~/Desktop/개발현황/0817_현황.md`·`0818_현황.md`.

## 완료조건

§79의 세 P1과 P2 2건을 받아 08-18 아침에 처리했다. **셋은 닫고 하나는 동결한다** —
41-g 구현과 예약 44는 각각 굽기와 부품을 기다리므로 닫힘을 주장하지 않는다.

| # | 예약 | 이번 묶음 결과 |
|---|---|---|
| 1 | **46 dmesg 취득 경계** | 🔶 **재현 2건 닫힘 · schema 잔여 동결**. 판정 회귀 **20건**(+취득 1건 skip). 🔴 identity 필드 전수 schema 는 4회차 상한으로 동결이며 "완전 닫힘" 이 아니다 |
| 2 | **41-g 사건별 MCU 계측** | 🔶 **계약 3판 확정**(§79.2 생존 표본 전송 경로 + §79.3 사건 보존·분류 폐포). 구현은 1.6.3 굽기가 있어야 하므로 `MASTER_PLAN §7` 예약 41-g 완료판정과 **~/Desktop/0818_12시간_작업계획.md** §4 로 뺐다 |
| 3 | **44 완만한 곡선 실효 윤거** | 🔴 **부품 대기**. 스페어 4짝 전에는 같은-run 재측정이 불가능하다. 0.829m·6.24%·3.13%p 를 선결론으로 쓰지 않는 금지만 유지 |
| 4 | 단계 표시·색인 정합 | ✅ §79.4 상단·목표·완료조건·진행표가 한 단계(§79)로, §79.6 색인 행이 41-g 3판으로 같다 |
| 5 | 기준선 유지 | ✅ `TODO(D+0)` **11건**·`§7-c-E` **13행 전량 수행** 불변 |

🔴 **예약 46의 "닫힘"은 도구가 옳게 판정한다는 뜻이지, 08-17 증거가 좋아졌다는 뜻이 아니다.**
독립 판정 전에는 `FIRMWARE_REBUILD §4`의 `.ino` 허용 지문을 이관하지 않는다.

## 완료 판정

현 상태는 **§79 독립검토 완료 · P1 3건 저장소 내 보완 완료 · P2 1건 동결 · 41-g 구현 대기**다.
`ee2f398`과 현 보드 운용은 조건부 수용하지만 1.6.2 승인 지문은 이관하지 않는다.
🔴 **41-g 3판은 계약이지 구현이 아니다** — 1.6.3이 실기에서 주입을 갈라내기 전에는
41-g 완료로 읽지 않는다. 수 초 감시-loop 정지의 재발 0은 현장 관측 범위에 한정하며,
349ms 단기 결측 때문에 R3 완료판정은 FAIL이다.
`TODO(D+0)` **11건**과 `§7-c-E` **13행 전량 수행** 계약은 그대로다.

## 완료 후 다음 단계

1. **B1 — 41-g 3판대로 1.6.3 계측을 구현**한다. 호스트 harness가 부정 회귀 ⓐ~ⓙ를
   먼저 갈라낸 뒤에 굽는다. 🔴 **계약과 구현을 묶어 한 번에 검토를 받는다.**
2. 08-18 실행 순서는 **~/Desktop/0818_12시간_작업계획.md** — 1.6.3 굽기와 R3 재시험이 그 안에 있다.
3. 41-g 계측이 실기에서 7사건을 실제로 가른 뒤에야 승인 지문 이관을 다시 논한다.
4. 별도 묶음으로 예약 45 `tunnel_interfaces`를 전달하고 §72.2·H′를 이어간다.
5. R4/R6 **판정**은 `R3 PASS`와 41-g 완료가 둘 다 선 뒤 R4, 이어 `R4 PASS` 뒤에만 연다.
   Nav2 **예행**은 아래 금지 범위의 조건부 예외다. 기존 지도는 보존한다.
6. 🔴 **예약 — `handoff_single_check.sh` 에 phase marker 대조.** §79.4 는 § 번호가 세 자리
   모두 같은데 **단계만** 달라 게이트가 초록을 냈다. 예약 23 묶음에서 같이 닫는다.

## 금지 범위

- §79 보완 구현자가 자기 diff를 승인하거나 미승인 source hash를 precheck 허용값으로 옮기지 않는다.
- 새 펌웨어 업로드·CMD_WHEEL_BASE·FF/게인·지도 재생성·URDF 수정은 하지 않는다.
- §72.2 보완과 H′를 D0-FW 독립검토 diff에 섞지 않는다.
- 08-14 torn odom 원인, 예약 36 IMU I2C, 예약 39 좌향, 예약 40 각속도 전달률,
  예약 41-g 링크 정지, 예약 43 R3, 예약 44 우전륜, 예약 46 dmesg 경계는 열린 상태다.
- `TODO(D+0)` **11건**을 추측으로 다시 열거나 닫지 않는다.
- 🔴 **R3 통과 전 R4/R6 금지 — 08-18 조건부 예외 (사용자 결정)**. 08-18 저녁
  **Nav2 실차 "예행" 은 R3 결과와 무관하게 연다.** 판정 순서는 그대로다.
  * **여는 것** = 스택 기동·TF 완결·goal 수납·직선 추종·collision 정지 관측.
  * **열지 않는 것** = **R6 판정**. 오차 ≤0.3m·정지거리·부정 회귀 실차판·H′ 종료는
    오늘 하지 않으며, 예행 결과를 R6 통과 근거로 쓰지 않는다.
  * **전제 6종** — ⓐ 라이다 연결·`/scan` 정상 ⓑ EKF·TF·readiness gate 통과
    ⓒ 물리 E-stop 담당자 상시·저속·넓은 공간 ⓓ **회전 최소 직선 goal 2~3m 부터**
    ⓔ bag 기록 ⓕ 🔴 **`nav2_params_real.yaml` 튜닝 금지**(FR 유격 차량에서 맞추면
    교체 후 다시 맞춰야 한다 — 예약 44와 같은 이유).
  * **중단 조건** — 제자리 회전만 반복(예약 40 증상) · `/odom` 공백 중 추종 이상 ·
    바퀴 이탈 조짐. ⚠ 예상 abort 지점은 R3가 아니라 **예약 40 × progress checker**다
    (`rotate_to_heading_min_angle 0.6` 초과 시 제자리 회전 → 전달률 25~32%로 90°에
    12~31초 → `movement_time_allowance 20.0` 초과 가능). **abort는 버그가 아니다.**
  * **재개방 조건** — 위 전제 중 하나라도 깨지면 예행을 닫는다. R6 **판정**은
    R3·R4 통과와 H′ 종료 뒤에만 연다.

- **⚠ 미해결 보류 — 예약 23 / 검토 §33 (`8fcc1a2`)**: P0 0 · **P1 1** · P2 1.
  `handoff_single_check.sh` 는 `§` 없는 새 작업 꼬리를 붙여도 `OK` 를 내고, 숫자 계약 전수
  증거는 기록 6자리와 독립 실행 21자리가 불일치한다. **승인 완료로 읽지 말 것.** 런타임
  `src/**` 변경·회귀는 없으므로 현재 D+0 실행은 막지 않되, 보완은 다른 묶음에 섞지 않고
  `MASTER_PLAN.md §7` 예약 23 의 완료판정과 부정 회귀로 별도 구현한다. 전문 =
  `~/Desktop/개발현황/CODEX 현황/0801검토현황.md §33`.
- **현재 미해결 보류**: 예약 36·39·40·41-g·43·**44**·**45**·**46**과 §72.2·H′는
  위 순서를 유지한다.
- 🔴 **예약 45 — `/detections` V1 계약은 닫혔는데 `tunnel_interfaces` 가 없다 (08-18 신설)**.
  schema·실패 표현·자세 판정은 확정됐고 **미제출은 역할 A 하나뿐**이다 — 패키지 커밋·
  tag·**40자 SHA** 가 역할 B 의 연결시험을 막고 있다. ⏸ **착수는 이 D0-FW 묶음 종료
  직후**, 이 묶음에 섞지 않는다. 전달 예정일은 사용자가 정한다. 상세·회신 대기 4건(T1~T4)
  = `PROJECT_CONTEXT §4.1`.
- 🔴 **예약 44 전제 (스페어 도착 전까지 매 시행에 적용)** — 우전륜(FR) 유격이 큰 상태다.
  ⓐ `ODOM_WHEEL_RADIUS`·윤거·FF/게인을 08-17 숫자로 바꾸지 않는다 ⓑ 예약 39(좌향)·
  40(각속도 전달률)을 08-17 데이터로 판정하지 않는다 ⓒ C10 **57.64mm**를 확정값으로
  인용하지 않는다 ⓓ 추가 지면 주행은 바퀴 공중 우선·물리 E-stop 담당자 상시·저속.
- 🔴 **예약 46 전제 (08-18 도구 보완 뒤에도 유지)** — `dmesg_coverage_check.py`의 rc=0만으로
  커널 무사고를 확정하지 않는다. 사건 부재와 관측 창 부재를 분리하고, 취득 경계가 없으면
  rc=2다. **`--trust-mtime`은 FAIL 방향으로만 쓰인다 — 그 플래그로 통과가 나오는 일은 없다.**
  다음 dmesg 증거부터는 `--capture`로 받아 sidecar를 bag과 같이 보관한다.
- 🔴 **예약 46 조건부 수용 — 잔여 2건 (08-18 동결, 되돌릴 수 있다)**. dmesg-coverage
  사슬 **4회차**라 `AGENTS §5` 회차 상한대로 P0만 반영한다. 둘 다 **fail-open 이 아니다.**
  * **잔여 ⓐ (§78.2 · P1)** — sidecar 의 `tool`/`version`/`log` 는 각각 소실돼도 셋을 동시
    변조해도 rc=0 이다. **전제** = sha256·시각 형상 검사는 살아 있고, identity 문자열은
    사람이 함께 위조할 수 있어 애초에 원본성 증명이 아니다.
  * **잔여 ⓑ (§79.5 · P2)** — sidecar 쓰기만 실패하면 log 만 남고 같은 이름 재시도가
    rc=2 로 끈적하게 막힌다. **전제** = 끊기면 **새 이름을 쓴다**(`JETSON_SETUP §5-d`).
  * 🔴 **재개방 조건** — ① ⓑ 가 현장에서 **1회라도** 나면 즉시 P1 로 올려 임시 경로 →
    양쪽 성공 시 승격으로 고친다 ② ⓐ 가 만든 rc=0 이 어떤 판정의 **단독 근거**로 쓰인
    사실이 나오면 즉시 P0 다 ③ 이 사슬에 새 P0 가 나오면 회차 상한과 무관하게 연다.
  * ⚠ *"이미 덮였다"* 가 아니라 *"이 전제 아래에서만 수용한다"* 이다.

## 진행표

| 트랙 | 묶음/근거 | 상태 |
|---|---|---|
| **진행 중 묶음** | §79 보완(41-g 계약 3판 + 단계 표시 정합 + 코퍼스 이관) | ✅ 구현 완료 · 🔴 독립검토 대기 |
| 1.6.2 현장 사다리 | ✅ 완료 · 구현자 주장 |
| 예약 41-g 링크 정지 | 🔴 계측 계약 **3판** 확정 · 1.6.3 굽기 전 미분류 |
| 예약 44 우전륜 유격 | 🔴 스페어 교체·동일 시행 재측정 전 상수 변경 금지 |
| 예약 46 dmesg 경계 | 🔶 도구 닫힘(rc=2 fail-closed) · 다음 증거를 `--capture`로 받기 전 무사고 증명 금지 |
| 승인 지문 이관 | 🔴 §77 P1 보완 독립 판정 전 금지 |
| 외장 판재 예약 42 | ✅ 재시험 완료 |
| R3 | 🔴 349ms 결측·scan 무표본으로 보류 |
| `TODO(D+0)` **11건** | ✅ 종결 계약 유지 |

## 최종 회귀

- `bash tools/doc_check.sh` rc=0.
- 기준선 표기: pytest **184 passed** / colcon **247 tests** /
  **test_harness_guards 24 검사** / test_gate_regression **14 케이스**.
- tools pytest **194/194 + skip 1**(커버리지 회귀 8 → §77.2 **17건** → §78.2 **20건**;
  skip 1건은 `--capture` 왕복으로, 노트북은 커널 버퍼 접근이 막혀 있고 **Jetson 에서는
  돌아야 한다**), mission pytest **184/184**.
- runtime/watchdog 묶음 **31/31**, firmware-info **61/61**(1407/1535).
- firmware precheck 픽스처 **74/74**; 실제 저장소는 승인 지문 미이관으로 rc=1이 정상.
- re-arm host **989/989 + 구조 11/11**.
- harness guards **24/24**, gate regression **14/14**.
- colcon **247/247**, `0 errors · 0 failures · 3 skipped` (`test-result --verbose`).
- 변경 Python flake8(99), `py_compile`, `git diff --check` rc=0.
- 알려진 P0/P1 코퍼스 **158/158**(§79 P1 3자리 + P0 판정 1자리 이관).

## 역할 교대

대상 `ee2f398` 구현자 = **Claude**, 독립검토 §79 = **Codex**.
이번 §79 보완 구현자도 **Claude**이고, 그 diff의 최종 판정은 구현자와 다른 검토자가 쓴다.
🔴 **41-g는 3판까지 전부 구현자 자신이 썼다** — 계약과 1.6.3 구현을 묶어 다음 회차에
한 번 더 독립 검토를 받은 뒤에야 41-g 완료를 주장한다.
이번 기록·코퍼스 이관은 firmware source·승인 지문을 바꾸지 않는다.
