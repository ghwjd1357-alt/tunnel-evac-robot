# CURRENT_HANDOFF.md — 지금 할 한 묶음

> 현재 묶음 + 미해결 보류만 유지한다. 구현자의 완료 주장은 독립검토 판정이 아니다.

- **동결 기준점**: `212885a8292d1e86677d5beeb9f5358d76fe9b40` =
  `platform-core-freeze-260724`.
- **저장소 기준**: `main`의 이 인계 커밋. 부모 `8846440` 위에 커밋
  `5eb1511`·`761cb83`의 **독립검토 §77과 코퍼스 이관**을 한 묶음으로 남긴다.
- **실제 보드**: 현장 시험 후보 `rearm-latch-pi-runtime-guard-1.6.2`,
  `build=Aug 17 2026 21:49:19`. 승인 지문은 여전히 1.6.1이라 둘을 섞지 않는다.
- **현재 단계**: D0-FW 1.6.2 현장 사다리 완료 → §76 보완 `5eb1511` + 우전륜
  사실 등록 `761cb83` → **검토 §77 완료(P0 0 · P1 3 · P2 3 · 조건부 수용)** →
  **§77 기록·코퍼스 이관 완료, 다음은 Claude 보완 구현**.
- 🔴 **§77이 세 결론을 다시 열었다.** ① dmesg 마지막 사건 시각은 스냅샷 취득
  경계가 아니다. 파일 mtime은 soak 시작보다 **8.50초**, 종료보다 **753.20초** 이르다.
  ② `loop_gap_max_us` 누적 최대 하나로 7개 동시 수신 공백의 위치를 갈라낼 수 없다.
  ③ 좌향 273mm의 근사는 타당하지만 제자리 회전용 실효 윤거 0.829m로 요구 회전수
  비 6.24%를 확정할 수 없다.

## 이번 한 묶음 목표 — §77 검토 기록과 정본 이관

구현자 **Claude**의 `5eb1511`·`761cb83`을 검토자 **Codex**가 독립 검토했다.
전문은 `~/Desktop/개발현황/CODEX 현황/0813검토현황.md` §77이다. 이번 커밋은
검토 서사와 P0/P1 코퍼스 4행을 같은 묶음으로 옮길 뿐 source·firmware를 고치지 않는다.

### §77 판정

**P0 0 · P1 3 · P2 3 · 조건부 수용.** 커밋 두 개와 현 보드 운용은 유지하지만,
🔴 **승인 지문의 1.6.1 → 1.6.2 이관은 보류**한다.

| | 독립 재현·반증 | 다음 보완의 한 문장 완료판정 |
|---|---|---|
| P1 §77.2 | 도구가 마지막 kernel *사건*을 취득 종료로 사용한다. 실제 mtime 기준 원본 조합은 시작 −8.50s·종료 −753.20s라 rc=1이지만, 조용한 사후 스냅샷도 거짓 rc=1이 된다 | 도구가 직접 남기거나 원본성이 검증된 취득 시각으로 양 끝을 판정하고, 메타데이터 불명은 rc=2로 닫는다 |
| P1 §77.3 | `/odom`·`/imu/data` 7사건은 2.2~18.9ms 안에서 동시지만, 누적 `loop_gap_max_us`는 loop 내부·판 사이·host/agent 정지를 사건별로 대응시키지 못한다 | 각 수신 공백과 MCU 사건 시각이 대응되고 loop gap·phase·publish 결과로 세 원인군이 갈린다 |
| P1 §77.4 | 3.11%는 맞고 `θ≈2y/s` 오차도 0.33%뿐이다. 그러나 6.24%는 완만한 곡선에 무효인 제자리 회전 실효 윤거 0.829m를 썼다 | 바퀴 교체 뒤 같은 시행의 회전수·독립 yaw/궤적으로 완만한 곡선의 실효 윤거와 잔차를 함께 산출한다 |
| P2 §77.5 | 포트 `1-2.1`은 초기 열거 1회 + 재열거 **4회**이며 6회가 아니다 | 정본 계수를 고치고 새 로그의 포트 사건을 기계 계수한다 |
| P2 §77.5 | 경계 회귀가 exact라면서 종료 +1초를 쓰고, docstring의 read failure rc=1은 구현 rc=2와 다르다 | 양 끝 동치·±1과 rc 계약을 구현·문서가 같은 값으로 고정한다 |
| P2 §77.5 | 예약 44가 진행표에서 빠졌다 | 예약 41-g·44·46을 활성 진행표와 금지 범위에 함께 둔다 |

§77 직접 재산출에서 7개 `/odom`·`/imu/data` 수신 공백의 시각차는
**14.4, 18.9, 2.2, 2.3, 2.5, 2.3, 2.4ms**였고, header stamp 기준 추정 유실은
각각 **72 대 2**다. 코퍼스 이관은 §77 P1 3행 + P0 판정 1행, 합계 **150행**이다.

### 왜 1.6.2가 생겼나

1.6.1은 08-17의 6.85초 감시-loop 정지를 막기 위해 주기 telemetry 8개를 전부
BEST_EFFORT로 바꿨다. 그러나 첫 비무장 bench에서 publisher 엔티티는 존재하는데
`/odom`·`/firmware/info`가 7초 동안 0건이었다. 설치
`micro_ros_arduino 2.0.8-humble`의 custom-transport MTU는 512B이고 두 표본은 이를
넘는다. BEST_EFFORT XRCE stream으로 큰 표본을 보낼 수 없다는 새 근거가 §75의 1.6.1
승인 범위를 반증했다.

1.6.2는 큰 두 publisher만 RELIABLE로 복구하고 각각 ACK 대기 상한을 **20ms**로 제한한다.
나머지 6개 telemetry, 400ms runtime guard, re-arm·E-stop·PWM 배선은 그대로다.

## 1.6.2 구현자가 주장한 완료판정(§77에서 범위 축소)

> 512B보다 큰 `/odom`·`/firmware/info`는 RELIABLE+20ms라 실제 수신되고, 나머지 6개는
> BEST_EFFORT다. 744.697초 지면 soak에서 한 loop의 감시 기능이 수 초 멈추는 결함은
> 재발하지 않았고 runtime overrun·자동 해제도 0이다. 단, RELIABLE odom publish 실패로
> 최대 349ms 수신 공백이 남았으므로 R3 주기 품질까지 해결됐다고 주장하지 않는다.

이 문장은 **구현자 주장**이다. §77은 수 초 정지 재발 0과 7사건의 원인을 사건별로
연결할 계측이 없음을 확인했으므로, 1.6.2 승인 지문 이관 근거로 쓰지 않는다.

## §3-10 전수 열거와 구현 대조

생산 소스와 소비자를 기계 검색해 다음 클래스를 고정했다.

| 클래스 | 전수 | 1.6.2 대조 |
|---|---:|---|
| 주기 publisher 초기화 | 8 | 큰 2 RELIABLE · 작은 6 BEST_EFFORT |
| RELIABLE session timeout | 2 | odom·firmware info 각각 20ms |
| 실제 `rcl_publish` 경로 | 8 | 전부 `publishMeasured` 경유 |
| runtime phase | 7 | spin·odom·IMU·diagnostics·info·sync·loop |
| runtime publish slot | 8 | info 배열과 enum 순서 일치 |
| executor spin | 1 | 실패한 ARMING은 사유 8로 해제 |
| 자동 해제 사유 | 5 | E-stop·nonfinite·nonzero·runtime·spin |
| 현장 직접 구독 | 12 | sensor-data/BEST_EFFORT 소비 유지 |
| bag override telemetry | 8 | 구독은 BEST_EFFORT라 양쪽 발행 QoS와 호환 |
| runtime info 계약 필드 | 7 | 존재·개수·배열 순서 변이 회귀 |

`tools/test_firmware_runtime_guard.py`는 publisher 증감 양쪽, 대형/소형 QoS 교환,
timeout 대상 교환·20→1000ms, publish 우회, 400ms `>=` 경계, spin 성공/실패 역전,
runtime 필드 소실·배열 교환, 현장 구독·bag topic 증감 변이를 실패시킨다.

## 08-17 현장 사다리 결과

### 펌웨어·통신

| 시행 | 결과 |
|---|---|
| 비무장 bench | `/odom`·전체 `/firmware/info` 수신, version/build 확인 |
| 5초 공중 저속 | 네 바퀴 방향·동작 이상 없음 |
| 비무장 QoS bag `fw162_qos_0817_2220` | 59.4초, odom/IMU 최대 22.52/22.29ms, stamp 엄격 단조 |
| 공중 부하 `fw162_air_0817_2230` | 409.4초, 최대 24.03/23.03ms, runtime 해제 0 |
| E-stop | `/estop/state` 개폐 **10/10** |
| 지면 soak `fw162_ground_soak_0817_2320` | 744.697초, 비영 명령 341.7초, odom 경로 22.256m |

지면 soak 최종 계수:

- `runtime_overruns=0`, `runtime_last=0,0`, `disarm_runtime=0`, `disarm_spin=0`.
- `phase_max_us=4487,41997,4255,20,1304,798,47165`.
- `publish_max_us=41993,23,7,6,5,5,6,1212`.
- `publish_failures=13→83(+70)`.
- E-stop 계수 `raw_edges=39→67`, `rejected=5→19`, rejected max **12ms**,
  `disarm_estop=3→3`. 30ms 미만 HIGH 펌스 14건은 전부 걸러졌지만 짧은 잡음은 남아 있다.
- `/odom`·`/imu/data` 수신 최대 공백 **346.35/349.41ms**, 33.33ms 초과 각 7회.
- `/odom` header stamp는 엄격 단조지만 7회 **210~297ms** 건너뜀.
- 🔴 **커널 층은 이번에도 미관측이다** (§77.2 정정). `dmesg_fw162_ground_soak_0817_2320.log`의
  마지막 사건은 **22:44:41**이지만 이것은 취득 경계가 아니다. 파일 mtime
  **23:18:34.889613**은 soak 시작 **23:18:43.389769**보다 8.50초, 종료
  **23:31:08.086613**보다 753.20초 이르다. 🔴 **08-18 보완 뒤 이 증거의 판정은
  `--trust-mtime` 없이는 rc=2(판정 불능), 붙이면 rc=1(끝을 12.6분 놓쳤다)이다** —
  어느 쪽도 "커널 무사고"가 아니므로 결론(미관측)은 그대로다. 앞 판의 "34.0/46.5분"과
  "마지막 사건이 끝 경계"는 취소한다(예약 46 도구 측 닫힘). 게다가 로그의
  Teensy 포트(`1-2.1`·`ttyACM0`)는 초기 열거 1회 뒤 **4회 재열거**됐고 그중
  `22:15:06→22:17:11`은 **2분 5초 단절**이라 굽기로 설명되지 않는다. USB 재열거는
  기각된 것이 아니라 **후보로 남는다**.
- `/scan`은 라이다 미연결로 0건이므로 독립 비교축은 없음.

7개 공백은 전체 loop 정지가 아니다. 각 공백 직후 odom stamp가 건너뛰고
`publish_failures`가 증가했지만 loop 최대는 47.165ms였다. 결론은 **원래 수 초 안전감시
정지는 현 시험에서 재발하지 않음 · EKF 입력 단기 결측은 남음**이다.

### 외장 판재 후 예약 42 재시험

- 축 높이 LF/LR/RF/RR `43.5/43.5/44.0/43.5mm`, 최저 차고 **19mm**.
- 실제 라이다 스캔 평면 **831mm**, 바닥 기준 TF 높이 **832mm**.
  URDF `lidar_joint z=0.779`는 유지했고 URDF를 수정하지 않았다.
- R1 `panel_r1_fw162_0817`: 경로 122.9mm, 횡 0.8mm(0.67%), yaw 0.60°.
- R2 직진 `panel_r2_line_fw162_0817_retry1`: 줄자 2735mm, odom 2751.1mm,
  `odom/줄자=1.006` 통과. 물리 좌향 273mm(**10.0%**)는 예약 39로 유지.
- C10 평균 57.64mm(08-14의 57.31mm 대비 +0.6%)라 반지름·윤거·FF/게인 무변경.
  🔴 **이 값은 우전륜(FR) 유격이 있는 차량에서 나왔다 — 예약 44.** 스페어 4짝 교체·
  재시행 전까지 확정값으로 인용하지 않는다.
  🔴 LR 회전수 표기 `4/7`은 문맥상 `7 4/7`로 해석했다 — **이 해석 위에 C10 평균이
  서 있고 그 값이 "반지름·윤거 무변경" 판정의 근거**이므로, 검토 §76.5-③에 따라
  **예약 42의 재개방 조건**으로 올린다: 원장부를 다시 읽어 `7 4/7`이 아니면 C10과
  무변경 판정을 같이 다시 세운다.
- R2 회전 retry: IMU yaw 391.60°, gyro 적분 391.54°, odom 389.73°,
  odom/IMU=0.995 통과. 육안 324°/396° 중 IMU는 396°를 지지한다.
- watchdog 총 정지 578.5ms로 현행 운용 수용선 ≤600ms 통과.

예약 42의 하중 재시험은 닫았지만, 19mm 차고는 평탄 바닥 전제이고 물리 좌향 10.0%는
예약 39를 닫지 않는다.

## 근거 문서

- D0-FW 원인·field 판정: `MASTER_PLAN §7` 41-e·41-f.
- runtime 숫자 해독: `REAL_ROBOT_VALUES §1-g`.
- 승인/미승인 경계: `FREEZE_MANIFEST §10.28·§10.28-a`.
- clean compile·보드 이력: `FIRMWARE_REBUILD §4-c-3·§4-c-4`.
- 판재 재시험: `MASTER_PLAN §7` 예약 42.
- R3·cmd_vel 기록: 같은 파일 예약 43.
- 전체 시간순 역사: `~/Desktop/개발현황/0817_현황.md`.

## 완료조건

§77의 세 P1을 받아 08-18 새벽에 보완했다. **셋 중 하나만 저장소 안에서 닫힌다** —
나머지 둘은 각각 굽기와 부품을 기다리므로 닫힘을 주장하지 않고 착수 조건만 고정한다.

| # | 예약 | 이번 묶음 결과 |
|---|---|---|
| 1 | **46 dmesg 취득 경계** | ✅ **도구 측 닫힘**. `--capture`가 취득 시작·종료+sha256을 sidecar에 남기고, 판정은 ⓐ~ⓔ + 정수초 exact·±1초 양끝 + §78.2 재현 2건(구간 역전·NaN)·덮어쓰기 거부를 **20건**(+취득 1건 skip)으로 고정한다 |
| 2 | **41-g 사건별 MCU 계측** | 🔶 **계측 계약만 확정**. 구현은 1.6.3 굽기가 있어야 하므로 `MASTER_PLAN §7` 예약 41-g의 완료판정에 넣고 **~/Desktop/0818_12시간_작업계획.md** §4에 시간표로 뺐다 |
| 3 | **44 완만한 곡선 실효 윤거** | 🔴 **부품 대기**. 스페어 4짝 도착 전에는 같은-run 재측정 자체가 불가능하다. 0.829m·6.24%·3.13%p를 선결론으로 넣지 않는 금지만 유지 |
| 4 | 포트 계수·진행표 정합 | ✅ 재열거 **4회**와 41-g·44·46이 정본 전부에서 같다 |
| 5 | 기준선 유지 | ✅ `TODO(D+0)` **11건**·`§7-c-E` **13행 전량 수행** 불변 |

🔴 **예약 46의 "닫힘"은 도구가 옳게 판정한다는 뜻이지, 08-17 증거가 좋아졌다는 뜻이 아니다.**
같은 증거는 이제 `--trust-mtime` 없이 rc=2, 붙이면 rc=1이다. 커널 층은 여전히 미관측이다.

위 보완의 독립 판정 전에는 `FIRMWARE_REBUILD §4`의 `.ino` 허용 지문을 이관하지 않는다.

## 완료 판정

현 상태는 **§77 독립검토 완료 · P1 3건 중 1건 저장소 내 보완 완료 · 2건 착수조건 고정**이다.
두 대상 커밋과 현 보드 운용은 조건부 수용하지만 1.6.2 승인 지문은 이관하지 않는다.
수 초 감시-loop 정지의 재발 0은 현장 관측 범위에 한정하며, 349ms 단기 결측 때문에
R3 완료판정은 FAIL이다. `TODO(D+0)` **11건**과 `§7-c-E` **13행 전량 수행** 계약은 그대로다.

## 완료 후 다음 단계

1. 이 묶음(예약 46 도구 + 41-g 계약 + 08-18 계획)을 **구현자가 아닌 쪽**이 독립 검토한다.
2. 08-18 실행 순서는 **~/Desktop/0818_12시간_작업계획.md** — 1.6.3 굽기와 R3 재시험이 그 안에 있다.
3. 41-g 계측이 실기에서 7사건을 실제로 가른 뒤에야 승인 지문 이관을 다시 논한다.
4. 별도 묶음으로 예약 45 `tunnel_interfaces`를 전달하고 §72.2·H′를 이어간다.
5. R3 통과 전 R4/R6로 가지 않는다. 기존 지도는 보존한다.

## 금지 범위

- §77 보완 구현자가 자기 diff를 승인하거나 미승인 source hash를 precheck 허용값으로 옮기지 않는다.
- 새 펌웨어 업로드·CMD_WHEEL_BASE·FF/게인·지도 재생성·URDF 수정은 하지 않는다.
- §72.2 보완과 H′를 D0-FW 독립검토 diff에 섞지 않는다.
- 08-14 torn odom 원인, 예약 36 IMU I2C, 예약 39 좌향, 예약 40 각속도 전달률,
  예약 41-g 링크 정지, 예약 43 R3, 예약 44 우전륜, 예약 46 dmesg 경계는 열린 상태다.
- `TODO(D+0)` **11건**을 추측으로 다시 열거나 닫지 않는다.
- 🔴 **R3 통과 전 R4/R6 금지 — 08-18 조건부 예외 (사용자 결정)**. 08-18 저녁
  **Nav2 실차 "예행" 은 R3 결과와 무관하게 연다.** 판정 순서는 그대로다.
  * **여는 것** = 스택 기동·TF 완결·goal 수납·직선 추종·collision 정지 관측.
  * **열지 않는 것** = **R6 판정**. 오차 ≤0.3m·정지거리·부정 회귀 실차판·H′ 종료는
    오늘 하지 않으며, 예행 결과를 R6 통과 근거로 쓰지 않는다.
  * **전제 6종** — ⓐ 라이다 연결·`/scan` 정상 ⓑ EKF·TF·readiness gate 통과
    ⓒ 물리 E-stop 담당자 상시·저속·넓은 공간 ⓓ **회전 최소 직선 goal 2~3m 부터**
    ⓔ bag 기록 ⓕ 🔴 **`nav2_params_real.yaml` 튜닝 금지**(FR 유격 차량에서 맞추면
    교체 후 다시 맞춰야 한다 — 예약 44와 같은 이유).
  * **중단 조건** — 제자리 회전만 반복(예약 40 증상) · `/odom` 공백 중 추종 이상 ·
    바퀴 이탈 조짐. ⚠ 예상 abort 지점은 R3가 아니라 **예약 40 × progress checker**다
    (`rotate_to_heading_min_angle 0.6` 초과 시 제자리 회전 → 전달률 25~32%로 90°에
    12~31초 → `movement_time_allowance 20.0` 초과 가능). **abort는 버그가 아니다.**
  * **재개방 조건** — 위 전제 중 하나라도 깨지면 예행을 닫는다. R6 **판정**은
    R3·R4 통과와 H′ 종료 뒤에만 연다.

- **⚠ 미해결 보류 — 예약 23 / 검토 §33 (`8fcc1a2`)**: P0 0 · **P1 1** · P2 1.
  `handoff_single_check.sh` 는 `§` 없는 새 작업 꼬리를 붙여도 `OK` 를 내고, 숫자 계약 전수
  증거는 기록 6자리와 독립 실행 21자리가 불일치한다. **승인 완료로 읽지 말 것.** 런타임
  `src/**` 변경·회귀는 없으므로 현재 D+0 실행은 막지 않되, 보완은 다른 묶음에 섞지 않고
  `MASTER_PLAN.md §7` 예약 23 의 완료판정과 부정 회귀로 별도 구현한다. 전문 =
  `~/Desktop/개발현황/CODEX 현황/0801검토현황.md §33`.
- **현재 미해결 보류**: 예약 36·39·40·41-g·43·**44**·**45**·**46**과 §72.2·H′는
  위 순서를 유지한다.
- 🔴 **예약 45 — `/detections` V1 계약은 닫혔는데 `tunnel_interfaces` 가 없다 (08-18 신설)**.
  08-17 재합의 4왕복으로 schema·실패 표현·자세 판정까지 확정됐고(`PROJECT_CONTEXT §4.1`),
  **미제출은 역할 A 하나뿐이다** — 패키지 커밋·tag·**40자 SHA** 가 역할 B의 깡통 퍼블리셔
  연결시험을 막고 있다. ⏸ **착수는 이 D0-FW 묶음 종료 직후** — 이 묶음에 섞지 않는다.
  🔴 전달 예정일은 사용자가 정한다. 역할 B 회신 대기 4건(T1~T4)은 `PROJECT_CONTEXT §4.1`.
- 🔴 **예약 44 전제 (스페어 도착 전까지 매 시행에 적용)** — 우전륜(FR) 유격이 큰 상태다.
  ⓐ `ODOM_WHEEL_RADIUS`·윤거·FF/게인을 08-17 숫자로 바꾸지 않는다 ⓑ 예약 39(좌향)·
  40(각속도 전달률)을 08-17 데이터로 판정하지 않는다 ⓒ C10 **57.64mm**를 확정값으로
  인용하지 않는다 ⓓ 추가 지면 주행은 바퀴 공중 우선·물리 E-stop 담당자 상시·저속.
- 🔴 **예약 46 전제 (08-18 도구 보완 뒤에도 유지)** — `dmesg_coverage_check.py`의 rc=0만으로
  커널 무사고를 확정하지 않는다. 사건 부재와 관측 창 부재를 분리하고, 취득 경계가 없으면
  rc=2다. **`--trust-mtime`은 FAIL 방향으로만 쓰인다 — 그 플래그로 통과가 나오는 일은 없다.**
  다음 dmesg 증거부터는 `--capture`로 받아 sidecar를 bag과 같이 보관한다.

## 진행표

| 트랙 | 묶음/근거 | 상태 |
|---|---|---|
| **진행 중 묶음** | §77 보완(예약 46 도구 + 41-g 계약 + 08-18 작업계획) | ✅ 구현 완료 · 🔴 독립검토 대기 |
| 1.6.2 현장 사다리 | ✅ 완료 · 구현자 주장 |
| 예약 41-g 링크 정지 | 🔴 계측 계약 확정 · 1.6.3 굽기 전 미분류 |
| 예약 44 우전륜 유격 | 🔴 스페어 교체·동일 시행 재측정 전 상수 변경 금지 |
| 예약 46 dmesg 경계 | 🔶 도구 닫힘(rc=2 fail-closed) · 다음 증거를 `--capture`로 받기 전 무사고 증명 금지 |
| 승인 지문 이관 | 🔴 §77 P1 보완 독립 판정 전 금지 |
| 외장 판재 예약 42 | ✅ 재시험 완료 |
| R3 | 🔴 349ms 결측·scan 무표본으로 보류 |
| `TODO(D+0)` **11건** | ✅ 종결 계약 유지 |

## 최종 회귀

- `bash tools/doc_check.sh` rc=0.
- 기준선 표기: pytest **184 passed** / colcon **247 tests** /
  **test_harness_guards 24 검사** / test_gate_regression **14 케이스**.
- tools pytest **194/194 + skip 1**(커버리지 회귀 8 → §77.2 **17건** → §78.2 **20건**;
  skip 1건은 `--capture` 왕복으로, 노트북은 커널 버퍼 접근이 막혀 있고 **Jetson 에서는
  돌아야 한다**), mission pytest **184/184**.
- runtime/watchdog 묶음 **31/31**, firmware-info **61/61**(1407/1535).
- firmware precheck 픽스처 **74/74**; 실제 저장소는 승인 지문 미이관으로 rc=1이 정상.
- re-arm host **989/989 + 구조 11/11**.
- harness guards **24/24**, gate regression **14/14**.
- colcon **247/247**, `0 errors · 0 failures · 3 skipped` (`test-result --verbose`).
- 변경 Python flake8(99), `py_compile`, `git diff --check` rc=0.
- 알려진 P0/P1 코퍼스 **154/154**(§78 P1 3자리 + P0 판정 1자리 이관).

## 역할 교대

대상 `5eb1511`·`761cb83` 구현자 = **Claude**, 독립검토 §77 = **Codex**.
다음 §77 보완 구현자는 **Claude**이고, 그 diff의 최종 판정은 구현자와 다른 검토자가 쓴다.
이번 기록·코퍼스 이관은 firmware source·승인 지문을 바꾸지 않는다.
