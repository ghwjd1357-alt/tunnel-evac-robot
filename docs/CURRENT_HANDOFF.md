# CURRENT_HANDOFF.md — 지금 할 한 묶음 (매 세션 시작점)

> 작업 완료 시 구현자가 갱신하고 검토자가 사실관계·테스트 결과를 확인한다.
> **현재 묶음 + 미해결 보류만** 유지한다 (`AGENTS.md §5`) — 완료된 서사는 정본으로 보내고 링크한다.

- **동결 기준점**: `212885a8292d1e86677d5beeb9f5358d76fe9b40` = tag
  **`platform-core-freeze-260724`**. 증거 = `docs/FREEZE_MANIFEST.md`.
- **현재 기준 커밋**: 이 파일과 같은 커밋(main).
- **현재 단계**: 마스터플랜 5단 완료 → 역할 B V1 합의(6단)는 회의 대기 보류
  → 실차 D+0 인수·연결 **검사 1~8 전항목 OK**(08-07) · 잔여 = R0 watchdog `#11` 하나
  → 전기계통 정본 v2~v3.3 = 실물 시공 완주(`7884b40`) · 예약 25 재종결(`04694e1`)
  → firmware precheck 사슬 §46~§49 → **§50 보완(`07a18ea`)으로 종료** · 🔴 **Codex 검토 대기**
  → 검토 서사 4분할(`fb2580c`)
  → **규율 경량화 5종 = `AGENTS.md` §6 신설** **(지금 — 2026-08-07)**

> 🔴 **왜 이 묶음인가**: 사용자가 *"개발 플로우가 너무 무겁고 따지는 것도 많다 · 오늘 팀원들과
> 시작하려 했는데 검토에 잡혀 아무것도 못 했다"* 고 판정했다. 실측이 그 판정을 뒷받침한다 —
> **최근 20커밋 중 `src/` 를 연 것 0개**, `AGENTS.md` 18일에 3배, `docs/` 39KB→638KB(16배),
> firmware precheck 한 결함이 **4회차**(§47→§50). 규율이 지키는 대상이 대부분 규율 자신이
> 만든 표면이 됐다. **걷어내지 않고 위험에 비례시킨다.**

**⚠ 미해결 보류 — 어느 것도 승인 완료로 읽지 말 것** (본문 정본 = `MASTER_PLAN.md §7`):

- **⚠ 미해결 보류 — 예약 23 / 검토 §33 (`8fcc1a2`)**: P0 0 · **P1 1** · P2 1.
  `handoff_single_check.sh` 는 `§` 없는 새 작업 꼬리를 붙여도 `OK` 를 내고, 숫자 계약 전수
  증거는 기록 6자리와 독립 실행 21자리가 불일치한다. **승인 완료로 읽지 말 것.** 런타임
  `src/**` 변경·회귀는 없으므로 현재 D+0 실행은 막지 않되, 보완은 다른 묶음에 섞지 않고
  `MASTER_PLAN.md §7` 예약 23 의 완료판정과 부정 회귀로 별도 구현한다. 전문 =
  `~/Desktop/개발현황/CODEX 현황/0801검토현황.md §33`.
- **예약 24 / §38** P1 2 · P2 4 — 라이다 실측 소유 시점과 readiness flake 소유 정본이 갈라짐.
  **라이다 높이는 D+1 소유**이며 `TODO(D+0)` 목록 계약은 그대로다.
- **예약 26 / §40** — 사용자 결정 **'방법 B'** 미구현(결정은 `MASTER_PLAN §8` 에 복원).
- **예약 27** — 이 Jetson 에 `nvcc`·`nvidia-jetpack` 이 **없다.** 역할 A 는 비차단이지만
  **역할 B 성능 전제는 통째로 바뀐다.**
- **예약 28** — §44·§45 전기 P1 **4건**, 등록만 됐다. **④ 는 P0 격하 되돌림이라 사용자 결정 선행.**
- **예약 29** (08-07 신설) — 정본의 활성 계약과 이력을 절로 분리. 라우팅 패킷의 **절반**이
  `FREEZE_MANIFEST §10`(30%)+`MASTER_PLAN §8`(22%)다. ⏸ 실차 사다리 뒤.
- **⏸ 6단 보류** — 역할 B 회의가 잡히면 `30c5e87`의 핸드오프 원문을 그대로 복원한다.

## 이번 한 묶음 목표 — 규율 경량화 **5종** (규칙 정본 = `AGENTS.md §6`)

| # | 항목 | 자리 |
|---|---|---|
| ① | **위험 등급 티어링** — Tier A(`src/**`·`firmware/**`·전기·지도)=§3~§5 전부 / Tier B(`tools/**`·`docs/**`·`console/**`)=재현→회귀→커밋, 검토 왕복 없음 | `AGENTS.md §6` |
| ② | **cold-start 판정을 비율→절대 상한** ≤42,000 bytes 이고 ≤13,500 o200k 토큰 | `test_ai_context.py` `COLD_START_*` |
| ③ | **검토 회차 계수기** — 같은 사슬 3회차부터 P0 만 반영·동결. `Review-round:` 트레일러 | `AGENTS.md §6` |
| ④ | **규칙 은퇴 절차** — `Caught-by:` 트레일러, 분기마다 미발화 규칙을 후보로 | `AGENTS.md §6` |
| ⑤ | **라우팅 절 단위화** — 통째 로딩 4→2 (`FREEZE_MANIFEST`·`REAL_ROBOT_VALUES`) | `ai_context.py` `PROFILE_REFS` |

🔴 **②는 완화다**(`ef25ad3` 대비 30%→약 25%). 사용자 결정이며 색인 = `MASTER_PLAN §8`.
🔴 **⑤ 실측 절감은 6%(bringup)·4%(mission)** — 구현자 사전 추정 **−36% 는 틀렸다**(앵커 12건이
`FREEZE_MANIFEST` 에 살아 통째로 못 뺀다). 그 측정이 예약 29 를 낳았다.

## 완료조건

1. Tier A/B 경계와 **"면제되는 것은 검토 왕복이지 회귀가 아니다"** 가 `AGENTS.md §6` 에 있다.
2. cold-start 가 **절대 상한**으로 판정되고, 비율·`ef25ad3` 기준이 판정에서 사라진다.
   상한을 지탱하는 규약(핸드오프는 현재 묶음+보류만)이 `AGENTS.md §5` 에 있다.
3. `Review-round:` · `Caught-by:` 두 계수기가 규칙과 함께 정의된다.
4. 통째 로딩이 4→2 로 줄고 **`test_02` 앵커 회수 85/85 가 그대로**다.
   🔴 `test_06`·`test_07` 이 **표기가 아니라 실질**(동결 예외의 존재 이유 문장 · `실효 0.62`)을 본다.
5. 회귀 정본 불변: pytest **182 passed** / colcon **245 tests** / harness guards **24 검사** /
   gate regression **14 케이스**.
   ⚠ `src/**` 무접촉이라 직접 실행 대상은 `doc_check`·pytest·`test_ai_context`·펌웨어
   픽스처뿐 — 나머지는 **재실행했다고 쓰지 않는다.** (픽스처 수치 정본 = `FIRMWARE_REBUILD §4`)
6. 🔴 **예약 23 을 구현했다고 쓰지 않는다. §50 을 닫힌 것으로 인용하지 않는다.**

## ★ 현장 운용 — 시공은 끝났고 **절차가 바뀌었다** (상세는 링크가 정본)

> 시공안 `§5-A`~`§5-G8` **완주**(08-06~07). `H2` 사진 · `H3` 여분 보관만 잔여.
> 물리 E-stop 은 **있고, 무부하·`0.05 m/s` 부하에서 실측으로 동작한다**(끼임 11A 는 미관측).

1. 🔴 **기동 순서를 어기면 Teensy 가 영구히 멈춘다** — `퓨즈 → XT90 → Jetson 부팅 대기 →
   agent → **Teensy VIN 만 재삽입**`. 재연결 로직이 없어 `errorLoop()` 에 갇힌다
   (`JETSON_SETUP.md §5-e`). 증상 = 토픽이 `rosout`·`parameter_events` 뿐.
2. 🔴 **종료는 `Jetson sudo shutdown -h now` 가 맨 앞** — 로직이 배터리로 도는 지금
   XT90 분리는 **리눅스 전원 뽑기**다 (`ELECTRICAL_BASELINE.md §7`).
3. 🔴 **배선을 만질 땐 XT90 분리다 — E-stop 으로 대신하지 않는다.** E-stop 은 **모터 가지만**
   끊는다. 유일한 예외 = 트레이스 절단 후의 펌웨어 업로드 (`FIRMWARE_REBUILD.md §5-b`).
4. 🔴 **되돌리면 안 되는 소스 3줄** — `.ino:111 = false`(되돌리면 `§2`-⑧ 재개방) ·
   `.ino:125-126 = 0.12 / 0.50`(릴레이 정격 수용의 전제). 🔴 **수령 원본 해시 대조는 이제
   `실패` 가 정상**이고 오염 판정은 **`bash tools/firmware_precheck.sh` 뿐**이다 — 굽기 전에
   돌린다. 🔴 **`rc=2` 는 판정 불능이지 통과가 아니다** (`FIRMWARE_REBUILD.md §4`).
5. 🔴 **브레드보드 상태다** — PWM/DIR 이 빠지면 "안 도는" 고장이 아니라 **"돌 수도 있는"**
   고장이다(가동 전 점검 = 시공기록지). 🔴 **`/firmware/info` 로 버전을 판별하지 않는다** —
   `version`·`source`·`git_sha` 가 v1_3 시절 고정 문자열이고 쓸 수 있는 건 `build` 뿐.
6. 🔴 **시공이 끝났다고 `TODO(D+0)` 11건이 닫힌 게 아니다** — 닫힌 것은 `#10` 하나고
   **`#11` R0 watchdog 은 미확인**이다. 다음 현장 세션 첫 명령 = `JETSON_SETUP.md §11-g`.

## 사용자 결정 — 유지

- 동결 예외 4회는 전부 소진됐다. `src/**`를 다시 열려면 사용자 승인이 필요하다
  (`FREEZE_MANIFEST.md §10`).
- 순서는 전기계통 시공 → E-stop 검증 → D+0 잔여 종결 → D+1 R3 rosbag → R4~R8이다
  (`MASTER_PLAN.md §3`).
- 예약 20·21·22·23·24·26·27·28·**29** 와 역할 B 6단은 정해진 트리거 전까지 별건으로 유지한다.
- `JETSON_SETUP.md §9` `TODO(D+0)` **11건** 중 ✅ **#10 물리 E-stop 이 08-07 에 닫혔다**.
  미해결은 **#11 R0 watchdog 실측 하나**다. 목록 계약은 바꾸지 않는다.
- ✅ **08-07 재실행 = 검사 1~8 전항목 OK**(전량 통과는 처음). ⚠ 단서는 그대로다 —
  **"여기 통과는 주기의 상한을 증명하지 않는다"**(상한 정본 = R3 rosbag 히스토그램).
- never-seen은 다른 놓침과 같이 역행 2회 → 보고 → 단독 탈출이다. 실차 R3 뒤 재평가한다.

| 트랙 | 범위 | 상태 |
|---|---|---|
| **진행 중 묶음** | §6 규율 경량화 5종 (`tools/` 2 + 문서) | ✅ **구현 완료 주장 · Tier B 라 검토 왕복 없음** |
| 🔴 firmware precheck §50 | `07a18ea` = 지문을 내용에 묶음 | 🔴 **Codex 독립 검토 대기** (구현자 = Claude) |
| 전기계통 시공 | `전기계통_최종작업안_0806.md §5` (사용자 직접) | ✅ **A~G8 완주** · 🔴 `H2` 사진 · `H3` 여분 보관 잔여 |
| 🔴 릴레이 | 예비 ✅(5개 `JD2914`) / 🔴 **§45.2 대조본이 다른 모델(`JD2912`)** | 🔴 **P0 격하 되돌림 = 사용자 결정. 예약 28-④** |
| 배터리화 후속 (`§8`-⑰⑱⑲) | XT90 무부하 전류 · 로직 국소 보호 · Jetson 급전 안정성 | **08-07 실측** — ✅ ⑱ 재평가 완료(12V=5A·5V=3A, 🔴 미설치) · 🔴 ⑰ **측정점 부재로 이월**(쿨롱 카운팅으로 대체) · 🟡 ⑲ **공중 부하 한정**(닫힘 아님) |
| 🔴 검토 슬롯 | §44~§50 수행됨 / `c397975..6af8997`(전기 v1→v2) 미검토 | 🔴 **§44·§45 불승인 P1 4 = 예약 28** |
| 실차 D+0 · AI 입력 | ✅ 검사 1~8 통과(08-07) / 라우팅 절 단위화 완료 | 🔴 `#11` **관측만 · 미확인**(519.9/532.0/537.1ms · 기준 결함) · ✅ 증거 복제 완료 · 방법 B = 예약 26 |
| 예약 21 · 24 | D0/R3 회귀 편입 + readiness 간헐성 / §38 정본 소유권 | ⏸ 다음 bringup 변경 전 |
| 예약 23 · 26 · 27 · 29 | 숫자 계약 폐포 / 방법 B / 역할 B CUDA / 정본 활성-이력 분리 | ❌ 각자 트리거 전 별건 |
| 역할 B V1 합의 | 커밋 `30c5e87` 원문 | ⏸ 회의 대기 |

## 직전 완료 — §50 보완(`07a18ea`) + 검토 서사 분할(`fb2580c`)

`07a18ea` = 펌웨어 사전검사의 지문을 git 렌더링에서 **파일 내용 sha256** 으로 옮겼다(픽스처
59→73 검사, 구판 판정 뒤집힘 0). 🔴 **Codex 검토 전이라 닫힌 것으로 인용하지 않는다.**
`fb2580c` = 331KB 검토문을 절 경계에서 4장으로 나눴다(본문 바이트 동일 검증).
상세 = `FREEZE_MANIFEST §10.19` · `FIRMWARE_REBUILD §4` · `PROJECT_CONTEXT §8-a`.

## 보존해야 할 안전 불변조건

- ✅ **`/cmd_vel` 금지 불변조건은 2026-08-07 검사 8 통과로 해제됐다.** `Ctrl+C` 를 E-stop 으로
  승격하지 않는다는 규칙은 그대로다. 🔴 **R1·R2 는 여전히 금지** — 사유는 E-stop 이 아니라
  **re-arm 래치 미구현**이다. 🔴 **속도는 `0.12` 상한 안에서만 쓴다.**
- ✅ **`/estop/state` 는 2026-08-07 부터 신뢰한다**(`§5-G6` 10/10) — 이 토픽을 소비하는 로직을
  만들어도 된다. 🔴 **배선을 만지거나 펌웨어를 다시 구우면 10회를 다시 하기 전까지 신뢰를
  회수한다.** ⚠ 관찰된 것은 무부하(`§5-E`)와 **`0.05 m/s` 부하(`§5-G8`)** 이고
  **끼임(약 11A) 차단은 미관측이다.**
- 🔴 **배선 작업 전 XT90 분리 + 퓨즈 제거** (메인 스위치가 없다), 배선을 만졌으면
  **개폐 10회 재검증**. 🔴 **XT90 분리 앞에 `Jetson shutdown` 이 온다.**
- 🔴 **펌웨어 업로드는 모터 전력 0V·바퀴 공중에서만** (`FIRMWARE_REBUILD.md §5`).
  빌드 지문 `ARDUINO=10607`·`TEENSYDUINO=158` 이 안 맞으면 업로드하지 않는다.
  🔴 **트레이스 절단 후에는 `E-stop 누른 채 XT90 연결`**(`§5-b`)이고 **모터 단자 0V 를 직접
  재고 시작한다** — E-stop 을 눌렀다는 사실로 대신하지 않는다.
- 🔴 **`.ino:111 = false` · `.ino:125-126 = 0.12 / 0.50` 을 되돌리지 않는다.**
- 🔴 **re-arm 래치는 아직 없다** — PIN21 완료가 이것을 닫지 않는다. 구현·부정/전환 시험 전
  R1·R2 금지 (`REAL_ROBOT_VALUES.md §1-d`). ✅ **08-07 선행 조사 완료** — `micro_ros_arduino`
  2.0.8 이 서비스를 지원해 설계 재작업은 없다. 🔴 슬롯 1개 · `executor_init` 3→4 필수.
- 🔴 **`#11` 은 08-07 에도 닫히지 않았다.** 워치독이 500ms 계약값에 **발동**하는 것은 3회
  재현됐지만(519.9/532.0/537.1ms), `§7-c-0` 조건 1(총 정지 ≤500ms)은 **펌웨어 구조상 달성
  불가**이고 1차 증거(영상)는 미분석이다. 🔴 **"watchdog 확인됨"으로 읽지 않는다** — 기준
  재정의는 **사용자 결정 + 검토자 확인**이다 (`JETSON_SETUP §7-c-0`).
- 🔴 **모터 명령 전에 네 바퀴가 같은 방향으로 도는지 본다** — `d0_check` 검사 6은 손으로 굴려
  보는 **엔코더 부호** 검사라 구동 방향을 검사하지 않는다. 08-07 에 DIR 핀 이탈로 좌전륜이
  혼자 역회전했고 **육안 배선 점검은 못 잡았다** (`PITFALLS.md §10`).
- 판정기가 **관측하지 않은 것을 말하지 않는다** — 상한 초는 계약값 표기이지 경과 주장이 아니다.
- 설정은 대상 소프트웨어가 실제로 읽는지 `param list`·`topic info -v`로 확인한다.
- N초 관측은 같은 관측자가 종료 상태와 **벽시계 경과시간**으로 창을 완주해야 한다.
- 토픽별 계약의 단일 출처는 `tools/bag_gap_report.py`의 `TOPIC_POLICY`다.
- `/odom`은 RELIABLE, `/imu/*`는 BEST_EFFORT다. EKF 소비자 QoS까지 함께 본다.
- Teensy 재연결 로직 없음, IMU 치명 실패 시 노드 자체가 안 뜸, 시각 역행 시 stamp가 사실상
  멈춘다는 펌웨어 전제를 유지한다.
- 실차 없이 확인한 합성 bag·가짜 `ros2` 결과를 실차 통과로 승격하지 않는다.
- `TODO(D+0)` **11건**을 추측값으로 닫지 않는다 — 미해결은 이유와 재확인 시점을 적는다.

## 금지 범위

- 사용자 추가 승인 없는 `src/**` 변경. **이 묶음의 변경 표면은 `tools/` 2 + 문서뿐이다.**
- 🔴 **티어링을 회귀 면제로 읽기.** Tier B 에서 면제되는 것은 **검토 왕복**이고,
  `doc_check`·pytest·픽스처는 Tier 와 무관하게 전부 돈다.
- 🔴 **②의 완화를 "게이트를 지켰다"로 쓰기.** 상한은 넓어졌다 — 사용자 결정이라고 적는다.
- 🔴 **⑤ 를 "패킷을 크게 줄였다"로 쓰기.** 실측은 6%·4%이고 절반은 예약 29 가 소유한다.
- 🔴 **위험을 "덮였다"로 기록하기** — 조건부 수용 3건·건접점·re-arm 은 전부 전제조건 +
  재개방 조건 형식으로만 쓴다.
- 🔴 **성공 입력이 통과하는 조건을 넓히기**(②는 사용자 결정으로 명시 기록된 예외다).
- 🔴 **등록을 종결로 쓰기.** 예약 28·29 가 생겼다고 고쳐진 것이 아니다.
- 🔴 **`§5-G8` 을 "부하 차단 검증 완료"로 뭉뚱그리기** — 관찰된 것은 `0.05 m/s` 정상 전류뿐이다.
- 🔴 **펌웨어를 실제로 굽거나 업로드하기** — 업로드는 사용자가 현장에서 직접 한다.
- 예약 20·21·22·23·24·26·27·28·29, 역할 B V1 을 이 묶음에 혼합.
- `make_map.sh` 실런, 지도 자산 변경, Gazebo E2E 동시 실행.

## 완료 판정

**규율의 무게가 위험에 비례하도록 Tier A/B 가 `AGENTS.md §6` 에 정의되고, cold-start 가
비율이 아니라 **절대 상한**(≤42,000 bytes 이고 ≤13,500 o200k 토큰)으로 판정되며, 검토 회차와
규칙 발화를 세는 두 트레일러가 규칙과 함께 정의되고, 라우팅 통째 로딩이 4→2 로 줄면서도
`test_02` 앵커 회수 **85/85** 와 `test_06`·`test_07` 의 실질 문장이 그대로면 완료다.**
단, 완료는 **구현자 주장**이다. 🔴 **이 묶음은 Tier B 라 독립 검토 왕복을 요구하지 않는다** —
다만 `07a18ea`(Tier A 인접)는 **Codex 검토가 여전히 필요**하다.

## 완료 후 다음 단계 — 🔴 **실차 사다리가 우선이다**

🔴 **`2026-08-15` 플랜 B 판정일까지 8일.** 크리티컬 패스는 검토가 아니라 **re-arm 래치**다.

1. **팀 미팅 자료** — 역할 B V1 계약(`/detections` 필드·주기·QoS·실패 표현) + 예약 27
   (CUDA·JetPack 부재로 역할 B 성능 전제가 바뀜). 승인·검토 불필요.
2. **전기 잔여 3건**(`ELECTRICAL_BASELINE §8`-⑰⑱⑲) — 🔴 **⑲ 가 "바닥 주행 금지"를 푸는 열쇠**다.
3. **R0 완성** — `cmd_vel` 단절 0.5초 정지만 남았다(`TODO(D+0)` **11건** 중 `#11` ·
   `JETSON_SETUP §7-c-0`). 60fps 영상 + raw pose 로 판정하며 zero Twist 시험이 아니다.
4. 🔴 **re-arm 래치 + 부정/전환 시험** — `src/**` 이고 동결 예외 소진이라 **사용자 승인 선행.**
   **R1 을 막는 유일한 항목.**
5. **R1**(0.05 m/s) → **R2 3m ±3% · yaw 2π ±10%**. 오픈루프 편향 미측정이라
   **첫 직진은 양옆 1m 이상 비운 자리.**
6. D+0 증거 복제 · `H2` 사진 → R3 rosbag → 예약 22·29 와 R4~R8.
   🔴 **`R0→R1→R2→R3` 순서를 건너뛰지 않는다** (사다리 정본 = `MASTER_PLAN §3`).

## 근거 문서

- 규율 티어·회차·은퇴 = `AGENTS.md §6` · 결정 색인 = `MASTER_PLAN §8` · 예약 = `§7`
- 전기 위험·안전선 = `ELECTRICAL_BASELINE §11`~`§13` · 시공 = `~/Desktop/전기계통_최종작업안_0806.md`
- 펌웨어 재빌드·기대 내용 sha256 = `FIRMWARE_REBUILD §4`,`§5`
- 실차 값 = `REAL_ROBOT_VALUES §1`,`§4`,`§5` · 실행 순서·TODO = `JETSON_SETUP §9`~`§11`
- 검토 라우팅 = `TEST_GATES §7` · 검토 전문 = `~/Desktop/개발현황/CODEX 현황/0801-4검토현황.md §44`~`§50`
