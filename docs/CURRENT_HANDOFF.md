# CURRENT_HANDOFF.md — 지금 할 한 묶음

> 현재 묶음 + 미해결 보류만 유지한다. 구현자의 완료 주장은 독립검토 판정이 아니다.

- **동결 기준점**: `212885a8292d1e86677d5beeb9f5358d76fe9b40` = `platform-core-freeze-260724`.
- **저장소 기준**: `main`. 부모 `c5becb9` 위에 **41-g 계약 4판 + 1.6.3 계측**(`3408e1f`)이 있고,
  그 위에 **08-19 실차 3시행과 그 결론**(`0ee10d3`·`912613e`)이 얹혀 있다.
- **실제 보드**: 현장 후보 `rearm-latch-pi-link-probe-1.6.3` `build=Aug 18 2026 12:32:35`.
  🔴 **승인 지문은 여전히 1.6.1** 이라 셋을 섞지 않는다.
  되돌릴 `.hex` = `~/d0_evidence/fw163/`(sha256 `c40880ad…`).
  ⚠ `/firmware/info` 의 `git_sha` 가 **전부 0** 이다(08-19 관측) — 보드가 자기 커밋을 모른다.
- **현재 단계**: 검토 §79 → 보완 `c5becb9` → 41-g 묶음 `b89f7df` → **검토 §80 조건부 승인
  (P0 0 · P1 1 · P2 1) 뒤 보완 + 08-20 실차 키트 제작**.
- 🔵 **§80 이 문 둘을 열었다** — ① 1.6.3 지문 이관 ② 예약 51 FF/게인 착수. 둘 다 조건부다.
- 🔵 **08-19 이 예약 40 의 원인을 바꿔 놨다** — 문턱이 아니라 **되먹임 덫**이었다.

## 이번 한 묶음 목표 — §80 보완 · 08-20 실차 키트

3판 계약을 host harness 가 반증해 4판으로 올리고, 그대로 1.6.3 을 구현해 굽고, 실기 bag 을
9행 분류표로 읽었다(08-18). 이어 08-19 오전 실차 3시행으로 **예약 40 튜닝안을 시험해
반증하고 진짜 원인을 특정**했다. 안전 경로(`rearm_gate.h`·`drive_wiring.h`·PWM 출력단)는
**한 글자도 바꾸지 않았다** — 이번 판은 계측 추가와 튜닝 **사본** 뿐이다.

### ✅ 구현자 주장 9개 — 검토 §80 이 독립 대조를 마쳤다

전문·수치·판정 = `MASTER_PLAN §7` 41-g 의 **검토 §80 블록** ·
`REAL_ROBOT_VALUES §1-g-1`(41-g 계측) · **§1-j**(08-19 실차).

```
A. 41-g 계측 (주장 1~5)   1·2·3·5 PASS · 4 PASS(근거 정밀화)
B. 08-19 실차 (주장 6~9)   6·8 PASS · 7 부분 PASS(P2) · 9 이론 PASS·실차 미승인
```

🔵 **검토가 고쳐준 둘** — ⓐ 주장 4 의 `publish_failures == evt_seq` 는 **같은 호출점
이중 계수**라 전달 증명이 아니었다(진짜 근거 = `evt_seq` 증가량 **35 == 수신 35**)
ⓑ 주장 7 의 *"거동 불변"* 은 **전칭 과잉**이었다 → *"교착 미해소·0.32 미돌파"* 로 좁히고
시간 프로파일 차(마지막 15초 A2 **0.037m** vs B1 **0.256m**)를 별도 사실로 보존했다.

⚠ 여전히 주장하지 **않는** 것 — 300ms 막힘의 **뿌리 원인**(분류는 "발행 층"까지) ·
`max_angular_accel: 10.0` 이 **실제로 고친다**는 것(실차 미검증 · 판재/PCB 가 불감대를 바꿈) ·
세 시행 다 **E-stop 종료**라 "완주 실패"도 주장 안 한다.
🔴 검토가 단 단서 — `0 → 0.16 → 0.32` 는 둘째 tick 에 **경계 도달**이라 **여유가 얇다.**

## §3-10 전수 열거와 구현 대조

**41-g** — 🔴 **8 → 9**(`/firmware/pulse`) · 사건 publisher **0 → 1**(`/firmware/event`).
🔴 **runtime phase 7 · 해제 5사유 · RELIABLE timeout 2 · 직접 구독 12 · info 필드 7 은
불변이 역회귀**다. `tools/test_firmware_runtime_guard.py` 가 둘을 갈라서 센다.

**예약 40** — 클래스 = *"Nav2 가 **실측** 속도를 읽어 명령을 제한하는 자리"*.
구현자 열거 **3곳** → 08-19 독립 점검이 **6곳**으로 넓혔다. 표 = `MASTER_PLAN §7`
예약 40 · 50. ⚠ `grep` 전수가 아니라 **파라미터 표와 헤더 서명**으로 했다 —
nav2 소스가 이 기기에 없어 `.cpp` 확인이 불가능하다. **전칭 주장이 아니다.**

## 완료조건

| # | 항목 | 결과 |
|---|---|---|
| 1 | 41-g 계약 4판 | ✅ 6행·④ 두 자리 개정 + 사유·재현 경로 명기 |
| 2 | 1.6.3 계측 구현 | ✅ 굽고 실기 확인(`build=Aug 18 2026 12:32:35` · publish slot 9칸) |
| 3 | 합성 주입이 갈리는가 | ✅ harness 3단 367·23·14 · 분류 9종 |
| 4 | 전수 표 8→9 이동 | ✅ 이 문서 + `test_firmware_runtime_guard` 같은 커밋 |
| 5 | 안전 경로 불변 | ✅ `rearm_gate.h`·`drive_wiring.h` 바이트 동일 · phase 7 · 해제 5사유 |
| 6 | R3 | 🔴 **FAIL** — `/scan` 닫힘, `/odom`·`/imu` 공백 남음 |
| 7 | 41-g 완료 주장 | 🔴 **안 한다** — 계약+구현을 묶어 독립검토 뒤에 |
| 8 | 굽기 뒤 검증 | ✅ `§7-c-E` **13행 전량 수행**(축소판은 예약 47 로 거부됨) |
| 9 | **예약 45** | ✅ **종결** — Jetson 빌드 · 양쪽 필드 지문 일치 · tag+40자 SHA 전달 |
| 10 | **예약 40 튜닝안 검증** | 🔵 **반증됨** — 3줄로는 안 바뀐다. 원인은 되먹임 덫(주장 8) |
| 11 | **예약 49** | ✅ 신설 — 라이다 yaw 미측정. 도구 `tools/lidar_yaw_check.py` |
| 12 | 기준선 유지 | ✅ `TODO(D+0)` **11건** 종결 계약 불변 |
| 13 | **독립검토 §80** | ✅ **P0 0 · P1 1 · P2 1 — 조건부 승인**(Codex GPT-5) |
| 14 | **§80.3 P1 보완** | ✅ `package.xml` 순서 — 보완 5/0 · 부정 회귀 FAIL 재현 · 필드 지문 불변 |
| 15 | **§80.4 P2 보완** | ✅ 주장 7 을 "교착 미해소·0.32 미돌파"로 좁히고 시간 프로파일 차 보존 |

## 완료 판정

현 상태는 **계약 4판 확정 · 1.6.3 계측 검토 §80 조건부 승인(P0 0) · 41-g 가 실기에서
원인 층을 분류 · 예약 45 종결 · 예약 40 의 원인이 되먹임 덫으로 이동 · §80 P1·P2 보완 완료**다.
🔵 **§80 이 연 것 둘** — ① 1.6.3 지문 이관을 **별도 묶음으로 진행 가능** ② 예약 51 FF/게인
착수 가능. 🔴 **둘 다 조건부다** — 08-20 선행 실측 5종 통과가 먼저고, 새 FF/게인 diff 는
**별도 Tier A 묶음으로 다시 독립검토**한다. §80 은 아직 없는 diff 를 승인한 것이 아니다.
🔴 **이 묶음에서는 지문을 옮기지 않는다** — `FIRMWARE_REBUILD §4` 허용 지문은 1.6.1 그대로다.
R3 는 FAIL 이므로 **R4·R6 판정에 진입하지 않는다**.
`TODO(D+0)` **11건**과 `§7-c-E` **13행 전량 수행** 계약은 그대로다.
🔴 **`max_angular_accel: 10.0` 은 가설이지 결과가 아니다** — 08-20 실차 전까지 그렇게 읽는다.

## 완료 후 다음 단계

1. ✅ **독립검토 §80 완료** — P0 0 으로 조건부 승인. 🔵 **1.6.3 지문 이관**은 이제 별도
   묶음으로 진행할 수 있다(승인 대상 blob SHA = `MASTER_PLAN §7` 41-g §80 블록).
   🔴 **이 묶음에서는 옮기지 않는다.**
2. 🔴 **08-20 재조립 뒤 첫 30분 — 이 순서를 바꾸지 않는다.**
   ① 무장 13행(`tools/rearm_field_wiring.py`) ② **3m 줄자 대조**(판정 `odom/줄자` 0.97~1.03)
   ③ `/firmware/pulse` 9.6~10.0Hz ④ **라이다 yaw**(예약 49) ⑤ **제자리 불감대 재측정**.
   🔴 **⑤에는 판정선이 붙는다** — 제자리 회전 실행 천장은 `MAX_WHEEL_CMD 0.15 ÷ 0.31`
   = **0.4839 rad/s** 이고 `MAX_ANGULAR_CMD 0.50` 은 도달 불가능한 죽은 상수다(예약 50-2).
   재측정 불감대 `D` 에 대해 **`D < 0.4839` 면 Nav2 로 간다**(`max_angular_accel = D/0.05`,
   상한 9.68) · **`D ≥ 0.4839` 면 Nav2 로 못 고친다** — 구동부(예약 51)로 넘어간다.
   이건 튜닝 숫자가 아니라 **어느 팀의 일인가를 가르는 숫자**다.
3. 🔴 **②가 `0.97~1.03` 밖이면 그날 튜닝 전면 중단** — 반지름 재측정부터. 08-13 에 판재가
   상수·문서·검토 3회차를 통째로 기각시킨 자리다(`MASTER_PLAN §7` 예약 32-d).
4. 🔵 **예약 51(구동부 FF/게인) 착수가 열렸다** — §80 이 *"1.6.3 검토 때문에 FF/게인
   착수를 더 막을 이유는 없다"* 로 판정했다. 🔴 **조건 둘** — ⓐ 위 2 의 선행 실측 5종 통과
   ⓑ 새 FF/게인 diff 는 **별도 Tier A 묶음으로 다시 독립검토**. §80 은 없는 diff 를
   승인한 것이 아니다. 펌웨어 보완 **B(메시지 타입 교체)** 도 별도 묶음이고 A·C·E 는
   **재시도 금지** 등록이다.
5. 🔴 **예약 47**(게이트 도구 기대 해시 미이관)과 🔴 **예약 48**(윤거 상수 하나로 두 상황)의
   전문은 `MASTER_PLAN §7` 이 소유한다.

## 🔴 일정 (전문 = `MASTER_PLAN §5-b`)

**촬영 = 08-22(토)~08-23(일) · 2분 원테이크.** 🔴 **08-19 오후·저녁을 전부 판재 천공과
재조립에 쓰기로 사용자가 결정**(12:50)해, 앞 작업일이 **08-20·21 이틀**로 줄었다.
그날 예정이던 재측정 3종·튜닝 확정·2분 주행 ×2 가 통째로 08-20 으로 밀렸다.
⚠ 축소 규칙 발동 조건은 충족됐고 **범위는 사용자 결정**이다 — 구현자가 채우지 않는다.

## 금지 범위

- 🔴 **1.6.3 source hash 를 `FIRMWARE_REBUILD §4` 허용 지문으로 옮기지 않는다.**
  구현자가 자기 diff 를 승인하지 않는다.
- 🔴 **`rearm_field_wiring.py` 의 기대 해시를 지금 옮기지 않는다** — 그 도구가 스스로
  적은 갱신 규칙이 *"13행 전량을 밟은 뒤 그 결과로 옮긴다"* 라, 건너뛰려고 옮기면 순환이다.
- 새 펌웨어 업로드·`CMD_WHEEL_BASE`·FF/게인·`ODOM_WHEEL_RADIUS`·지도 재생성·URDF 수정
  은 하지 않는다. 🔴 **URDF 금지에 예약 49 의 `lidar_joint` `rpy` 보정도 포함**된다 —
  값은 재고 반영은 별도 묶음이다.
- 🔴 **`nav2_params_real.yaml` 원본 수정 금지** — 튜닝은 `_db.yaml` 사본으로만.
  현재 차이는 **값 4개**(키 114/114 동일). 되돌리기 = 런치 인자에서 `nav2_params:=` 제거.
  시험 통과해도 원본 이관은 **독립검토 뒤 별도 묶음**이다.
- 🔴 **`ekf_real.yaml` 수정 금지** — 바꾸면 역할 B 통보 의무가 생긴다(`PROJECT_CONTEXT §4.2`).
  예약 41(EKF NaN)은 이 묶음에서 건드리지 않는다.
- §72.2 보완과 H′ 를 이 묶음의 독립검토 diff 에 섞지 않는다.
- 08-14 torn odom 원인, 예약 36 IMU I2C, 39 좌향, **40**, 41-g, 43 R3, **44** 우전륜,
  46 dmesg 경계, **47** 게이트 지문, **48** 윤거 상수, **49** 라이다 yaw, **50** 명령↔실행능력
  13건, **51** 구동부 제어 여유 는 열린 상태다.
- 🔴 **예약 50 조건부 수용 — P1 7건 (08-19 신설 · 되돌릴 수 있다)**. 전문·전제조건·재개방
  조건은 `MASTER_PLAN §7` 예약 50 이 소유한다. **P0 0건이라 멈추는 것은 없다.**
  🔴 **즉시 P0 로 오르는 둘** — ⓐ 촬영용 waypoints 를 만드는 순간 `normal_speed 0.26`
  (실차 상한의 2.17배 · 미션이 런타임에 덮어쓴다) ⓑ 게이트가 무장 상태를 안 봐서 생긴
  원인 오분류가 1회라도 나오면. ⚠ 이것은 **독립검토 판정이 아니다** — Codex 검토는 대기 중.
- 🔴 **예약 51 은 펌웨어 굽기라 독립검토가 선행 조건이다** — 08-19 아침 결정의 재개방
  조건이 그대로 걸린다. `§6` 재측정 3종 전에는 게인·FF 를 건드리지 않는다.
- 🔴 **08-18·08-19 구동 실측은 판재 천공·PCB 이식 *전* 값이다** — 그 작업이 무게·강성·배선을
  바꾸므로, 뒤에 최소 ① `§7-c-E` 무장·해제·watchdog ② 3m 직진 좌향 ③ `/firmware/pulse`
  10Hz 를 다시 재기 전에는 `ODOM_WHEEL_RADIUS`·윤거·게인·Nav2 파라미터를 바꾸지 않는다.
  🔴 **`max_angular_accel: 10.0` 도 이 규칙에 걸린다** — 재측정 뒤 다시 계산한다.
- `TODO(D+0)` **11건**을 추측으로 다시 열거나 닫지 않는다.
- 🔴 **R3 통과 전 R4/R6 금지 — 08-18 조건부 예외 (사용자 결정)**. Nav2 실차 **예행**은
  R3 결과와 무관하게 연다. 판정 순서는 그대로다.
  * **여는 것** = 스택 기동·TF 완결·goal 수납·직선 추종·collision 정지 관측.
  * **열지 않는 것** = **R6 판정**. 오차 ≤0.3m·정지거리·부정 회귀 실차판·H′ 종료는
    하지 않으며, 예행 결과를 R6 통과 근거로 쓰지 않는다. **촬영은 판정이 아니다.**
  * **전제 6종** — ⓐ 라이다 연결·`/scan` 정상 ⓑ EKF·TF·readiness gate 통과
    ⓒ 물리 E-stop 담당자 상시·저속·넓은 공간 ⓓ 회전 최소 goal 부터 ⓔ bag 기록
    ⓕ 🔴 `nav2_params_real.yaml` 원본 튜닝 금지.
  * **중단 조건** — 제자리 회전만 반복 · `/odom` 공백 중 추종 이상 · 바퀴 이탈 조짐.
    **abort 는 버그가 아니다.**
  * **재개방 조건** — 전제 중 하나라도 깨지면 예행을 닫는다.
- 🔴 **스택 가동 중 로봇을 들어 옮기지 않는다 (08-19 실측)** — 바퀴가 안 굴러 odom 이
  안 변하고 `slam_toolbox` 국소 정합이 못 따라잡는다(원위치로 옮겼는데 위치추정이
  **7.45m 앞을 가리켰다**). 시행 사이 복귀는 **바퀴를 굴리며 밀거나** 런치를 재기동한다.
  수치·재현 = `MASTER_PLAN §7` 예약 40.
- 🔴 **예약 44 전제 (스페어 도착 전까지 매 시행)** — 우전륜(FR) 유격이 크다.
  ⓐ `ODOM_WHEEL_RADIUS`·윤거·FF/게인을 08-17/08-18 숫자로 바꾸지 않는다 ⓑ 예약 39·40 을
  이 데이터로 판정하지 않는다 ⓒ C10 **57.64mm** 를 확정값으로 인용하지 않는다
  ⓓ 추가 지면 주행은 바퀴 공중 우선·물리 E-stop 담당자 상시·저속.
- 🔴 **예약 46 전제** — `dmesg_coverage_check.py` 의 rc=0 만으로 커널 무사고를 확정하지
  않는다. 08-18 취득은 구간을 덮었고 USB 0건이지만, 그것이 가르는 것은 **커널이 기록하는
  종류의 사고**뿐이다. 버퍼 포화·스케줄·autosuspend 는 로그를 안 남긴다.
- 🔴 **조건부 수용 3건은 정본이 소유한다** — 예약 46 잔여 ⓐⓑ(dmesg sidecar) ·
  1.6.3 publisher 슬롯 10/10 · pulse slot 400ms→사유 7 = `MASTER_PLAN §7` 예약 46 · 41-g.
  🔴 가장 시끄러운 재개방 둘 — 실기 **`y=7` 이 1회라도** 나오면 즉시 P1 · 새 telemetry
  토픽이 **하나라도** 필요해지면 vendor drop 판단을 새로 한다.
- **⚠ 미해결 보류 — 예약 23 / 검토 §33 (`8fcc1a2`)**: P0 0 · **P1 1** · P2 1.
  `handoff_single_check.sh` 는 `§` 없는 새 작업 꼬리를 붙여도 `OK` 를 내고, 숫자 계약 전수
  증거는 기록 6자리와 독립 실행 21자리가 불일치한다. **승인 완료로 읽지 말 것.** 런타임
  `src/**` 변경·회귀는 없으므로 현재 D+0 실행은 막지 않되, 보완은 다른 묶음에 섞지 않고
  `MASTER_PLAN.md §7` 예약 23 의 완료판정과 부정 회귀로 별도 구현한다. 전문 =
  `~/Desktop/개발현황/CODEX 현황/0801검토현황.md §33`.
- **현재 미해결 보류**: 예약 36·39·**40**·41-g·43·**44**·46·**47**·**48**·**49** 과
  §72.2·H′ 는 위 순서를 유지한다.
- 🔴 **예약 41-g 예약 진입 금지 문장은 유지된다** — R4/R6 **판정**은 `R3 PASS` AND
  41-g 완료가 둘 다 선 뒤 R4, 이어 `R4 PASS` 뒤에만 R6 다.

## 진행표

| 트랙 | 묶음/근거 | 상태 |
|---|---|---|
| **진행 중 묶음** | §80 보완 — P1·P2 종결 + 08-20 실차 키트 | ✅ P1·P2 보완 완료 · 🔶 키트 제작 중 |
| 1.6.3 굽기 | ✅ `build=Aug 18 2026 12:32:35` · 현장 후보(승인 아님) |
| 예약 41-g 링크 정지 | 🔶 **발행 층으로 분류됨** · 후보 ①④ 기각 · ③ 약화 · ② 잔존 |
| 예약 43 R3 | 🔴 **FAIL** — `/scan` 닫힘 · `/odom`·`/imu` 300ms 공백 3건 |
| 예약 39 좌향 | 🟢 로봇 8.37°/3m + 바닥 1.12°/3m |
| **예약 40 각속도** | 🔵 **원인 이동** — 문턱 아님. RPP 가속 상한 × odom 실측의 되먹임 덫 |
| 예약 44 우전륜 유격 | 🔶 허브 조치·반지름 일치 · 🔴 실효 윤거 미해결 → 예약 48 |
| **예약 45 `tunnel_interfaces`** | ✅ **종결** — 양쪽 빌드·지문 일치·tag+SHA 전달 |
| 예약 46 dmesg / 47 지문 | ✅ 구간 덮음 / 🔴 축소판 6행 거부 |
| **예약 49 라이다 yaw** | 🔶 신설 — 도구 있음 · 🔴 08-20 재조립 뒤 측정 |
| **예약 50 명령↔실행능력** | 🔴 **P1 7 · P2 6 · P0 0** — 조건부 수용 등록 · Codex 검토 대기 |
| **예약 51 구동부 제어 여유** | 🔵 신설 — 62 PWM 미사용 · FF 에 회전항 없음 · 🟢 **§80 이 착수를 열었다** |
| **독립검토 §80** | ✅ **P0 0 · 조건부 승인** — P1·P2 둘 다 08-19 밤 보완 완료 |
| 08-20 실차 키트 | 🔶 제작 중 — 스윕·즉석판정·preflight·연쇄goal·런시트 |
| 승인 지문 이관 | 🔴 독립검토 전 금지 |
| `TODO(D+0)` **11건** | ✅ 종결 계약 유지 |

## 최종 회귀

- `bash tools/doc_check.sh` rc=0.
- 기준선 표기: pytest **184 passed** / colcon **252 tests** /
  **test_harness_guards 24 검사** / test_gate_regression **14 케이스**.
- tools pytest **222/222 + skip 1**(skip = `--capture` 왕복 · Jetson 에서는 돌아야 한다),
  mission pytest **184/184**.
- 🔴 **link-stall harness 3단** `bash tools/link_stall_host_test.sh`:
  MCU 동작 **367** · 호스트 분류 **23**(서로 다른 분류 9종) · 구조 **14**.
- re-arm host **989/989 + 구조 11/11** (역회귀 — 숫자 불변).
- runtime/watchdog 묶음 **11/11**, firmware-info 상한 **1418/1664**(여유 245).
- firmware precheck 실제 저장소는 승인 지문 미이관으로 **rc=1 이 정상**.
- harness guards **24/24**, gate regression **14/14**, colcon **252/252**
  (🔴 08-19 갱신 — `tunnel_interfaces` 신설로 lint 5건 편입. `76b2ed5` 이후 아무도
  `colcon test` 를 안 돌려 08-19 Codex 회귀에서야 드러났다. 검사는 설계대로 작동했다).
- 변경 Python flake8(99), `py_compile`, `git diff --check` rc=0.
- 알려진 P0/P1 코퍼스 **160/160**.
- 🔴 **`nav2_params_real_db.yaml` 대조** — 원본과 키 **114/114 동일 · 값 4개만 차이**
  (`rotate_to_heading_min_angle` 0.6→0.25 · `min_approach_linear_velocity` 0.05→0.10 ·
  `movement_time_allowance` 20.0→30.0 · `max_angular_accel` 2.0→10.0).

## 역할 교대

대상 `c5becb9` 구현자 = **Claude**. `b89f7df` 까지의 구현자도 **Claude** 이고, 그 판정은
**Codex GPT-5 가 §80 으로 썼다**(구현자 ≠ 검토자 유지). §80.3·§80.4 보완의 구현자도
Claude 이므로, 그 보완의 최종 판정은 다시 다른 검토자 몫이다 — 다만 검토가 **완료판정과
부정 회귀를 미리 명시**해줘서 그 목록에 한 줄씩 대조했다(`AGENTS §3-10 ★★④`). 🔴 **41-g 4판은 구현자 논증이 아니라 host harness 가 3판을
반증한 결과**이고 6행 개정은 사용자 결정이다. 🔴 **예약 40 의 원인 이동도 구현자 논증이
아니라 실차 bag 276표본이 3줄 튜닝을 반증한 결과**다 — 그 반증의 해석은 검토 대상이다.

## 근거 문서

- `MASTER_PLAN §7` — **41-e·41-f·41-g**(4판) · 예약 **40**(08-19 실차) · **42·43** ·
  **45**(종결) · **47·48·49** · **50**(독립 점검 13건) · **51**(구동부 제어 여유).
- `REAL_ROBOT_VALUES §1-g` runtime · `§1-g-1` 1.6.3 실측 · **§1-h** 구동 특성 ·
  `§1-i` Q1 기울기 · **§1-j** 08-19 Nav2 실차 3시행 · `§3-a-1` 라이다 오프셋.
- `FREEZE_MANIFEST §10.28` · `FIRMWARE_REBUILD §4-c-3`.
- `PROJECT_CONTEXT §4.1·§4.2` `/detections` 계약 · `ELECTRICAL_BASELINE §4-f` 계열.
- `JETSON_SETUP §7-c-E` 13행 · `PITFALLS §13` 인터페이스 워크스페이스 함정.
- 도구: `tools/link_stall_host_test.sh` · `link_stall_classify.py` ·
  `link_stall_bag_extract.py` · `goal_ahead.py` · `nav2_rotate_probe.py` ·
  `lidar_yaw_check.py`.
- 시간순 역사: `~/Desktop/개발현황/0817_현황.md`·`0818_현황.md`.
