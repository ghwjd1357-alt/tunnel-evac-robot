# CURRENT_HANDOFF.md — 지금 할 한 묶음

> 현재 묶음 + 미해결 보류만 유지한다. 구현자의 완료 주장은 독립검토 판정이 아니다.

- **동결 기준점**: `212885a8292d1e86677d5beeb9f5358d76fe9b40` = `platform-core-freeze-260724`.
- **저장소 기준**: `main`. §82(08-20 실차) 위에서 시작한다.
- **실제 보드**: `rearm-latch-pi-link-probe-1.6.3` `build=Aug 18 2026 12:32:35`.
  🔴 **08-20 에 구동부가 올린 다른 스케치를 이 `.hex` 로 복원했다**(`~/d0_evidence/fw163/`,
  sha256 `c40880ad…`). 재컴파일하지 않았다 — `firmware_precheck.sh rc=1` 차단이 정상 작동.
  🔴 **승인 지문은 여전히 1.6.1** 이라 셋을 섞지 않는다. `/firmware/info` 의 `git_sha` 는 전부 0.
- **현재 단계**: 08-20 실차 완료(시나리오 **A 확정**) → **§83 · 08-21 지도 재촬영 ·
  keepout · 미션 좌표 · 리허설** → 08-22~23 촬영.

## 이번 한 묶음 목표 — 08-21 촬영 전날 (검토 §83 대상)

🔵 **08-20 이 "되느냐"에 답했다면, 08-21 은 "그대로 찍을 수 있느냐"에 답한다.**
자율주행은 08-20 에 실증됐다(연결통로 통과 12.4m SUCCEEDED). 남은 것은 **지도 품질 ·
경로 경계 · 미션 좌표 · 리허설** 넷이다.

```
① 지도 재촬영 (111m·약 23분)   ② 예약 55 keepout filter
③ 예약 53 미션 좌표 재작성      ④ 시나리오 A 리허설 (겸 검증)
```

🔴 **08-20 에 못 밟은 둘을 여기서 닫는다** — 완료조건 9(2분 주행 ×2)는 **재촬영 주행이
대신하고**(20분 넘는 연속 주행), 완료조건 11(시작점 좌표)은 **재촬영이 시작점을 원점으로
만들어** 사라진다.

### 08-20 이 남긴 미해결 셋 (이 묶음에서 본다)

- 🔴 **제자리 회전 구간의 EMI 가 안 잡혔다** — 직진은 `/odom` 공백 96.7ms(13건)로 나았는데
  **제자리 회전은 360.1ms(27건)** 다. **시나리오 A 는 코너가 많다.** 예약 43.
- 🔴 **회복동작은 항상 시간초과한다** — Spin 명령 `1.0` vs 실측 `0.0888` (11배). 예약 50-1.
- 🔶 **`max_angular_accel` 10.0 은 물리 천장 9.68 을 넘는다** — 정합값은 `D/dt ≈ 4.9`.
  08-20 에는 C1 비교 실험의 변수 고정을 위해 안 바꿨다.

## §3-10 전수 열거와 구현 대조

**예약 55 keepout** — 클래스 = *"경로가 지도 밖으로 샐 수 있는 자리"*. 🔴 **`.pgm` 편집은
이 클래스에 안 든다** — `real_bringup` 은 `map_server` 를 안 띄우고 `slam_toolbox` 가
`.posegraph` 에서 `/map` 을 만든다(런치 256줄). 열거 대상은 **costmap 플러그인 체인 2곳**
(global·local)과 **필터 노드 2종**(`costmap_filter_info`·`filter_mask_server`)이다.

**예약 53 미션 좌표** — 클래스 = *"폭 6m 시뮬 터널을 전제한 파라미터"*. 좌표만이 아니다 —
`search_back.detect_range` · `cluster_max_width` · `normal_speed` 가 같은 클래스다
(표 = `MASTER_PLAN §7` 예약 53). 🔴 **`cluster_max_width 0.8` 이 가장 위험하다** —
1.65m 복도에서는 사람이 벽에서 최대 0.8m 밖에 못 떨어져 **사람이 벽으로 배제된다.**

🔴 **08-20 회의로 이 항목이 "대본 선결 조건"으로 승격됐다** (`MASTER_PLAN §5-b` 표 ⓐ~ⓕ).
확정 대본을 대조해 **2건이 새로 나왔다**:
- 🔴 **`corridor_graph` 가 T자인 채로는 화재 알람이 접수 자체를 못 한다** —
  `mission_node.py:957` 이 *"복도에서 N m 떨어진 좌표 — 지도 밖 오클릭"* 으로 **거부**한다.
  → 리허설이 **첫 장면에서 멈춘다.** H자로 다시 그리는 것이 좌표 작업의 **1순위**다.
- 🔴 **`gather_dist: 8.0` 재산정** (예약 53 표에 없던 항목) — 폭 6m·길이 26m 시뮬 전제라
  전체 14m 남짓인 H자에서는 **집결지가 지도 밖으로 나간다.**

🔴 **그리고 리허설에는 사람이 한 명 필요하다.** `SEARCH_BACK` 은 `GUIDE` 중 **추종자를
놓쳤을 때만** 진입하는 하위 상태다 — 유도 대상이 없으면 도달 자체가 불가능하므로
`cluster_max_width` 실측도 리허설도 **사람을 세워야** 성립한다.

## 완료조건

| # | 항목 | 통과선 |
|---|---|---|
| 1 | 🔴 **지도 백업** | `diff -r` 로 원본과 동일 확인 · 노트북 사본 포함 3벌 |
| 2 | 지도 재촬영 | START(위 복도 서쪽)에서 출발·복귀 · 문 닫음 · 양끝 도달 · 루프 닫힘 |
| 3 | 저장 | `.posegraph`+`.data`(serialize) **와** `.pgm`+`.yaml`(map_saver) **둘 다** |
| 4 | 🔴 **무장 유지** | 재촬영 23분 동안 `/drive/diag z` 2→0 **0회** (= 구 완료조건 9) |
| 5 | 새 지도 검증 | 연결통로 통과 재확인 · 위치추정 원점 오차 |
| 6 | 🔴 **예약 55 keepout** | H자 밖 goal **거부** · 🔴 역회귀 = 통로 goal 은 **여전히 통과** |
| 7 | 🔴 **예약 53 미션 좌표** | 실측 기반 재작성 · `normal_speed` 구동부 상한 이내 · 🔴 `corridor_graph` H자화 + `gather_dist` 재산정 + `cluster_max_width` **사람 세워 실측** |
| 8 | 회전 EMI 재확인 | 제자리 회전 포함 시행에서 `/scan` 상한 초과 **0건** |
| 9 | 회복동작 튜닝 | Spin 이 예산 안에 끝나거나, 안 뜨는 경로로 설계됨 |
| 10 | 🔴 **시나리오 A 리허설** | 08-20 확정 대본(`MASTER_PLAN §5-b`)대로 완주 1회 이상 · 무장 해제 0회 · 🔴 **사람 1명 + 2차 화재 `reset` 재발사 포함** |
| 11 | bag | 시행마다 교체 · 실패 시행도 남긴다 |
| 12 | 기준선 유지 | `TODO(D+0)` **11건** 종결 계약 불변 · `§7-c-E` **13행 전량 수행** |

## 완료 판정

🔴 **이 묶음은 아직 시작 전이다.** 위 12행은 통과선이지 결과가 아니다.

직전 묶음(§82)의 결과는 **정본이 소유한다** — `REAL_ROBOT_VALUES §1-k`(08-20 실차 계측
전량) · `MASTER_PLAN §7` 예약 39·43·49·50·52 갱신 + **예약 54·55 신설** · `§8` 결정 색인 ·
`PITFALLS §14`(모터 EMI). 요지만: **시나리오 A 확정**(조건 ①②③ 전부 통과) ·
독립 고장 셋 수리(펌웨어 복원 · RR 엔코더 · 라이다 EMI) · 예약 52 종결 · 50-4 종결 ·
Jetson 부하 최초 측정(**YOLO 여유 있음**).

`TODO(D+0)` **11건** 과 `§7-c-E` **13행 전량 수행** 계약은 그대로다.

🔴 **08-20 은 독립검토를 받지 않았다** — 측정 묶음이고 `src/**` 변경이 `_db.yaml` 한 파일
(튜닝 사본)뿐이다. 🔴 **그 한 파일은 Tier A 경계에 있다** — 원본은 안 건드렸지만 costmap
동작을 바꾼다. **검토 대상으로 남겨 둔다.**

## 완료 후 다음 단계

1. 🔴 **08-22~23 촬영.** 08-21 이 마지막 작업일이다.
2. 🔵 관제에 `/scan` 실시간 표시 추가 (Tier B · `console/index.html` 은 이미 `/map`·`/tf`·
   `/plan`·`/alarm` 을 그린다. 추가할 것은 `/scan` 점 렌더 하나).
3. 🔵 관제 표시용 `.pgm` 다듬기 — 🔵 **항법 영향 0** 이므로 언제 해도 된다(예약 55 참조).
4. 🔴 예약 51(구동부 FF/게인)은 **열려 있으나 사유가 없다** — `D` 여유가 52% 라 Nav2 로
   풀린다. 펌웨어를 굽는다면 별도 Tier A 묶음 + 독립검토 선행.
5. 🔴 예약 54(IMU 절대 방위)는 **촬영을 막지 않는다** — 2~5분 시연에서 자이로 드리프트는
   문제되지 않는다. 08-21 이후.

## 🔴 일정

**촬영 = 08-22(토)~08-23(일) · 2분 원테이크(배속 허용).** 앞 작업일은 **08-21 하루**뿐이다.
⚠ 시나리오 A 초안 경로가 약 **55m ≈ 8~9분 실주행**으로 목표(4~5분)의 두 배다 —
**구간 길이 축소는 사용자 결정**이다(구현자가 채우지 않는다).

## 금지 범위

- 🔴 **`nav2_params_real.yaml` 원본 수정 금지** — 튜닝은 `_db.yaml` 사본으로만.
  🔴 **08-20 에 `_db.yaml` 계약이 바뀌었다** — 구 *"키 114/114 동일 · 값 4개 차이"* 에서
  **신규 키 4개**(`obstacle_layer.scan.marking`·`clearing` × local/global)가 늘었다.
  원본 이관은 **독립검토 뒤 별도 묶음**이다.
- 🔴 **`ekf_real.yaml` 수정 금지** — 바꾸면 역할 B 통보 의무(`PROJECT_CONTEXT §4.2`).
  예약 41(EKF NaN)·예약 54(IMU 방위)를 이 묶음에서 건드리지 않는다.
- 🔴 **새 펌웨어 굽기 금지** — 예약 51 은 별도 Tier A 묶음 + 독립검토 선행.
  1.6.3 지문 이관·`rearm_field_wiring.py` 기대 해시 이관도 하지 않는다(예약 47).
- 🔴 **URDF 수정 금지** — 예약 49 `lidar_joint rpy` 포함. 🔵 다만 **보정할 값이 없다**
  (0.13°±0.19° = 0 과 구분 불가).
- 🔴 **지도 자산은 백업 뒤에만 건드린다** — 완료조건 1 을 밟기 전에 재촬영하지 않는다.
  구 지도 `corridor_0814c` 는 **지우지 않는다**(항법상 문제없음이 08-20 에 증명됐다).
- 🔴 **스택 가동 중 로봇을 들어 옮기지 않는다** — 08-19 실측(원위치로 옮겼는데 위치추정이
  7.45m 앞을 가리켰다). 🔴 **손으로 밀 수도 없다**(08-20 확인 — 감속비가 크고 뻑뻑하다).
  → 이동 수단은 **① Nav2 goal ② 스택 내리고 들어 옮긴 뒤 재기동** 둘뿐이다.
- 🔴 **매핑 중 `speed`·`repeat_rate` 를 기본값으로 쓰지 않는다** — teleop 기본 `0.5 m/s` 는
  펌웨어 상한의 4배고, `repeat_rate` 가 없으면 키를 뗄 때마다 watchdog(500ms)이 세운다.
  `-p speed:=0.10 -p turn:=0.35 -p repeat_rate:=10.0`.
- 🔴 **`inflation_radius` 를 0.40(외접반경) 밑으로 내리지 않는다.** 🔵 08-20 에 **0.9 그대로
  1.5m 통로를 통과**했으므로 내릴 사유 자체가 없다.
- 🔴 **예약 44 전제 (스페어 도착 전까지 매 시행)** — 우전륜(FR) 유격. ⓐ `ODOM_WHEEL_RADIUS`·
  윤거·FF/게인을 08-17/08-18 숫자로 바꾸지 않는다 ⓑ 예약 39·40 을 이 데이터로 판정하지
  않는다 ⓒ C10 **57.64mm** 를 확정값으로 인용하지 않는다.
  🔵 08-20 `pcb_wiring_check` 에서 **FR 엔코더 응답은 정상**이었다 — 유격이 엔코더를 죽이진 않는다.
- 🔴 **08-18 이전 구동 실측을 인용하지 않는다** — 판재 천공·PCB 이식 **전** 값이다.
  대체 정본 = `REAL_ROBOT_VALUES §1-k`.
- 🔴 **예약 46 전제** — `dmesg_coverage_check.py` 의 rc=0 만으로 커널 무사고를 확정하지
  않는다. 버퍼 포화·스케줄·autosuspend 는 로그를 안 남긴다.
- 🔴 **조건부 수용 3건은 정본이 소유한다** — 예약 46 잔여 ⓐⓑ · 1.6.3 publisher 슬롯 10/10 ·
  pulse slot 400ms→사유 7 = `MASTER_PLAN §7` 예약 46 · 41-g. 재개방 둘 — 실기 **`y=7` 이
  1회라도** 나오면 즉시 P1 · 새 telemetry 토픽이 **하나라도** 필요해지면 vendor drop 재판단.
- 🔴 **예약 50 조건부 수용 — P1 7건 (08-19 신설)**. 전문 = `MASTER_PLAN §7` 예약 50.
  🔵 **50-4 는 08-20 에 닫혔다.** 🔴 **즉시 P0 로 오르는 하나가 남는다** — 촬영용 waypoints 를
  만드는 순간 `normal_speed 0.26`(실차 상한의 2.17배)이 발동한다 → **예약 53 과 같은 묶음**.
- **⚠ 미해결 보류 — 예약 23 / 검토 §33 (`8fcc1a2`)**: P0 0 · **P1 1** · P2 1.
  `handoff_single_check.sh` 는 `§` 없는 새 작업 꼬리를 붙여도 `OK` 를 내고, 숫자 계약 전수
  증거는 기록 6자리와 독립 실행 21자리가 불일치한다. **승인 완료로 읽지 말 것.** 런타임
  `src/**` 변경·회귀는 없으므로 현재 D+0 실행은 막지 않되, 보완은 다른 묶음에 섞지 않고
  `MASTER_PLAN.md §7` 예약 23 의 완료판정과 부정 회귀로 별도 구현한다. 전문 =
  `~/Desktop/개발현황/CODEX 현황/0801검토현황.md §33`.
- **현재 미해결 보류**: 예약 36·39·**40**·41-g·43·**44**·46·**47**·**48**·49·**50**·51·
  **53**·**54**·**55** 과 §72.2·H′.
- `TODO(D+0)` **11건**을 추측으로 다시 열거나 닫지 않는다.
- 🔴 **예약 41-g 진입 금지 문장은 유지된다** — R4/R6 **판정**은 `R3 PASS` AND 41-g 완료가
  둘 다 선 뒤 R4, 이어 `R4 PASS` 뒤에만 R6.

## 진행표

| 트랙 | 묶음/근거 | 상태 |
|---|---|---|
| **진행 중 묶음** | §83 — 08-21 재촬영·keepout·미션좌표·리허설 | 🔶 착수 전 |
| **시나리오** | 🟢 **A(H자) 확정** — 조건 ①195° ②통과 ③D=0.233 전부 통과 | ✅ 08-20 결정 |
| **자율주행 실증** | 🟢 연결통로 1.5m 통과 · **12.414 m SUCCEEDED** · 132초 | ✅ |
| 예약 39 좌향 | 🔵 **성질 재정의** — 결함 아님, 시행마다 달라지는 값 |
| 예약 40 각속도 | 🔵 되먹임 덫 — `D`=0.233 로 여유 52% 확인, Nav2 로 푼다 |
| 예약 43 R3 | 🔶 **원인 후보 = EMI** — 직진 개선, 🔴 **회전 미해결(360ms/27건)** |
| 예약 44 우전륜 | 🔶 유격 미해결 · 🔵 엔코더 응답은 정상 |
| 예약 46 dmesg / 47 지문 | ✅ 구간 덮음 / 🔴 축소판 6행 거부(실측으로 확인) |
| 예약 49 라이다 yaw | 🔶 **0.13°±0.19° = 0 과 구분 불가** · 🔴 180° 미밟음 · 도구 결함 2건 |
| **예약 50 명령↔실행** | 🔴 P1 7 중 **50-4 종결** · **50-1 실측 완료**(11배 괴리) |
| 예약 51 구동부 | 🔵 열려 있으나 **착수 사유 없음** (`D` 여유 52%) |
| **예약 52 연결통로** | ✅ **종결** — `inflation 0.9` 그대로 통과 |
| **예약 53 미션 좌표** | 🔴 **이 묶음** — 좌표 + 판별 파라미터 3종 |
| **예약 54 IMU 방위** | 🔵 신설 — 발행 안 됨(`cov −1`) · 촬영 미차단 |
| **예약 55 keepout** | 🔴 **신설 · 이 묶음** — 부채꼴로 경로가 샐 수 있다 |
| 지도 | 🔴 **재촬영 예정(관제 표시 목적)** · 🟢 항법상 구 지도로 충분함은 증명됨 |
| **Jetson 부하** | ✅ **최초 측정** — GPU 0% · RAM 여유 85% · **YOLO 여유 있음** |
| `TODO(D+0)` **11건** | ✅ 종결 계약 유지 |

## 최종 회귀

- `bash tools/doc_check.sh` rc=0.
- 기준선 표기: pytest **184 passed** / colcon **252 tests** /
  **test_harness_guards 24 검사** / test_gate_regression **14 케이스**.
- tools pytest **222/222 + skip 1**, mission pytest **184/184**.
- 🔴 **link-stall harness 3단** `bash tools/link_stall_host_test.sh`:
  MCU **367** · 호스트 분류 **23**(9종) · 구조 **14**.
- re-arm host **989/989 + 구조 11/11**, runtime/watchdog **11/11**,
  firmware-info 상한 **1418/1664**.
- firmware precheck 실제 저장소는 승인 지문 미이관으로 **rc=1 이 정상**.
- 알려진 P0/P1 코퍼스 **160/160**.
- 🔴 **`nav2_params_real_db.yaml` 대조 (08-20 갱신)** — 값 **4개** 차이
  (`rotate_to_heading_min_angle` 0.6→0.25 · `min_approach_linear_velocity` 0.05→0.10 ·
  `movement_time_allowance` 20.0→30.0 · `max_angular_accel` 2.0→10.0)
  **+ 신규 키 4개** (`obstacle_layer.scan.{marking,clearing}` × local/global).
  🔴 구 계약 *"키 100% 동일"* 은 더 이상 성립하지 않는다.

## 역할 교대

§82(08-20)의 구현자 = **Claude**. 🔴 **측정 묶음이라 "구현자가 주장하는 것"이 적다** —
숫자는 도구가 냈고 bag 이 남아 있다. 다만 **해석은 구현자 논증**이다: ⓐ 라이다 사망의
원인을 EMI 로 좁힌 것 ⓑ 예약 43 과의 연결을 *"원인 후보"* 로 한정한 것 ⓒ 예약 49 의
180° 시험 논리가 성립하지 않는다고 판단한 것 — **셋 다 검토 대상이다.**
🔴 **사용자 정정 2건이 구현자 오류를 잡았다** — ① 좌향 편향의 성질(재조립 단위 → 시행
단위) ② H 방향 기준(구현자가 세운 H 와 눕힌 H 를 혼동). 둘 다 정본에 반영했다.

## 근거 문서

- `REAL_ROBOT_VALUES §1-k` — **08-20 실차 계측 전량**(기본·Nav2·회복동작·부하·고장 3건·
  라이다 yaw·IMU 방위). `§1-h`·`§1-j` 는 재조립 **전** 값이다.
- `MASTER_PLAN §7` 예약 **39·43·49·50·52**(갱신) · **53·54·55** · `§8` 결정 색인 ·
  `§5-b` 시나리오 A(방향 정본화).
- `PITFALLS §14` 모터 EMI · `§14-a` 커넥터 핀 순서.
- `FIRMWARE_REBUILD §5` 업로드 안전 규칙 · `JETSON_SETUP §7-c-E` 13행.
- 도구: `drive_deadband_sweep.py` · `nav2_trial_report.py` · `nav2_preflight.py` ·
  `pcb_wiring_check.py` · `drive_encoder_check.py` · `lidar_yaw_check.py`(🔴 결함 2건).
- bag: `~/d0_evidence/` 의 `r2_line_0820` · `deadband_0820_{01,2,3}` · `tune_0820_{C1,C1c}` ·
  `passage_0820` · `turn180_0820` · `recovery_0820` · `ghost_0820` · `enc_check_0820`.
