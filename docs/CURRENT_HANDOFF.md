# CURRENT_HANDOFF.md — 지금 할 한 묶음 (매 세션 시작점)

> 작업 완료 시 구현자가 갱신하고 검토자가 사실관계·테스트 결과를 확인한다.
> 이전 묶음 내용은 날짜별 현황으로 보내고, 이 파일에는 현재 작업과 직전 주장만 유지한다.

- **동결 기준점**: `212885a8292d1e86677d5beeb9f5358d76fe9b40` = tag
  **`platform-core-freeze-260724`**. 증거 = `docs/FREEZE_MANIFEST.md`.
- **현재 기준 커밋**: 이 파일과 같은 커밋(main).
- **현재 단계**: 마스터플랜 5단 완료 → 역할 B V1 합의(6단)는 회의 대기 보류
  → 실차 D+0 인수·연결 **부분 완료(`d0_check` 종료 2 · 검사 8 미확인)**
  → 검토 §39 불승인·§40~§42 회신 수령(08-04)
  → **전기계통 정본 v2 전면 개정**(08-04)
  → §39 문서 표면 보완 + 정본 최신화(08-05 `870f1da`~`d86cafe`)
  → **§39 코드 표면 보완 = 예약 25 종결** **(지금 — 2026-08-05)**
- **⚠ 미해결 보류 — 예약 23 / 검토 §33 (`8fcc1a2`)**: P0 0 · **P1 1** · P2 1.
  `handoff_single_check.sh` 는 `§` 없는 새 작업 꼬리를 붙여도 `OK` 를 내고, 숫자 계약 전수
  증거는 기록 6자리와 독립 실행 21자리가 불일치한다. **승인 완료로 읽지 말 것.** 런타임
  `src/**` 변경·회귀는 없으므로 현재 D+0 실행은 막지 않되, 보완은 다른 묶음에 섞지 않고
  `MASTER_PLAN.md §7` 예약 23 의 완료판정과 부정 회귀로 별도 구현한다. 전문 =
  `~/Desktop/개발현황/CODEX 현황/0801검토현황.md §33`.
- **⚠ 미해결 보류 — 예약 24 / 검토 §38**: P0 0 · **P1 2** · P2 4. 라이다 실측 소유 시점과
  readiness flake 소유 정본이 갈라졌다. **라이다 높이는 D+1 소유**이며 `JETSON_SETUP.md §9`
  의 `TODO(D+0)` 목록 계약은 그대로다.
- **⚠ 미해결 보류 — 예약 26 / 검토 §40**: 사용자 결정 **'방법 B'** 가 구현되지 않았고 저장소
  에서도 사라졌던 건. 결정은 `MASTER_PLAN.md §8` 에 복원했다. **§37.3 을 닫힌 것으로 인용하지
  않는다.**
- **⚠ 미해결 보류 — 예약 27**: 이 Jetson 에 `nvcc`·`nvidia-jetpack` 이 **없다**. 역할 A 는
  비차단, **역할 B 성능 전제는 통째로 바뀐다.** 역할 B 회의 안건.
- **⏸ 6단 보류**: 역할 B 회의가 잡히면 커밋 `30c5e87`의 핸드오프 원문을 그대로 복원한다.

```bash
git show 30c5e87:docs/CURRENT_HANDOFF.md
```

## 이번 묶음 — 예약 25 종결: `d0_check` 판정기 벽시계·원인 문자열 (구현자 = Claude, 독립 검토자 = Codex 예정)

★ **역할 교대 계속 (`AGENTS.md §5` 예외 경로)**: **§39 의 검토자는 Claude 였다.** 08-05
사용자 요청으로 그 검토자가 구현자가 됐고, 이 묶음은 **그 교대의 두 번째이자 마지막 diff**다
(문서 표면 `870f1da` → 코드 표면 = 이 커밋). ① 이 커밋은 **Codex 가 독립 검토**한다
② 구현자 주장은 아래에 있다 ③ **역할은 이 묶음 경계에서 원위치**한다.

⚠ **교대 고유 위험에 대한 이번 회차의 답** (`AGENTS.md §5` ③): 지난 묶음에서 *"§39.6.2 는
검토가 준 목록을 따라간 것"* 이라고 공개했던 그 약점을 이번엔 **기계 열거로 먼저 뒤집었다.**
`grep -n "hard_timeout " tools/d0_check.sh` = 8자리를 세 축으로 대조했더니 **검토가 준
목록(2자리)이 부분집합**이었다 — 원인 문자열을 못 보여주는 자리가 4개였고, 재확인 자리는
stderr 를 캡처조차 안 했다. 검토자는 여전히 **내가 준 목록 밖**을 먼저 공격한다.

`src/**` 는 열지 않았다. 변경 표면은 `tools/d0_check.sh` · `tools/udev/**` · 문서다.
구현자가 주장하는 것:

- **P1 §39.2 를 자리가 아니라 클래스로 닫았다.** 상한 초를 문장에 **손으로** 적던 구조가
  근인이라, `hard_timeout` **8자리 전부**를 같은 두 함수(`clock_begin`/`clock_end`)로 재게
  하고 실패 문장은 `why_snapshot()` **한 곳**에서만 쓰게 했다. 상한은 '주장'이 아니라
  **계약값** 표기로 격하했고 발동 여부는 rc 가 말한다. 표와 구현이 갈라질 자리를 없앤 것이지
  숫자를 고친 것이 아니다(`AGENTS.md §3-10 ★②`).
- **검토가 `374` 를 "허용(설명문)"으로 분류한 것은 실측에서 틀렸다.** 구판을 같은 입력으로
  돌리면 rc=1 인데도 `"(20초 상한 발동 = 124)"` 를 출력한다. 그래서 그 자리도 함께 고쳤다.
- **관측 창 클래스에서도 한 자리를 더 닫았다.** `topic hz` 는 rc 와 벽시계를 둘 다 보는데
  부호 검사는 rc 만 봤다. 즉시 `rc=124` 를 뱉는 상대에게 구판은 *"값을 한 줄도 못 읽었다"* 로
  **원인을 잘못 지목**했고, 지금은 *"창을 다 안 썼다"* 로 갈린다.
- **P2-1 — `--secs`(사용자가 고르는 관측 창)와 스냅샷 상한(DDS 응답 대기)을 다른 축으로
  분리했다.** 한쪽에 묶으라는 권고는 **채택하지 않았고 근거를 남겼다**: `--once` 성공 실측이
  1.996~2.315초라 `--secs 3` 을 물리면 정상 발행을 거짓 FAIL 낸다. 머리말은 숫자를 다시 적지
  않고 실제 상한 변수로 **예산을 계산**한다.
- **P2-2 는 '고쳤다'가 아니라 '전제를 달고 수용했다'.** `16c0:0483` 은 PJRC **제품군**
  식별자라 규칙이 개체를 고르지 않는다. 주석을 사실대로 고쳤고, **전제조건(현재 Teensy 1대)
  + 재개방 조건(두 번째 USB CDC 장치 연결 시점)** 을 규칙 파일과 `MASTER_PLAN.md §8` 에 남겼다.
  지금 serial 을 안 넣은 이유는 값이 없어서가 아니라 **Jetson 에서만 검증 가능**해서다.
- **합성 입력 부정·역회귀를 실제로 돌렸고 명령을 기록에 남겼다** — 부정(토픽 부재)·역회귀
  (발행자 생존)·반대 경계(rc=124)·관측 창 공격(즉시 124)·`--secs` 양방향·E-stop 3경로.
  관측값 표 = `MASTER_PLAN.md §7` 예약 25.
- **회귀 기준선은 그대로다** — 이 표면의 실행 대상(pytest · harness guards ·
  `test_ai_context` · `test_todo_scan`)을 직접 돌렸고 수치는 **완료조건 6 한 자리**에만 적는다
  (같은 숫자를 두 곳에 쓰면 `gate_baseline_scan` 이 자리 증식으로 FAIL 낸다 — 검토 §19 계약).
  colcon 과 gate regression 은 `src/**` 무접촉이라 **재실행하지 않았다.**
- ⚠ **닫지 않은 이웃 하나를 공개한다**: `scan_unbounded_cli` 의 **기계 강제 범위는 게이트
  5파일**이고 `d0_check.sh` 는 그 밖이다(`test_harness_guards.sh:448`). 지금 0건인 것은 손으로
  확인한 것이라 **회귀가 아니다.** 판정기 회귀 편입은 **예약 21** 이라 여기 섞지 않았다.

## 직전 완료 — §39 문서 표면 + 정본 최신화 (구현자 = Claude, 독립 검토자 = Codex 예정)

`870f1da`·`01861a8`·`e5f20d0`·`fa77112`·`f57d454`·`d86cafe`. 08-03 현장·08-04 검토 4회·08-04
전기계통이 저장소 정본에 반영되지 않은 채 이틀이 지난 것을 바로잡았다. 안전 P1 §39.6.1 을
닫고, inventory 계약을 64 → 67 로 올리고, 무상한 `topic echo` 10자리를 전량 상한으로 덮고,
`docs/FIRMWARE_REBUILD.md` 와 `firmware/` 작업본을 신설했다. **업로드는 하지 않았다.**

## 직전 완료 — 실차 D+0 현장 기록 (구현자 = Codex, 검토 = §39 **불승인**)

`c397975`. Claude 의 §39 독립 검토가 **P0 0 · P1 2 · P2 12 로 불승인**했다. 08-05 로 §39 의
발견은 문서·코드 양쪽에서 **저장소상 전부 닫혔지만**, `c397975` **자체의 판정은 불승인
그대로**이고 그것을 뒤집는 것은 보완 커밋이 아니라 검토자다.

## ★ 이번 회차 최우선 확인 사항 — 전기계통 시공 (사용자 직접 · **미착수**)

> 2026-08-05 사용자 확인: **아직 시작하지 않았다.** 따라서 아래 안전 불변조건은 **전부
> 그대로 유효**하고, 물리 E-stop 은 **없는 것으로 취급**한다.

1. 시공 정본은 `~/Desktop/전기계통_최종작업안_0804.md` **§4 작업 순서**다. 판단 근거·위험
   논리가 필요하면 `docs/ELECTRICAL_BASELINE.md`, 부품 계약값은 `REAL_ROBOT_VALUES.md §1-e`.
2. **작업 전 메인 스위치 OFF + XT90 분리 + 멀티미터 0V 확인.** E-stop 만 누르고 배선을
   만지지 않는다 — E-stop 은 **모터 가지만** 끊고 Wago·퓨즈·로직은 살아 있다.
3. **퓨즈는 맨 마지막에 끼운다.** 다이오드 방향을 눈대중하지 않는다(거꾸로 = 코일 통전 순간
   배터리 단락). **87a 선은 반드시 절연**한다 — E-stop 을 누르면 거기에 +25V 가 나타난다.
4. `§5 중단 기준` 5가지 중 하나라도 걸리면 **멈추고 상의**한다(퓨즈 끊김 · 발열 · 눌러도
   전압 유지 = 융착 의심 · 릴레이 "딱" 무음 · 타는 냄새/연기/부푼 배터리).
5. 시공 끝 → **E-stop 개폐 10회 토글**로 실제 차단을 확인한다. 배선을 다시 만졌으면 10회를
   **다시** 한다.
6. 다음 현장 세션의 **첫 명령**은 D+0 증거 복제다(`JETSON_SETUP.md §11-g` 보존 규정) —
   bag 2개·tegrastats 로그가 아직 Jetson 한 대에만 있다.
7. 전기 시공 자체는 `TODO(D+0)` **11건**을 닫지 않는다. 그 목록의 정본은
   `JETSON_SETUP.md §9` 한 곳이며, 시공 결과는 E-stop 항목의 **전제**만 만든다.
8. ★ **08-05 Arduino 라이브러리 수령 완료** (`§5-d` 에 버전·해시·전수 대조). 이것이 푼 것은
   **펌웨어 재빌드 능력**이며, `ESTOP_ACTIVE_LOW`·re-arm 래치·(뒤에) `EFFECTIVE_WHEEL_BASE`
   세 변경이 한 환경에서 가능해졌다. ⚠ **푼 것과 안 푼 것을 섞지 않는다** — PIN21 2단계는
   릴레이3·풀업이 필요한 **하드웨어** 작업이고 최종작업안 `§7-2`·`§8` 에서 **구동부 파트 기판
   작업**으로 넘어가 있다. 따라서 검사 8은 여전히 통과 불가이고 `§7-c` 전제 1번도 그대로다.

## 사용자 결정 — 유지

- 동결 예외 4회는 전부 소진됐다. `src/**`를 다시 열려면 사용자 승인이 필요하다
  (`FREEZE_MANIFEST.md §10`).
- 순서는 전기계통 시공 → E-stop 검증 → D+0 잔여 종결 → D+1 R3 rosbag → R4~R8이다
  (`MASTER_PLAN.md §3`).
- 예약 20·21·22·23·24·26·27과 역할 B 6단은 각각 정해진 트리거 전까지 별건으로 유지한다
  (`MASTER_PLAN.md §7`). **예약 25 는 08-05 에 종결**됐다.
- `JETSON_SETUP.md §9`의 `TODO(D+0)` **11건** 중 **열 자리**가 값과 함께 닫혔고, 미해결은
  **물리 E-stop 하나**다(08-05 라이브러리 수령으로 6번 종결). 목록 계약은 바꾸지 않는다.
- 08-03 D+0 판정: 장치·주기·간격·QoS·전진 부호·EKF 생존·펌웨어 정체는 **통과**했고, 수정판
  `--no-manual` 재실행은 자동 항목 전부 통과·**종료 2**였다. 남은 항목은 물리 E-stop 과
  그 전제가 필요한 R0 watchdog이다. **전량 통과로 기록하지 않는다.**
- 🔴 **이번 회차 E-stop 은 PIN 21 미연결이므로 `d0_check.sh` 검사 8 은 원리적으로 통과할 수
  없다.** 종료 0 은 2단계(신호릴레이 a접점 + `ESTOP_ACTIVE_LOW = false`) 이후에만 가능하다.
  그때까지 `--no-estop` 종료 2 가 이 트랙의 정상 상한이며, 이는 **물리 차단이 없다는 뜻이
  아니라 Teensy 가 그것을 못 본다는 뜻**이다. 물리 차단 자체는 개폐 10회 토글로 증명한다.
- never-seen은 다른 놓침과 같이 역행 2회 → 보고 → 단독 탈출이다. 실차 R3 뒤 재평가한다.

| 트랙 | 범위 | 상태 |
|---|---|---|
| **진행 중 묶음** | §39 코드 표면 = 예약 25 종결 (`d0_check` 벽시계·원인 문자열·`--secs`·udev) | **착수** |
| 전기계통 시공 | `전기계통_최종작업안_0804.md §4` (사용자 직접) | ⏸ **미착수** (08-05 확인) |
| 실차 D+0 | 연결·주기·QoS·부호·펌웨어 통과, 종료 2 | ⏸ E-stop 설치 뒤 잔여 종결 |
| AI 입력 최적화 | `ef25ad3` 기준, 품질 불변 + raw 입력 30% 절감 | §40 승인 · 방법 B 잔여 = 예약 26 |
| 예약 21 | D0/R3 판정기 회귀 편입 + readiness case 9·11·12·14 간헐성 규명 | ⏸ 다음 bringup 변경 전 |
| 예약 23 | 핸드오프 ID·숫자 계약 전수 폐포 | ❌ 별도 보류 |
| 예약 24 | 검토 §38 정본 소유권·예약 완료판정 정합 | ❌ 다음 bringup/예약 21 전 |
| 예약 26 | 검토 §40.3 방법 B 계약 표면 재설계 | ❌ AI 라우터 표면 여는 묶음 |
| 예약 27 | 역할 B CUDA·JetPack 전제 | ❌ 역할 B 회의 |
| 역할 B V1 합의 | 커밋 `30c5e87` 원문 | ⏸ 회의 대기 |

## 이번 한 묶음 목표 — 검토 §39 코드 표면 보완 (예약 25 종결)

`d0_check.sh` 가 **기다린 적 없는 시간을 사실로 말하는** 것을 끝낸다. 기한 계약이
*"다음 `d0_check.sh` 현장 실행 전"* 이었고, 전기 시공이 끝나면 곧바로 현장 재실행이 오므로
**그 전에** 닫는다. 새 절차를 설계하는 회차가 아니라 **판정기가 관측한 것만 말하게 만드는**
회차다. 문서 묶음·현장 작업과 섞지 않는 별도 커밋이다.

## 완료조건

1. `grep -n "hard_timeout " tools/d0_check.sh` **8자리 전량**이 실측 rc·경과를 말하고,
   **상한 초를 사실로 주장하는 문장이 0건**이며, 그 대조표가 커밋에 남는다.
2. 합성 입력으로 ① 부정(토픽 부재) ② 역회귀(발행자 생존) ③ 반대 경계(rc=124 · 경과 ≈ 상한)
   가 **전부 실행**되고 **명령이 기록에 적혀 있다**(`TEST_GATES.md §7` 판정 도구 행).
3. `--secs 3`·`--secs 20` 두 방향에서 머리말·주석이 실제 상한과 어긋나지 않는다.
4. udev 주석이 규칙의 **실제 선택도**를 서술하고, 남긴 위험은 **전제조건 + 재개방 조건**
   형식으로 공개된다.
5. `bash -n` · `scan_unbounded_cli`(`d0_check.sh` 0건) · `bash tools/doc_check.sh` 전 항목 OK ·
   `bash tools/handoff_single_check.sh` OK.
6. 회귀 정본이 그대로다: pytest **182 passed** / colcon **245 tests** /
   harness guards **24 검사** / gate regression **14 케이스**.
   ⚠ 이번 변경 표면은 판정 도구·문서라 `TEST_GATES.md §7` 라우팅상 **직접 실행 대상은
   pytest·harness guards·`test_ai_context` 전량·`test_todo_scan`** 이다. colcon 245 와
   gate regression 14 는 `src/**` 무접촉이므로 재실행하지 않고 문서 수치 스윕으로만
   확인한다 — **재실행했다고 쓰지 않는다.**

## 보존해야 할 안전 불변조건

- 물리 E-stop 이 없거나 검사 8이 미확인인 동안 **`/cmd_vel` 발행·R0~R2·Nav2·미션 금지**.
  `Ctrl+C` 를 E-stop 으로 승격하지 않는다. **08-05 현재 전기 시공은 미착수다.**
- **`/estop/state` 는 이번 회차에 항상 `false`** 다. 이 토픽을 소비하는 로직을 만들지 않는다.
- 배선 작업 전 **메인 스위치 OFF + XT90 분리**, 배선을 만졌으면 **개폐 10회 재검증**.
- 판정기가 **관측하지 않은 것을 말하지 않는다** — 상한 초는 계약값 표기이지 경과 주장이 아니다.
  `--once` 실패를 '센서 두절'로 단정하지 않고 rc 로 원인을 가른다.
- 설정은 대상 소프트웨어가 실제로 읽는지 `param list`·`topic info -v`로 확인한다.
- N초 관측은 같은 관측자가 종료 상태와 **벽시계 경과시간**으로 창을 완주해야 한다.
- 토픽별 계약의 단일 출처는 `tools/bag_gap_report.py`의 `TOPIC_POLICY`다.
- `/odom`은 RELIABLE, `/imu/*`는 BEST_EFFORT다. EKF 소비자 QoS까지 함께 본다.
- Teensy 재연결 로직 없음, IMU 치명 실패 시 노드 자체가 안 뜸, 시각 역행 시 stamp가 사실상
  멈춘다는 펌웨어 전제를 유지한다(`REAL_ROBOT_VALUES.md §1-d`). **re-arm 래치는 아직 없다** —
  구현·부정/전환 시험 전 R1·R2 금지.
- 실차 없이 확인한 합성 bag·가짜 `ros2` 결과를 실차 통과로 승격하지 않는다.
- `TODO(D+0)` **11건**을 추측값으로 닫지 않는다 — 미해결은 이유와 재확인 시점을 적는다.

## 금지 범위

- 사용자 추가 승인 없는 `src/**` 변경.
- 예약 20·21·22·23·24·26·27, 역할 B V1, 관제 통보 표면, 깜빡임 원인 규명을 이 묶음에 혼합.
  특히 **예약 21(판정기 회귀 편입)** 은 harness 기준선 24 를 흔들므로 별도 묶음이다 —
  `d0_check.sh` 를 `scan_unbounded_cli` 기계 강제 범위에 넣는 것도 거기 속한다.
- `make_map.sh` 실런, 지도 자산 변경, Gazebo E2E 동시 실행.
- 가짜 `ros2` 픽스처 결과를 **실차 통과**로 기록하기, `d0_check.sh` 종료 2를 PASS로 기록하기,
  전기 시공이 미착수인데 E-stop 관련 항목을 닫은 것으로 기록하기.
- Claude가 구현한 이번 판정기 diff를 Claude가 최종 승인하기.

## 완료 판정

**`hard_timeout` 8자리 전량이 실측 rc·경과로 말하고 상한 초 주장이 0건이며, 부정·역회귀·
반대 경계가 명령과 함께 기록됐고, `--secs` 양방향에서 문장이 실제 상한과 일치하며,
`doc_check`·`handoff_single_check` 와 회귀 4수치가 전부 그대로면 이 묶음은 완료다.**
단, 완료는 **구현자 주장**이고 승인은 Codex 독립 검토다.

## 완료 후 다음 단계

1. **전기계통 시공**(사용자 직접) — `전기계통_최종작업안_0804.md §4` 완주 + 개폐 10회.
2. D+0 증거 복제(`JETSON_SETUP.md §11-g`) 후 `d0_check.sh` 현장 재실행 — 이번 보완으로
   실패 시 원인(상한 발동 vs 토픽 부재)이 화면에서 갈린다.
3. `TODO(D+0)` **11건** 중 남은 물리 E-stop, `/scan` 포함 판정, R0 watchdog 실측.
4. R0 watchdog 실측으로 `FREEZE_MANIFEST.md §6` 조건부 수용 재판정.
5. `docs/D1_FIRST_STEP.md`의 D+1 R3 rosbag 분석 → 예약 22와 R4~R8.

## 근거 문서

- 예약 25 전수 열거·실행 증거·구판 대조: `docs/MASTER_PLAN.md §7`, `§8`
- 검토 §39 인벤토리와 재검출 앵커: `docs/FREEZE_MANIFEST.md §10.13`
- 변경 표면별 검토 범위: `docs/TEST_GATES.md §7`
- 전기계통 사실·위험·최소 안전선: `docs/ELECTRICAL_BASELINE.md`
- 전기 시공 절차(BOM·결선·순서·검증): `~/Desktop/전기계통_최종작업안_0804.md`
- 실행 순서·TODO: `docs/JETSON_SETUP.md §9`, `§10`, `§11`
- 실차 값·펌웨어 전제: `docs/REAL_ROBOT_VALUES.md §1`, `§4`, `§5`
- 사다리·보류·유효 결정: `docs/MASTER_PLAN.md §3`, `§7`, `§8`
- 검토 전문: `~/Desktop/개발현황/CODEX 현황/0801검토현황.md §39`
