# CURRENT_HANDOFF.md — 지금 할 한 묶음 (매 세션 시작점)

> 작업 완료 시 구현자가 갱신하고 검토자가 사실관계·테스트 결과를 확인한다.
> 이전 묶음 내용은 날짜별 현황으로 보낸다. 항상 이 한 묶음만 유지.

- **동결 기준점**: `212885a8292d1e86677d5beeb9f5358d76fe9b40` = tag **`platform-core-freeze-260724`**
  (annotated, 원격 push 완료). 증거 전량 = `docs/FREEZE_MANIFEST.md`.
  ★ 회귀가 나오면 `git diff platform-core-freeze-260724 --stat` 이 1차 용의선상이다.
- **현재 기준 커밋**: 이 파일과 같은 커밋 (main)
- **현재 단계**: 마스터플랜 5단 완료 → **6단(역할 B V1 합의)은 ⏸ 보류**(역할 B 회의 일정 대기 —
  `MASTER_PLAN.md §1` 6) → 순서 밖 소묶음 **tunnel-bringup-s4**(예약 9 ✅) → 예약 10·12·14·15 ✅
  → **E2E 하네스 신뢰성 ✅**(예약 16 규명 · 예약 4 수리) → 외부 증거 파서 fail-closed ✅
  → **예약 17 봉쇄 ✅** → **검사기 트랙 ✅**(검토 §11~§20 — 서사 = `0730_현황.md §7`~`§15`)
  → **예약 18 = §25 결정 B 로 동결·종료**(§21~§24 4연속 P2 · 잔여 = `TEST_GATES.md §2` 상한 4·5)
  → **예약 16 `①`+`②′` = 구현 완료**(08-01 · **동결 예외** · 아래 '직전 완료')
  → **검토 §26** = 독립 재검토 대기 **(지금)** — 동결 예외 수리라 재검토를 생략하지 않는다.
- **⏸ 6단 보류 기록**: 준비된 YOLO V1 핸드오프 원문(목표·함정·완료조건·허용 범위 전량)은
  **커밋 `30c5e87` 에 고정 보존**했다. 역할 B 회의가 잡히면 **그 원문을 그대로 되살려 재개**한다
  — 새로 쓰지 말 것. 6단은 7단과 병렬이라 이 보류가 실차 트랙을 막지 않는다. 복원 명령:

```bash
git show 30c5e87:docs/CURRENT_HANDOFF.md            # 원문 확인
git show 30c5e87:docs/CURRENT_HANDOFF.md > docs/CURRENT_HANDOFF.md   # 그대로 되살리기
```

- **직전 완료 (08-01 예약 16 `①`+`②′` — 동결 예외 수리)**: 07-31 사용자 승인으로 열린
  동결 예외를 구현했다. 서사·증거 전량 = `0801_현황.md §1`~`§9`.
  - **고친 결함**: GUIDE 추종감시에서 **엄격한 `visible` 이 `last_seen` 을 쓰고 관대한 `lost`
    가 그걸 읽었다.** 검출이 깜빡이면 `visible` 은 한 번도 참이 안 되는데 `lost` 는 참이 되고,
    그때 `enter_search_back()` 이 `last_seen is None` 으로 **조용히 return** → 시도 횟수 0 고정
    → `give_up`('관제 보고: 추종자 확인 불가') **도달 불가**. 그동안 `/mission_state` 는 계속
    `GUIDE` 를 발행했다 = **"놓친 걸 알면서 돌아가지도 알리지도 않고 혼자 나간다."**
  - **`①`** `record_last_seen()` 의 `except Exception: pass` → throttle 로그 (**동작 변경 0**).
    **`②′`** 기록 조건을 **소비 술어(`lost`)와 같은 타이머**로 + `/scan` 두절 중 보류 +
    `last_seen is None` 진입이 예산을 소모해 보고 경로에 도달하는 안전망.
    ~~`③` `visible` 완화~~ 는 **열지 않았다** — `follower_monitor.py` **무변경**.
  - **★ 보완 전 FAIL 을 관측했다** (`AGENTS.md §3-7`): 부정 회귀를 먼저 쓰고 코드를 한 줄도
    안 건드린 상태에서 **5 failed, 6 passed** 를 눈으로 봤다. 보완 후 **11 passed**.
    E2E 에서 1/12 로만 보이던 증상을 단위층에서 **결정적으로** 재현했다 — 관측된 실패 로그가
    07-30 실측 문장(`마지막 목격 지점 없음 — 역행 불가, 유도 계속`)과 **글자까지 같다**.
  - **회귀 실행 (재실행 0회)**: pytest **170 passed**(159→170) · colcon **233 tests**(0f·3s) ·
    `mission_e2e` T자 **PASS**(SEARCH_BACK 15s) · `mission_e2e twin` 쌍굴 **PASS**(GATHER 74s ·
    SEARCH_BACK 14s · ESCAPED 169s) · `abort_e2e` **PASS**(실정지 0.0m). 순차 실행, 동시 실행 0.
  - **변경 범위**: 생산 코드 **`mission_node.py` 1파일 · 3곳** 뿐(기본 `git diff` 3 hunk ·
    `-U0` 로는 5 — 기록 조건 한 곳이 3조각). 동결 예외 사용 기록 =
    `docs/FREEZE_MANIFEST.md §10.1`.
  - **구현자가 공개한 자기 반증 4건** = `0801_현황.md §8` — 특히 **"한 번도 안 보인 추종자는
    여전히 갇힌다"**(§8.1)는 이번 범위 밖의 **미해결**이며 전제·재개방 조건을 함께 적었다.

## ★ 이번 회차 최우선 확인 사항 — **검토 회차다. 구현하지 않는다**

1. **이 묶음은 코드 변경이 목표가 아니다.** 직전 커밋(예약 16)에 대한 **독립 재검토(§26)**
   결과를 받는 것이 다음 행동이다. **동결 예외 수리이므로 재검토를 생략하지 않는다**
   (`AGENTS.md §1` — 구현자 ≠ 검토자).
2. **검토자는 별도 요청문 없이 이 파일부터 읽는다** (`AGENTS.md §2`). 요청은 **대상 커밋
   해시만** 주면 된다. 실행 범위 = `docs/TEST_GATES.md §7` — 변경 표면이 `mission_node.py`
   이므로 **`mission_e2e` + `abort_e2e`** 가 필수 E2E 다(전량 아님).
3. **공격 목록은 주지 않는다** (`AGENTS.md §5`). `0801_현황.md §8` 의 자기 반증 4건은
   *구현자가 못 덮었다고 신고한 것*이지 "여기만 보라"가 아니다 —
   **07-20 P1 2건은 전부 구현자가 "안전하다"고 논증한 경로에서 나왔다.**
4. **검토 결과 기록 위치** = `~/Desktop/개발현황/CODEX 현황/0729검토현황.md` 에 **§26 을
   이어서 추가**한다 (새 파일 금지).
5. ⚠ **`test_gate_regression` 간헐 실패(§24.5a)는 이 검토의 대상이 아니다** — 변경 표면이
   `mission_manager` 라 그 셸 게이트는 애초에 실행 대상이 아니다. **다음 `tunnel_bringup`
   묶음 착수 전에는 반드시** 규명한다.

## ★ 사용자 결정 — **이미 내려진 것들**

### 결정 A. 동결 예외 (예약 16) — **07-31 승인 · 08-01 소진**

> ✅ **07-31 사용자 승인 → 08-01 구현 완료.** 열린 범위는 `mission_node.py` 의 **`last_seen`
> 기록 경로**뿐이었고, 실제 diff 도 그 안(3 hunk)에서 끝났다. 사용 기록 =
> `docs/FREEZE_MANIFEST.md §10.1`.
> ⚠ **이 예외는 이번 묶음으로 소진됐다** — `mission_manager` 를 다시 열려면 **새 승인**이
> 필요하다. `visible` 술어 완화(③)와 `follower_monitor.py` 술어는 **여전히 금지**다.

### 결정 B. 순서 — **검토 §26 → (승인 시) 실차 S6**

> ✅ **08-01 갱신.** 검사기 트랙은 §20 승인으로, 예약 18 은 §25 결정 B 로 닫혔고,
> 예약 16 은 이번 묶음으로 구현이 끝났다. **남은 것은 그 구현에 대한 독립 판정 하나다.**
> ⚠ **"이미 덮였다"가 아니다** — `TEST_GATES.md §2` 검증 상한 4·5 와 `0801_현황.md §8` 의
> 재개방 조건이 걸리면 그 즉시 다시 연다.
> ⚠ 실차 S6(`MASTER_PLAN.md §3 S6`)는 인수일이 다가오면 **최우선**으로 돌아온다.

> **★ 외부 트랙 (07-31 발동)**: 구동부 실물 **인수일 확정** — 로봇·Jetson **일괄 수령(B안)**.
> ⚠ **R2 통과 선언으로 열린 것이 아니다.** 인수를 먼저 받고 **R3 부터** 시작하며, R0~R2 잔여
> 항목은 인수 후 공동 진행한다 (`REAL_ROBOT_VALUES.md §5`).
> 구동부 2차 회신 검토 전문 = `~/Desktop/TEENSY_실차연동_인수요청_및_2차회신검토_0731.md`.
>
> | 트랙 | 범위 | 상태 |
> |---|---|---|
> | **진행 중 묶음** | §26 검토 — 예약 16 커밋(`①`+`②′`)의 독립 재검토 | **지금** |
> | ~~검사기 트랙~~ | ~~§17→§18→§19 기준선/핸드오프 검사기~~ | ✅ **§20 승인·종료** |
> | ~~예약 18~~ | ~~`gate_fakes.py` IMU 주기~~ | ✅ **§25 결정 B 로 동결·종료** |
> | ~~예약 16~~ | ~~결정 A `①+②′`~~ | ✅ **08-01 구현 완료 — 판정 대기** |
> | **별건 3종** | ① `test_gate_regression` 간헐(§24.5a) ② 관제 통보 표면 부재 ③ 깜빡임 15초 원인 | ①은 다음 `tunnel_bringup` 착수 전 · ②③은 열어 둠 |
> | **실차 S6** | `MASTER_PLAN.md §3 S6` 준비물 6종 + `REAL_ROBOT_VALUES.md §5` 수령 | 병렬 |

## 이번 한 묶음 목표 — **검토 §26: 예약 16 커밋의 독립 재검토**

★ **구현 묶음이 아니다.** 구현자(Claude)는 이 회차에 `src/` 를 건드리지 않는다.

**검토자가 판정할 것 — 직전 커밋이 아래를 실제로 만족하는가:**

1. **부정 회귀가 진짜 게이트인가** — `test_search_back_entry.py` 가 생산 코드를 **흉내내지
   않고 실행**하는가(핸드오프 함정 8). 구현자는 진짜 `FollowerMonitor` + 진짜
   `MissionNode.tick()` + 진짜 `waypoints.yaml` 을 쓴다고 **주장한다**.
2. **보완 전 FAIL 이 사실인가** — 구판을 격리 사본(`git show <구판>:… `)으로 꺼내 같은
   회귀를 돌리면 정말 5건이 깨지는가. ⚠ **`src/` 와 `build/` 는 하드링크**다 — 저장소를
   건드리지 말고 격리 사본에서 돌린다(함정 3).
3. **`②′` 가 불변조건을 만족하는가** — *기록은 그 기록을 소비하는 술어와 같은 타이머를
   쓴다.* 그리고 **`visible` 안전 술어가 실제로 안 풀렸는가**(`follower_monitor.py` diff 0).
4. **안전망이 과하지도 모자라지도 않은가** — `last_seen is None` 이 예산을 소모하는 것이
   맞는 판단인가. 구현자는 "그 상태는 GUIDE 내내 TF 가 한 번도 안 풀린 것이라 회복 불가"
   라고 **주장한다**(`0801_현황.md §6.2`). 화재 클램프 포기(예산 미소모)와 다르게 다루는
   근거가 성립하는가.
5. **변경이 승인 범위를 안 벗어났는가** — `git diff` 로 `mission_node.py` 위 3곳 밖이
   없는지 확인 (`docs/FREEZE_MANIFEST.md §10.1`). ⚠ 구현자는 '3 hunk'라고 쓰지만 그건
   기본 context 기준이다 — `-U0` 로는 5조각이다. **개수가 아니라 자리를 본다.**
6. **자기 반증 4건이 정직한가** (`0801_현황.md §8`) — 특히 §8.1(한 번도 안 보인 추종자)이
   정말 범위 밖인지, 아니면 이번에 같이 닫았어야 하는지.

**실행 범위**: `docs/TEST_GATES.md §7` — 항상(pytest·colcon·doc_check) + **`mission_e2e`·
`abort_e2e`**. 조기 판정 규칙 적용(P0/P1 재현 시 남은 게이트 생략).

## 완료조건 (검토 §26)

1. 검토 결과가 `~/Desktop/개발현황/CODEX 현황/0729검토현황.md` **§26** 에 이어서 기록됐다.
2. 발견사항마다 `재현·근거 → 안전/동작 영향 → 한 문장 완료판정 → 최소 보완 방향 →
   필수 부정 회귀·역회귀` 가 적혔다 (`AGENTS.md §5`).
3. **승인이면** → 이 파일을 **실차 S6 준비물**로 전진시킨다.
   **불승인이면** → P0/P1 만 보완하고 재검토 1회(발산 시 `AGENTS.md §5` 탈출 조항).
4. 검토자는 `colcon test` 를 **직접 실행**한다 — `test-result` 만 읽으면 구현자가 남긴
   결과를 재독해하는 것이라 위조·환경의존이 검출되지 않는다.

## ★ 함정 (먼저 읽을 것)

1. **`gz model -p` 는 상한이 없으면 무한 행한다** (07-30 실측 — ⑪ 11분, 고아 21분).
   게이트가 안 끝나면 **먼저 `ps -eo pid,etimes,args | grep "[g]z model"`** 을 본다.
   게이트 5파일은 07-31 에 봉쇄됐다(예약 17). 아직 무방비인 곳 = `make_map.sh`·`accuracy_bench.sh`·
   `test_gate_regression.sh` — 정본 목록은 `python3 tools/scan_unbounded_cli.py <파일>` 출력.
2. **`pkill -f "…"` 를 손으로 치지 말 것** — 패턴이 자기 명령줄에도 매칭돼 셸 자신을 죽인다.
   스크립트는 PID 로 죽인다. 그리고 **서브셸을 죽여도 그 안의 CLI 자식은 고아로 살아남는다.**
3. **`src/` 와 `build/` 는 하드링크다.** 구판과 대조할 때는 저장소를 건드리지 말고
   `git show <hash>:<경로>` 로 **격리 사본**을 만들어 거기서 돌린다.
   ⚠ 이번 검토는 **구판 재현이 판정 항목 2**라 이 함정을 실제로 밟게 된다.
4. **Gazebo E2E 동시 실행 금지.** `tools/test_gate_regression.sh` 도 같이 돌리면 안 된다
   (정리 단계가 nav2·slam·ekf 를 전역 kill 한다 — `AGENTS.md §4`).
5. **"재실행하니 됐다"로 닫지 말 것.** 이 층은 분산이 크다 — 실측 GATHER 48~126s ·
   SEARCH_BACK 9~≈90s (`FREEZE_MANIFEST.md §8`). 08-01 실행은 **3건 모두 1회 PASS ·
   재실행 0회**였고, 그것은 "안정적이다"가 아니라 "보완 전과 같은 판정"이라는 뜻일 뿐이다.
6. **기준선**: pytest **170 passed** · colcon **233 tests** · gate regression **14 케이스** ·
   harness guards **24 검사**. ⚠ 이 줄의 수치는 `tools/gate_baseline_scan.py` 가 **전수로**
   대조한다 — 단위어(`passed`/`tests`/`검사`/`케이스`)를 떼면 검사 대상에서 빠진다.
   ⚠ **자리 수도 정확한 계약이다**(08-01 §19) — 값이 맞아도 자리를 새로 만들면 FAIL 한다.
   늘리거나 줄이려면 `gate_baseline_scan.py` 의 `GATES` 등록 수를 **같이** 고친다.
   동결 태그 **자체**의 colcon 수치는 165 로 불변 — 동결본과 대조할 때만 165 를 쓴다
   (`TEST_GATES.md §1`).
7. **가짜 센서 주기는 '주기 정본' 블록 한 곳에만 있다** (`gate_fakes.py`). 검사는 표기가
   아니라 **의존 폐포**를 보고, 스코프 판단은 **`symtable`(CPython 자신)** 에 묻는다.
   ⚠ **§25 결정 B 로 이 트랙은 닫혔다 — 검사기를 더 조이지 않는다.** 안 막히는 3표기
   (블록 안 `class _H: X = X - c` · `_r = globals` · `(_r,) = (globals,)`)는 공개돼 있으니
   **정본 블록에 그 세 모양을 쓰지 말 것.** 전제·재개방 조건 = `TEST_GATES.md §2` 검증 상한 4.
8. ★ **테스트가 생산 코드를 '흉내' 내면 그 테스트는 게이트가 아니다** (08-01 §21 P2-①).
   이번 신규 회귀(`test_search_back_entry.py`)가 이 선을 지켰는지가 **검토 판정 항목 1**이다.

## 완료 판정

**한 문장**: "**검토자가 독립적으로** ① 구판에서 부정 회귀 5건이 실제로 깨지는 것을 격리
사본으로 재현하고, ② 신판에서 11건이 통과하며, ③ `mission_e2e`·`abort_e2e` 가 PASS 이고,
④ 변경이 `mission_node.py` 의 그 3곳을 안 벗어났음을 확인하면, **예약 16 은 종결**하고
정본을 실차 S6 로 전진시킨다."

## 완료 후 다음 단계

- **승인 시** → **실차 S6 준비물**(`MASTER_PLAN.md §3 S6`) 6종 + `REAL_ROBOT_VALUES.md §5`.
  인수일이 다가오면 최우선이다.
- ⚠ **`test_gate_regression` 간헐 실패(§24.5a) 규명은 다음 `tunnel_bringup` 묶음 착수
  '전에' 별건으로** 한다 — 필수 게이트가 4회 중 3회 `13/14` 로 갈렸다. **잊고 넘어가면
  안 되는 항목**이다.
- ⚠ **관제 통보 표면이 없다는 갭은 여전히 열려 있다** — `give_up` 의 '관제 보고'는 실제로는
  로그다(`0801_현황.md §8.3`). 이번 수리로 그 경로에 **도달**하게 됐지만 관제가 자동으로
  알게 되는 것은 아니다. **별건으로 남긴다.**
- ⚠ **깜빡임이 15초 이상 지속되는 원인은 여전히 미규명**이다(`0801_현황.md §8.2`).
  `②′` 는 원인을 없앤 것이 아니라 **원인이 무엇이든 갇히지 않게** 만든 수리다.
  `①` 진단 로그가 다음 재현 때 그 원인을 결판낼 재료다 — 둘을 혼동하지 않는다.
- ⚠ **"한 번도 안 보인 추종자"는 여전히 갇힌다**(`0801_현황.md §8.1`) — `lost()` 술어
  변경이 필요해 이번 범위 밖이었다. **재개방 조건**: 실차 R3 rosbag 에서 GUIDE 진입 후
  검출이 끝까지 0인 구간이 **1회라도** 관측되면 동결 예외를 새로 신청한다.

**아래 두 트랙은 외부 대기이며, 풀리는 즉시 이 순서보다 우선한다** (서로 독립):
- 역할 B 회의가 잡히면 → **6단 재개**: 위 '⏸ 6단 보류 기록' 의 복원 명령으로 원문을 되살린다
- 구동부 트리거(R2 "3m 직진 3%" 선언)가 오면 → **7단 실차 R0~R8** (`MASTER_PLAN.md §3`).
  R0 실측에서 **cmd_vel watchdog** 결과를 받는 즉시 `FREEZE_MANIFEST.md §6` 조건부 수용을
  확정/재개방. R5 이전에 **라이다 장착 오프셋 실측 반영**이 선행돼야 한다.

## 보존해야 할 안전 불변조건

- **★ 기록·소비 술어 일치 불변조건 (08-01 신설 — 예약 16)** — *기록은 그 기록을 소비하는
  술어와 같은 타이머를 써야 한다.* 엄격한 술어가 쓰고 관대한 술어가 읽으면 **"관대한 쪽은
  발동했는데 엄격한 쪽이 한 번도 안 써준"** 구멍이 구조적으로 생긴다. 새 상태를 추가할 때
  이 짝을 먼저 확인한다 (`0801_현황.md §2.1` 전수 방법).
- **★ R0 watchdog 리마인더 (잊으면 위험이 조용히 남는다)**: 실차 7단 R0 에서 **cmd_vel watchdog(단절
  0.5s 내 정지)** 실측 결과를 받는 즉시 `FREEZE_MANIFEST.md §6` 의 잔류 cmd_vel 활주 **조건부 수용을
  확정 또는 재개방**한다. 이 항목은 계속 열려 있다.
  ★ 07-30 보강: `abort_e2e` 가 이제 그 두 경우를 자동으로 갈라 보고한다 — R0 판정 때 이 분류를 쓴다.
- **★ 08-15 플랜 B 판정일** (`MASTER_PLAN.md §6`) — 구동부 진척 주 1회 확인, 그날까지 R2 통과 선언이
  없으면 '구동부 지연' 행을 발동한다. **판정을 미루지 않는다.**
- **★ 라이다 장착 높이 미수령** (`MASTER_PLAN.md §7` 예약 11 · `REAL_ROBOT_VALUES.md §4`) —
  기구·센서 담당에게 **재요청이 필요한 사용자 액션**이다. 이것 없이는 R5 지도 제작이 무의미하다.
- **★ IMU 주기 조건부 수용 (08-01 신설)** — 41.63Hz 수용의 **전제**는 '평균 41.63Hz · 최대 간격
  30ms · σ 0.55ms 수준 유지'다. **재개방 조건 2가지**(구동부 요청 ② dt 회신 / R3 rosbag 에서
  최대 간격 33.33ms 초과)는 `REAL_ROBOT_VALUES.md §1` 에 있다. 하네스 주기도 같이 다시 판단한다.
- **★ 게이트 술어 불변조건** (`MASTER_PLAN.md §8` 07-30) — 조건 기동 게이트의 충족 판정은
  **"한 번 관측 = 통과" 금지**. 유효기간을 조이는 것은 보완이 아니다.
- **★ 세대 불변조건** (`MASTER_PLAN.md §8` 07-30) — **비동기 증거는 세대에 속한다.**
  서비스 소실을 관측하면 카운터뿐 아니라 **그때 날아가 있던 요청까지** 무효화한다.
- **★ 증거 수집 불변조건 (07-30 신설)** — 단언이 깨지는 순간의 증거는 **`fail()` 보다 먼저**
  수집한다. `fail()` 은 즉시 cleanup+exit 이라 정작 필요한 순간에 증거가 노드와 함께 사라진다.
- **★ 외부 CLI 상한 불변조건 (07-30 신설)** — `ros2` 든 `gz` 든 **예외 없이** hard 상한을 씌운다.
  **사람이 개입해서 얻은 PASS 는 판정이 아니다.**
- **★ 남은 검증 상한 (닫히지 않았다 — 조건부 수용)**: DDS 가 알아채기 전에(<1폴링) 서버가
  재시작하면 `service_is_ready()` 는 계속 True 라 게이트가 두 세대를 구분할 수 없다.
  `lifecycle_msgs/GetState` 응답에 세대 식별자가 없어 **이 계층에서는 원리적으로 못 닫는다.**
  - **수용 전제**: Nav2 lifecycle 서버는 0.5s 안에 죽었다 살아나지 않는다(관리 노드가 순차 전이)
  - **재개방 조건**: R0~R2 실차에서 Nav2 노드 crash-restart 가 **1회라도** 관측되면 재개방
- E2E cleanup 순서 불변조건(부모 `ros2 launch` 먼저 kill → `pkill -9 -f "lib/nav2[_]"`, 브래킷 트릭 —
  `AGENTS.md §4`)은 회귀 실행 시 그대로 지킨다.

## 금지 범위

- **재검토는 기본 read-only** — 사용자 명시 요청 전 코드 수정 금지 (`AGENTS.md §1`).
- **`src/mission_manager/**` 변경 금지 — 07-31 동결 예외는 08-01 예약 16 으로 소진됐다.**
  다시 열려면 **새 승인**이 필요하다. `visible` 술어 완화(③)·`follower_monitor.py` 의
  술어 자체는 **애초에 승인된 적이 없다** (`FREEZE_MANIFEST.md §10.1`).
- **`src/tunnel_bringup/**` 변경 금지** — `7a96f7f` 로 승인받았고 예약 18 로 주기 정본이 들어갔다.
  게이트 술어를 되돌리지 말 것. 주기 수치를 '주기 정본' 블록 밖에 다시 적지 말 것.
  ⚠ **§25 결정 B 로 주기 회귀·폐포 검사기는 동결됐다** — `TEST_GATES.md §2` 검증 상한 4 의
  **재개방 조건이 걸리기 전에는 `test_gate_fakes_periods.py` 를 더 조이지 않는다.**
- **`src/tunnel_sim/**` 변경 절대 금지** — 동결 대상(`docs/FREEZE_MANIFEST.md §3` 해시 고정)이자
  T자·쌍굴 회귀의 **살아있는 기준선**이다.
- **미실측 값을 채우는 것 금지** (전수 확인 `grep -rn "TODO: " src/tunnel_bringup/`).
  ★ 08-01 확장: 하네스의 **가짜 입력값도 같은 규칙**이다 — 실측이 없으면 '미확보'로 선언한다.
- **Jetson 실행·빌드 시도 금지** — 장비가 없다. 노트북 검증까지만.
- `make_map.sh` 실런 금지. Gazebo E2E 동시 실행 금지.
- 실패를 원인 한 줄 분류 없이 재실행 금지 (`AGENTS.md §3-6` · `TEST_GATES.md §5`).

## 근거 문서

`docs/MASTER_PLAN.md §3` · `docs/MASTER_PLAN.md §6` · `docs/MASTER_PLAN.md §7` ·
`docs/MASTER_PLAN.md §8` · `docs/REAL_ROBOT_VALUES.md §1` · `docs/REAL_ROBOT_VALUES.md §2` ·
`docs/REAL_ROBOT_VALUES.md §4` · `docs/REAL_ROBOT_VALUES.md §5` · `docs/TEST_GATES.md §1` ·
`docs/TEST_GATES.md §2` · `docs/TEST_GATES.md §7` · `docs/FREEZE_MANIFEST.md §3` ·
`docs/FREEZE_MANIFEST.md §6` · `docs/FREEZE_MANIFEST.md §10` · `0801_현황.md §1` ·
`0801_현황.md §8` · `0730_현황.md §2` · `0730_현황.md §16` ·
`~/Desktop/개발현황/CODEX 현황/0729검토현황.md` ·
`~/Desktop/TEENSY_실차연동_3차요청_0730.md` · `~/Desktop/TEENSY_실차연동_합의사항.md`
