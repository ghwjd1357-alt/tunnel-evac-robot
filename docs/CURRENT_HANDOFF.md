# CURRENT_HANDOFF.md — 지금 할 한 묶음

> 현재 묶음 + 미해결 보류만 유지한다. 구현자의 완료 주장은 독립검토 판정이 아니다.

- **동결 기준점**: `212885a8292d1e86677d5beeb9f5358d76fe9b40` = `platform-core-freeze-260724`.
  🔴 08-21 에 여덟·아홉 번째 예외(`FREEZE_MANIFEST §10.29`·`§10.30`).
  🔴 **08-22 에 열 번째를 열었다 — 펌웨어를 구웠다**(아래 §펌웨어. `FREEZE_MANIFEST` 미기재 = 부채).
- **저장소 기준**: 노트북 `main`. 🔴 **젯슨은 `314d3a0`(detached)** — 아래 §젯슨.
- **실제 보드**: 🔴 **08-22 16:20 재굽기.** `build=Aug 22 2026`. 승인 지문은 여전히 1.6.1 이라
  `firmware_precheck.sh` 는 `rc=1`(FAIL)이고 **그 상태로 구웠다**(의식적 무시).
- **항법 지도**: `jetson:~/Desktop/maps_0820/corridor_0820` (원점 오차 1 cm).
- **현재 단계**: §89(08-21 촬영준비) → **§90 = 08-22 실차 · 펌웨어 재교정**
  → 08-22~23 촬영. 🔴 **복귀 후 추가 촬영이 없다.**

## 이번 한 묶음 목표 — 08-22 (§90)

🔵 **08-21 이 "그대로 찍을 수 있느냐"에 답했다면, 08-22 는 "왜 못 찍었는가"에 답했다.**
하루를 고장 셋을 가르는 데 썼고, 셋 다 원인을 찾았다. **본편 ① 은 아직 못 찍었다.**

## 🔴 젯슨은 `314d3a0` 에 있다 — main 으로 복귀 금지

`314d3a0`(08-21 23:17) = **HOLD 도입 직전 커밋**이고, 그 안의 `mission_node.py` 는
08-21 10:47 이후 무변경 = **21:32 리허설을 `ESCAPED 446.5s` 로 완주시킨 그 코드**다.

🔴 **복귀하면 안 되는 이유** — 검토 §87→§88→§89→§90 **4회차, P0 가 아직 열려 있다**
(`safety_stop` 비동기 등록·deadline. §90 이 *"이 P0 계열의 **네 번째 우회로**"* 라고 적었다).
보완 `8213030`·`ad2b3e4` 도 **불승인**이다.

🔵 **`314d3a0` 은 그 P0 에 도달하지 않는다** — 코드로 확인했다:
- `HOLD`·`RESCUE`·`NO_VICTIM` **문자열 0건** (기능 자체가 없다)
- `BLOCKED` 는 있으나 `on_alarm` 에서 집결지–화재 거리가 `min_fire_dist`(1.5) 미만일 때만.
  대본 화재 `(12.5, −0.1)` → 집결지 `(10.50, −0.08)` = **2.0 m, 여유 0.5 m** → 안 뜬다.
- 🔴 **전제 = 사람이 지킨다. 알람을 대본 좌표로만 쏜다.** 탈출구 근처에 쏘면 경로가 짧아져
  집결지가 화재로 끌려오고 `BLOCKED` 가 뜬다 → 그때 P0 경로가 살아난다.

🔴 **검토–보완 사슬은 08-22 사용자 결정으로 보류**다. 사유: YOLO 합류가 무산돼 그 기능
(HOLD·사람 판정)을 이번 촬영에 쓸 수 없고, 4회차까지 매 보완이 새 구멍을 만들었다
(`AGENTS §6` 발산 조항). **재개 = 촬영 종료 후 별도 묶음.**

## 🔥 펌웨어를 구웠다 (08-22 16:20) — 검토·정본 없이

`teensy_integrated_base_v1_4.ino`:

| 상수 | 구 → 신 | 근거 |
|---|---|---|
| `ODOM_WHEEL_BASE` | 0.829 → **0.859** | 08-22 실측(아래) |
| `ODOM_WHEEL_RADIUS` | **0.05698 유지** | 줄자 2회가 상수를 지지 |
| `MAX_LINEAR_CMD` | 0.12 → **0.20** | 사용자 결정 |
| `MAX_ANGULAR_CMD` | 0.50 → **0.83** | 비례 |
| `MAX_WHEEL_CMD` | 0.15 → **0.30** | 🔴 최종 관문. 이것만 낮으면 위 둘이 무의미 |
| `WHEEL_KP` | 30 → **60** | 스톨에서 111/160 PWM 만 썼다 |
| `INTEGRAL_PWM_LIMIT` | 20 → **40** | 같음 |

**실측 근거** (줄자가 유일한 진실 — odom 도 IMU 도 자기 자로 자기를 재는 상황이었다):
```
직진 3 m ×2   odom/줄자 = 0.9936 · 0.9919   → 반지름 유지
제자리 ×3     odom|w|/IMU|w| = 1.0467·1.0394·1.0433 → BASE = 0.829×1.0431×0.9936 = 0.859
실효속도      2.588 m / 30 s = 0.086 m/s (명령 0.10 의 86%) — 정본 0.087 과 일치
```

🔴 **미검증 — 굽고 지면에서 아직 안 쟀다.** FF 계수 335 의 적합 구간은 재구성 PWM
54.6~66.5 뿐이라 **0.20 은 외삽**이다. `desired_linear_vel` 은 **0.15 부터** 시작하고,
0.15/0.18/0.20 세 점을 줄자로 잰 뒤에 올린다.

🔴 **전기 전제 파기** — `ELECTRICAL_BASELINE` 의 릴레이 **DC 25.2V 차단 정격 미증명(P0)**
조건부 수용은 *"`MAX_LINEAR_CMD 0.12` · `MAX_ANGULAR_CMD 0.50` 유지"* 를 전제로 선다.
**그 전제가 깨진 채 로봇이 돈다.** 끼임 전류 추정 **11A → 약 21A**(PWM 64→124).

## 🔴 08-22 부채는 **예약 62~70 으로 잠갔다**

검토 없이·정본 없이 구운 뒤 저녁에 정본을 맞췄고(`REAL_ROBOT_VALUES §1-n` ·
`MASTER_PLAN` 예약 40·§8 · `ELECTRICAL_BASELINE` 파기 배너 · `ai_known_p0_p1` 215건),
**남은 것은 전부 예약 번호를 받았다** — 번호만 따라가면 복귀 후에 집어들 수 있다.

```
62 펌웨어 사후 독립 검토      63 전기 전제 재작성(끼임 전류 실측 · 안전)
64 nav2 원본 height 누락      65 시뮬 2벌 같은 누락(E2E 사각)
66 카메라 반복 이탈           67 직선 복도 세로 미고정(좌표 ±1 m)
68 저장소 밖 자산 3종         69 runtime 전용값 정본화 + _db 머리말
70 예약 44 재정의(좌우 구동계 편차 — 예약 48 선결)
```
🔴 **62·63 이 안전 항목이다.** 속도 상한을 더 올리는 결정보다 먼저 닫는다.

## 🔴 08-22 실차 — 고장 셋을 갈랐다

**① 라이다 무성 사망 = 제미나이 카메라** — `urb -32`(disconnect 없음·헤드는 회전)는
EMI/전원 지문이고, 셋이 **버스파워 허브** 하나에 매달려 있었다. 카메라가 혼자 반복
재열거하며 5V 를 흔들었다. 뽑으니 안 죽는다. 08-21 "원인 미상" 이 닫혔다 → 예약 66.
🔵 안전망: 런치에 `on_exit=Shutdown`·`respawn` 이 없어 **라이다 노드만** 5초에 되살릴 수
있다(위치추정 유지). `jetson:~/lidar.yaml`.

**② 회전 붕괴(5.54→0.00 °/s) = 바퀴** — 전기·펌웨어·배터리·엔코더 전부 무죄였고 공중에선
잘 돌았다. 보강으로 **9.6 °/s**(08-21 최고 8.34 보다 좋다). 🔴 밸브가 없어 근본 수리
불가 — **재발 감시가 유일한 대비**다(`drive_health` 테이크 전 · `drive_watch` 테이크 중).

**③ 좌향 4.3% 드리프트 — 원인 미확정** — 180° 왕복이 바닥을 배제했고, 바퀴축 높이
실측은 반대 방향을 예측해 눌림도 배제했다. 예약 48 없이는 판별 불가 → 예약 70.

**④ Teensy USB 이탈은 고장이 아니다** — `~/s.sh` 뒤 전원 재투입이라는 정상 절차다.
🔴 런북 §1 에 그 단계가 없어 오늘 시간을 태웠다.

## 🔴 08-22 저녁 — 곡선 주행 A/B 여덟 시행 · `true` 유지

윗복도 → 연결통로 90° 코너를 **제자리 회전 없이** 통과할 수 있는지 A/B 했다.
전량·근거 = **`REAL_ROBOT_VALUES §1-n-4`** · 서사 = `0822_현황.md §7`.

🔴 **완료판정(벽 여유 ≥ 250 mm)을 여덟 번 중 한 번도 못 채웠다.** 같은 설정에서
220 mm 성공과 150 mm 실패가 갈려 **재현성이 없다.**
🎯 성패를 가른 건 속도가 아니라 **코너 진입 횡위치**(성공 y −0.27 / 실패 y +0.05)다.
🔴 코너 감속도 경유점도 처방이 아니었다 — 둘 다 실패했다.

🔵 **채택 = `use_rotate_to_heading: true` · `desired_linear_vel: 0.20`** (78 s · 정지
18회 × 0.23 s). 여덟 중 가장 빠르고 유일하게 반복 성공했다. 젯슨 파일을 노트북
커밋본으로 덮어써 **`true`·0.20·`0.6`·`false`·`0.6`·`max_obstacle_height 2.0` ×2** 로 맞췄다.

**부수 발견 둘** (정본 = `§1-n-5`·`§1-n-6`):
- 🔴 **사람이 통로에 서면 지도 밖으로 31 m 우회로가 생긴다** — 로봇이 180° 돌아 서진했다
  돌아왔다(60초 손실). `allow_unknown:false` 는 정상이고, **지도에 free 로 그려진 미답
  영역**을 쓴 것이다. 🎯 예약 55 keepout 배선의 실증 근거.
- 🔴 **절대 좌표 정밀도는 1 m 급이다** — `map→odom` 보정이 0.9 m 쌓였고, 20 m 직선
  복도에서 `slam_toolbox` 는 진행 방향을 고정하지 못한다. 대본 좌표가 ±1 m 밀린다.

## 🔴 촬영 방침이 바뀌었다 (08-22 사용자 결정)

```
① 본편 = 카메라 미결합 (camera:=false + 수동 /alarm)
② YOLO 카메라 화면 = 별도 촬영     ③ 편집에서 합친다
④ 촬영 중에는 카메라가 있는 것처럼 연출한다
```
🔵 **이 결정이 미해결 항목 대부분을 무효화한다** — 라이다 사망(원인=카메라)·카메라 수명
측정·어댑터·`person_gate` 전부 촬영 경로 밖으로 나간다.

🔴 지켜야 할 셋: ① **카메라를 달되 USB 는 안 꽂는다**(전류 0 이라 라이다 무해 + 외관 정합)
② **bag 에 `/alarm` 을 넣는다**(편집에서 "인식 시점"이 된다) ③ **화면 녹화 3종을 주행보다
먼저 켠다**(나중에 못 만든다).

🔴 **서술 금지 유지** — 짜깁기 영상은 연출이지 통합의 증거가 아니다. *"인지 통합을
완료했다"* · *"카메라로 인식해 대응했다"* 는 쓰지 않는다.

## 🔴 검토 §87~§90 P0 사슬 — **조건부 동결** (승인 아님)

4회차까지 P0 가 안 닫혔고 보완마다 같은 클래스가 다른 자리에서 재발했다(§90 = *"네 번째
우회로"*). `AGENTS §6` 발산 조항 대상이고, 위 촬영 방침으로 **그 코드를 실차에서 돌리지
않게 됐다.**

```
전제조건 (사람이 지킨다)
  · 젯슨은 314d3a0 에서 돈다 · HOLD·RESCUE·NO_VICTIM 부재(문자열 0건 확인)
  · BLOCKED 는 알람 (12.5,−0.1) 에서 집결지 거리 2.0 m > min_fire_dist 1.5 로 도달 불가
재개방 조건 (하나라도 발생 = 즉시 사슬 재개)
  · HOLD·사람 판정을 실차에서 쓰기로 결정 / 젯슨을 main 으로 복귀 / BLOCKED 1회라도 관측
```
⚠ **P0 는 살아 있다. 우리가 그 코드를 안 돌리는 것뿐이다.**

**🔴 동결 시점에 살아 있는 §90 발견 넷** — 미수정인 채 조건부 수용으로 등록했다
(`ai_known_p0_p1.json` · 앵커는 `goal_manager.py` 머리 주석 · 전문 = `~/Desktop/개발현황/CODEX 현황/0822검토현황.md §90`).
90.1·90.2 는 P0, 90.3·90.4 는 P1 이다. 위 전제조건이 이것들을 실행 경로 밖에 두는 것뿐이다.

## 완료조건

| # | 항목 | 통과선 |
|---|---|---|
| 1 | 🟢 구동계 진단 | ✅ 고장 3종 원인 규명 · 엔코더 4개 정상 |
| 2 | 🟢 펌웨어 재교정·굽기 | ✅ `build=Aug 22 2026` |
| 3 | 🟢 **지면 실측** | ✅ 0.15/0.18/0.20 = 0.130/0.169/0.175 m/s (`§1-n-2`) |
| 4 | 🟢 **nav2 젯슨 반영** | ✅ 노트북 커밋본으로 덮어써 6개 값 일치 확인 |
| 4-b | 🟢 **곡선 주행 A/B** | ✅ 여덟 시행 · `true` 유지 판정 (`§1-n-4`) |
| 5 | 🔴 **본편 원테이크 ×3** | **미착수. 오늘 최우선** |
| 6 | 🔴 M4 `low_west` 실측 | 리허설이 탈출구 가면 공짜 |
| 7 | 🔴 정본 6자리 + 사후 검토 | 위 §부채 |
| 8 | bag | 시행마다 교체 · 실패 시행도 남긴다 |
| 9 | 기준선 유지 | `TODO(D+0)` **11건** 종결 계약 불변 · `§7-c-E` **13행 전량 수행** |

## 완료 판정

🔴 **`firmware/**` 변경이 독립검토를 안 받았다.** 굽힌 뒤에도 안 받았다.
🔴 **`src/**` 의 HOLD·사람 판정 사슬은 4회차 불승인 상태로 보류**했다 — 승인이 아니라 보류다.
🔴 **본편 ① 이 없다.** 08-22 종료 시점에 쓸 수 있는 영상이 0 이다.

`TODO(D+0)` **11건** 과 `§7-c-E` **13행 전량 수행** 계약은 그대로다.

## 완료 후 다음 단계

1. 🔴 **지면 실측 → `desired_linear_vel` 확정 → 본편 ① 촬영.**
2. 🔴 **정본 6자리 + 펌웨어 사후 독립 검토.**
3. 🔴 **P0 사슬 재개**(§87~§90) — 촬영 종료 후 별도 묶음. 5회차는 P0 만.
4. 🔴 **카메라 하드웨어** — 케이블 교체부터. `usbcore autosuspend` 끄기도 미시도.
5. 🔴 **예약 48 바퀴별 엔코더 발행** — ③의 원인 규명에 필수. 다음 굽기.
6. 🔴 예약 58 `SEARCH_BACK` yaw 본수정 · 예약 55 keepout 배선 — 촬영 후.

## 금지 범위

- 🔴 **젯슨을 `main` 으로 복귀시키지 않는다** — P0 4회차 미종결. 필요하면 파일 단위로 옮긴다.
- 🔴 **알람은 대본 좌표 `(12.5, −0.1)` 로만 쏜다** — `BLOCKED` 도달 = P0 경로 활성화.
- 🔴 **시험·촬영 중 사람이 통로 안에 서지 않는다** — planner 가 31 m 우회로를 낸다(`§1-n-5`).
  E-stop 은 통로 밖에서 쥔다.
- 🔴 **`use_rotate_to_heading` 을 `false` 로 되돌리지 않는다** — 여덟 시행이 기각했다(예약 40).
- 🔴 **카메라·열화상 ESP32 를 꽂은 채 주행하지 않는다** — 라이다가 죽는다.
- 🔴 **`nav2_params_real.yaml` 원본 수정 금지** — 튜닝은 `_db.yaml` 사본으로만.
- 🔴 **`ekf_real.yaml` 수정 금지** — 바꾸면 역할 B 통보 의무.
- 🔴 **`MAX_*_CMD` 를 더 올리지 않는다** — 전기 전제가 이미 깨져 있다. 다음 상향 전에
  `ELECTRICAL_BASELINE` 전제 재작성이 선행한다.
- 🔴 **URDF 수정 금지** · **`waypoints.yaml`(시뮬 T자) 수정 금지**(회귀 184건이 쓴다).
- 🔴 **`lidar_port` 를 `/dev/ttyUSB0` 로 되돌리지 않는다** — 번호는 재열거마다 바뀐다
  (08-22 에 `ttyUSB0 → ttyUSB1` 로 옮겨갔다). 판단은 **`/dev/rplidar` 심링크**로 한다.
- 🔴 **스택 가동 중 로봇을 들어 옮기지 않는다.** 손으로 밀 수도 없다(감속비).
- 🔴 **`inflation_radius` 를 0.40 밑으로 내리지 않는다.**
- 🔴 **예약 44 전제 유지** · **08-18 이전 구동 실측 인용 금지.**
- 🔴 **08-22 이전 bag 은 `--pre-0822`(윤거 0.829)로 푼다** — 새 값으로 풀면 `kR` 이
  0.525 → 0.507 로 조용히 어긋난다. `bag_drive_report.py` 에 플래그를 넣어뒀다.
- `TODO(D+0)` **11건**을 추측으로 다시 열거나 닫지 않는다.
- **⚠ 미해결 보류 — 예약 23 / 검토 §33 (`8fcc1a2`)**: P0 0 · **P1 1** · P2 1.
  `handoff_single_check.sh` 는 `§` 없는 새 작업 꼬리를 붙여도 `OK` 를 내고, 숫자 계약 전수
  증거는 기록 6자리와 독립 실행 21자리가 불일치한다. **승인 완료로 읽지 말 것.** 런타임
  `src/**` 변경·회귀는 없으므로 현재 D+0 실행은 막지 않되, 보완은 다른 묶음에 섞지 않고
  `MASTER_PLAN.md §7` 예약 23 의 완료판정과 부정 회귀로 별도 구현한다. 전문 =
  `~/Desktop/개발현황/CODEX 현황/0801검토현황.md §33`.
- **현재 미해결 보류**: 예약 36·39·**40**·41-g·43·**44**·46·**47**·**48**·49·**50**·
  51·**53**·**54**·**55**·**58**·**60**·**61** · 🆕 **62~70** 과 §72.2·H′ ·
  **검토 §87~§90 P0 사슬**.

## 진행표

| 트랙 | 묶음/근거 | 상태 |
|---|---|---|
| **진행 중 묶음** | §90 — 🟢 고장 3종 규명 · 🟢 펌웨어 · 🟢 실측·곡선 A/B · 🔴 **본편 미촬영** | 🔶 5/9 |
| **곡선 주행 (예약 40)** | ✅ **A/B 여덟 시행으로 판정 종결** — `true` 유지 (`§1-n-4`) | ✅ |
| **촬영 방침** | 🔵 **08-22 변경** — 카메라 미결합 + 짜깁기. YOLO 는 별도 촬영 | ✅ 확정 |
| **좌향 4.3% 드리프트** | 🔴 원인 미확정 — 바닥·눌림 배제됨. **예약 70·48** (`§1-n-3`) | 🔴 |
| **08-22 백로그** | 🆕 **예약 62~70 신설** — 펌웨어 검토·전기 전제·height 누락·카메라·좌표·자산 | 📋 등록 |
| **펌웨어 1.6.4(가칭)** | 🔥 **08-22 16:20 굽힘** · 🔴 검토 0 · 정본 6자리 부채 | 🔴 미검증 |
| **라이다 무성 사망** | ✅ **원인 규명 = 카메라 + 버스파워 허브** · 우회 확보 | ✅ |
| **구동계 회전 붕괴** | ✅ **원인 = 바퀴** · 보강으로 9.6 °/s | 🔶 밸브 없음 = 재발 대비만 |
| **좌향 드리프트 4.3%** | 🔴 **원인 미확정** — 바닥 아님. 예약 48 필요 | 🔴 신설 |
| **카메라(제미나이)** | 🔴 **자체 고장 — 반복 이탈.** 오늘·내일 사용 불가 | 🔴 신설 |
| **검토 P0 사슬 §87~§90** | 🔴 **4회차 불승인 · 사용자 결정으로 보류** | ⏸ |
| **어댑터** | 🟢 노트북 회귀 통과 · 🔴 실물 0회 (카메라 불가) | ⏸ |
| **역할 B / YOLO** | 🔴 **합류 무산** — 카메라 고장 | ⏸ |
| `TODO(D+0)` **11건** | ✅ 종결 계약 유지 |

## 최종 회귀

- 기준선(**08-23 갱신** — §91 P0-2 +2 · P0-3 거동고정 +2 · P2-1 라이다등가 +3): pytest **273 passed** / adapter **120** / tools **349 + skip 1** /
  colcon **460 tests, 0 fail, 3 skip** / **test_harness_guards 24 검사** /
  test_gate_regression **14 케이스**.
- 🔴 `colcon` · `doc_check.sh` 는 **이번 묶음에서 재실행하지 않았다** — 정본 부채 때문에
  `doc_check` 는 지금 통과하지 않는 것이 정상이다.
- 🔴 `firmware_precheck.sh` = **FAIL**. 승인 지문 미이관 + 08-22 변경. 굽고 나서도 그대로다.
- 🎯 회귀가 잡은 것: `ODOM_WHEEL_BASE` 를 베껴 쓴 도구 **3자리**(`drive_health`·
  `bag_drive_report`·`drive_encoder_check`)를 `.ino` 대조 테스트가 전부 지목했고,
  고친 뒤에는 **08-21 픽스처가 새 자로 어긋나는 것**을 또 잡았다 → `WHEEL_BASE_PRE_0822` 신설.

## 역할 교대

§90 의 구현자 = **Claude**(펌웨어·곡선 A/B) + **Codex**(오후 속도·장애물, `AGENTS §5` 교대).
🔴 **양쪽 다 독립 검토를 안 받았다.** 🔴 **§87~§90 P0 사슬은 조건부 동결이지 승인이 아니다** —
Codex 가 검토한 사슬이므로 Codex 가 그 구현자가 될 수 없다.
🔴 **검토자 미지정 — 아직 아무도 안 봤다.**
08-22 저녁부터 **Codex 가 실차 묶음의 구현자로 교대**한다(사용자 명시 요청, `AGENTS §5`).
🔴 조건: ① 자기 diff 를 자기가 승인하지 않는다 ② 완료판정을 여기 남긴다 ③ 클래스 전수
④ 묶음 경계에서 원위치. **단 §87~§90 P0 사슬은 Codex 가 구현자가 될 수 없다**(자기 지적).

🔴 **감추지 않는 것**: ① 펌웨어가 미검증·미문서 상태로 실차에서 돈다 ② 전기 전제가 깨져
있다 ③ 좌향 드리프트 원인을 모른다 ④ 카메라가 고장이라 인지 계통 전체가 실물 0회다
⑤ 본편 ① 이 없다 ⑥ P0 사슬은 해결이 아니라 회피(구버전 운용)다.

## 근거 문서

- `REAL_ROBOT_VALUES §1-l`(08-20) · **`§1-m`**(08-21) — 🔴 **08-22 절은 아직 없다(부채)**.
- `PITFALLS §14`(모터·케이블 EMI 판별표 3종) · **`§17`**(조용한 실패 4종) · `§15` USB 식별.
- `MASTER_PLAN §7` 예약 44·48·57·58·60·61 · `§8` 결정 색인.
- `~/Desktop/개발현황/CODEX 현황/0822검토현황.md` **§87~§90** — P0 사슬 전문.
- 운영: `~/Desktop/0822_촬영_명령묶음.md` · `0822_촬영_샷리스트.md` · `0822_작업계획.md`.
- 젯슨 로컬 자산: `~/lidar.yaml`(라이다 단독 재기동) · `~/wp_shoot.yaml`(순찰 서쪽 앵커 제거).
