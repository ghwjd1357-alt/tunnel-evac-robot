# CURRENT_HANDOFF.md — 지금 할 한 묶음

> 현재 묶음 + 미해결 보류만 유지한다. 구현자의 완료 주장은 독립검토 판정이 아니다.

- **동결 기준점**: `212885a8292d1e86677d5beeb9f5358d76fe9b40` = `platform-core-freeze-260724`.
  🔴 **08-21 에 여덟 번째·아홉 번째 예외를 열었다**
  (`FREEZE_MANIFEST §10.29` 런치 2건 · `§10.30` 검토 §82 반영 2건).
- **저장소 기준**: `main`. §83(08-20 밤 지도 재촬영) 위에서 시작한다.
- **실제 보드**: `rearm-latch-pi-link-probe-1.6.3` `build=Aug 18 2026 12:32:35`.
  🔴 승인 지문은 여전히 1.6.1 이라 셋을 섞지 않는다. `firmware_precheck.sh rc=1` 이 정상.
- **항법 지도**: `jetson:~/Desktop/maps_0820/corridor_0820` (원점 오차 1 cm).
- **현재 단계**: §84(08-21 새벽 노트북 전량) → **§85 = 독립검토 반영 묶음**
  → 08-22~23 촬영. 🔴 **복귀 후 추가 촬영이 없다** — 못 찍은 것은 영상에 안 들어간다.

## 이번 한 묶음 목표 — 08-21 (검토 §85 대상)

🔵 **08-20 이 "되느냐"에 답했다면, 08-21 은 "그대로 찍을 수 있느냐"에 답한다.**

🟢 **08-21 새벽에 노트북 작업 전량이 끝났고, 그 위에 독립검토 §82 를 받아 전량 반영했다.**
남은 것은 **로봇 앞에서만 되는 것**이다.

🔴 **검토 §82 (Codex GPT-5) — P0 0 · P1 7 · P2 2. 9건 전부 닫았다.**
그중 2건이 동결 안쪽이라 **아홉 번째 예외**를 열었다(`FREEZE_MANIFEST §10.30`).
🔴 **구현자 논증 5건 중 2건이 기각됐다** — *"별도 패키지라 회귀 위험 0"* 은 껐을 때만
참이었고(켜면 `/alarm`·TF·미션 상태가 공유다), *"detect_range 는 지렛대가 아니다"* 는
분모가 만든 결론이었다. 판정·재현·정책은 `MASTER_PLAN §8` 마지막 8줄이 소유한다.

🔵 **네 건은 우리 손으로 먼저 재현했다** — apply_measurements 원본·백업 동시 오염,
ConfirmTracker 가 같은 프레임 5장을 확정, `confidence=NaN` 이 best 로 채택,
탈출구 옆 화재에서 집결지가 화재 0.50 m. **검토 결과도 재현 후 수용이다.**

```
🟢 새벽(노트북)  어댑터 17/17 · waypoints 실측본 · 도구 10종 · 관제 인지패널(실행 검증)
                 · 금/토 명령 묶음 2벌 · 샷 리스트 · 동결 예외 여덟·아홉 번째
                 · 검토 §82 전량 반영 (신규 회귀 43건)
🔴 금 오전       배포 → 측정 4종 (M1 선형 불감대 · M2 180° 선회 · M3 사람폭 · M4 서쪽 좌표)
🔴 금 오후 A     리허설 (카메라 없이) ← 본편 보증
🔴 금 오후 B     역할 B 합류 · 장착 · YOLO · 어댑터
🔴 토 09:00      무슨 상태든 촬영 개시
```

### 🔴 관문 — 시각을 지킨다. 판단을 그 순간에 하지 않는다

| | 시각 | 통과 조건 | ❌ 실패 시 |
|---|---|---|---|
| **D** | 즉시 | 🔴 **Jetson 배포** — `git pull` + `colcon build` · 패키지 2 + yaml 3 | 🔴 **오후 전체(G4~G6)가 성립하지 않는다** |
| **G0** | 즉시 | `/dev/rplidar` 존재 · serial `78824f27…` | 규칙 재설치(1분) |
| **G1** | 12:00 | 측정 4종 | 오후로 밀고 **도전 포기** |
| **G2** | 🔴 15:00 | **미션 전구간 리허설 1회 성공** | 금요일 전부를 리허설에 |
| **G3** | 16:30 | 장착 + 라이다 시야 · 위치추정 | 🔴 **카메라 떼고** 진행 |
| **G4** | 18:30 | YOLO 합류 · `/detections` · 부하/EMI | **USB 뽑고** 복구 |
| **G5** | 20:30 | 어댑터 좌표 정확 | 관제 **수동 클릭** 후퇴 |
| **G6** | 22:00 | 어댑터 포함 리허설 | 수동 클릭 본편 |

🔵 **G3~G6 은 전부 "뽑으면 08-20 검증 상태로 복구"** 다. 잃는 것은 시간뿐이다.
🔴 **G2 만 성격이 다르다** — 막히면 도전이 아니라 **본편이 위험**하다.

### 🔴 08-21 새벽이 만든 새 사실 넷

- **`SEARCH_BACK` 의 180° 턴은 유도 방향이 서쪽일 때만 난다** (예약 58).
  역행 목표 yaw 가 `0.0` 하드코딩이고 목표 위치는 로봇 자기 좌표다.
  → **탈출구를 아래 복도 서쪽으로** 잡아 회피했다. 코드 미변경.
- **2차 화재 `reset` 우회를 철회했다.** `reset` 은 임무를 통째로 초기화해
  **유도 중인 사람을 잃는다.** → 소품 배치 + 관제 팝업으로만 표현한다.
  🔵 이 방식이 *"다중 화재를 스스로 처리한다"* 는 주장을 아예 안 하게 만든다.
- **열화상 ESP32 가 라이다와 같은 CP2102 다** (역할 B 6차 §6). `ttyUSB` 번호를
  서로 뺏고 **에러를 안 낸다.** 라이다가 죽으면 **사람 추종까지** 죽는다.
  → 동결 예외 여덟 번째로 런치 기본값을 `/dev/rplidar` 로 바꿨다.
- **실효속도 0.087 m/s (4시행)** → 대본 전체 **약 440초**.
  🔴 08-20 밤 구현자 보고 *"약 305초"* 는 **틀렸다** — 순찰·집결·서치백이 빠져 있었다.

## 완료조건

| # | 항목 | 통과선 |
|---|---|---|
| 1 | 🟢 **어댑터** | ✅ 전구간 13/13 · 단위 25/25 · 거부 경로 9종 전량 차단 |
| 2 | 🟢 **미션 좌표**(예약 53) | ✅ H자 실측 재작성 · 13/13 좌표가 통행가능 영역 안 · 🔴 `low_west` 는 M4 대기 |
| 3 | 🟢 도구 | ✅ 선형 불감대 · 부호전환 · 클러스터폭 · keepout 마스크 |
| 4 | 🔴 **M1 선형 불감대** | `D_lin < 0.12` 이고 `guide_speed` 를 그 위로 띄울 수 있는가 |
| 5 | 🔴 **M2 180° 선회 ×3** | 부호전환 <10회/10초 · 30초 안에 180° 완주 |
| 6 | 🔴 **M3 사람 클러스터 폭** | 🔴 **절차가 바뀌었다** — 사람 bag + **대조군 bag** 두 벌. 여유(사람−대조군)가 1.0 근처인 문턱이 있는가. 근거 = `REAL_ROBOT_VALUES §1-l-13` |
| 7 | 🔴 M4 아래복도 서쪽 좌표 | `tf2_echo` 실측 |
| 8 | 🔴 **리허설** | 확정 대본대로 완주 1회 · 무장 해제 0회 · 🔴 **사람 1명 필수** |
| 9 | 🔶 예약 55 keepout | 🟢 마스크 생성 완료 · 🔴 **Nav2 필터 배선 미실행**(런치 노드 2종 필요) |
| 10 | 🔴 G3~G6 | 각 관문 판정 기록 |
| 11 | bag | 시행마다 교체 · 실패 시행도 남긴다 |
| 12 | 기준선 유지 | `TODO(D+0)` **11건** 종결 계약 불변 · `§7-c-E` **13행 전량 수행** |

## 완료 판정

🔴 **이 묶음의 절반은 새벽에 끝났고 절반은 통과선이지 결과가 아니다.**
🔴 **새벽분은 독립검토를 안 받았다** — `src/**` 변경이 셋이다:
① `perception_adapter` **신규 패키지**(기존 코드 무변경 · 별도 노드)
② `tunnel_bringup/launch/**` **2파일**(동결 예외 여덟 번째)
③ `mission_manager/config/**` **yaml 3벌 신규**(기존 `waypoints.yaml` 무변경).

🔴 **어댑터 시연은 합의사항 §9 사다리 2단계 → 5단계 점프다.** 3·4단계(rosbag
depth 유효율 · Jetson 실시간)를 반복 시험한 적이 없다. 08-21 일정이 압축판이지만
**각 단계를 한 번씩만** 본다. → **"1회 시연"이지 "통합 완료"가 아니다.**
성공해도 `mission-v1-freeze` 로 올리지 않는다.

직전 묶음(§83)의 결과는 **정본이 소유한다** — `REAL_ROBOT_VALUES §1-l`(재촬영·
6점 실측·경로 4시행·실효속도) · `MASTER_PLAN §7` 예약 57 · `PITFALLS §6`.

`TODO(D+0)` **11건** 과 `§7-c-E` **13행 전량 수행** 계약은 그대로다.

## 완료 후 다음 단계

1. 🔴 **08-22~23 촬영.** 샷 리스트 = `~/Desktop/0822_촬영_샷리스트.md`.
2. 🔴 **예약 58 본수정** — `SEARCH_BACK` yaw 를 `로봇 yaw + π` 로. Tier A ·
   `mission_manager` 회귀 184건 + 독립검토 선행. **촬영 후.**
3. 🔴 **예약 55 배선** — costmap 필터 노드 2종을 런치에 넣는 일이 남았다.
   동결 예외를 또 열어야 하므로 **촬영 후 별도 묶음**.
4. 🔵 **`/thermal/*` 계약 3종**(QoS·frame_id·실패 표현) — 9월 V1.1.
5. 🔴 예약 51(구동부 FF)은 **착수 사유가 없다**(`D` 여유 52%).

## 🔴 일정

**촬영 = 08-22(토)~08-23(일). 🔴 복귀 후 추가 촬영 없음.**
대본 전체 **약 440초(7.4분)** — 실효속도 0.087 m/s × 33.1 m + 정지·회전 73초.
⚠ 이 값은 **계획용**이다. `guide_speed` 가 M1 으로 바뀌면 같이 바뀐다.

## 금지 범위

- 🔴 **`nav2_params_real.yaml` 원본 수정 금지** — 튜닝은 `_db.yaml` 사본으로만.
  값 4개 차이 + 신규 키 4개(`obstacle_layer.scan.{marking,clearing}` × local/global).
- 🔴 **`ekf_real.yaml` 수정 금지** — 바꾸면 역할 B 통보 의무. 예약 41·54 미개입.
- 🔴 **새 펌웨어 굽기 금지** — 예약 51 은 별도 Tier A 묶음 + 독립검토 선행.
- 🔴 **URDF 수정 금지** — 카메라 TF 는 `perception_adapter/launch` 의
  `static_transform_publisher` 로 붙인다(동결·URDF 둘 다 피한다).
- 🔴 **`mission_node.py` 수정 금지** — 예약 58 포함. 어댑터는 별도 노드다.
- 🔴 **`waypoints.yaml`(시뮬 T자) 수정 금지** — 회귀 184건이 이 파일을 쓴다.
  실차는 `waypoints_real_H.yaml`.
- 🔴 **`lidar_port` 를 `/dev/ttyUSB0` 로 되돌리지 않는다** — ESP32 와 번호를 뺏는다.
  되돌려야 할 상황이면 번호를 **눈으로 확인**하고 인자로만 준다.
- 🔴 **`/thermal/*` 을 게이트에 넣지 않는다** (역할 B 6차 §2-b 요청 수용).
  열화상이 없다고 로봇 전체가 안 뜨면 안 된다.
- 🔴 **스택 가동 중 로봇을 들어 옮기지 않는다.** 손으로 밀 수도 없다(감속비).
  이동 = ① Nav2 goal ② 스택 내리고 옮긴 뒤 재기동, 둘뿐.
- 🔴 **매핑 중 teleop 기본값 금지** — `-p speed:=0.10 -p turn:=0.35 -p repeat_rate:=10.0`.
- 🔴 **`inflation_radius` 를 0.40 밑으로 내리지 않는다.** 0.9 로 1.5m 통로를 통과했다.
- 🔴 **예약 44 전제** — 우전륜 유격. ⓐ `ODOM_WHEEL_RADIUS`·윤거·FF/게인을
  08-17/18 숫자로 바꾸지 않는다 ⓑ 예약 39·40 을 이 데이터로 판정하지 않는다
  ⓒ C10 **57.64mm** 를 확정값으로 인용하지 않는다.
- 🔴 **08-18 이전 구동 실측을 인용하지 않는다** — 판재 천공·PCB 이식 **전** 값이다.
- 🔴 **예약 46 전제** — `dmesg_coverage_check.py` rc=0 만으로 커널 무사고 확정 금지.
- 🔴 **조건부 수용은 정본이 소유한다** — 예약 46 잔여 ⓐⓑ · 1.6.3 publisher 10/10 ·
  pulse slot 400ms. 재개방 = 실기 `y=7` 1회라도 / 새 telemetry 토픽 1개라도.
- 🔴 **예약 50 조건부 수용 P1 7건.** 🔵 50-4 종결 · 🔵 `normal_speed` 는
  `waypoints_real_H.yaml` 에서 0.10 으로 내렸다(구 0.26 = 실차 상한의 2.17배).
- **⚠ 미해결 보류 — 예약 23 / 검토 §33 (`8fcc1a2`)**: P0 0 · **P1 1** · P2 1.
  `handoff_single_check.sh` 는 `§` 없는 새 작업 꼬리를 붙여도 `OK` 를 내고, 숫자 계약 전수
  증거는 기록 6자리와 독립 실행 21자리가 불일치한다. **승인 완료로 읽지 말 것.** 런타임
  `src/**` 변경·회귀는 없으므로 현재 D+0 실행은 막지 않되, 보완은 다른 묶음에 섞지 않고
  `MASTER_PLAN.md §7` 예약 23 의 완료판정과 부정 회귀로 별도 구현한다. 전문 =
  `~/Desktop/개발현황/CODEX 현황/0801검토현황.md §33`.
- **현재 미해결 보류**: 예약 36·39·**40**·41-g·43·**44**·46·**47**·**48**·49·**50**·
  51·**53**·**54**·**55**·**58**·**60** 과 §72.2·H′.
- `TODO(D+0)` **11건**을 추측으로 다시 열거나 닫지 않는다.
- 🔴 **예약 41-g 진입 금지 문장 유지** — R4/R6 **판정**은 `R3 PASS` AND 41-g 완료 뒤.

## 진행표

| 트랙 | 묶음/근거 | 상태 |
|---|---|---|
| **진행 중 묶음** | §85 — 🟢 노트북 전량 + 검토 §82 반영 완료 · 🔴 잔여 = 측정·리허설·합류 | 🔶 3/12 |
| **어댑터** | 🟢 **신규 패키지 · 13/13 · 25/25** · 🔴 실물 미검증 | ✅ 노트북 |
| **시나리오** | 🟢 A(H자) · 🔴 **08-21 대본 3건 변경**(탈출구·2차화재·인원) | ✅ 확정 |
| **자율주행 실증** | 🟢 연결통로 1.5m 통과 · 12.414 m SUCCEEDED | ✅ |
| 예약 39 좌향 | 🔵 결함 아님 — 시행마다 달라지는 값 |
| 예약 40 각속도 | 🔵 되먹임 덫 — `D` 여유 52%, Nav2 로 푼다 |
| 예약 43 R3 | 🟢 조건부 종결 · 🔴 전제 = 케이블 배치 · **카메라 미합류** ← 08-21 오후 깨진다 |
| 예약 44 우전륜 | 🔶 유격 미해결 · 🔵 엔코더 응답 정상 |
| 예약 49 라이다 yaw | 🔶 0.13°±0.19° = 0 과 구분 불가 |
| **예약 53 미션 좌표** | 🟢 **재작성 완료** · 🔴 판별 파라미터 3종 + `low_west` 는 실측 대기 |
| **예약 55 keepout** | 🔶 **마스크 생성 완료** · 🔴 Nav2 배선 미실행 |
| **예약 56 2차 화재** | 🟢 **우회 확정** — 상태머신 미개입. `reset` 철회 |
| **예약 57 정지선회 진동** | 🔴 M2 재현 대기 · 🟢 판정 도구 신설 |
| **예약 58 SEARCH_BACK yaw** | 🔴 **신설** — 탈출구 서쪽으로 회피. 본수정은 촬영 후 |
| **예약 59 udev 미사용** | ✅ **종결** — 동결 예외 여덟 번째 |
| **예약 60 depth 미검증** | 🔴 **신설** — 어댑터 실패 시 1순위 용의자 |
| **역할 B** | 🟢 6차 회신 = 회신 종결본. 🔴 **금 오후 합류** · depth FATAL 미구현 |
| `TODO(D+0)` **11건** | ✅ 종결 계약 유지 |

## 최종 회귀

- `bash tools/doc_check.sh` rc=0.
- 기준선 표기: pytest **191 passed** / colcon **252 tests** /
  **test_harness_guards 24 검사** / test_gate_regression **14 케이스**.
- tools pytest **251/251 + skip 1**, mission pytest **191/191**.
- 🟢 `perception_adapter` 단위 **38/38** · `tools/e2e_adapter.py` **17/17**
  (좌·우·정면 optical 축 + 존재하는 엉뚱한 frame 거부 포함).
- 🟢 **§82 신규 회귀** — 미션 S1-3 **7** · 어댑터 tracker/파라미터/confidence **13** ·
  optical 회전 **9** · 게이트 사슬 구조 **5** · 판별 연속성 **8** · 런북 지문 **7**.
- 🔴 **link-stall harness 3단**: MCU **367** · 호스트 분류 **23**(9종) · 구조 **14**.
- re-arm host **989/989 + 구조 11/11**, runtime/watchdog **11/11**,
  firmware-info 상한 **1418/1664**. 알려진 P0/P1 코퍼스 **168/168**.
- firmware precheck 실제 저장소는 승인 지문 미이관으로 **rc=1 이 정상**.

## 역할 교대

§85(08-21, 검토 §82 반영)의 구현자 = **Claude**. 🔴 **이번 묶음의 검토 대상**:

ⓐ **동결 예외 아홉 번째의 범위 2건이 최소인가** — 게이트 직렬화가 하류를 안 바꾼다는
   주장의 근거는 구조 검사 5건뿐이고, `camera:=true` 실차 실행은 0회다.
ⓑ **S1-3 을 "거부" 로 만든 정책 판단** — 안전한 집결지를 못 만들면 자동 출동하지
   않고 관제로 넘긴다. 대안(대체 탈출구·집결지 재탐색)을 안 만든 것이 옳은가.
ⓒ **`min_fire_dist` 미선언 시 검사를 건너뛴 것** — 거동 불변을 위해서지만,
   "선언 안 하면 안전 요구가 없다" 는 해석이 맞는가.
ⓓ **촬영시각 TF 실패 시 최신 TF 로 후퇴하는 기본값(`allow_latest_tf_fallback=True`)** —
   격하 표시는 하지만, 촬영에서 조용히 나쁜 좌표를 쓸 여지를 남긴 것이 옳은가.
ⓔ **M3 판정 기준(대조군 run 0 · 각 거리 run ≥ 1)이 옳은 기준인가** — 만족 조합이
   없으면 rc=1 로 끝내는데, 그러면 촬영 당일 값을 못 정한다.

🔴 **감추지 않는 것**: ① 검토가 구현자 논증 5건 중 **2건을 기각**했다
(*"별도 패키지라 회귀 위험 0"*, *"detect_range 는 지렛대가 아니다"*).
② `--compare` 를 **사람 있는 bag 으로 여전히 검증 못 했다** — 그런 bag 이 없다.
③ `passage_0820` 이 이 노트북에 없어 **27~30% 수치를 재확인하지 못했다.** 대신 사람
없는 다른 bag(`r2_line_0813`)에서 visible run 3~4회를 새로 관측했다.
④ 어댑터·게이트·S1-3 **전부 실차 미검증**이다. ⑤ 관제는 여전히 브라우저로 안 열었다.

## 근거 문서

- `REAL_ROBOT_VALUES §1-l` — 08-20 밤 전량(재촬영·6점·경로 4시행·**§1-l-12 실효속도**).
- `MASTER_PLAN §7` 예약 **57·58·59·60** · **§5-b 08-21 갱신** · `§8` 결정 색인.
- `PITFALLS §15` USB 장치 식별 · **`§16` 산출물과 전달 경로** · `§6` 코너 waypoint · `§14` 모터 EMI.
- **`FREEZE_MANIFEST §10.30`** 아홉 번째 예외 · **`MASTER_PLAN §8` 마지막 8줄** = §82 판정 전량.
- `tools/runbook_check.py` — 커밋이 인용한 저장소 밖 런북의 지문·관문 대조(§82.10).
- `FREEZE_MANIFEST §10.29` 여덟 번째 예외.
- 협업: `~/Desktop/YOLO_탐지연동_역할B_6차회신_0820.md`(수신) ·
  `YOLO_탐지연동_역할A_9차회신_0821.md`(발신).
- 운영: `~/Desktop/0821_명령묶음.md`(금) · `~/Desktop/0822_촬영_명령묶음.md`(토·일) ·
  `~/Desktop/0822_촬영_샷리스트.md`.
- 도구 신규 **7종**: `drive_linear_deadband.py` · `rotation_stall_report.py` ·
  `cluster_width_report.py`(`--compare` 대조군 모드) · `make_keepout_mask.py` ·
  `e2e_adapter.py` · `apply_measurements.py` · `record_screen.sh`.
