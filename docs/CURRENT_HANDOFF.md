# CURRENT_HANDOFF.md — 지금 할 한 묶음 (매 세션 시작점)

> 작업 완료 시 구현자가 갱신하고 검토자가 사실관계·테스트 결과를 확인한다.
> 이전 묶음 내용은 날짜별 현황으로 보낸다. 항상 이 한 묶음만 유지.

- **동결 기준점**: `212885a8292d1e86677d5beeb9f5358d76fe9b40` = tag **`platform-core-freeze-260724`**
  (annotated, 원격 push 완료). 증거 전량 = `docs/FREEZE_MANIFEST.md`.
  ★ 회귀가 나오면 `git diff platform-core-freeze-260724 --stat` 이 1차 용의선상이다.
- **현재 기준 커밋**: 이 파일과 같은 커밋 (main)
- **현재 단계**: 마스터플랜 5단 완료 → **6단(역할 B V1 합의)은 ⏸ 보류**(역할 B 회의 일정 대기 —
  `MASTER_PLAN.md §1` 6) → 순서 밖 소묶음 **tunnel-bringup-s4**(예약 9 ✅) → **예약 10 ✅**
  (+1차 Codex P1 보완 + 예약 12 ✅) → **2차 Codex P1 보완 ✅**(+예약 14 · 15 ✅, **3차 재검토 승인
  `7a96f7f`**) → **E2E 하네스 신뢰성 ✅**(예약 16 규명 · 예약 4 수리 — 아래 '직전 완료')
  → 외부 증거 파서 P1 보완 3회 → **YAML 판독 폐기·고정 Twist CSV 전환 완료**
  → **예약 17 봉쇄**(§10 P2) → **검토 §11 P1 2건 보완**(전수 검사기 교체 · 침묵 근거의 관측 창 결합)
  → **검토 §12 P1 보완**(wrapper 옵션 사각 fail-closed)
  → **검토 §13 P1+P2 보완**(인용 안 명령치환 · wrapper 목록 삭제)
  → **검토 §14 P1 2건 보완**(`&>` 리다이렉션 사각 · **침묵 창의 시간 경계**)
  → **지금 = 그 보완 커밋의 독립 재검토 대기 + 사용자 결정 2건.**
- **⏸ 6단 보류 기록**: 준비된 YOLO V1 핸드오프 원문(목표·함정·완료조건·허용 범위 전량)은
  **커밋 `30c5e87` 에 고정 보존**했다. 역할 B 회의가 잡히면 **그 원문을 그대로 되살려 재개**한다
  — 새로 쓰지 말 것. 6단은 7단과 병렬이라 이 보류가 실차 트랙을 막지 않는다. 복원 명령:

```bash
git show 30c5e87:docs/CURRENT_HANDOFF.md            # 원문 확인
git show 30c5e87:docs/CURRENT_HANDOFF.md > docs/CURRENT_HANDOFF.md   # 그대로 되살리기
```

- **직전 완료 (07-30 3차)**: **E2E 하네스 신뢰성** — 예약 16 **규명** + 예약 4 **수리**.
  서사·근거 전량 = `0730_현황.md §1`. 변경 표면은 **`tools/` 뿐**이다.
  - **예약 16 (규명)**: `mission_e2e` T자를 **10회 순차** 실행 — 증상 **0회**(과거 합산 1/12 ≈ 8%,
    "2회 중 1회"는 표본 2개의 착시였다). **가설 ② 기각** (동일 TF 조회 **1030회 예외 0건**),
    **가설 ① 확정(부분)** — 깨지는 술어는 `visible(any)` 이고, 그 재료인 **1초 깜빡임이 정상
    회차에서도 회당 1~4회** 관측된다. 근인 = **`visible`/`lost` 의 깜빡임 내성 비대칭**.
    ⚠ **왜 15초 이상 지속되는가는 미규명** — FAIL 을 재현 못 했다. 예약 16 은 **열린 채**다.
  - **예약 4 (수리)**: ⑦ 실패 시 `fail()` **전에** `/cmd_vel` 을 수집·분류해 FAIL 메시지에 담는다.
    분류 3갈래(코드 결함 / 잔류 명령·시뮬 / **판독 실패**) — '판독 실패'를 '잠잠'으로 뭉개지 않는다.
  - **범위 확대 1건 (고지)**: `gz model -p` 무한 행을 실측(⑪ 11분 대기, 고아 21분)해
    `gz_model_xy`(hard 상한 10s)로 봉쇄 + `abort_e2e` 의 '못 읽음=인프라' 분리. 회귀 케이스 13.
  - ★ 순서 증거 = `0730_현황.md §1.8`: 보완 **전** 코드에서 먼저 **10 PASS / 3 FAIL** 을 관측했고,
    **함수는 두고 배선만 되돌리면 케이스 12 만 실패**한다(배선 회귀의 독립 검출력 확인).
- **직전 완료 (07-31)**: **Codex §7 불승인 P1 2건 보완** — 외부 증거 파서의 fail-closed 계약.
  판정 원문 = `~/Desktop/개발현황/CODEX 현황/0729검토현황.md §7`, 서사 = `0730_현황.md §3`.
  - **P1-①**: `/cmd_vel` 판독기가 **값이 하나도 안 걸려도 `0건`** 을 찍어, 빈·경고문뿐·필드누락·
    NaN/Inf 덤프가 전부 '잔류 없음'으로 둔갑하고 ⑧ 에서 **그대로 PASS** 였다 — 내가 07-30 에
    "막았다"고 선언한 '조용한 통과'가 **그대로 남아 있었다**. 케이스 11·12 는 완전한 zero/nonzero
    두 종만 넣고 **세 번째 갈래를 한 번도 입력하지 않았다**(`AGENTS.md §3-7` 위반, 내 테스트에서).
  - **P1-②**: `gz_model_xy` 가 숫자·유한값을 검증하지 않아 `model -m`·`nan nan`·`inf inf` 가
    "빈 문자열이 아니다"라는 이유로 인프라 분기를 **우회**, ⑦ 에서 실정지 실패로 오분류됐다.
  - 보완: 두 파서를 **fail-closed 계약**으로 + ⑦·⑧ **단일 계약**(`measure_cmdvel_residual`).
  - ★★ **완료판정 1건을 조정했다 (§7.2)** — 아래 '재검토 최우선 확인 사항' 참조. 숨기지 않는다.
  - 부수 발견: **`grep -E` 의 `.*` 가 한글·em대시 섞인 줄을 못 넘어 조용히 0** 을 낸다
    (`-F` 는 1, LC_ALL 무관). 배선 단언은 **`-F` 로만**. `tools/*.sh` 전수 점검 완료.
  - 실측: harness guards **16/16**(13 → 16) · gate regression **14/14** · pytest **159** ·
    colcon **188·0f·3s** · E2E 4종 PASS(`abort_e2e` = ⑧ '관측된 침묵'으로 통과).
  - ★ 순서 증거 = `0730_현황.md §3.7`: 보완 **전** 코드에서 먼저 **12 PASS / 4 FAIL** 을 관측했다
    (12·14·15·16). 두 P1 은 **구현자가 먼저 손으로 재현**한 뒤 수용했다(`AGENTS.md §3-1`).
- **직전 완료 (07-31 특별 구현자 전환)**: **Codex §9 불승인 P1 보완 — YAML 자체 파싱 폐기**.
  판정 원문 = `~/Desktop/개발현황/CODEX 현황/0729검토현황.md §9`, 구현 서사 =
  `0730_현황.md §5`. 사용자가 이번 한 묶음에 한해 Codex를 검토자에서 **구현자**로 전환했다.
  - **P1**: `angular.x` 뒤 미지 `metadata.{y,z}`를 넣으면 줄 상태기가 부모 전환을 무시하고
    그 y/z를 `angular`의 누락 키로 오귀속해 `0건`으로 통과했다. 완전 표본 뒤 경고문 꼬리도
    마지막 불완전 레코드로 승인됐다.
  - **구조 보완**: YAML을 더 패치하지 않고, 수집을 명시 `geometry_msgs/msg/Twist` +
    Humble `--csv`로 바꿨다. ROS가 타입 역직렬화를 끝낸 뒤 내는 한 메시지=고정 6열만 소비한다.
    모든 줄이 정확히 6개 유한 실수일 때만 수치 반환; YAML·경고문·열 부족/초과·빈 줄·임의
    꼬리는 전부 판독 실패다.
  - **침묵 근거 강화**: 빈 덤프를 0건으로 인정하려면 발행자≥1뿐 아니라 토픽 타입이
    `geometry_msgs/msg/Twist`인지도 확인한다. 타입 불일치는 daemon 복구 1회 후 fail-closed.
  - **순서 증거**: 보완 전 코드 + 최종 테스트 = **13 PASS / 4 FAIL**(11·12·15·17);
    보완 후 = **17/17 PASS**. 중간 첫 재실행은 `sed` 캡처 이스케이프 결함으로 16/17,
    원인 한 줄 분류 뒤 수정하고 전량 17/17로 재실행했다.
  - **실측**: 실제 Humble CSV `0.12,0,0,0,0,-0.02` 확인 · pytest 159 · colcon 188·0f·3s ·
    gate regression 14/14 · E2E 4종 PASS(negative 0.076m, 3goals 0.144m,
    mission 완주, abort 실정지 0.0m + CSV 관측된 침묵).
  - **역할 불변식**: 이 묶음의 구현자는 **Codex(GPT-5)**다. 따라서 다음 독립 검토자는 이
    구현 세션과 분리된 Claude/Fable/다른 검토 주체여야 한다. 이 Codex 세션이 자기 구현을
    승인하면 `AGENTS.md §1`의 구현자≠검토자 불변식을 위반한다.
- **직전 완료 (07-31 예약 17)**: **상한 없는 외부 CLI 전수 봉쇄 + 검토 §10 P2 2건**.
  서사·근거 전량 = `0730_현황.md §6`. 변경 표면 = `tools/` 5파일 + 정본 문서 3.
  - **예약 17**: 전수 점검으로 **10곳** 봉쇄(`gz model` 4 · `topic pub -w 1` 1 · `service call` 1 ·
    `daemon` 4). ★ `abort_e2e:71` 은 07-24 §16 P1 때 **빠뜨렸던 한 곳**이다 — "전수"라고
    말했지만 전수가 아니었다. 판정용 좌표는 **'못 읽음=인프라'** 로 분리.
  - **P2-①**: 증거 수집을 그래프 조회보다 **먼저**로. 구판 관찰값 **34초** → 보완 후 **0초**.
  - **P2-②**: 코드 변경 없이 `TEST_GATES.md §2` 에 **검증 상한 명시**(손실 ≠ 침묵)로 닫음.
  - 회귀 **17 → 19** (케이스 18=수집 순서 동작 단언 · 19=**전수 기계 검사**).
  - ★ 순서 증거: 보완 전(`941953c`) **17 PASS / 2 FAIL**, 케이스 19 가 10곳을 그대로 열거.
  - 실측: guards **19/19** · pytest **159** · gate regression **14/14** · colcon **188·0f·3s** ·
    E2E 4종 PASS(단 `mission_e2e`·`regression_negative` 는 **인프라 FAIL 1회 후 분류·재실행 PASS**).
  - **제외**: `make_map.sh`·`accuracy_bench.sh` — 실행 검증 불가로 역회귀 앵커 없음(`§7` 예약 17 등록).
- **직전 완료 (07-31 §11)**: **Codex §11 불승인 P1 2건 보완** — '전수'와 '관측 창'.
  판정 원문 = `~/Desktop/개발현황/CODEX 현황/0729검토현황.md §11`, 서사 = `0730_현황.md §7`.
  두 P1 은 모두 직전 커밋(`d9f23ac`)의 **중심 주장 자체**를 깼다. 둘 다 **먼저 재현하고** 수용했다.
  - **P1-①**: "전수 봉쇄"를 지키는 케이스 19 의 검사기가 **하위 명령 화이트리스트**였다.
    `ros2 action` 이 목록에 없어 `regression_negative.sh:44` 의 일반 `timeout` 이 **남아 있는 채
    19/19 PASS** 했다 — **거짓 녹색**. `ros2 lifecycle`·`ros2 action info` 도 같은 사각이었다.
    → 그 1곳을 `hard_timeout` 으로 통일하고, 검사기를 `tools/scan_unbounded_cli.py` 로 분리해
    **모든 foreground `ros2`·`gz`** 로 뒤집었다(인용 상태 추적 파서 · 명령 단위 결합).
  - **P1-②**: 침묵 근거를 **수집 창 밖**에서 셌다. 창에는 발행자가 없고 창 직후에만 생긴
    경우도 `0건` PASS — 들을 대상이 없었는데 '정상 침묵'이 됐다. 선-조회와 후-조회는
    **방향만 다른 같은 크기의 구멍**이고, 발행자 '수'로는 세대 전환을 애초에 못 가른다.
    → 근거를 **GID(DDS GUID)** 로 바꾸고 **창 양끝을 브래킷**한다. 창-시작 조회는 수집과
    동시에 띄우고 상한을 창 길이로 묶어 **P2-①(수집 지연 금지)을 유지**했다.
  - **실 그래프 실증**(stub 아님): 세대 전환 A→B = 판독 실패 / 침묵 발행자 생존 = `0` /
    발행자 없음 = 판독 실패. daemon 캐시가 죽은 발행자를 살려 보이지 **않았다**.
  - 회귀 **19 → 20**(케이스 20 = 관측 창 결합 8종). 케이스 19 는 개수가 아니라 **검사기를 교체**
    하고 **검사기 자체의 부정 회귀**(양성 5·음성 5 픽스처)를 붙였다.
  - ★ 순서 증거: 새 게이트를 구판 코드에 먼저 돌려 **17 PASS / 3 FAIL**, 케이스 19 가 정확히
    `regression_negative.sh:44` 한 곳을 짚었다. 보완 후 **20/20**, 케이스 18 은 여전히 **0s**.
  - 실측: guards **20/20** · pytest **159** · gate regression **14/14** · colcon **188·0f·3s** ·
    **E2E 4종 전부 1회에 PASS**(abort 0.0m · negative 0.063m · 3goals 0.136m · mission 완주).
  - **정정**: 직전 묶음의 제외 등록이 **과소 집계**였다(등록 5곳 vs 실제 21곳). 목록의 정본을
    사람이 아니라 **검사기 출력**으로 바꿨다(`MASTER_PLAN §7` 예약 17).
- **직전 완료 (07-31 §12 특별 구현자 전환)**: **Codex §12 불승인 P1 보완 — wrapper 옵션 사각**.
  판정 원문 = `~/Desktop/개발현황/CODEX 현황/0729검토현황.md §12`, 구현 서사 =
  `0730_현황.md §8`. 사용자 명시로 이번 묶음에 한해 Codex가 검토자에서 **구현자**로 전환했다.
  - **P1**: 새 검사기가 `xargs -n1`·`time -p`·`command --`·`env -i` 뒤의 무상한 `ros2`
    4종을 전부 출력 0건으로 승인했다. wrapper 토큰 하나만 건너뛴 뒤 옵션을 "다른 명령"으로
    오인해 성공 반환한 것이 근인이다.
  - **보완**: 지원 wrapper를 본 단위는 옵션 문법을 불완전하게 재구현하지 않고 끝까지
    보수적으로 훑는다. wrapper 뒤 unquoted `ros2/gz`는 모호해도 위반으로 보내며,
    `hard_timeout N env … ros2 …`처럼 실제 hard-timeout 아래인 형상만 통과한다.
  - **회귀**: 케이스 19 픽스처를 **양성 13(옵션 wrapper 8종 포함)·음성 6**으로 확대.
    독립 공격 4/4 검출 · 현재 게이트 5파일 출력 0건 · harness **20/20** · pytest **159** ·
    colcon **188·0f·3s**. 셸 도구 표면이라 E2E는 `TEST_GATES §7`에 따라 생략했다.
  - **역할 불변식**: 이 묶음의 구현자는 **Codex(GPT-5)**다. 다음 독립 검토자는
    Claude/Fable/제3 주체여야 하며, 이 Codex 세션은 자기 보완을 승인할 수 없다.
- **직전 완료 (07-31 §13)**: **Claude §13 불승인 P1+P2 보완 — 인용 안 명령치환 · wrapper 목록 삭제**.
  판정 원문 = `~/Desktop/개발현황/CODEX 현황/0729검토현황.md §13`, 서사 = `0730_현황.md §9`.
  ⚠ 같은 자리(예약 17 검사기)가 **네 번째** 재개방됐다. 매번 형태만 다른 **같은 실수**였다:
  *"여기서 멈춰도 된다"고 판단한 지점이 틀렸다.*
  - **P1 (§13.2)**: 큰따옴표 안쪽을 통째로 가리는 마스킹이 셸에서 **실제로 활성인**
    `$( )`·백틱까지 지웠다. `x=$(gz model …)` 는 잡히는데 `y="$(gz model …)"` 는 **안 잡힌다**
    — 따옴표 하나 차이다. 이 5파일은 `"$( )"` 를 이미 10곳 넘게 쓴다(실사용 표기).
    → `_mask()` 를 **문맥 스택**으로 재작성. 큰따옴표 안이라도 명령치환은 셸 문맥으로 되돌린다.
    ⚠ 작은따옴표와 `${VAR}` 는 **계속 가린다** — 같이 열면 `source "$(dirname "${BASH_SOURCE[0]}")"`
    류가 전부 거짓 양성이 되어 게이트가 즉시 무너진다. 케이스 19 음성 7·8·9 가 이 경계를 박제한다.
  - **P2 (§13.3)**: wrapper **화이트리스트** 자체가 남은 구멍이었다 — `setsid`·`nice -n 5`·
    `ionice -c2`·`taskset -c 0`·`unbuffer` 뒤가 전부 통과했다.
    → 목록을 **삭제**하고 단위 전체를 훑는다. 상한 판정은 `hard_timeout` 결합 하나뿐이다.
    ★ 결정 근거는 의견이 아니라 **측정**이다: 이 전환으로 늘어난 검출이 `tools/` 셸 도구
    **8개 전량에서 0건**(범위 밖 `make_map`·`accuracy_bench`·`test_gate_regression` 포함).
  - **회귀**: 케이스 19 픽스처 **양성 19·음성 10**(케이스 수는 20 그대로).
  - ★ 순서 증거: 새 게이트를 구판 검사기(`f6b4110`)에 돌려 **양성 1~13만 검출, 14~19 누락**.
    보완 후 1~19 전부 검출, 음성 10종은 양쪽 다 미검출(거짓 양성 무증가).
  - 실측: guards **20/20** · pytest **159** · gate regression **14/14** · colcon **188·0f·3s** ·
    `doc_check --strict` 전 항목 OK. ⚠ Gazebo E2E 는 **돌리지 않았다**(→ §14 묶음에서 전량 실행해 해소).
- **직전 완료 (07-31 §14)**: **Codex §14 불승인 P1 2건 보완 — `&>` 사각 · 침묵 창의 시간 경계**.
  판정 원문 = `~/Desktop/개발현황/CODEX 현황/0729검토현황.md §14`, 서사 = `0730_현황.md §10`.
  ★ 이 검토는 §12·§13 이 미뤄 온 **GID 브래킷을 조기 판정 뒤에도 생략하지 않고** 함께 봤다 —
  그래서 `§11 P1-②` 는 이제 **"미검토"가 아니라 "독립 검토 완료 · P1 불승인 · 보완됨"** 이다.
  - **P1-① (§14.2)**: 같은 자리(예약 17 검사기)가 **다섯 번째** 재개방. 이번엔 목록이 아니라
    **`&` 한 글자에 네 가지 뜻이 섞여 있던 것**이 원인이다. 배경 제외의 근거는 '블록하지
    않는다'뿐인데 구판은 앞의 `>`(`2>&1`)만 보고 **뒤의 `>`** 를 안 봐서, foreground 리다이렉션
    `ros2 … &>/dev/null` · `gz … &>>log` 가 배경으로 분류돼 **단위 전체가 사라졌다**.
    → 역할 판정을 `_amp_role()` 한 곳으로 모아 `&`·`&&`·`&>`/`&>>`·`>&` 를 명시적으로 가른다.
  - **P1-② (§14.3)**: '수집과 **동시에** 띄운' 조회는 창 시작을 고정하지 못한다. 그 조회가
    그래프를 **실제로 읽은 시점**이 창 시작보다 앞이라는 보장이 없고, 상한은 늦은 조회를
    유한하게 만들 뿐이다. **재현(시간축은 실제 벽시계): 같은 DDS 진실인데 조회 지연 0.1s →
    `''`(정상), 1.2s → `0`(거짓 승인)** — 판정이 그래프가 아니라 **조회 속도**를 따라갔다.
    → 잔류 수집을 조회 0회로 맨 먼저 열고(P2-① 유지), 비었을 때만 **시작 스냅샷을 확정한 뒤
    그 뒤에** 침묵 검증 창을 연다. ⚠ **pre 와 post 는 비대칭** — 늦은 pre 는 옛 세대를 증거에서
    지워 치명적이고, 늦은 post 는 판정을 보수적으로만 만든다.
  - ⚠ **내 1차 보완이 회귀를 만들었고 실제 DDS 가 잡았다** — 두 조회 다 daemon 복구를 허용했더니
    '발행자 없음 → 창 도중 생성'이 `0` 으로 승인됐다(복구 26s 동안 승인 창이 잔류 창에서 30초
    떨어져 나간다). `pre` 만 `retry=0` 으로 되돌리고 케이스 21 에 **동작 회귀**로 박제했다.
  - **회귀 20 → 21**. 케이스 21 = **값이 아니라 순서**를 단언한다(스텁이 수집·조회의 시작/끝을
    트레이스에 남긴다). 케이스 19 픽스처는 **양성 22·음성 13**. 케이스 20 의 스텁도 고쳤다 —
    pre/post 를 `retry` **인자**로 가르던 배선 결합이라 배선이 바뀌자 무의미해졌다(→ 호출 순서).
  - ★ 순서 증거: 새 게이트를 구판(`fc5b915`)에 돌려 **19 PASS / 2 FAIL** — 케이스 19 는
    `&>`·`&>>` 2종만 정확히 누락, 케이스 21 은 9종 FAIL 하며 겹침(`C1< G1< C1> G1>`)이 찍혔다.
    **케이스 20 은 구판에서도 PASS** — 검토자 지적("시간축을 만들지 않는다")이 실증됐다.
    역회귀 앵커: `pre` 를 `retry=1` 로 되돌리면 정확히 그 케이스 하나만 FAIL 한다.
  - **실 DDS 실증**: 검증 창 도중 A(`…53.26.cc.9a`)→B(`…91.26.f2.0c`) = 판독 실패 /
    같은 무발행 GID 양끝 생존 = `0` / 시작 스냅샷 전 발행자 없음 = 판독 실패.
  - 실측: guards **21/21** · pytest **159** · gate regression **14/14**(`✗` 0) ·
    colcon **188·0f·3s** · `doc_check --strict` OK · **E2E 4종 전부 1회에 PASS**
    (negative 0.159m · 3goals 0.14m · mission 완주 · **abort 실정지 0.0m + 관측된 침묵**).
    ★ `abort_e2e` 의 `cmdvel.log` 가 **0바이트**였다 — 새 검증 창 경로가 실제 Nav2 그래프에서
    돌아 GID 브래킷이 승인했다는 실물 증거다(§14.3 마지막 역회귀).

## ★ 사용자 결정 2건 — 이것이 다음 묶음을 정한다

### 결정 A. 동결 예외 (예약 16 수리 착수 조건) — **07-31 재판단으로 개정됨**

> ✅ **07-31 사용자 승인 — `①` + `②′` 를 한 묶음으로 착수한다. 동결 예외가 열렸다.**
> 착수 조건 = 아래 ②′ 불변조건 준수 + **부정 회귀 5단계 신설**(`0730_현황.md §2.6`) +
> **역회귀 앵커 T자·쌍굴 E2E**. 둘 다 없이 커밋하지 않는다.
> ⚠ 승인 범위는 **①+②′ 에 한정**된다 — `visible` 술어 완화(③)는 **여전히 열지 않는다.**
> ⚠ 이 예외는 `mission_node.py` 의 **`last_seen` 기록 경로**에만 적용된다. 동결 영역의
> 다른 부분으로 번지면 그 시점에 다시 승인을 받는다 (`FREEZE_MANIFEST.md`).

⚠ **심각도 분류가 바뀌었다.** 07-30 에는 이 건을 '판정 도구(E2E 게이트) 신뢰성' 문제로
올렸으나, 갇힌 상태에서 로봇이 실제로 무엇을 하는지를 끝까지 따라간 결과
**미션 안전 결함**으로 재분류한다. 전문·근거 = `0730_현황.md §2`.

**코드로 확정된 사실** (추정 아님 — 전수 확인):

- `search_attempts += 1` 은 `last_seen is None` 검사를 **통과한 뒤에만** 실행된다
  (`mission_node.py:791`) → 이 경로에선 시도 횟수가 **0 에 고정**된다.
- `give_up = True` 와 `'관제 보고: 추종자 확인 불가'` 는 **오직** `search_attempts >=
  max_attempts` 분기 안에만 있다(`:765~768`) → **영영 도달하지 못한다.**
- `mission_node` 의 발행 토픽은 `/mission_state`·`/siren` **둘뿐**이고, 그동안
  `/mission_state` 는 계속 **`GUIDE`** 를 발행한다 → **관제 화면상 정상으로 보인다.**
- 갇힘을 푸는 정상 출구는 **`visible` 회복(1.0s 연속 검출)** 하나뿐이다. `monitor.reset`
  2곳은 SEARCH_BACK 안이라 도달 불가, FAULT 복귀도 `last_seen` 을 건드리지 않는다.

즉 로봇은 **추종자를 놓친 것을 판정하고도 역행하지 않고, 보고도 하지 않고, 탈출구로 계속
간다.** 그리고 회복이 안 되는 경우가 하필 **사람이 뒤처졌거나 쓰러진 경우** — SEARCH_BACK
이 존재하는 이유 그 자체다.

| # | 요청 | 성격 | 동결 위험 |
|---|---|---|---|
| **①** | `record_last_seen()` 의 예외를 로그로 남긴다 | 진단. **동작 변경 0** | 거의 없음 (throttle 필요) |
| **②′** | **기록 조건을 소비 술어와 같은 타이머로** + `last_seen=None` 진입 안전망 | 근인 수리 | 중 — 부정 회귀 + 역회귀 앵커 필수 |
| ~~③~~ | ~~`visible` 무관용 리셋 완화~~ | **철회 — 구현자가 반대한다** | 안전 술어를 푸는 방향 |

**②′ 의 불변조건**: *"기록은 그 기록을 소비하는 술어와 같은 타이머를 써야 한다."*
지금은 **엄격한 `visible` 이 쓰고 관대한 `lost` 가 읽는다** — 이 불일치가 구멍의 본체다.
`last_seen` 은 로봇 자기 좌표 저장일 뿐 **안전 판단이 아니므로** 1초 연속 검출로 잠글
이유가 없다. 안전 술어(`visible`)는 그대로 두고 **기록 조건만** 맞춘다.
⚠ 구체 구현 선택은 열어 둔다 (`AGENTS.md §5`).

**③을 반대하는 이유**: 비대칭은 버그가 아니라 의도된 안전 설계로 보인다 — "따라오고 있다"는
틀리면 사람을 두고 가므로 엄격해야 하고, "놓쳤다"는 비싼 역행을 발동하므로 관대해야 한다.
`visible` 을 풀면 실물 라이다 노이즈에서 **가짜 '따라오는 중'** 이 생긴다. 방향이 반대다.

**★ 이 수리는 E2E 재현을 기다릴 필요가 없다.** 갇힌 상태는 Gazebo 없이 **결정적으로**
만들 수 있다(검출 1장 → 미검출 1장 → `lost_sec` 경과 → GUIDE tick). 그 5단계가 그대로
보완 전 실패를 관측할 **부정 회귀**가 된다. "가끔 실패한다"가 아니라 **"이 입력에서 항상
갇힌다"** 이므로 예외 요청의 근거로 비교할 수 없이 강하다. 상세 = `0730_현황.md §2.6`.

### 결정 B. 다음 묶음을 무엇으로 할지

> ✅ **07-31 갱신 — 실차 트랙은 사용자 판단으로 보류.** 이 세션에서 계획했던 범위를
> 전부 닫고 넘어간다. 원래 계획(예약 16 규명 · 예약 4 수리)은 `941953c`+§10 승인으로,
> 그 과정에서 생긴 부채(예약 17 · 검토 §10 P2 2건)는 **직전 완료 (07-31 예약 17)** 로 닫혔다.
> **남은 것 = ① 이 커밋의 독립 재검토 → ② 예약 18(`gate_fakes.py`) → ③ 예약 16(①+②′).**
> ⚠ 07-31 반복 P1 뒤 §12 보완까지 왔다. **구현자≠검토자 원칙상 재검토를 생략하지 않는다.**
> ⚠ 실차 S6(`MASTER_PLAN §3 S6`)는 보류 상태이며 인수일이 다가오면 최우선으로 돌아온다.


**구현자 권고 순서** (07-31 재판단):

1. **예약 17 먼저** — `mission_e2e.sh ⑪` 의 `gz model` 무한 행 + `tools/` 전체의 상한 없는
   외부 CLI 호출 **전수 점검**. 동결 무관·허용 표면 `tools/` 뿐·**위험 0**. 바로 가능하다.
2. **결정 A 의 ① + ②′ 를 한 묶음으로** — ①은 다음 재현 때 원인을 결판내고, ②′는 원인이
   무엇이든 **갇히지 않게** 만든다. 둘은 목적이 다르므로 하나로 다른 하나를 대신할 수 없다.
   부정 회귀 = `0730_현황.md §2.6` 의 5단계, 역회귀 앵커 = T자·쌍굴 E2E.
3. **③은 열지 않는다.**
- **후보 ③(대안): 대기** — 외부 2트랙 중 하나가 풀리면 그쪽이 이 대기 구간보다 우선한다.

⚠ **외부 2트랙이 풀리면 위 후보보다 우선한다** (아래 '완료 후 다음 단계').

---

> ✅ **07-31 확정 — 위 순서로 가되, 외부 트랙이 실제로 풀렸다.**
>
> **★ 외부 트랙 발동**: 구동부 실물 **인수일이 확정**됐다 — 로봇·Jetson **일괄 수령(B안)**,
> D-0 기준 **4일**. '완료 후 다음 단계'의 두 트랙 중 **구동부 트랙이 열렸으므로** 위
> 경고가 발동한다.
> ⚠ 단 **R2 통과 선언으로 열린 것이 아니다.** 인수를 먼저 받고 **R3 부터** 시작하는
> 형태이며, R0~R2 잔여 항목은 인수 후 공동 진행한다 (`REAL_ROBOT_VALUES.md §5`).
> 구동부 2차 회신 검토 전문·요청 목록 = `~/Desktop/TEENSY_실차연동_인수요청_및_2차회신검토_0731.md`.
>
> **트랙 배분** — 파일이 겹치지 않아 편집은 병렬 가능하다.
> ⚠ **회귀 실행만은 배타적으로** 한다 (Gazebo E2E 동시 실행 금지 — 아래 '함정' 4번).
>
> | 트랙 | 범위 | 상태 |
> |---|---|---|
> | **진행 중 묶음** | §12 P1 보완 (`tools/` 2종) | **독립 재검토 대기** |
> | **하네스 연장** | `gate_fakes.py` IMU 주기를 실측값으로 (아래 ★) | 신규 |
> | **예약 16** | 결정 A `①+②′` (승인됨). `mission_manager` — **로봇·Gazebo 불필요** | 별도 묶음 |
> | **실차 S6** | `MASTER_PLAN.md §3 S6` 준비물 6종 + `REAL_ROBOT_VALUES.md §5` 수령 | 병렬 |
>
> **★ `gate_fakes.py` — 신규 항목 (07-31)**
> 구동부 2차 회신 실측으로 **하네스가 실물과 어긋나 있다**는 것이 드러났다.
> 지금 하네스는 IMU 를 **50Hz 등간격**으로 흉내내는데(`gate_fakes.py:91`, 주석은 `:138`
> 에서 아직 100Hz 라 서로 불일치), 실차 실측은
> **41.63Hz · 평균주기 24.02ms · min 18ms · max 30ms · σ 0.55ms** 다.
> **EKF 가 30Hz 인데 IMU 최대 간격이 30ms 인 조합이 재현되지 않는다.**
> 성격이 예약 17 과 같다 — *"판정기·하네스가 실물과 다른 입력을 만들어 통과를 준다."*
> ⚠ `tools/` 와 **같은 커밋에 섞지 않는다** (게이트 실패 시 원인 분리 불가).

## 이번 한 묶음 목표 — **검토 §14 P1 2건 보완** (구현)

Codex 독립 검토 §14 가 `fc5b915` 를 **불승인(P1 2건)** 했다. 이 묶음은 그 둘을 닫는다.
다음 묶음은 다시 **독립 재검토**이며, 구현자가 Claude Opus 5 이므로 검토는 Codex 가 맡는다.

### ✅ GID 브래킷(§11 P1-②) — 상태가 **바뀌었다**

이전 핸드오프는 이 항목을 "아직 아무 독립 검토도 받지 않았다"로 기록했다.
**그 기록은 이제 틀렸다.** §14.6 이 명시적으로 정정을 요구했다:

- `4321ded` 에서 **Claude 가 구현** → `§12`(Codex)는 P1 조기 판정으로 제외 →
  `§13` 은 Claude 가 구현자라 검토 불가.
- **`§14` 에서 Codex 가 조기 판정 뒤에도 생략하지 않고 독립 검토했다.**

→ 정확한 상태 = **"Codex 독립 검토 완료 · §14.3 P1 불승인 · 이 묶음에서 보완"**.
결함은 집합 비교가 아니라 **첫 집합을 얻는 시간 경계**였다(§14.4 가 파싱·비교는 확인해 줬다).

**구현자가 주장하는 것** (검토자가 반증할 대상 — 공격 목록은 주지 않는다):

1. 게이트 5파일(`lib_e2e`·`abort_e2e`·`mission_e2e`·`regression_negative`·`regression_3goals`)의
   **모든 foreground `ros2`·`gz` 호출**이 `hard_timeout` 의 피연산자다 — 하위 명령 목록이 아니라
   명령 전체가 대상이고, 예외는 백그라운드(`… &`)뿐이며 그 예외는 명시돼 있다.
2. 그 판정은 `tools/scan_unbounded_cli.py` 가 한다. 인용 상태를 **문자 단위로 추적**해
   `python3 -c '…'` 본문·주석·줄 이어쓰기를 가르고, `if`/`until`/`$( )`/`<( )`/백틱/파이프/
   wrapper 뒤 호출도 잡는다. wrapper 옵션은 종류마다 달라 자체 파싱하지 않고, wrapper를 본
   단위는 끝까지 보수적으로 훑어 unquoted `ros2/gz`를 fail-closed로 잡는다.
3. **검사기 자체에도 부정 회귀가 있다** — 케이스 19 안의 양성 **22**·음성 **13** 픽스처.
   느슨해지는 쪽(양성 누락)뿐 아니라 **과잉 강화되는 쪽**도 잡는다: 음성 7·8·9 가
   `"${BASH_SOURCE[0]}"` 류 변수 확장·외부 CLI 아닌 명령치환·중첩 인용을 지킨다.
3b. **wrapper 목록이 없다.** 상한 판정은 `hard_timeout` 결합 하나뿐이고, 단위 전체를 훑는다.
   그 대가(거짓 양성)는 `tools/` 셸 도구 8개 전량에서 **증가 0건**으로 측정했다.
3c. **큰따옴표 안의 명령치환이 인용 밖과 동일하게 해석된다** — `y="$(gz model …)"` 검출.
   반대로 `${VAR}` 는 명령치환이 아니므로 열지 않았다(열면 게이트가 무너진다).
3d. **`&` 의 네 가지 뜻이 갈라져 있다** — 배경 제외의 근거는 '블록하지 않는다'뿐이므로
   `&>`·`&>>`(foreground 리다이렉션)는 제외하지 않는다. 역할 판정은 `_amp_role()` 한 곳.
4. 빈 `/cmd_vel` 덤프를 승인하는 **침묵 창은 비어 있지 않은 시작 GID 스냅샷이 확정된 뒤에
   시작**하며, 그 시작 GID 전부가 창 끝에도 생존해야 한다. 창 뒤에 새로 생긴 발행자는
   근거로 재사용되지 않는다.
5. 그 구조가 **P2-①(잔류 수집을 지연시키지 않는다)을 되살리지 않았다** — 잔류 수집은
   조회 **0회**로 맨 먼저 열리고, 내용이 있으면 조회 없이 즉시 판정한다(케이스 18·21 이 동작으로 단언).
5b. **pre 와 post 를 비대칭하게 쓴다** — 늦은 pre 는 옛 세대를 증거에서 지워 치명적이므로
   순서 제약과 daemon 복구 금지를 걸고, 늦은 post 는 판정을 보수적으로만 만들므로 복구를 허용한다.
6. 위 4·5 는 stub 만이 아니라 **실제 DDS 그래프에서** 확인됐다 — 검증 창 도중 세대 전환
   (A `…53.26.cc.9a` → B `…91.26.f2.0c`)은 판독 실패, 무발행 생존 발행자는 `0`,
   시작 스냅샷 전 발행자 부재는 판독 실패.
6b. **회귀가 값이 아니라 순서를 단언한다** — 케이스 21 은 스텁이 수집·조회의 시작/끝을
   트레이스에 남겨 승인 창이 스냅샷 확정 **뒤에** 열렸는지 본다. 구판(`fc5b915`)에 돌리면
   9종이 FAIL 하고 겹침(`C1< G1< C1> G1>`)이 그대로 찍힌다.
7. 제외 대상 목록의 **정본이 사람이 아니라 검사기 출력**으로 바뀌었다. 직전 등록이 과소
   집계(5곳 vs 실제 21곳)였음을 정정하고 `test_gate_regression.sh` 2곳을 신규 등록했다.
8. 이 묶음은 `src/**` 를 **한 글자도** 바꾸지 않았다.

**★ 구현자가 스스로 밝히는 약점** (검토자가 여기부터 봐도 좋다):

- **검사기는 여전히 정적 분석이다.** `eval` 이나 변수로 조립한 명령(`$CMD topic echo`)은
  원리적으로 못 본다. heredoc 본문도 셸로 계속 읽는다 — 게이트 5파일엔 heredoc 이 없어
  지금은 무해하지만, 생기면 거짓 양성이 난다(안전 방향이긴 하다).
- **이제 단위 안의 unquoted `ros2`/`gz` 는 실행 위치가 아니어도 위반이다** — `echo ros2`·
  `grep ros2 file` 도 잡힌다. 의도된 fail-closed 이고 오늘 비용은 0 이지만, **앞으로 게이트에
  그런 줄이 추가되면 거짓 양성으로 막힌다**(인용하면 풀린다). 이 트레이드오프를 공격해도 좋다.
- ⚠ **내 1차 보완이 회귀를 만들었고 실제 DDS 가 그걸 잡았다** — 두 조회 모두 daemon 복구를
  허용했더니(`retry=1`) '발행자 없음 → 창 도중 생성'이 `0` 으로 승인됐다. 복구 26초 동안
  그래프가 바뀌면 승인 창이 잔류 수집 창에서 30초 떨어져 나간다. `pre` 만 `retry=0` 으로
  되돌리고 케이스 21 에 동작 회귀로 박제했다. **스텁 게이트는 이걸 못 잡았다** — 실물이 잡았다.
- ⚠ **승인 창은 이제 잔류 수집 창이 아니다.** 빈 덤프일 때 승인하는 것은 뒤이어 연 **검증 창**이라
  두 창은 다른 구간이다. §10.4 의 30초 이탈을 막기 위해 의도적으로 택한 구조적 대가이고,
  복구 금지가 그 완화책(검증 창을 첫 창에 최대한 붙인다)이다. **이 트레이드오프를 공격해도 좋다.**
- **침묵 경로가 창 하나(2s) + 조회 한 번만큼 느려졌다** — 실측 `⑧` 정상 침묵 약 5s(구판 2.3s).
  잔류 경로는 그대로다.
- **브래킷은 '연속 생존'을 증명하지 않는다.** 같은 GID 가 창 양끝에 있어도 ⓐ 창 내내 살아
  있었는지 ⓑ 발행할 **의도**가 있었는지는 이 층에서 증명 못 한다. 실차 R0 watchdog 몫으로 남겼다.
- ⚠ **실제 DDS 실험 첫 2회는 오염돼 무효였다** — `pgrep … | head -1` 이 python 이 아니라 부모
  셸을 잡아 A 가 안 죽었고, 이전 회차의 발행자가 살아남아 GID 가 2개 잡혔다. 그 회차의 `''` 는
  세대 전환이 아니라 '발행자 소실'을 본 것이라 근거로 쓰지 않았다. 매 단계 그래프 상태를
  확인하는 절차로 다시 얻은 것이 위 6번이다(`0730_현황.md §10.7`).
- **`make_map.sh`·`accuracy_bench.sh`·`test_gate_regression.sh` 는 봉쇄하지 않았다**(§7.8).
  케이스 19 의 대상에서도 **의도적으로 제외**했으므로, 그 21곳은 기계 검사가 지키지 않는다.
- 메시지 손실 ≠ 침묵 상한(§10.6 P2-②)은 **그대로 열려 있다** — 코드 변경 0 으로 문서화만 했다.
- 케이스 18·20·21 의 주요 경로는 **stub** 이다. 실물 확인은 `0730_현황.md §10.7` 의 3종뿐이며,
  그 실증은 Gazebo 없이 rclpy 단일 노드로 만든 것이라 Nav2 실제 세대 전환과 같다는 보장은 없다.
- **케이스 21 의 트레이스 단언은 '순차 실행'을 요구한다.** 앞으로 이 함수를 의도적으로
  병렬화하면(예: post 조회를 검증 창과 겹치기) 결함이 없어도 FAIL 한다. 그때는 계약을
  다시 쓰는 것이 맞지만, **테스트를 먼저 느슨하게 하지 말 것** — 그게 §11.3→§14.3 의 경로였다.

## 완료조건 (재검토)

1. `docs/TEST_GATES.md §7` 범위대로 재실행 + 위 주장 대조.
2. 발견사항은 P0/P1/P2 로 분류하고, 각각
   `재현·근거 → 안전/동작 영향 → 한 문장 완료판정 → 최소 보완 방향 → 필수 부정 회귀·역회귀` 순으로 기록.
3. 기록 위치 = `~/Desktop/개발현황/CODEX 현황/0729검토현황.md` 에 **절을 이어서 추가**
   (`AGENTS.md §5` — **새 파일 금지**. 한 파일에서 서사가 이어져야 재검토 이력이 추적된다).
4. 승인/불승인 판정 후 `CURRENT_HANDOFF` 를 결정 B 의 후보 중 하나로 교체.

## ★ 함정 (먼저 읽을 것)

1. **`gz model -p` 는 상한이 없으면 무한 행한다** (07-30 실측 — ⑪ 11분, 고아 21분).
   재검토 중 게이트가 안 끝나면 **먼저 `ps -eo pid,etimes,args | grep "[g]z model"`** 을 본다.
   게이트 5파일은 07-31 에 봉쇄됐다(예약 17). 아직 무방비인 곳 = `make_map.sh`·`accuracy_bench.sh`·
   `test_gate_regression.sh` — 정본 목록은 `python3 tools/scan_unbounded_cli.py <파일>` 출력.
2. **`pkill -f "…"` 를 손으로 치지 말 것** — 패턴이 자기 명령줄에도 매칭돼 셸 자신을 죽인다.
   스크립트는 PID 로 죽인다. 그리고 **서브셸을 죽여도 그 안의 CLI 자식은 고아로 살아남는다.**
3. **`src/` 와 `build/` 는 하드링크다.** 동결 영역에 임시 계측을 넣고 `git checkout` 으로
   원복하면 **링크가 끊겨 `build/` 에 계측판이 남는다** — 원복했다고 믿는 채로 동결이 뚫린다.
   저장소를 안 건드리는 그림자 관측 노드를 쓴다 (원본 = `~/Desktop/개발현황/0730_shadow_monitor.py`).
4. **Gazebo E2E 동시 실행 금지.** `tools/test_gate_regression.sh` 도 같이 돌리면 안 된다
   (정리 단계가 nav2·slam·ekf 를 전역 kill 한다 — `AGENTS.md §4`).
5. **"재실행하니 됐다"로 닫지 말 것.** 예약 16 은 10회 PASS 로도 닫히지 않았다.
6. **기준선**: pytest **159** · colcon **188** · gate regression **14** · harness guards **21**.
   동결 태그 **자체**의 colcon 수치는 165 로 불변 — 동결본과 대조할 때만 165 를 쓴다
   (`TEST_GATES.md §1`).

## 완료 판정

**한 문장**: "직전 커밋의 위 주장이 독립 재실행으로 대조되고, 발견사항이 P0/P1/P2 로
분류돼 각각 완료판정·최소 보완 방향·필수 회귀까지
`~/Desktop/개발현황/CODEX 현황/0729검토현황.md` 에 절로 이어 기록되며,
그 결과로 `CURRENT_HANDOFF` 가 다음 묶음으로 교체된다."

## 완료 후 다음 단계

**아래 두 트랙은 외부 대기이며, 풀리는 즉시 이 대기 구간보다 우선한다** (서로 독립):
- 역할 B 회의가 잡히면 → **6단 재개**: 위 '⏸ 6단 보류 기록' 의 복원 명령으로 원문을 되살린다
- 구동부 트리거(R2 "3m 직진 3%" 선언)가 오면 → **7단 실차 R0~R8** (`MASTER_PLAN.md §3`).
  R0 실측에서 **cmd_vel watchdog** 결과를 받는 즉시 `FREEZE_MANIFEST.md §6` 조건부 수용을
  확정/재개방. R5 이전에 **라이다 장착 오프셋 실측 반영**이 선행돼야 한다.

## 보존해야 할 안전 불변조건

- **★ R0 watchdog 리마인더 (잊으면 위험이 조용히 남는다)**: 실차 7단 R0 에서 **cmd_vel watchdog(단절
  0.5s 내 정지)** 실측 결과를 받는 즉시 `FREEZE_MANIFEST.md §6` 의 잔류 cmd_vel 활주 **조건부 수용을
  확정 또는 재개방**한다. 이 항목은 계속 열려 있다.
  ★ 07-30 보강: `abort_e2e` 가 이제 그 두 경우를 자동으로 갈라 보고한다 — R0 판정 때 이 분류를 쓴다.
- **★ 08-15 플랜 B 판정일** (`MASTER_PLAN.md §6`) — 구동부 진척 주 1회 확인, 그날까지 R2 통과 선언이
  없으면 '구동부 지연' 행을 발동한다. **판정을 미루지 않는다.**
- **★ 라이다 장착 높이 미수령** (`MASTER_PLAN.md §7` 예약 11 · `REAL_ROBOT_VALUES.md §4`) —
  기구·센서 담당에게 **재요청이 필요한 사용자 액션**이다. 이것 없이는 R5 지도 제작이 무의미하다.
- **★ 게이트 술어 불변조건** (`MASTER_PLAN.md §8` 07-30) — 조건 기동 게이트의 충족 판정은
  **"한 번 관측 = 통과" 금지**. 유효기간을 조이는 것은 보완이 아니다.
- **★ 세대 불변조건** (`MASTER_PLAN.md §8` 07-30) — **비동기 증거는 세대에 속한다.**
  서비스 소실을 관측하면 카운터뿐 아니라 **그때 날아가 있던 요청까지** 무효화한다.
- **★ 증거 수집 불변조건 (07-30 신설)** — 단언이 깨지는 순간의 증거는 **`fail()` 보다 먼저**
  수집한다. `fail()` 은 즉시 cleanup+exit 이라 정작 필요한 순간에 증거가 노드와 함께 사라진다.
- **★ 외부 CLI 상한 불변조건 (07-30 신설)** — `ros2` 든 `gz` 든 **예외 없이** hard 상한을 씌운다.
  **사람이 개입해서 얻은 PASS 는 판정이 아니다.**
- **★ 남은 검증 상한 (닫히지 않았다 — 조건부 수용)**: DDS 가 알아채기 전에(<1폴링) 서버가
  재시작하면 `service_is_ready()` 는 계속 True 라 게이트가 두 세대를 구분할 수 없다.
  `lifecycle_msgs/GetState` 응답에 세대 식별자가 없어 **이 계층에서는 원리적으로 못 닫는다.**
  - **수용 전제**: Nav2 lifecycle 서버는 0.5s 안에 죽었다 살아나지 않는다(관리 노드가 순차 전이)
  - **재개방 조건**: R0~R2 실차에서 Nav2 노드 crash-restart 가 **1회라도** 관측되면 재개방
- E2E cleanup 순서 불변조건(부모 `ros2 launch` 먼저 kill → `pkill -9 -f "lib/nav2[_]"`, 브래킷 트릭 —
  `AGENTS.md §4`)은 회귀 실행 시 그대로 지킨다.

## 금지 범위

- **재검토는 기본 read-only** — 사용자 명시 요청 전 코드 수정 금지 (`AGENTS.md §1`).
- **`src/mission_manager/**` 변경 금지** — platform-core 동결 안. 예약 16 의 근인이 여기지만
  **사용자 결정 A 없이는 열지 않는다.**
- **`src/tunnel_bringup/**` 변경 금지** — `7a96f7f` 로 승인받았다. 게이트 술어를 되돌리지 말 것.
- **`src/tunnel_sim/**` 변경 절대 금지** — 동결 대상(`docs/FREEZE_MANIFEST.md §3` 해시 고정)이자
  T자·쌍굴 회귀의 **살아있는 기준선**이다.
- **미실측 값을 채우는 것 금지** (전수 확인 `grep -rn "TODO: " src/tunnel_bringup/`).
- **Jetson 실행·빌드 시도 금지** — 장비가 없다. 노트북 검증까지만.
- `make_map.sh` 실런 금지. Gazebo E2E 동시 실행 금지.
- 실패를 원인 한 줄 분류 없이 재실행 금지 (`AGENTS.md §3-6` · `TEST_GATES.md §5`).

## 근거 문서

`docs/MASTER_PLAN.md §3` · `docs/MASTER_PLAN.md §6` · `docs/MASTER_PLAN.md §7` ·
`docs/MASTER_PLAN.md §8` · `docs/REAL_ROBOT_VALUES.md §1` · `docs/REAL_ROBOT_VALUES.md §2` ·
`docs/REAL_ROBOT_VALUES.md §4` · `docs/TEST_GATES.md §1` · `docs/TEST_GATES.md §2` ·
`docs/TEST_GATES.md §7` · `docs/FREEZE_MANIFEST.md §3` · `docs/FREEZE_MANIFEST.md §6` ·
`0730_현황.md §1` · `0729_현황.md §3` · `~/Desktop/개발현황/CODEX 현황/0729검토현황.md` ·
`~/Desktop/TEENSY_실차연동_합의사항.md`
