# CURRENT_HANDOFF.md — 지금 할 한 묶음 (매 세션 시작점)

> 작업 완료 시 구현자가 갱신하고 검토자가 사실관계·테스트 결과를 확인한다.
> **현재 묶음 + 미해결 보류만** 유지한다 (`AGENTS.md §5`) — 완료된 서사는 정본으로 보내고 링크한다.

- **동결 기준점**: `212885a8292d1e86677d5beeb9f5358d76fe9b40` = tag
  **`platform-core-freeze-260724`**. 증거 = `docs/FREEZE_MANIFEST.md`.
- **현재 기준 커밋**: 이 파일과 같은 커밋(main).
- **현재 단계**(이력 정본 = `MASTER_PLAN §8`): 5단 완료 · 6단(역할 B V1)은 회의 대기
  → D+0 검사 1~8 OK(08-07) · 전기 시공 완주 · re-arm 래치 굽기 완주(08-11)
  → 예약 32 교정 계수 `335` 굽기·지면 확정(08-12)
  → ❌ 예약 32-d odom 재교정은 **전제 오류로 기각**(08-13 밤)
  → ✅ **예약 32-e 상수 정정 재굽기 완주 + R2·A-10 완주**(08-14)
  → **🔴 `MASTER_PLAN §7` 예약 41 EKF NaN 발산 규명 · Tier A(`src/**` 설정)** (지금 — 2026-08-14 밤)

> ✅ **08-14 에 닫은 것** — 굽기(`build=Aug 14 2026 11:00:55` · `d0_check rc=0`) ·
> `§5-G6` **10/10** · 배선 6행 **6 PASS/0 FAIL** · URDF 라이다 `0.779`(동결 예외 5회차) ·
> udev `/dev/rplidar` · **R2 직진 `0.997` · 회전 `0.993`** · 바퀴별 C10 · **A-10 3구간**.
> 서사·근거 = `MASTER_PLAN §7` **32-c-2**·**40**·**42** · `FREEZE_MANIFEST §10.26`.

**⚠ 미해결 보류 — 어느 것도 승인 완료로 읽지 말 것** (본문 정본 = `MASTER_PLAN.md §7`):

- **🔴 예약 41** (08-14) — **이 묶음.** EKF 가 21분 주행 `+129.6초`에 NaN → 회복 0회.
- **🔴 예약 40** (08-14) — 주행 중 각속도 **전달률 25%**. 곡률 권한은 편향의 6배라 추종은
  성립하나 모서리가 빡빡하다. `rotate_to_heading` 동결 예외는 **지도가 선 뒤**에 요청한다.
- **⏸ 예약 42** (08-14) — 아크릴 3mm 4면(+3.2kg). **오늘 미장착.** 붙이면 R2·C10·차고·
  위험문장·URDF 를 **전부** 다시 한다. 08-14 R2 는 **"현 하중 한정 잠정"** 이다.
- **🔴 08-14 미확인 1건** — 지도 세션 21분에 `/cmd_vel` 이 **27건(전부 0, 첫 2.6초)** 뿐인데
  `/odom` 은 **82.62 m** 를 갔다. **손으로 밀었는지 텔레옵이 발행했는지 사용자 확인 전이다.**
  후자면 관측성 결함이므로 다음 주행 전에 가른다.
- **⚠ 미해결 보류 — 예약 23 / 검토 §33 (`8fcc1a2`)**: P0 0 · **P1 1** · P2 1.
  `handoff_single_check.sh` 는 `§` 없는 새 작업 꼬리를 붙여도 `OK` 를 내고, 숫자 계약 전수
  증거는 기록 6자리와 독립 실행 21자리가 불일치한다. **승인 완료로 읽지 말 것.** 런타임
  `src/**` 변경·회귀는 없으므로 현재 D+0 실행은 막지 않되, 보완은 다른 묶음에 섞지 않고
  `MASTER_PLAN.md §7` 예약 23 의 완료판정과 부정 회귀로 별도 구현한다. 전문 =
  `~/Desktop/개발현황/CODEX 현황/0801검토현황.md §33`.
- **예약 24·26** — 라이다 실측·readiness 소유 분리 / 사용자 결정 '방법 B' 미구현.
- **예약 27** — Jetson 에 `nvcc`·`nvidia-jetpack` **없다.** 🔴 역할 B 전제가 바뀐다.
- **예약 28·29·30** — 전기 P1(§44·§45 ①② · §53 6건) · 정본 절 분리. ⏸ 실차 사다리 뒤.
- **예약 31** (08-11) — ✅ 로컬 **72건**. 🔴 **원격 push 는 사용자 몫** — 아직 백업이 아니다.
- **⏸ 예약 32-b** (08-12) — 조건부 수용 보류. **재개방 = 재발현 3회 + 만충.**
- **🔴 예약 36** — IMU 반복 사망(I2C 물림) · 복구 = 전원 재인가 3분.
- **예약 33·34·39** — PI 재조정+`appliedPwm`(R6 전) · 문서→코드 취약 참조 48곳 · 좌향 편향.
- **⏸ 6단 보류** — 역할 B 회의가 잡히면 `30c5e87` 핸드오프 원문을 복원한다.

## 이번 한 묶음 목표 — `MASTER_PLAN §7` 예약 41 EKF NaN 규명

**왜 이것이 먼저인가** — `slam_toolbox` 가 쓰는 **로봇 기준 TF 의 유일한 발행자가 EKF**
다(`ekf_real.yaml:51` · 프레임 이름과 사슬 전량 = `MASTER_PLAN §7` 예약 41). 08-14 지도 세션에서 EKF 가 `+129.6초`에
NaN 이 되고 끝까지 회복하지 못해(**유한 3,878 · NaN 32,948 = 89.5%**) SLAM 이 2분 만에
자세를 잃었다. 🔴 **이걸 안 고치면 지도를 다시 돌려도 같은 지도가 나온다.** Nav2 도 같은
TF 위에 서므로 자율주행 전체가 걸린다.

✅ **로봇은 정상이었다** — `/odom` NaN 0건 · 총 이동 **82.62 m** · `/estop/state` True 0건 ·
`z=2 ARMED` 유지. **H자 복도를 실제로 다 돌았고 기록만 못 따라갔다.**

🔴 **오프라인 재현이 가능하다** — `map_0814_1350` bag 에 `/odom`·`/imu/data` 가 남아 있다.
**로봇 없이 재현한다.** ⚠ 그 bag 은 `metadata.yaml` 결손 + 일부 페이지 손상이라 파이썬
`sqlite3` 직접 읽기로 접근한다(`ros2 bag` 은 못 연다).

**유력 후보(확정 아님)** = `/imu/data` 의 `orientation_covariance[0]` 이 **모든 표본 `-1.0`**
(ROS 규약 = 무효 선언)인데 `ekf_real.yaml:107` `imu0_config` 는 **yaw = true** 로 그 방위를
융합한다. ⚠ **즉사가 아니다** — 같은 `-1` 인데 1분짜리 bag 3종은 NaN 0 이었다.

## 완료조건

1. 🔴 **재현 먼저** — bag 재생으로 `+129.6초` NaN 을 **로봇 없이 재현**한다.
   재현 실패면 원인 가설을 기각하고 기록한다(`AGENTS §3-1`).
2. `+129.6초` 직전의 `/odom`·`/imu/data` 입력을 본다. **무엇이 그 순간 달라졌는가**를
   한 문장으로 적는다.
3. 🔴 **보완은 재현 뒤에.** `imu0_config` 를 만지려면 **yaw 를 끄는 대가**(절대 방위 상실 →
   장시간 드리프트)를 전제조건·재개방 조건 형식으로 같이 적는다. 🔴 `src/**` = **동결 예외**.
4. NaN 을 **fail-closed** 로 — 지금은 조용히 NaN 을 계속 발행한다.
5. 🔴 회귀 3종 — ① `-1` 공분산 주입을 잡는다 ② **20분 상당** 재생에서 NaN 0
   (🔴 **짧은 시행으로 대체 금지** — 1분짜리 3종이 전부 통과했는데 21분이 깨졌다)
   ③ 역회귀 앵커 = R2·A-10 bag 3종이 여전히 NaN 0. **전부 변이로 자기 감도를 증명한다.**
6. 회귀 보조: `rearm_gate_host_test` **967+7** · `test_ai_context` **37 검사** ·
   `firmware_precheck` **rc=0**.
7. 기준선 유지: pytest **184 passed** · colcon **247 tests** · harness guards **24 검사** ·
   gate regression **14 케이스**.
8. ⚠ **이 묶음은 `firmware/**` 를 굽지 않는다** — 그러므로 `§7-c-E2` **배선 6행**과
   `§5-G6` 10/10 을 다시 하지 않는다. 🔴 **계약은 그대로 6행이다**(축소 근거·거부 조건 =
   `JETSON_SETUP §7-c-E2`). 굽는 묶음이 다시 열리면 그때 되살아난다.
9. `JETSON_SETUP.md §9` `TODO(D+0)` **11건**은 08-11 에 전량 종결됐다 — 이 묶음에서
   다시 열리는 것은 없다.

**금지** — `firmware/**` 재굽기 · `CMD_WHEEL_BASE` 변경 · FF/게인(예약 33) ·
`rotate_to_heading`·`lookahead_dist`(예약 40 — **지도가 선 뒤**) · 아크릴 장착(예약 42) ·
`make_map.sh` 실런 · Gazebo E2E 동시 실행.

## 🔴 이번 묶음의 함정

**① 🔴 짧은 시행으로 판정하지 않는다.** 이 결함의 판정 길이는 **시간**이다. 1분 bag 세
개가 전부 NaN 0 이었고 21분이 깨졌다. 통과를 보고 닫으면 다음 지도에서 또 나온다.

**② 🔴 후보를 원인으로 승격하지 않는다.** `-1` 공분산은 **모든** bag 에 있었고 대부분은
멀쩡했다. "그럴듯함"과 "재현"은 다르다.

**③ 🔴 NaN 이 조용한 것이 결함의 절반이다.** 발산을 스스로 선언하지 않으면 지도가 다
그려진 뒤에야 안다. 08-14 가 정확히 그랬다.

**④ 손상된 bag 을 "못 쓴다"로 버리지 않는다** — `quick_check` 는 통과했고 파이썬
`sqlite3` 로 토픽 전량을 읽어냈다. 도구가 못 연다고 데이터가 없는 것이 아니다.

## ★ 현장 운용 (상세는 링크가 정본)

1. 🔴 **기동 순서** — `퓨즈 → XT90 → Jetson 부팅 대기 → agent → **Teensy VIN 만 재삽입**`.
   재연결 로직이 없어 `errorLoop()` 에 갇힌다. ⚠ `real_mapping.launch.py` 는 agent 를
   **자기가 띄우므로** 런치를 먼저 올리고 VIN 을 마지막에 꽂는다.
2. 🔴 **종료는 `Jetson sudo shutdown -h now` 가 맨 앞** (`ELECTRICAL_BASELINE §7`).
3. 🔴 **배선을 만질 땐 XT90 분리.** 유일한 예외 = 펌웨어 업로드(`FIRMWARE_REBUILD §5-b`).
4. 오염 판정은 **`firmware_precheck.sh` 뿐** · 🔴 `rc=2` 는 판정 불능이지 통과가 아니다.
5. 🔴 **브레드보드 상태다** — PWM/DIR 이 빠지면 **"돌 수도 있는"** 고장이다.
6. 🔴 정체 판별의 정본은 **`build`** 다 — `d0_check --expect-build '<컴파일 시각>'`.
   ⚠ 값은 손으로 적지 말고 ELF 에서 뽑는다(`strings … | grep '^[0-9]{2}:'`).
7. 🔴 **지도 저장은 둘이고 쓰는 곳이 다르다** — `map_saver_cli`(pgm+yaml, Nav2) 와
   `serialize_map`(posegraph+data, localization). 타입은 **`SerializePoseGraph`** 다.
   🔴 **저장 → 파일 4개 확인 → 그 다음 종료.** 상세 = `PITFALLS §7`.

## 사용자 결정 — 유지

- 동결 예외 **5회 소진**(08-14 §10.26) — `src/**` 를 다시 열려면 사용자 승인.
- 순서 = **예약 41 EKF** → 재지도 → 예약 40 `rotate_to_heading` → R3 (`MASTER_PLAN §3`).
- 🔴 **지면 주행 승인은 "그날 하루" 단위다.** 08-14 분은 받았다. 안전요원 E-stop ·
  양옆 1m · 경사 금지 · **네 바퀴 방향 육안**은 매번 그대로다.
- 🔴 **위험 수용 문장은 08-14 에 개정됐다** — 정본 = `MASTER_PLAN §7` **32-c-2**.
  적용 속도 `0.128 m/s` · 정지거리 `6.6 cm` · **재개방 = ① 안전요원 없음 ② 11 cm 초과
  ③ 순항 0.2 m/s 초과.** 🔴 **사용자가 직접 정하고 확정했다 — 구현자가 안 고친다.**

| 트랙 | 범위 | 상태 |
|---|---|---|
| **진행 중 묶음** | §7 예약 41 EKF NaN 규명 | 🔴 **Tier A.** 완료조건 = 위 1~9 |
| 🔴 예약 40 | 주행 중 각속도 전달률 25% | 🔴 지도 선 뒤 동결 예외 요청 |
| ⏸ 예약 42 | 아크릴 3mm 4면 (+16%) | ⏸ 미장착. R2 는 **잠정** |
| ✅ 예약 32-e | 상수 정정 재굽기 | ✅ **닫힘**(08-14). R2 `0.997`·`0.993` |
| ⏸ 예약 32-b | 무장 해제 12회+ | ⏸ 조건부 수용. "해소" 아님 |
| 🔴 예약 36 | IMU I2C 물림 | 🔴 복구 = 전원 재인가 3분 |
| 🔴 예약 39 | 좌향 계통 편향 | 🔴 08-14 C10: 좌측만 3.2% 갈림(잡음 경계) |
| ⚠ 릴레이 | 정격 미증명 · 08-10 수용(`§16-e`) | 🔴 끼임 11A 미관측 |
| ⏸/❌ 예약 21·23·24·26·27·29·30·31·33·34 · 역할 B | — | ❌ 이 묶음에 섞지 않는다 |

## 보존해야 할 안전 불변조건

- 🔴 **정지 수단은 E-stop 이다.** `Ctrl+C` 를 E-stop 으로 승격하지 않는다.
- 🔴 **`§5-G6` 10/10 이 보인 것은** *"누르면 토픽이 따라온다"* 이지 *"안 눌렀을 때 안
  뜬다"* 가 아니다. **거짓 양성은 그 시험이 원리적으로 못 잡는다.**
- 🔴 **`/estop/state`·`/drive/diag` 는 1Hz 다** — 수십 ms 사건은 표본 사이로 빠진다.
  **토픽이 조용한 것을 "아무 일도 없었다"로 읽지 않는다.**
- 🔴 **배선 전 XT90 분리 + 퓨즈 제거** · 만졌으면 **개폐 10회 재검증**.
- 🔴 **펌웨어 업로드는 모터 전력 0V·바퀴 공중에서만** · 지문 `10607`/`158` 불일치면 금지.
- 🔴 **펌웨어를 또 구우면 `§5-G6` 10회 + `§7-c-E` 를 다시 한다.**
- ⚠ **watchdog 정지는 무장을 풀지 않는다** — 발행자가 살아나면 재무장 없이 다시 구른다.
- 🔴 **모터 명령 전에 네 바퀴가 같은 방향으로 도는지 본다** — `d0_check` 검사 6은 **엔코더
  부호** 검사라 구동 방향을 안 본다(`PITFALLS §10`).
- 🔴 **지면 주행 전 `ros2 bag record -a`** · 지도 세션은 `-x "/map|/map_updates"` 로 줄인다.
- 🔴 **무장 직후 500 ms 는 정숙 장벽이다** — 그 안에 비영 명령을 넣으면 `y=6` 으로 풀린다.
- 🔴 **토픽 발행 명령은 값 인자가 없으면 에러 없이 `0.0` 을 발행** — `publishing #1:` 줄의
  값을 눈으로 본다. 🔴 `-r` 은 Ctrl+C 까지 무한 발행 → 정해진 시행은 `timeout Ns` 로 감싼다.
- 🔴 **각도를 양끝점으로 재지 않는다** — 쿼터니언 yaw 는 ±180° 로 접힌다. 누적 언랩을
  쓴다(`PITFALLS §12`). 08-14 에 427° 가 73° 로 나와 거짓 실패를 낼 뻔했다.
- 판정기가 **관측하지 않은 것을 말하지 않는다** — 상한 초는 계약값이지 경과 주장이 아니다.
- 실차 없이 확인한 합성 bag·가짜 `ros2` 결과를 실차 통과로 승격하지 않는다.
- `TODO(D+0)` **11건**을 추측값으로 닫지 않는다 — 미해결은 이유와 재확인 시점을 적는다.
- 🔴 **실물이 안 하는 일을 해 주는 합성 데이터는 검사를 통과시키는 일만 한다**(08-14 실증 —
  테스트 헬퍼가 yaw 를 안 접어서 접힘 결함이 회귀를 통과했다).

## 금지 범위

- 🔴 **`330Ω` 을 25V·5V·VIN 에 붙이기** — Teensy 자체 `3.3V` 핀뿐이다.
- 🔴 **짧은 시행으로 예약 41 을 닫기** — 판정 길이는 **시간**이다.
- 🔴 **후보를 재현 없이 원인으로 쓰기** — `-1` 공분산은 멀쩡한 bag 에도 전부 있었다.
- 🔴 **08-14 R2 결과를 "확정"으로 쓰기** — 아크릴 미장착 상태의 **잠정**이다(예약 42).
- 🔴 **`applied_pwm_max` 를 epoch 가 다른 표본끼리 비교하기.**
- 🔴 **도구 상수를 손으로 베껴 적기** — `tools/firmware_constants.py` 로 `.ino` 에서 읽는다.
- 🔴 **위험을 "덮였다"로 기록하기** — 조건부 수용은 전제조건 + 재개방 조건 형식으로만.
- 🔴 **등록을 종결로 쓰기.** 예약 40·41·42 가 생겼다고 고쳐진 것이 아니다.
- 🔴 **핸드오프 상한에 걸렸다고 상한을 올리기.** 먼저 **옮길 완료 서사**를 찾는다.
- 사용자 추가 승인 없는 `src/**` 변경 · `make_map.sh` 실런 · 지도 자산 변경.
- 예약 20~34·39·40·42, 역할 B V1 을 이 묶음에 혼합.

## 완료 판정

**`map_0814_1350` bag 재생으로 `+129.6초` NaN 이 로봇 없이 재현되고, 그 순간 입력에서
무엇이 달라졌는지가 한 문장으로 적히고, 보완 뒤 같은 재생에서 `/odometry/filtered` 의
NaN 표본이 0 이며, 그 사실을 회귀 3종이 변이로 감도를 증명한 채 지키면 완료다.**
🔴 재현이 실패하면 **가설을 기각하고 그 사실을 기록한다** — 못 고친 것을 고친 것으로
쓰지 않는다. 🔴 **1분짜리 시행 통과는 판정에 쓰지 않는다.**
⚠ 굽기가 없는 묶음이라 `§7-c-E2` **배선 6행**·`§5-G6` 10/10 은 판정에 안 들어간다 —
**계약이 바뀐 게 아니라 이번에 해당이 없는 것**이다. `TODO(D+0)` **11건**도 전량 종결
상태 그대로다.

## 완료 후 다음 단계

🔴 **`2026-08-15` 플랜 B 판정일 · `2026-08-18` 완주.**

1. **재지도(R5)** — 경로는 08-14 그대로면 된다(H자를 82.6 m 잘 도셨다).
   🔴 저장 순서 = **저장 → 파일 4개 확인 → 종료**(`PITFALLS §7`).
2. **예약 40** `rotate_to_heading_min_angle 0.6→0.2` 동결 예외 요청 — 🔴 **지도가 선 뒤.**
3. **A-5 판정** — 지도상 최협부가 `2.00 m ±0.1` 인지. 08-14 지도는 2.2~2.3 m 였으나
   EKF 고장 위에 그려진 값이라 **판정으로 쓰지 않는다.**
4. **블록 12** 증거 복제 · **예약 31** Desktop md 72건 private repo 화(**아직도 안 했다**).
5. R3 rosbag → 예약 22·29 와 R4~R8. 🔴 **`R0→R1→R2→R3` 순서를 건너뛰지 않는다.**
   `TODO(D+0)` **11건** 전량 종결이라 여기서 다시 열리는 것은 없다.

## 근거 문서

- 규율 티어·회차·은퇴 = `AGENTS.md §6` · 결정 색인 = `MASTER_PLAN §8` · 예약 = `§7`
- EKF 설정 = `src/tunnel_bringup/config/ekf_real.yaml` · 지도 함정 = `PITFALLS §7`
- 각도·줄자 함정 = `PITFALLS §12` · 반지름 3종 = `REAL_ROBOT_VALUES §1-b-0`
- 동결 예외 5회차(라이다) = `FREEZE_MANIFEST §10.26` · 전기 = `ELECTRICAL_BASELINE §7`
- 펌웨어 재빌드 = `FIRMWARE_REBUILD §4`,`§5` · 실행 순서 = `JETSON_SETUP §4-d`,`§6-b`,`§9`
