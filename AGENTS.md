# AGENTS.md — 한이음 지하터널 대피로봇 · AI 공통 규칙 + 정본 라우터

> 모든 AI(Claude/Codex/기타) 세션의 공통 진입점. 2026-07-20 문서 개편으로 신설 (커밋 `67468eb`).

## 0. 프로젝트 한 줄

자율주행 + 멀티모달 센서퓨전 AI **지하터널 전용 대피 유도 로봇** (2026.4~10, 한이음).
GPS 불가 터널에서 재난 시: 위치 파악 → 출동 → 대피자 집결 → 안전구역 선두 유도.

## 1. 역할 분담

| 주체 | 역할 |
|---|---|
| 사용자 (손민우, 역할 A) | 목적·정책·우선순위·최종 승인. 실차 안전·정책 결정 책임 |
| **구현 Claude** (Opus 기본, Fable 도 가능) | 한 번에 한 묶음 구현 + 자체 테스트 + 커밋·push |
| **검토 Codex** | 구현 커밋의 독립 검토·공격 테스트·회귀 재실행 + 발견사항별 최소 보완 방향 제시(**범위 = `docs/TEST_GATES.md §7`** — 전량 아님) (**기본 read-only — 명시 요청 전 코드 수정 금지**) |
| Fable | 위 구현 역할 + 반복 장애 미수렴·새 구조 결정 시 진단 (07-20 문서 개편·SpeedManager `f94da44` 담당) |

★ 불변식은 모델 이름이 아니라 **구현자 ≠ 검토자** 다 (자기 논증을 자기가 승인하는 것 방지).
세션 모델 선택은 사용자 몫이며, 커밋 메타데이터에 실제 구현 모델을 남긴다.

팀 경계: 역할 A = Jetson(SLAM·Nav2·미션·관제) / 역할 B = YOLO 인지 / 구동부 = Teensy(모터·엔코더·micro-ROS).

## 2. 읽기 순서 (매 세션 필수)

1. 이 파일 (공통 규칙)
2. **`docs/CURRENT_HANDOFF.md` — 지금 할 한 묶음. 작업 시작 전 반드시 읽는다.**
3. 작업에 필요한 절만 선택해 읽는다:
   - `docs/PROJECT_CONTEXT.md` — 구조·계약·버전 고정값·문서 맵
   - `docs/MASTER_PLAN.md` — 단계 순서·동결 게이트·실차 사다리·유효 결정 색인
   - `docs/TEST_GATES.md` — 테스트·런치 실행법·PASS 기준
   - `docs/PITFALLS.md` — 영역별 함정 (해당 영역 작업 시)
   - `docs/FREEZE_MANIFEST.md` — 동결 증거(해시·게이트 수치·조건부 위험·검토 결과). **platform-core 를 건드리기 전 필수**
   - `docs/REAL_ROBOT_VALUES.md` — 구동부 실측값 정본·실차 반영 지점 (실차 작업 시)

**검토자(Codex)도 같은 순서로 진입한다** — 핸드오프가 직전 완료 묶음과 그 기록 절을
가리키므로 별도 요청문이 필요 없다. 요청은 **대상 커밋 해시만** 주면 된다.
실행 범위 = `docs/TEST_GATES.md §7` (전량 아님), 결과 기록 위치 = 아래 §5.

정본 우선순위: **CURRENT_HANDOFF > MASTER_PLAN > PROJECT_CONTEXT** > Desktop 날짜별 현황(역사·근거).
충돌 시 repo `docs/` 가 정본. Desktop `~/Desktop/개발현황/` 은 판단 근거·학습 서사의 역사 기록.

## 3. 사고 프로토콜 ★ (07-19 신설 — 실효 증명된 판단·수정·검증 순서)

1. **주장은 재현 후 수용** — 외부 검토·버그 보고는 코드/메모리 재현부터. 재현 성공 = 반영, 실패 = 기각+근거 기록.
2. **수정 전 완료판정 선정의** — "무엇이 관찰되면 성공인가"를 코드 작성 전에 한 문장으로.
3. **경계조건 질문 3종을 매 수정마다** — ① 타이머/디바운스: 리셋 시점? 상태 전환(단절→복구) 순간? ② 비동기: 호출≠접수≠종결≠실효 — 어디까지 확인? ③ 외부 입력: NaN/Inf/bool/음수/빈값이 오면?
4. **완료 선언 전 자기 논증 반증 1회** — "이 논증이 틀리는 입력이 존재하나?"
5. **검사를 조이면 역회귀 앵커 동반** — 실배포 설정 2종(T자·쌍굴)이 여전히 통과하는 테스트를 같이 추가.
6. **테스트 실패는 '코드 vs 인프라' 분류 후 대응** — 원인 한 줄 기록 전에 재실행 금지 (flake 영구 은폐 방지). 분류법 = `docs/TEST_GATES.md` §5.
7. **성공 경로만 보는 테스트는 미완성** — 부정 회귀("안 돼야 하는 게 안 되는가") + 전환 순간 케이스까지.
8. **내린 결정은 재논의 금지** — 새 근거가 생기면 해당 정본 문서를 개정하고 사유를 남긴다 (색인 = MASTER_PLAN §8).
9. **작업 단위는 잘게, 회귀는 즉시** — 한 세션 한 묶음 이하. 변경 직후 pytest 는 무조건, E2E 는 해당 영역만.
10. **고치기 전에 '같은 종류'를 전수로 센다** ★ (08-01 신설 — 검토 이력 실측이 요구한 규칙)
   지적받은 **그 자리만** 고치면 다음 회차에 **바로 옆**이 다시 지적된다. 실측: 검토 4문서의
   P1 약 30건 중 **20건이 단 5개 연쇄**에 몰려 있었다 — 검사기 전수성 **7연속**, 관측 시점 4,
   증거 파서 3, CLI 상한 3, SEARCH_BACK 전이 3. 새 영역의 결함이 아니라 **직전 보완의 이웃**이었다.
   → 착수 **전에** 그 결함이 속한 **클래스의 전체 목록을 열거**하고 그 목록을 커밋에 남긴다.
   ⚠ 근거는 **기억이 아니라 기계·문서**여야 한다: `grep` 전수 · 공식 설명서/문법표 · 전이표.
     "내가 아는 경우는 이 네 가지" 같은 자기 열거가 정확히 위 7연속의 원인이었다.
   ⚠ 전수 열거가 원리적으로 불가능하면 **그 사실 자체를 약점으로 공개**한다(숨기고 '전수'라고
     쓰지 않는다). '전수'는 전칭 명제라 반례 하나로 무너진다.
   실증: `read_param_float`→daemon→topic pub 로 3회 왕복한 CLI 상한은, 나중에 `grep` 한 번으로
   **21곳이 한꺼번에** 드러났다(예약 17). 처음에 셌으면 3회가 1회였다.
   ★ **세는 걸로 끝내지 마라 — 커버리지 폐포** (08-01 추가. 이 규칙을 세운 바로 그 커밋 `3a87741`
   이 이 절반을 빠뜨려 다음 회차에 불승인됐다 = 검토 §18). 열거는 실제로 됐었다: 착수 때 `grep`
   출력에 **13자리가 다 찍혀 있었는데** 손으로 쓴 검사 패턴은 **7개**였고, "내가 쓴 7개가 13자리를
   덮나?"를 아무도 묻지 않았다. 사람이 목록을 보고 코드로 옮겨 적는 순간 둘은 갈라진다.
   → ① 열거 목록의 **각 항목이 구현에 덮이는지 항목별로 대조**하고 그 대조를 커밋에 남긴다.
     ② 가능하면 **열거 자체를 검사기 안으로 옮겨** 목록과 구현이 갈라질 자리를 없앤다
        ('아는 표기를 찾아간다' → '전부 훑다가 걸리는 것을 잡는다'). 그러면 표기가 바뀌어도
        검사가 안 부서진다 — 단, **새 자리를 자동으로 받아줄지는 별도 정책**이다(아래 ★★).
     ③ 열거를 좁히는 판단(무엇을 역사로 볼 것인가 등)은 **규약으로 문서와 코드 한 곳에 고정**한다.
   실증: 스윕으로 뒤집자 검토가 지목한 4자리 밖에서 `MASTER_PLAN:197`·pytest·colcon 자리가
   더 나왔고(13자리 중 사각 6), 규칙을 고친 직후 harness 22→24 갱신에서 **4자리를 한 번에** 지목했다.
   ★★ **대조 대상에는 '검토가 준 목록'도 들어간다 · 경계는 양쪽을 잠근다** (08-01 추가.
   바로 위 ★절을 세운 커밋 `87a2b64` 가 **또 불승인됐다** = 검토 §19. §3-10 이 **2회 연속**
   같은 자리에서 깨졌다.) §18 은 필수 부정 회귀로 **"같은 표기를 하나 더 추가하면 FAIL"** 을
   문장으로 줬는데, 구현은 자리 수를 **하한**으로 뒀다 — 줄면 잡고 늘면 통과. 핸드오프에
   "**최소** 자리 수를 둔다"라고 스스로 써놓고도 그 문장을 받은 완료판정과 대조하지 않았다.
   → ④ 검토가 준 **완료판정·필수 부정 회귀 목록을 한 줄씩 구현에 대조**한다. 대조 대상은
      내가 만든 열거만이 아니다 — **남이 준 열거가 더 위험하다**(내 머릿속 설계와 문장이
      비슷해 보여서 다 덮었다고 착각한다).
     ⑤ 개수·크기·시간 같은 **경계 조건은 증가·감소 양쪽**을 회귀로 박는다. 부등호 하나(`<`)로
      끝냈다면 반대 방향을 아직 안 물은 것이다.
   실증: 보완 `0271360` 은 **§20 에서 승인**됐다 — 검토자 독립 실행에서 자리 증식 4/4 · 자리
   소실 4/4 · 값+증식 동시 보고 1/1 검출. `3a87741`→`87a2b64`→`0271360` **3회차**에 닫혔다.
   ⚠ 단, 상한이 **옳은** 자리(벽시계 상한처럼 빨리 끝나는 게 결함이 아닌 곳)까지 등호로 바꾸지는
   않는다. ⑤ 는 '열거된 자리의 개수 계약'처럼 **양쪽이 다 결함인 경계**에만 건다.

## 4. 세션 파괴급 함정 5종 ★ (첫 명령어부터 필요한 반사신경 — 전체 목록은 docs/PITFALLS.md)

1. **`pkill/pgrep -f` 는 자기 명령줄도 매칭해 자살** → 브래킷 트릭 `pgrep -f "ros2[ ]launch"`. **`-x` 는 comm 15자 잘림**(`robot_state_pub`, `async_slam_tool`) → 긴 이름은 `pkill -f "…[_]…"`.
2. **`ros2 topic pub --once` 는 디스커버리 매칭 전에 쏘고 죽어 유실** → 반드시 `-w 1 --times 2~3`.
3. **시뮬 자작 노드는 `use_sim_time:=true` 필수** — 하나라도 빠지면 TF 시간 불일치·타이머 오작동.
4. **colcon test 는 종료코드 믿지 말 것** → `colcon test-result --verbose` 로 확인.
5. **launch 부모 kill -9 = 고아 nav2 노드** → 좀비 bt_navigator 가 goal 가로챔(증상 매번 다름, 자기 로그 "Begin navigating" 0건이 판별법) → cleanup 에 `pkill -9 -f "lib/nav2[_]"` + 부모(`ros2 launch`)부터 kill.

## 5. 운영 규칙 (공통)

- **한 세션 = 한 커밋 묶음** (마스터플랜 표 행 단위 이하). 묶음 밖 파일·기능을 섞지 않는다.
- **묶음이 끝나면 문서도 같이 갱신한다** — `CURRENT_HANDOFF` 를 다음 묶음으로 교체(현재 위치의 유일한 정본),
  `TEST_GATES` 기준선, 필요 시 `MASTER_PLAN` ✅·§8 색인. 커밋 직전 **`bash tools/doc_check.sh`**,
  push 직후 **`bash tools/doc_check.sh --after-push`** 로 검사한다 (문서 불일치 = 다음 세션의 실행 버그).
  같은 사실을 두 문서에 쓰지 않는다 — 링크로 연결한다.
- **구현 기록 절에는 '구현자가 주장하는 것'을 남긴다** (검토자가 반증할 대상).
  공격 목록("이걸 공격해줘")은 **주지 않는다** — 구현자의 위험 모델은 이미 틀린 것이
  증명된 적이 있다(07-20 P1 2건 모두 구현자가 "안전하다"고 논증한 경로에서 나왔다).
  **주장은 반증 범위를 넓히고, 공격 목록은 좁힌다.**
- **검토 발견사항에는 수정 방향도 함께 남긴다** — 각 P0/P1/P2마다
  `재현·근거 → 안전/동작 영향 → 한 문장 완료판정 → 최소 보완 방향 → 필수 부정 회귀·역회귀`
  순서로 기록한다. 검토자는 불변조건·책임 경계·필요한 상태를 제시하되 구체 구현 선택은
  구현자에게 남긴다. 단일 안전 해법만 가능한 경우 의사코드/API 형태까지 제안할 수 있지만,
  사용자 명시 요청 전에는 직접 패치하지 않는다. 보완 뒤에는 제안한 코드 모양이 아니라
  완료판정과 회귀 관찰값으로 독립 재검토한다.
- **검토 결과 기록 위치** = `~/Desktop/개발현황/CODEX 현황/07xx검토현황.md` 에 **절을 이어서 추가**
  (새 파일 금지 — 한 파일에서 서사가 이어져야 재검토 이력이 추적된다).
- ★ **역할 교대 예외 (08-03 신설 — `66db5c5` 가 실제로 이 경로를 탔다)**: 기본은 위 규칙대로
  **검토자는 직접 패치하지 않는다.** 다만 **사용자가 명시로 요청하면** 검토자가 **그 보완 diff
  한 번에 한해** 구현자가 될 수 있다. 그때 다음 네 가지가 함께 성립해야 한다.
  1. **자기 diff 를 자기가 승인하지 않는다** — 그 커밋은 **반대쪽 AI 가 독립 검토**한다.
     "구현자 ≠ 최종 승인자"는 사람이 바뀌어도 유지되는 불변조건이지, 역할 이름에 붙은 게 아니다.
  2. **구현자 자격의 완료판정을 남긴다** — 검토 절의 근거가 아니라, `CURRENT_HANDOFF` 에
     *구현자가 주장하는 것*(위 규칙)으로 적는다. 그래야 다음 검토자가 반증할 대상이 생긴다.
  3. ⚠ **검토자가 구현하면 '지적한 그 자리'만 고치기 쉽다.** 지적은 사례이고 고칠 것은
     **클래스**다 — `§3-10` 전수 열거는 역할이 바뀌어도 구현자의 의무다. 실제로 `66db5c5` 는
     지목된 런북 한 곳만 고쳐 같은 클래스가 다른 활성 런북에 남았다(검토 §32.2).
  4. **묶음 경계에서 역할은 원위치**한다. 교대 사실은 `CURRENT_HANDOFF` 의 그 묶음 기록과
     검토 절 머리(`구현자 = … / 검토자 = …`)에 **명시**한다 — 나중에 이력을 못 읽게 된다.
- **핸드오프 '현재 단계' 체인에 칸을 추가하면 진행표·본문 목표도 같이 옮긴다** — 셋 중 하나만
  바꾸면 그 문서는 **동시에 두 묶음을 지시**한다. 기계 검사 = `tools/handoff_single_check.sh`
  (`doc_check.sh` 안에서 자동 실행). ⚠ 08-03 에 이 검사가 **문구 하나에만 의존**해 우회된 적이
  있다 — 검사가 초록이어도 세 자리를 눈으로 한 번 맞춰 본다(검토 §32.2).
- **커밋 = push 한 세트.** 원격 `https://github.com/ghwjd1357-alt/tunnel-evac-robot` (private).
- 빌드는 항상 `colcon build --symlink-install` (수동 실행).
- **`make_map.sh` 실런 금지** — 지도 자산 변경은 사용자 명시 승인 + 정기 지도 제작 때만. E2E 스크립트는 전용 시뮬 PC 전용(Jetson 실행 금지).
- 같은 branch/worktree 를 두 AI 가 동시 수정 금지. **Gazebo E2E 는 어떤 worktree 에서도 동시 실행 금지** (전역 프로세스 cleanup 충돌).
- **검토 루프 탈출 조건** (발산 방지 — 07-19 하루 검토 4회가 실증한 리스크): 재검토는 보완 diff 당 1회(새 P0/P1 발견 시에만 1회 연장), 발산 시 P0 만 반영하고 동결. 품질은 회귀 스위트가 지키고, 검토는 단계 전환 게이트에만.
- 사용자 변경 보존, destructive 명령 금지. 구현자와 최종 승인자 분리.
